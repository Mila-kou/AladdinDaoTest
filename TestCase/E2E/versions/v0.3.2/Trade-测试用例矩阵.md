# v0.3.2 交易与订单（Trade & Order）测试用例矩阵

> 本文件是版本级 Trade 测试设计入口；执行状态只记录在 [results.md](results.md)。详细公式引用 `Docs/contract-releases/v0.3.2/contract-fields-and-calculation-rules.md` 和 `05-重要参数边界场景.md`；执行时使用 CURRENT 环境的部署参数重算，不照抄历史 fixture。产品只有 Standard 和 Flash One-Click/1CT 两种公开模式；direct Relay/Gasless 是第三条内部/遗留技术提交路径。详细行为见 [Standard-Relay-Flash-OneClick-(v0.3.2).md](Standard-Relay-Flash-OneClick-(v0.3.2).md)，需求时间线与未决冲突见 [Trade-需求来源与冲突台账-(v0.3.2).md](Trade-需求来源与冲突台账-(v0.3.2).md)。

## 1. 覆盖范围

| 域 | 必测功能 |
|---|---|
| 开仓 | 市价开仓、限价开仓、条件开仓，`long` / `short`、Standard / Flash One-Click（1CT）两种产品模式、direct Relay 技术路径、附带 TP/SL |
| 定价 | 正/负动态点差、MIN/MAX clamp、price impact、acceptablePrice |
| 仓位 | 市价加仓、市价部分减仓、市价关仓、保证金、杠杆、PnL、Est. Receive、清算保护期结束后的关闭并重新开仓 |
| 减仓 | `minOutputAmount`、向上取整导致全平、剩余成本 USD 单位、事件一致性 |
| 条件单 | TP/SL、Reduce-only、触发、更新、取消、Frozen、autoCancel |
| Funding | 连续 EMA/指数更新、逐仓结算、协议 claimable/Market Vault 路由、领取、取整和清算边界 |
| 风险处置 | 清算、ADL、特殊订单 effective MIN、手续费档位、保护期与 insolvent 早退 |
| Relay 与一键交易 | 直接 Relay/Gasless、Flash One-Click/1CT、Relay Fee、子账户命名槽位、EIP-712、feeToken=COLLATERAL_TOKEN、授权、撤权、过期与计数 |
| 前端 | 表单校验、预览、签名、状态、历史、错误语义、移动端关键路径 |

## 2. 用例设计方法与数据约定

| 设计方法 | 在本模块的用法 |
|---|---|
| 等价类 | 合法/非法字段、Standard / Flash One-Click（1CT）两种产品模式、direct Relay 技术路径、USD/Token、普通/Mock 市场、成功/取消/冻结 |
| 边界值 | trigger、acceptablePrice、validFrom、minOutputAmount、maxFeeAmount、杠杆、余额、精度 |
| 判定表 | long/short × increase/decrease × market/limit/stop × 触发是否满足 × acceptablePrice 是否满足 |
| 状态迁移 | Created → Open → Executed/Cancelled/Frozen；Frozen → Updated(Open) / Cancelled，或由 `FROZEN_ORDER_KEEPER` 重试至 Executed |
| 场景法 | 页面选市场 → 填单 → 预览 → 签名 → Keeper → Position/History |
| 错误推测 | 重复提交、跨市场残留、订单错型、哨兵破坏、旧 ABI、Indexer 延迟、错误 decimals |
| 组合覆盖 | TP/SL × 部分减仓 × autoCancel × 保护期 × Standard/Relay/1CT；关闭并重新开仓 × long/short × Standard/1CT × 单仓位/多仓位选择隔离 |

### 2.1 术语规范

| 对象 | 统一写法 | 使用规则 |
|---|---|---|
| 市价开仓 | 市价开仓 / `MarketIncrease` | 用户动作和用例标题写“市价开仓”；合约订单类型写 `MarketIncrease` |
| 限价开仓 | 限价开仓 / `LimitIncrease` | 用户动作和用例标题写“限价开仓”；合约订单类型写 `LimitIncrease` |
| 条件开仓 | 条件开仓 / `StopIncrease` | 用户动作和用例标题写“条件开仓”；合约订单类型写 `StopIncrease` |
| 加仓 | 市价加仓 / `MarketIncrease` | 已有同市场同方向仓位时再执行 `MarketIncrease` |
| 部分减仓 | 市价部分减仓 / `MarketDecrease` | 请求规模小于完整仓位；不使用“部分平仓”或“平 50%” |
| 关仓与全平 | 市价关仓 / `MarketDecrease`；结果为“全平” | “市价关仓”表示用户动作；“全平”只表示 Position 规模归零的结果 |
| 订单更新 | 更新订单 / `updateOrder` | 不再混用“编辑”、“修改”和 Update |
| 订单取消 | 取消订单 / `cancelOrder` | 用户动作写“取消订单”；终态写 `Cancelled` |
| 保护单 | TP/SL 保护单 | 不再混用“附属单”、“子单”和“保护订单” |
| 自动取消 | 自动取消 / `autoCancel` | 行为写“自动取消”；字段和列表名写 `autoCancel` |
| 多空方向 | `long` / `short`；结果为“多仓” / “空仓” | 标题与测试数据使用 `long` / `short`；期望结果可使用中文仓位名称 |
| 订单状态 | `Open` / `Executed` / `Cancelled` / `Frozen` | 只用这组精确状态值，不用“完成”代替具体终态 |
| 费用 | Execution Fee、Position Fee、UI Fee、Funding Fee、Relay Fee | 根据实际费用类型列出，不以单独的 `fee` 作为断言名称 |
| 标准交易 | 标准交易 / Standard | 业务名称写“标准交易”；路径值或产品标识保留 Standard |
| 直接 Relay | Relay/Gasless / 内部 mode=`flash` | 主钱包逐动作签 EIP-712、Relayer 调用 `RelayRouter`；不得与 1CT session 签名混写 |
| 一键交易 | Flash One-Click / 1CT | 主钱包签授权、session key 签动作、Relayer 调用 `SubaccountRelayRouter`；“建连后无逐笔钱包弹窗”不等于首次只签一次 |
| 代付费 | 代付费 / Relay Fee | 直接 Relay 与 1CT 均收取；Standard 不收取；不得与 Order Execution Fee 混算 |
| 关闭并重新开仓 | 关闭并重新开仓 / Close & Reopen | 前端在清算保护期结束后编排一笔全量 `MarketDecrease` 和一笔新的 `MarketIncrease`；两笔订单、两次 Keeper 执行，不是合约原子 Roll 动作，也不是 `Liquidation` |

### 2.2 入口分类

| 入口 | 含义 | 可判定范围 |
|---|---|---|
| 页面 | 用户从 Trade 页面填写、点击并连接钱包完成操作 | 可判定前端层；结合链上证据时可判定跨层一致性 |
| RPC | 测试账户直接签名并调用用户可用的合约接口 | 只判定合约层，不代替页面交互 |
| RPC 只读 | 只读取链上配置、代码、Reader 或余额，不发送交易 | 只判定环境与数据基线 |
| Admin RPC | 使用管理员或配置角色设置参数、Oracle 或测试夹具 | 只作为数据准备或管理权限验证，不代替用户入口 |
| Time RPC | 在支持时间控制的测试环境推进或指定区块时间 | 只作为时间边界的数据准备手段 |
| Keeper RPC | 使用 Order、Liquidation 或 ADL Keeper 执行相应订单 | 判定执行阶段；必须与订单创建入口的证据关联 |
| Relay RPC | 通过 Relay 提交主钱包逐动作 EIP-712 或 1CT session 请求 | 判定 RelayRouter/SubaccountRelayRouter、signer、Relay Fee、task 与签名链路 |
| 故障注入 RPC | 人为注入 RPC、Oracle、Keeper 或 Indexer 异常 | 只用于异常恢复与幂等性验证 |
| 复用关联用例证据 | 不重复发送业务交易，只复用已登记原子用例的同一份证据 | 只补充订单类型映射或跨域断言，不新增执行次数 |

组合入口统一使用 ` + ` 连接；例如 `RPC + 页面` 表示同一原子用例分别补齐合约层与前端层证据，不表示一次操作同时从两个入口提交。

### 2.3 标准操作步骤

| 标准操作 | 统一步骤 |
|---|---|
| 市价开仓 | 选择市价订单和 `long` / `short`；填写订单参数；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 |
| 限价开仓 | 填写限价订单参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 |
| 市价加仓 | 读取市价加仓预览；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 |
| 市价部分减仓 | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 |
| 市价关仓标准操作 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 |
| 市价关仓并校验保护单自动取消 | 确认关联 TP/SL 保护单为 `Open`；执行“市价关仓标准操作”；查询关联 TP/SL 保护单并重试执行 |
| 更新订单 | 选择目标订单；更新指定字段；点击“更新”并确认交易；查询更新前后字段与订单状态 |
| 取消订单标准操作 | 选择目标订单；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 |
| TP/SL 保护单执行 | 读取 `graceEnd`；将 Mock Oracle 价格更新至 TP 或 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 |
| 清算 | 将 Mock Oracle 价格更新至清算条件；Liquidation Keeper 执行清算；查询 Position、关联订单与账本 |
| 关闭并重新开仓 | 确认 `block.timestamp>=graceEnd` 且原仓位仍存在；点击“关闭并重新开仓”；提交并执行全量 `MarketDecrease`；只在本次关仓订单确认 `Executed` 且旧 Position 归零后自动提交 `MarketIncrease`；执行重开订单；查询两个 orderKey、新 Position、新 `graceStart/graceEnd`、TP/SL、OI、费用与页面状态 |
| 代付请求 | 按用例设置 Relay Fee 参数；分别按 direct Relay 或 1CT 生成相应 EIP-712 签名并提交；查询 Router、signer、task、业务订单、`RelayFeePaid` 与相关余额 |
| 交易路径对比 | 页面只切换 Standard 与 Flash One-Click/1CT；direct Relay/Gasless 仅通过 Relay RPC/SDK 构造。使用相同市场、方向、订单类型、规模与价格对比三条技术路径的 signer、`tx.from/to`、payload、Order、Position、费用、task 与 action count |
| 条件订单冻结 | 创建非市价条件订单并确认为 `Open`；记录创建时容量、仓位、抵押、价格和费用基线；在触发前只改变本用例指定的一项执行依赖；将 Mock Oracle 价格更新至触发条件；Keeper 执行；查询 `OrderFrozen`、`reasonBytes`、Order Store、Position、OI、OrderVault、费用与前端状态 |
| Frozen 订单更新恢复 | 准备一笔 `Frozen` 订单；更新 size、triggerPrice 或 acceptablePrice 中本用例指定的字段；按链上要求补足 Execution Fee；确认订单解除冻结并重新为 `Open`；再由 Keeper 执行；查询订单、仓位、费用和页面终态 |
| Frozen 订单取消 | 准备一笔 `Frozen` 订单；选择目标订单；点击“取消”并确认交易；查询 Order Store、初始抵押、冻结时已支付的 Execution Fee、余额、Open Orders 与 History |
| 无效输入 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 |
| 无关仓动作 | 不提交 `MarketDecrease`；查询 Position、OI、Order 与余额，确认没有非预期变化 |

### 2.4 数据约定

- 主测市场从 CURRENT 部署清单选择；不得把部署 fixture 名称当作固定产品需求。
- Mock 合成市场的 symbol、indexToken、Oracle、marketIndex、collateralToken 和 decimals 均运行时读取。
- Token 精度至少覆盖 8 位和 18 位；Collateral 按实际 decimals；USD 金额按合约 1e30 口径。
- Relay/1CT Max 专项统一使用：`B`=钱包 fee/pay token 余额 raw，`C`/`M`=本次 signed `maxFeeAmount`，`U`=1 个 6 位 USDC=`1_000_000 raw`，`T`=业务动作从同一 feeToken 实际占用的 gross raw amount，`F`=`RelayFeePaid.feeAmount`。`M` 是准入上限、`F` 是实扣，二者不得互换；Pay 已是 gross debit 时 Position Fee 不再重复加到 `T`。
- 当前 8/18 位覆盖首先指 Mock **index token** 的 size 换算，不代表前端已经支持 8/18 位 Relay fee/pay token；后者若无 CURRENT 可达配置，登记为结构性 `GAP`，不得假装已执行。
- 价格边界中的“±1”指该字段原始整数精度的一个最小单位，不是页面显示的一美元或一美分。
- 每个数据集独立 snapshot/revert 或使用独立账户，避免前序 OI、仓位和订单污染。
- 每条写链用例至少核对 payload、Order 事件、Order Store、Reader、Position/OI、余额差分和页面最终状态。

### 2.5 条件订单 `Frozen` 原子用例约束

`Frozen` 是“订单已进入执行校验，但当前业务状态无法履约”的持久状态，不是所有 Keeper 执行失败的通用结果。本约束适用于 `LimitIncrease`、`StopIncrease`、`LimitDecrease` 和 `StopLossDecrease`；`MarketIncrease` / `MarketDecrease` 遇到同类业务错误时终态为 `Cancelled`，不得写成 `Frozen`。

| 执行分类 | 必须单独设计的原子用例 | 期望状态 |
|---|---|---|
| 未进入履约校验 | 触发价未达到；`validFromTime` 未到 | 执行交易 revert，订单保持 `Open` |
| 系统性不可执行 | Oracle 错误、`DisabledFeature`、回调/取消/自动取消 Gas 不足 | 执行交易 revert，订单保持 `Open` |
| 增仓容量在等待期间变化 | `MAX_POSITION_SIZE_USD`、`MAX_OPEN_INTEREST`、`MAX_OPEN_INTEREST_FACTOR`、`RESERVE_FACTOR` 分别设计；创建时可履约，触发前由其他仓位或同一目标仓位占用余量 | 触发后为 `Frozen` |
| 增仓履约条件在等待期间变化 | acceptablePrice 失效、抵押不足、费用/Funding 导致仓位风险校验失败、市场被禁用 | 触发后为 `Frozen` |
| 减仓履约条件在等待期间变化 | acceptablePrice 失效、实际输出低于 `minOutputAmount`、无法支付费用/亏损、取出抵押后不安全、剩余仓位风险校验失败、市场被禁用 | 触发后为 `Frozen` |
| 非市价订单的明确例外 | `EmptyPosition`、`EmptyOrder`、`InvalidKeeperForFrozenOrder`、`UnsupportedOrderType` | 执行交易 revert，订单保持原状态 |
| 结构性无效 | `InvalidPositionMarket`、`InvalidCollateralTokenForMarket`、`InvalidPositionSizeValues` | `Cancelled` |

凡功能存在“创建态—等待态—执行态”的时间间隔，必须对每一个适用的 `Frozen` 原因单独登记“触发时执行失败”用例；不得用一条“容量不足”或“订单冻结”合并不同公式和错误来源。恢复流程可使用一个代表性 `Frozen` 原因，但必须拆成三条独立用例：容量/条件恢复后由 `FROZEN_ORDER_KEEPER` 重试、用户更新订单解除冻结并补足 Execution Fee、用户取消冻结订单。

每条 `Frozen` 原子用例统一核对：

- 订单仍存在且 `isFrozen=true`，`executionFee=0`；产生 `OrderFrozen`，`reasonBytes` 能解析为本用例的精确 error selector 和参数，不产生 `OrderExecuted` / `OrderCancelled`。
- 执行子调用的 Position、OI、Funding 和余额变化全部回滚；增仓订单的初始抵押仍留在 OrderVault；`autoCancel` 关联不得被误清理。
- 前端在约定的同步时间内，在同一笔 orderKey 上显示“已冻结”、本地化的准确原因、实际冻结时间和冻结交易；刷新后状态不回退，不得误用创建交易/创建时间代替冻结证据。
- 前端只显示实际可用的更新/取消动作，不得显示为已成交或已取消；若提供更新入口，必须重新计算并允许补足已在冻结时归零的 Execution Fee；不得显示实际不存在的用户重试入口。

## 3. 开仓、关仓、仓位调整、主单取消及开仓后保护链路（155 条原子用例）

### A. 部署与交易基线（9 条原子用例）

> **目的**：本节不是验证交易盈亏，而是确认后续开仓用例使用了“同一条链、同一套 v0.3.2 合约、正确市场、正确参数和可工作的 Oracle/Keeper”。**暂停规则（2026-09-02）**：CT-BASE-001～008 任一 P0 失败时，暂停 B 节合约层执行；XT-BASE-009 失败只暂停 B 节前端层核对点，不影响合约层。否则后续失败可能只是环境或接入错误，不能归因于 Trade 功能。
>
> **执行环境（2026-09-02）**：A、B 两节全部在 tx-fork（chainId 99911，须为 v0.3.2 部署；准入以 `CURRENT.primary.admissionScope["tx-fork:contract"]` 实时值为准；恢复标准 = 本矩阵 A 节 CT-BASE-001～008 正式批次全 PASS）用 Mock token 执行：抵押品 Mock USDC（6 位），index token 为 market 1 合成 BTC 与 market 2 WETH，**两者在链上与前端 SDK 口径下均为 18 位**（2026-09-02 核实：SDK `tokens.ts` 与合约仓 `configureOracle.ts` 一致；TestCode 部署清单标的 8 位是登记错误，已交测试看板 session 修正）。8 位精度用例需要另建 8 位 index token 的 Mock 市场，未建前相关行记 BLOCKED。其余用例不依赖精确控价与时间推进；触发类用例用 tx-fork 的 Mock Oracle 推价（min=max）即可，不需要 oracle-fork。
>
> **复用条件**：CT-BASE-001～008 与准入读检是同一件事。deploymentId、manifest 与参数快照 block 不变时，直接引用 `TestCase/config/v0.3.2/tx-fork/260902/`（`read-checks-live-2026-09-02.md`、`read-checks-offline-2026-09-02.md`、`verification/`）作为本轮结果；换部署、换 fork 或参数被改写后才重跑。
>
> **地址约定**：只读基线行不占测试账户；需要账户的行直接使用对应测试用例“测试账户 / 地址”列中分配的地址。地址来自 `TestCode/config/noise-traders.json` 花名册，执行时按用例需要注资 ETH、抵押品并设置 allowance；用例之间通过独立账户或 snapshot/revert 隔离，不依赖其他测试体系的账号占用情况。
>
> **取值原则**：部署地址、marketIndex、symbol、token/Oracle 地址、decimals、角色和参数均以本次 `CURRENT` 部署清单及链上实时读取结果为准。`ETH-USD` 仅作为当前主测市场示例；Mock 市场不预设为 BTC，Mock Token 也不预设 8 位或 18 位精度。
>
> **基线证据**：每次执行保留 chainId、fork/source block、部署版本或 commit、合约地址及 code hash、市场元数据、关键参数快照、Oracle 价格与时间戳、角色检查结果和前端实际请求地址。上述证据应关联到本轮测试记录，便于复现。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|
| CT-BASE-001 / P0 | 环境身份_当前测试端点_确认为目标 v0.3.2 环境 | 合约层 | RPC 只读 | v0.3.2 tx-fork 已启动；可访问 RPC 与 CURRENT 部署清单 | RPC URL（脱敏）；预期 chainId；fork/source block；部署版本或 commit | 读取 RPC chainId、最新区块、fork/source block；与 CURRENT 和本轮测试记录比对 | chainId、block number/hash、fork/source block、版本标识、RPC 响应时间 | 所有身份信息与本轮目标环境一致；区块可读且未连接到错误网络或过期 fork；测试记录保存实际值 | 环境有效性；准入门禁 | 不占（只读） |
| CT-BASE-002 / P0 | 核心合约_CURRENT 地址清单_地址存在代码且版本一致 | 合约层 | RPC 只读 | CT-BASE-001 通过 | CURRENT 中 Trade/Order/Reader/DataStore/Oracle/Vault/Relay 等本轮依赖地址 | 逐一读取地址 code、必要的版本/接口探针和代理 implementation；比对部署证据 | address、bytecode/code hash、implementation、ABI/接口返回、零地址检查 | 必需地址均非零且存在代码；代理实现和 v0.3.2 部署证据一致；Reader/写合约接口可调用；不得混用旧版本地址 | 配置一致性；错误环境防护 | 不占（只读） |
| CT-BASE-003 / P0 | 主测市场_运行时市场元数据_市场、Token 与 Oracle 映射正确 | 合约层 | RPC 只读 | CT-BASE-002 通过 | 从 CURRENT 选择主测市场；不在用例中写死 marketIndex | 从部署清单、DataStore/Reader 分别读取 marketIndex、symbol、indexToken、collateralToken、Oracle | 两来源的 marketIndex、symbol、token/Oracle 地址、enabled/active 状态 | 清单与链上结果完全一致；市场可交易且必填地址非零；不存在同 index/symbol 指向不同市场的冲突 | 配置一致性检查+多源交叉验证（部署清单、DataStore、Reader） | 不占（只读） |
| CT-BASE-004 / P0 | 模拟市场（Mock）_Token 类型与 decimals 运行时读取_不依赖 MOCK-BTC 或固定精度 | 合约层 | RPC 只读 | 已部署至少一个 Mock 合成市场 | CURRENT 登记的 Mock 市场；实际 symbol、indexToken、Oracle、decimals | 读取 Mock 市场元数据及 ERC20 decimals；用 `1 token` 和最小原始单位做显示/原始值互转 | symbol、indexToken、Oracle、decimals、格式化值、原始整数值 | Mock Token 可为任意资产名称；8 位、18 位或其他合法 decimals 均按实际值换算；不得出现 BTC/1e8/1e18 硬编码 | 等价类+精度边界（实际 symbol、token、Oracle、decimals） | 不占（只读） |
| CT-BASE-005 / P0 | 抵押品链路_全局、市场、Vault 与账户资产_Token 和精度一致 | 合约层 | RPC 只读 | 市场元数据已确认；测试账户已准备 | 全局 COLLATERAL_TOKEN；市场 collateralToken；Vault asset；测试账户 = T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（基线探针，注资后读余额/allowance） | 读取四处 Token 地址与 decimals；检查账户余额、allowance 和 Vault 可用性 | Token 地址、decimals、balance、allowance、Vault balance/asset | 全局、市场和 Vault 使用预期抵押品；精度一致；账户余额与授权足以执行最小开仓及费用；无零地址或资产串用 | 数据一致性；B32-3 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（探针） |
| CT-BASE-006 / P0 | 开仓参数_杠杆、最低抵押、费用与容量_参数完整且边界自洽 | 合约层 | RPC 只读 | DataStore/配置 Reader 可读 | long、short；主测与 Mock 市场 | 逐市场、逐方向读取 `L_min`、`L_max`、最低抵押相关参数、Position Fee、UI Fee、Execution Fee、Relay Fee、OI/Reserve cap、MIN/MAX_DYNAMIC_SPREAD | 参数 key、原始值、精度、作用域、读取角色、方向差异 | 必需参数均可解析且不是漏配默认值；`L_min>0`、`L_min≤L_max`、`MIN≤MAX`；费用和容量允许构造 B 节普通及边界数据 | 参数组合检查；B32-1-01 | 不占（只读） |
| CT-BASE-007 / P0 | 预言机（Oracle）基线_市场价格与时间戳_价格有效、未过期且映射正确 | 合约层 | RPC 只读 | Oracle 服务/Mock Oracle 可用 | 主测市场和 Mock 市场；各自 index/collateral token | 读取 Oracle 地址、价格 min/max、decimals、更新时间；必要时由授权角色更新一次价格并再次读取 | Oracle 地址、token 映射、min/max、timestamp/age、更新前后值、事件 | Oracle 与市场映射一致；价格非零且 `min≤max`；时间戳在允许窗口内；授权更新可被 Reader/交易链路读取，两个市场不串价 | 有效等价类+时效边界 | 不占（只读） |
| CT-BASE-008 / P0 | 执行权限_Trader、Keeper 与配置角色_各角色只能执行授权动作 | 合约层 | Admin RPC + RPC | tx-fork 角色现状（`read-checks-offline-2026-09-02.md`）：ORDER/LIQUIDATION/ADL Keeper 同为 `0x542fe841228416F6a3D65eb534a90Ebc5cc36354`；CONTROLLER、CONFIG_KEEPER、LIMITED_CONFIG_KEEPER、MARKET_KEEPER 同为 E2E 管理账户 `0xf82CF35C5c0019861c2cDC65041C8Ac32970D403`，因此链上没有“只有 LIMITED”的账户。夹具：在 snapshot 内由 DEFAULT_ADMIN `0x4b32f9bC794B0298b9373cf0d67E36162A2e9247`（Admin RPC 免签名）给 T89 授予**仅** LIMITED_CONFIG_KEEPER 作受限反例；无权限账户 = T100 | 合法角色：Keeper `0x542f…`、CONFIG_KEEPER `0xf82c…`；LIMITED 夹具 T89 `0xed66c8C7B589CEC33e0850ADeB6342B6e2B29162`；无权限 T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`；受限动作：`Config.setInt(MIN_DYNAMIC_SPREAD, …)`（CONFIG 专属键）、`OrderHandler.executeOrder` | 查询角色；合法 Keeper 做只读/最小执行探针；LIMITED 夹具与无权限账户分别尝试受限动作 | role membership、caller、revert selector 与参数、交易回执、状态变化 | 合法角色具备本轮所需能力；无权限账户调 `Config.setInt` → `Unauthorized(msg.sender, "LIMITED / CONFIG KEEPER")`；LIMITED 夹具调 CONFIG 专属键 → `InvalidBaseKey(baseKey)`；非 Keeper 调 `executeOrder` → 对应 Unauthorized；以上均状态不变（与 UT-B32-01-08～11 同口径）；不存在因角色漏配导致订单永久无法执行 | 权限等价类+反向验证；UT-B32-01-08～11 | 合法角色按 manifest；LIMITED 夹具 T89 `0xed66c8C7B589CEC33e0850ADeB6342B6e2B29162`；无权限 T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB` |
| XT-BASE-009 / P0 | 前端接入_页面、SDK、Reader 与链上基线_使用同一环境和市场配置 | 合约层 + 前端层 | RPC + 页面 | CT-BASE-001～008 通过；前端已指向本轮环境（本地 :3010 + fork 补丁；前端 commit 基于 v0.3.2 ABI/SDK，即 06 §5 C-2 前端项，等 `tx-fork:frontend` 准入）；页面钱包 = T100 探针地址。本行失败只暂停 B 节前端层核对点 | 主测市场与 Mock 市场各一组 | 打开 Trade 页面并切换市场；记录页面/SDK 请求的 chainId、合约和市场参数；与链上快照比对 | chainId、RPC、合约地址、marketIndex、symbol、token/Oracle 地址、decimals、价格、关键开仓参数 | 页面、SDK、Reader 与链上基线完全一致；切换市场后无旧市场字段残留；Mock Token 正确显示实际名称与精度；不使用历史缓存或旧 ABI | 端到端配置一致性（页面、SDK、Reader、链上四方核对） | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |

### B. 开仓、关仓、仓位调整与主单取消（107 条原子用例）

> 执行前从当前市场配置读取：最小允许杠杆 `L_min`、最大允许杠杆 `L_max`、`MIN_COLLATERAL_USD`、`MIN_COLLATERAL_FACTOR`、Position Fee、UI Fee、Execution Fee、抵押品价格与 decimals。对固定 size 计算本次开仓的有效最小抵押 `C_eff`；`C_eff` 必须同时满足固定最低抵押、最低保证金率、费用和精度取整。不得把某次部署示例值写死为长期需求。

`C_eff` 的测试数据准备口径：先按 `max(MIN_COLLATERAL_USD, sizeInUsd × MIN_COLLATERAL_FACTOR)` 得到费用前最低有效抵押 USD，再加入开仓后会从抵押中承担的 Position Fee、UI Fee、Funding Fee 等成本，最后按 collateral price 和 token decimals 向风险侧取整。该值只是链下造数结果，执行时仍以合约 `remainingCollateralUsd` 与 collateral sufficiency 校验为最终权威。Execution Fee、Relay Fee 若从钱包单独支付，只进入总 Pay/余额校验，不混入仓位 collateral。

**原子与分层执行约定（2026-09-02）**

- 一行 = 一个链上动作的原子。同一原子带三组核对点：合约层（Order/Position/OI/账本/事件/公式）、前端层（表单、预览、签名弹窗、Toast、仓位行）、交叉一致（页面提交参数 = OrderCreated = Reader = 余额差分）。原子身份用 orderKey；同一原子同一数据集的前端层证据后补时挂到同一条记录，不重跑合约层。
- 入口二选一：`RPC`（私钥签名 createOrder + Inline Keeper 执行）或 `页面`。RPC 入口只能把合约层记 PASS，前端层记 GAP，交叉一致不判；只有页面入口能记前端层与交叉一致。ID 前缀 XT/FT 表示该行设计的完整核对面，不表示本轮必须走页面。
- 先跑合约层：本轮所有 XT 行先以 RPC 入口在 tx-fork 执行；FT 行与 XT 行的前端层等 `tx-fork:frontend` 准入（06 §5 C-2）后按同一原子补跑。
- 环境：tx-fork Mock token；两个现有市场的 index token 均为 18 位口径，18 位精度行用 market 2，8 位精度行 BLOCKED 待 8 位 Mock 市场；其余行默认主测市场 market 2；触发类行用 Mock Oracle 推价（min=max），不需要 oracle-fork；只有 XT-LMT-TIME-012 涉及时间。
- 地址：每条写链用例分配一个独立测试账户（见 B1～B4 用例表）；只做表单校验、不创建订单的前端用例可共用前端探针账户。执行前按用例要求注资并设置 allowance，数据集之间使用 snapshot/revert；账号映射以本矩阵用例 ID 为键，不复用或依赖其他测试体系的编号。
- §4 的 XT-ORD-MI-001/002、XT-ORD-LI-001/002 与本节 XT-MKT-OPEN-001/002、XT-LMT-OPEN-001/002、XT-LMT-TRG-007/008、XT-LMT-ACC-015 是同一动作：§4 行引用本节原子与数据集，不单独执行，一次执行两处断言。
- 第二批（需新增驱动）：XT-LMT-UPD-013（updateOrder）、XT-LMT-CAN-014（cancelOrder）；含 Relay family 数据集的行（FT-MKT-COL-012、XT-LMT-CAN-014）分别拆出 direct Relay 与 1CT 组，等待 Relay 驱动与 `tx-fork:frontend` 准入。Standard/Relay/1CT 对比、方向、TP/SL 与费用不足组合统一在 C 节专项设计，不在 B 节重复。

**开仓与关仓成对执行约定**：市价关仓是被测功能，不是环境清理。每条成功开仓用例的自动化必须继续执行对应市价关仓；开仓与关仓分别保留独立 ID、测试标题和断言，但共享同一次自动化、同一测试账户、同一 snapshot 及前后态证据。只有开仓和对应市价关仓都通过，整条自动化流程才通过。

- B1 将每条成功的市价开仓与一条显式 `XT/FT-MKT-CLOSE-*` 用例配对。市价关仓用例核对 Est. Receive（页面入口）、`MarketDecrease` 创建与执行、Position 删除、对应方向 OI 回落、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、适用时的 Relay Fee，以及钱包/Vault 余额差分。
- 带 TP/SL 的配对市价关仓用例必须在关仓前确认保护单为 `Open`，并在全平后确认所有保护单同流程自动取消、`autoCancel` 列表清空、退款正确且 Keeper 不能再次执行残单。
- B3/C 中的“市价关仓标准操作”与“市价关仓并校验保护单自动取消”均是被测功能阶段，不是测试清理；断言口径与 B1 的显式市价关仓用例一致。
- “取消订单标准操作”用于尚未触发的条件单：取消订单并核对 Store 删除、抵押品/Execution Fee 退款、无仓位与无残单。
- “无关仓动作”用于提交前拦截、执行失败或无仓位数据：不提交 `MarketDecrease`，仅确认 Position、OI、Order 与余额没有非预期变化。

TP/SL 价格校验中的 `P_ref` 是页面本次校验与构造 payload 使用的同一笔新鲜 Oracle 参考价；每个数据集必须记录其原始精度值，不能用截图中的舍入显示值代替。

B1.1 中的非法 TP/SL 组合已拆为独立原子用例；每行只验证一个非法方向或一个等号边界，不再用一条结果覆盖多组不同失败原因。

**Cap 前端校验与 Frozen 的职责边界**：市价开仓/加仓表单在打开时 Cap 已不足，或本次输入直接越过 Cap，归入 B1.1/B2.1，验证明确提示、CTA 禁用、无钱包/Relay 请求且无订单；调整杠杆归入 B2.2，验证 Cap 对最大可选杠杆的限幅、零值语义和已知提示 GAP。订单创建时容量充足、等待执行期间被其他仓位占用，归入 §5 E 的条件订单 `Frozen` 用例。两类用例验证的时间点、状态迁移和用户反馈不同，不得互相替代。

#### B1. 市价开仓与市价关仓（`MarketIncrease` / `MarketDecrease`；55 条原子用例）

##### B1.1 市价开仓（`MarketIncrease`；38 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-MKT-OPEN-001 / P0 | 市价开仓_long_建立一笔多仓 | 合约层 + 前端层 | RPC + 页面 | 无仓位；余额、Oracle、Keeper 正常 | long；普通 size；中间杠杆；不带 TP/SL | 选择市价订单和 `long`；填写普通 size 与中间杠杆；不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 页面预览、type=0、isLong、size、collateral、entryPrice、费用明细、OI、余额 | 建立一笔多仓；无 TP/SL 保护单；页面、Order、Reader、账本一致 | XT-MKT-CLOSE-001 | 场景法（市价开仓 long 主流程） | T9 `0x2fd6F79c404B17d68FDc111f8EF8C93891682624` |
| XT-MKT-OPEN-002 / P0 | 市价开仓_short_建立一笔空仓 | 合约层 + 前端层 | RPC + 页面 | 无仓位；余额、Oracle、Keeper 正常 | short；普通 size；中间杠杆；不带 TP/SL | 选择市价订单和 `short`；填写普通 size 与中间杠杆；不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | type=0、isLong=false、acceptablePrice、Position、OI、余额 | 建立一笔空仓；方向、成交价不利侧和 PnL 符号正确 | XT-MKT-CLOSE-002 | 场景法+方向等价类（市价开仓 short 主流程） | T65 `0xC402eb4e767c5F85e2197a752b2e593171783C71` |
| XT-MKT-LEV-003 / P0 | 市价开仓_long_leverage=L_min_等号允许 | 合约层 + 前端层 | RPC + 页面 | long；L_min 来自前端配置（链上没有最小杠杆参数，2026-09-02 核实：`FX100Keys.sol` 无 LEVERAGE 键，只有 `MIN_COLLATERAL_FACTOR` 约束上限） | leverage=L_min；普通 collateral；无 TP/SL；RPC 入口按 size = collateral × L_min 换算 sizeDelta | 选择市价订单和 `long`；填写 leverage=L_min 且不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 合约层：sizeDelta、collateral、Position、OI；前端层：杠杆输入、强平价、payload | 合约层：链上不拒绝，仓位规模与抵押关系 = 换算值（按精度取整）；前端层：等号允许，无越界钳制造成经济含义变化 | XT-MKT-CLOSE-003 | 最小边界（L_min 规则只在前端层判） | T5 `0xe32CFC8F6AB02AC6F5bC0345dc896072Ef287395` |
| XT-MKT-LEV-004 / P0 | 市价开仓_short_leverage=L_min_等号允许 | 合约层 + 前端层 | RPC + 页面 | short；L_min 来自前端配置（链上无最小杠杆参数，由前端校验） | leverage=L_min；普通 collateral；无 TP/SL | 选择市价订单和 `short`；填写 leverage=L_min 且不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | sizeDelta、collateral、Position、short OI、杠杆输入、强平价方向、payload | 合约层：链上不拒绝，方向和仓位字段正确；前端层：等号允许 | XT-MKT-CLOSE-004 | 最小边界+方向 | T89 `0xed66c8C7B589CEC33e0850ADeB6342B6e2B29162` |
| XT-MKT-LEV-005 / P0 | 市价开仓_long_按市场产品 L_product 封顶_下单表单与市场列表一致 | 合约层 + 前端层 | RPC + 页面 | 至少两个产品上限不同的市场；运行时读取前端 productMaxLeverage 与链上 minCollateralFactor | long；L_product−1/L_product/L_product+1 个输入单位；切换高/低上限市场 | 记录市场列表标称；切市场后分别拖滑杆和手输三点；在 L_product 提交并执行；用 RPC 探测产品上限与合约风险上限之间的值 | 市场 productMaxLeverage、列表/滑杆/手输值、payload size/collateral、minCollateralFactor、Order/Position、可清算性 | 列表和 OrderForm 同显示 L_product；等号允许且仓位不立即可清算；超一输入单位被夹回且不发超额单；直调合约可能接受更高的风险上限，不得把它写成前端产品上限 | XT-MKT-CLOSE-005 | 三点边界+多市场+层次分离；OC-17/20/21 Closed | T33 `0x4715c16a87833f7C00Cf7df8669A34CF84ED162B` |
| XT-MKT-LEV-006 / P0 | 市价开仓_short_按市场产品 L_product 封顶_调整杠杆弹窗与开仓表单一致 | 合约层 + 前端层 | RPC + 页面 | 至少两个产品上限不同的市场；各有 short 仓位；运行时读前端/链上配置 | short；L_product−1/L_product/L_product+1；OrderForm/PositionLeverageDialog；高/低上限市场 | 切换市场并在开仓表单、仓位调杠杆弹窗各用滑杆/手输测三点；在边界提交；查 Order/Position 与清算价 | productMaxLeverage、两个滑杆和输入值、payload、Position leverage、强平价方向、风险校验 | 两个面板及列表对当前 marketIndex 使用同一 L_product；切换市场后立即更新；等号可提交，超一输入单位被夹回；合约 minCollateralFactor 是更底层风险边界，不代替产品封顶 | XT-MKT-CLOSE-006 | 多入口一致+方向+配置漂移；OC-17/20/21 Closed | T90 `0x1928c42d1366d618DB7Fb5d687e83e4e314a18AB` |
| FT-MKT-LEV-007 / P0 | 市价开仓_leverage=L_min−最小输入单位_明确拦截 | 前端层 | 页面 | 页面可用 | leverage=L_min−最小输入单位 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | 输入归一化、错误文案、按钮、签名/交易请求 | 明确拦截；不得静默改为 L_min；不得请求签名或创建订单 | “无关仓动作” | 边界外无效类 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-LEV-008 / P0 | 市价开仓_leverage=L_max+最小输入单位_提交前明确拦截 | 前端层 | 页面 | 页面可用 | leverage=L_max+最小输入单位 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | 输入值、错误文案、按钮、payload、钱包请求、订单列表 | 明确提示超过最大杠杆并禁止提交；不得静默改为 L_max；不请求钱包且不创建订单 | “无关仓动作” | 边界外无效类 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-MKT-COL-009 / P0 | 市价开仓_initialCollateral=C_eff−1 最小原始单位_不得成交 | 合约层 + 前端层 | RPC + 页面 | 固定方向、size、杠杆；已计算 C_eff | initialCollateral=C_eff−1 最小原始单位 | RPC 入口：提交并签名 `MarketIncrease`，由 Keeper 执行后查询 Order、Position、OI 与余额；页面入口：填写同值并点击“提交”，核对字段提示、提交按钮、钱包请求与订单列表 | 合约层：订单终态、取消/冻结原因选择器与参数、抵押退回、Execution Fee 扣减、Position；前端层：页面校验文案、是否请求签名 | 合约层：订单可创建，执行时 `validatePosition` 判 `LiquidatablePosition`（reason `min collateral` 或 `min collateral for leverage`）→ 订单 `Cancelled`，抵押全额退回，Execution Fee 按规则处理，无仓位；前端层：提交前拦截并说明最低抵押/保证金不足，不得请求签名 | “无关仓动作” | 三点边界 P1 | T34 `0xE44B303a37cA7F92a467c56835B5EaC2d78A97Ed` |
| XT-MKT-COL-010 / P0 | 市价开仓_initialCollateral=C_eff_等号按合约精确规则成功 | 合约层 + 前端层 | RPC + 页面 | 固定方向、size、杠杆；已计算 C_eff | initialCollateral=C_eff | 选择市价订单和指定方向；填写 initialCollateral=C_eff；不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 抵押原始值、费用后 remaining collateral、Position | 等号按合约精确规则成功；若费用使其不足，则 C_eff 推导必须已包含该费用 | XT-MKT-CLOSE-010 | 三点边界 P2 | T91 `0x485aDeB62C1FC53d8907d121d09089A25Aba6248` |
| XT-MKT-COL-011 / P0 | 市价开仓_initialCollateral=C_eff+1_成功且不会因页面舍入回落至边界下 | 合约层 + 前端层 | RPC + 页面 | 固定方向、size、杠杆；已计算 C_eff | initialCollateral=C_eff+1 | 选择市价订单和指定方向；填写 initialCollateral=C_eff+1 最小原始单位；不设置 TP/SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 抵押原始值、费用后 remaining collateral、Position | 成功且不会因页面舍入回落至边界下 | XT-MKT-CLOSE-011 | 三点边界 P3 | T92 `0x279034F80D88b6ACa42Ab1D63320b3BF4a1f4F10` |
| FT-MKT-COL-012 / P1 | 市价开仓_点击 Max_填充值不超过可用资金 | 前端层 | 页面 | 钱包余额 B；预留 Execution Fee、Relay Fee、Position Fee 已知 | 点击 Max；Standard 组本轮执行；direct Relay 与 1CT 组等待各自驱动和准入 | 选择市价订单；点击“Max”；核对 Pay；提交并签名；等待订单终态；查询 Position 与余额 | Max 填充值、总 Pay、余额余量、实际扣款 | 填充值不超过可用资金；完整费用后不因余额不足失败；Standard 的 Pay 与实际扣款一致；Relay 两组另按费用上限和 1 USDC 保留规则执行 | FT-MKT-CLOSE-012 | 等价类+资金边界 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-MKT-TP-013 / P0 | 市价开仓_long_仅设置 TP_开仓成功且只生成 TP 订单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | long；仅 TP；TP>P_ref | 选择市价订单和 `long`；仅设置 TP；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 主单+TP 两单字段、type=3、size、triggerPrice、acceptablePrice、autoCancel | 开仓成功；只生成 TP，无 SL；TP 方向、规模和 Reduce-only 语义正确 | XT-MKT-CLOSE-013 | 单条件组合+方向等价类 | T93 `0x65E78AFA02850D9343A1AcAb244810bE4736949f` |
| XT-MKT-SL-014 / P0 | 市价开仓_long_仅设置 SL_开仓成功且只生成 SL 订单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | long；仅 SL；SL<P_ref | 选择市价订单和 `long`；仅设置 SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 主单+SL 两单字段、type=4、size、triggerPrice、acceptablePrice、autoCancel | 开仓成功；只生成 SL，无 TP；SL 方向、规模和保护参数正确 | XT-MKT-CLOSE-014 | 单条件组合+方向等价类 | T94 `0xb5d77121327deb6a096D627202C4bCBE23998ADC` |
| XT-MKT-TPSL-015 / P0 | 市价开仓_long_同时设置 TP 和 SL_开仓成功且生成两笔 TP/SL 保护单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | long；TP>P_ref；SL<P_ref | 选择市价订单和 `long`；同时设置 TP 和 SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 三笔 orderKey、关联关系、type=3/4、size、触发方向、autoCancel | 主单、TP、SL 字段和关联关系正确；两笔保护单保持 `Open` | XT-MKT-CLOSE-015 | 条件组合+状态迁移 | T15 `0x772e4264b1CD75CbdDd5C4E37b6a58Bb34E2cc46` |
| XT-MKT-TPSL-016 / P0 | 市价开仓_short_同时设置 TP 和 SL_开仓成功且生成两笔方向正确的 TP/SL 保护单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | short；TP<P_ref；SL>P_ref | 选择市价订单和 `short`；同时设置 TP 和 SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 三笔 orderKey、short 方向、type=3/4、acceptablePrice 哨兵、autoCancel | 主单、TP、SL 字段和关联关系正确；short 触发方向不得复用 long 条件 | XT-MKT-CLOSE-016 | 方向组合+状态迁移 | T66 `0x2b5E7a2D0d15Adf9Bf5227fCEe3fe0F9EC6AaBb4` |
| XT-MKT-TP-020 / P0 | 市价开仓_short_仅设置 TP_开仓成功且只生成 TP 订单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | short；仅 TP；TP<P_ref | 选择市价订单和 `short`；仅设置 TP；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 主单+TP 两单字段、type=3、short 触发方向、size、autoCancel | 开仓成功；只生成 TP，无 SL；TP 按 short 有利方向配置 | XT-MKT-CLOSE-020 | 单条件组合+方向等价类 | T54 `0x627B1355AEBbE39C6a5774F3c5C57Ff88A106642` |
| XT-MKT-SL-022 / P0 | 市价开仓_short_仅设置 SL_开仓成功且只生成 SL 订单 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | short；仅 SL；SL>P_ref | 选择市价订单和 `short`；仅设置 SL；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 主单+SL 两单字段、type=4、short 触发方向、acceptablePrice、autoCancel | 开仓成功；只生成 SL，无 TP；SL 按 short 不利方向配置 | XT-MKT-CLOSE-022 | 单条件组合+方向等价类 | T55 `0x2C60805D6485fE3ff7a744C9b137DD1E6d69a417` |
| FT-MKT-TP-023 / P0 | 市价开仓_long_仅设置反向 TP（TP<P_ref）_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | long；仅 TP；TP=P_ref−Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | TP 字段错误、按钮、payload、钱包请求、订单列表 | TP 字段明确提示 long TP 必须高于参考价；不构造 payload、不请求钱包、不创建订单 | “无关仓动作” | 无效等价类+方向判定 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-SL-024 / P0 | 市价开仓_long_仅设置反向 SL（SL>P_ref）_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | long；仅 SL；SL=P_ref+Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | SL 字段错误、按钮、payload、钱包请求、订单列表 | SL 字段明确提示 long SL 必须低于参考价；不构造 payload、不请求钱包、不创建订单 | “无关仓动作” | 无效等价类+方向判定 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-TP-025 / P0 | 市价开仓_short_仅设置反向 TP（TP>P_ref）_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | short；仅 TP；TP=P_ref+Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | TP 字段错误、按钮、payload、钱包请求、订单列表 | TP 字段明确提示 short TP 必须低于参考价；不构造 payload、不请求钱包、不创建订单 | “无关仓动作” | 无效等价类+方向判定 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-SL-026 / P0 | 市价开仓_short_仅设置反向 SL（SL<P_ref）_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | short；仅 SL；SL=P_ref−Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | SL 字段错误、按钮、payload、钱包请求、订单列表 | SL 字段明确提示 short SL 必须高于参考价；不构造 payload、不请求钱包、不创建订单 | “无关仓动作” | 无效等价类+方向判定 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-TP-027 / P0 | 市价开仓_long_TP=P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | long；仅 TP；TP=P_ref 原始精度值 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | TP 原始值、参考价原始值、字段错误、payload、钱包请求 | 等号不属于合法 long TP；提交前拦截且不得因显示舍入误判为大于 | “无关仓动作” | 等号边界 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-SL-028 / P0 | 市价开仓_long_SL=P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | long；仅 SL；SL=P_ref 原始精度值 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | SL 原始值、参考价原始值、字段错误、payload、钱包请求 | 等号不属于合法 long SL；提交前拦截且不得因显示舍入误判为小于 | “无关仓动作” | 等号边界 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-TP-029 / P0 | 市价开仓_short_TP=P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | short；仅 TP；TP=P_ref 原始精度值 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | TP 原始值、参考价原始值、字段错误、payload、钱包请求 | 等号不属于合法 short TP；提交前拦截且不得因显示舍入误判为小于 | “无关仓动作” | 等号边界+方向等价类 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-SL-030 / P0 | 市价开仓_short_SL=P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | short；仅 SL；SL=P_ref 原始精度值 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | SL 原始值、参考价原始值、字段错误、payload、钱包请求 | 等号不属于合法 short SL；提交前拦截且不得因显示舍入误判为大于 | “无关仓动作” | 等号边界+方向等价类 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-TPSL-031 / P0 | 市价开仓_long_TP=SL 且共同高于 P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | long；TP=SL=P_ref+Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | 两字段值、SL 方向错误、组合错误、payload、钱包请求 | 明确指出 SL 不在 long 合法区间或 TP/SL 区间冲突；不得静默换型、请求钱包或创建订单 | “无关仓动作” | 无效组合+判定表 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-TPSL-032 / P0 | 市价开仓_short_TP=SL 且共同低于 P_ref_提交前拦截 | 前端层 | 页面 | 页面可用；Oracle 参考价新鲜 | short；TP=SL=P_ref−Δ | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | 两字段值、SL 方向错误、组合错误、payload、钱包请求 | 明确指出 SL 不在 short 合法区间或 TP/SL 区间冲突；不得静默换型、请求钱包或创建订单 | “无关仓动作” | 无效组合+判定表 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-MKT-PREC-018 / P1 | 市价开仓_8 位 Mock Token_最小可表示 token size 与再加 1 单位换算正确 | 合约层 + 前端层 | RPC + 页面 | 需要 index token 为 8 位的 Mock 市场；tx-fork 现有 market 1 合成 BTC 实为 18 位口径（2026-09-02 核实，04 §7-6 已裁决），未部署 8 位市场前本行 BLOCKED | 8 位 Mock Token；最小可表示 token size、再加 1 原始单位 | 选择市价订单和 Token 模式；填写最小可表示 token size 与再加 1 原始单位；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 输入、sizeDelta、isSizeDeltaUsd=false、Reader token size | 不丢失或放大精度；页面与链上原始值一致；开仓 BLOCKED 时配对市价关仓同为 `BLOCKED` | XT-MKT-CLOSE-018 | 精度边界 | T52 `0x1647744222a8FB9e03AA751Ac7da4D05F4fA1161` |
| XT-MKT-PREC-019 / P1 | 市价开仓_18 位 Mock Token_最小可表示 token size 与再加 1 wei 换算正确 | 合约层 + 前端层 | RPC + 页面 | tx-fork market 2（WETH，18 位，decimals 运行时读取） | 18 位 Mock Token；最小可表示 token size、再加 1 wei | 选择市价订单和 Token 模式；填写最小可表示 token size 与再加 1 wei；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | 输入、sizeDelta、isSizeDeltaUsd=false、Reader token size | 18 位换算正确；不得复用 8 位假设 | XT-MKT-CLOSE-019 | 精度等价类+最小单位边界 | T32 `0x8Eb46e63424439E4a5CcA7b845d5F6ba9aedE4fC` |
| FT-MKT-CAP-033 / P0 | 市价开仓_long_MAX_POSITION_SIZE_USD 边界_C 可输入且 C+0.01 USD 提示超过最大仓位 | 前端层 | 页面 + Admin RPC + RPC 只读 | 无仓位；将单仓上限 C 配置为两位小数；同方向 OI/Reserve 容量与杠杆充足 | long；size=C、C+0.01 USD；不带 TP/SL | 每个数据点独立打开市价开仓表单；填写 size；读取按钮、明细提示和页面计算的 nextSize；C+0.01 时尝试点击提交；查询钱包请求、payload 和订单列表 | C、nextSize、`MAX_POSITION_SIZE`、主提示、明细提示、CTA 状态、钱包/Relay 请求、Order | C 点不出现 Cap 错误且 CTA 可用（本行不实际提交）；C+0.01 主提示为“超过最大仓位”、明细为“仓位超过市场上限”；CTA 禁用，不请求钱包/签名且不创建订单 | “无关仓动作” | 两点边界+前端错误语义；页面 USD 输入精度为 0.01；`nextSizeUsd > maxPositionSizeUsd` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-034 / P0 | 市价开仓_short_MAX_OPEN_INTEREST 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 硬 OI 上限为当前唯一最小容量来源；将页面可用余量 H 配置为两位小数且 H>0；软 OI、Reserve 和单仓上限充足 | short；H=`MAX_OPEN_INTEREST−short cumulativeOpenCosts`；size=H、H+0.01 USD | 每个数据点独立填写市价开空 size；读取 Reader 容量与表单提示；H+0.01 时尝试提交；查询钱包请求、payload 和订单列表 | hardCapUsd、short OI、H、`INSUFFICIENT_MARKET_CAPACITY`、主/明细提示、CTA、钱包/Relay 请求、Order | H 点不出现容量错误且 CTA 可用（不提交）；H+0.01 主提示为“流动性不足”，明细为“可用流动性不足，最多可开 $H”（H 按两位小数格式化）；CTA 禁用，不发单 | “无关仓动作” | 两点边界+公式隔离；页面 USD 输入精度为 0.01；`MAX_OPEN_INTEREST` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-035 / P0 | 市价开仓_long_MAX_OPEN_INTEREST_FACTOR 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 软 OI 比例上限为当前唯一最小容量来源；将页面可用余量 H 配置为两位小数且 H>0；硬 OI、Reserve 和单仓上限充足 | long；H=`poolUsd×MAX_OPEN_INTEREST_FACTOR−long cumulativeOpenCosts`；size=H、H+0.01 USD | 每个数据点独立填写市价开多 size；读取 poolUsd、factor、Reader 容量和表单提示；H+0.01 时尝试提交；查询钱包请求、payload 和订单列表 | poolUsd、factor、long OI、hardCapUsd、主/明细提示、CTA、钱包/Relay 请求、Order | H 点无容量错误且 CTA 可用（不提交）；H+0.01 显示“流动性不足”与“可用流动性不足，最多可开 $H”；CTA 禁用，不发单 | “无关仓动作” | 两点边界+公式隔离；页面 USD 输入精度为 0.01；`MAX_OPEN_INTEREST_FACTOR` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-036 / P0 | 市价开仓_short_RESERVE_FACTOR 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 市场组合 Reserve 上限为当前唯一最小容量来源；将页面可用余量 H 配置为两位小数且 H>0；硬/软 OI 和单仓上限充足 | short；H=前端/Reader 基于 `poolUsd×RESERVE_FACTOR−totalNominalOI` 得到的可增规模；size=H、H+0.01 USD | 每个数据点独立填写市价开空 size；读取双向 OI、indexPrice.max、poolUsd、Reader 容量和表单提示；H+0.01 时尝试提交 | totalNominalOI、maxAllowed、availableShortUsd、hardCapUsd、主/明细提示、CTA、钱包/Relay 请求、Order | H 点无容量错误且 CTA 可用（不提交）；H+0.01 显示“流动性不足”与“可用流动性不足，最多可开 $H”；CTA 禁用，不发单 | “无关仓动作” | 两点边界+组合容量；页面 USD 输入精度为 0.01；`RESERVE_FACTOR` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-037 / P0 | 市价开仓_long_MAX_POSITION_SIZE_USD=0_页面不限制单仓规模 | 前端层 | 页面 + Admin RPC + RPC 只读 | 无仓位；将 `MAX_POSITION_SIZE_USD` 设为 0；三种聚合容量、抵押、杠杆、余额与费用均充足 | long；普通合法 size=S>0；不带 TP/SL | 打开市价开仓表单；填写 S；核对校验结果、CTA、预览与 payload 草稿；本行不实际提交 | `MAX_POSITION_SIZE_USD`、S、`MAX_POSITION_SIZE`、主/明细提示、CTA、payload 草稿 | 0 表示不限制单仓规模；不出现“超过最大仓位”或“仓位超过市场上限”；其他校验满足时 CTA 可用且 payload 中 size=S | “无关仓动作” | 零值语义+有效等价类；`MAX_POSITION_SIZE_USD=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-038 / P0 | 市价开仓_long_MAX_OPEN_INTEREST=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 无仓位；当前 long 的 `MAX_OPEN_INTEREST=0`；软 OI、Reserve 和单仓上限足够高；其他表单校验通过 | long；任意合法 size=S>0；不带 TP/SL | 打开市价开仓表单；填写 S；读取 Reader availableLongUsd 与页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | `MAX_OPEN_INTEREST`、availableLongUsd、hardCapUsd、CTA、详情提示、钱包/Relay 请求、Order | 0 表示当前方向容量为 0；CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；CTA 禁用，不请求钱包/签名且不创建订单 | “无关仓动作” | 零值语义+公式隔离；`MAX_OPEN_INTEREST=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-039 / P0 | 市价开仓_short_MAX_OPEN_INTEREST_FACTOR=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 无仓位；当前 short 的 `MAX_OPEN_INTEREST_FACTOR=0`；硬 OI、Reserve 和单仓上限足够高；其他表单校验通过 | short；任意合法 size=S>0；不带 TP/SL | 打开市价开仓表单；填写 S；读取 poolUsd、Reader availableShortUsd 与页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | factor、poolUsd、availableShortUsd、hardCapUsd、CTA、详情提示、钱包/Relay 请求、Order | 比例上限为 0 时容量为 0；CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；CTA 禁用且不创建订单 | “无关仓动作” | 零值语义+公式隔离；`MAX_OPEN_INTEREST_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-CAP-040 / P0 | 市价开仓_long_RESERVE_FACTOR=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 无仓位；`RESERVE_FACTOR=0`；硬/软 OI 和单仓上限足够高；其他表单校验通过 | long；任意合法 size=S>0；不带 TP/SL | 打开市价开仓表单；填写 S；读取 poolUsd、双向 OI、Reader availableLongUsd 与页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | reserveFactor、poolUsd、totalNominalOI、availableLongUsd、hardCapUsd、CTA、详情提示、Order | Reserve 上限为 0 时组合容量为 0；CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；CTA 禁用且不创建订单 | “无关仓动作” | 零值语义+组合容量；`RESERVE_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |

##### B1.2 市价关仓（`MarketDecrease`；17 条原子用例）

> 下表不是再次开仓。每条市价关仓用例直接承接“关联开仓用例”的成功仓位，由同一个自动化脚本继续执行，并分别输出开仓 ID 与市价关仓 ID 的断言结果。若开仓失败或阻塞，对应市价关仓记 `BLOCKED`，不得伪造为 `PASS`。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 关联开仓用例 / 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|
| XT-MKT-CLOSE-001 / P0 | 市价关仓_long_普通杠杆多仓_100% 关仓后仓位全平且账本一致 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-OPEN-001 开仓断言通过；同一 snapshot 中多仓仍为 Open | 100% Position size；当前 Oracle；合法 acceptablePrice | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | type=2、isLong=true、sizeDelta、executionPrice、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、Position、long OI、余额 | `MarketDecrease` 订单成功执行；多仓删除；long OI 按实际关仓规模回落；预览与实际到账在规定舍入内一致；账本守恒 | 关仓基本流+状态迁移 | T9 `0x2fd6F79c404B17d68FDc111f8EF8C93891682624` |
| XT-MKT-CLOSE-002 / P0 | 市价关仓_short_普通杠杆空仓_100% 关仓后仓位全平且账本一致 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-OPEN-002 开仓断言通过；同一 snapshot 中空仓仍为 Open | 100% Position size；当前 Oracle；合法 acceptablePrice | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | type=2、isLong=false、sizeDelta、executionPrice、PnL 符号、Funding Fee、Position Fee、UI Fee、Execution Fee、Position、short OI、余额 | `MarketDecrease` 订单成功执行；空仓删除；short OI 回落；价格方向、PnL、预览和账本均正确 | 关仓基本流+方向等价类+状态迁移 | T65 `0xC402eb4e767c5F85e2197a752b2e593171783C71` |
| XT-MKT-CLOSE-003 / P0 | 市价关仓_long_L_min 杠杆多仓_100% 关仓后仓位全平 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-LEV-003 开仓断言通过；L_min 多仓仍为 Open | Reader 返回的完整仓位规模 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 页面 100% 值、sizeDelta、Position、OI、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额 | 不因最小杠杆或页面取整留下残仓；Position 删除且账本一致 | 杠杆边界后的状态迁移 | T5 `0xe32CFC8F6AB02AC6F5bC0345dc896072Ef287395` |
| XT-MKT-CLOSE-004 / P0 | 市价关仓_short_L_min 杠杆空仓_100% 关仓后仓位全平 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-LEV-004 开仓断言通过；L_min 空仓仍为 Open | Reader 返回的完整仓位规模 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 页面 100% 值、sizeDelta、Position、short OI、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额 | 不因最小杠杆或 short 方向换算留下残仓；Position 删除且账本一致 | 杠杆边界+方向状态迁移 | T89 `0xed66c8C7B589CEC33e0850ADeB6342B6e2B29162` |
| XT-MKT-CLOSE-005 / P0 | 市价关仓_long_L_max 杠杆多仓_100% 关仓后仓位全平 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-LEV-005 开仓断言通过；L_max 多仓未被清算 | Reader 返回的完整仓位规模；价格保持在可正常关仓区间 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 市价关仓 acceptablePrice、executionPrice、Position、long OI、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额 | 高杠杆多仓通过普通市价关仓后全平；不得误走 Liquidation；仓位与 OI 清零 | 高风险边界+状态迁移 | T33 `0x4715c16a87833f7C00Cf7df8669A34CF84ED162B` |
| XT-MKT-CLOSE-006 / P0 | 市价关仓_short_L_max 杠杆空仓_100% 关仓后仓位全平 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-LEV-006 开仓断言通过；L_max 空仓未被清算 | Reader 返回的完整仓位规模；价格保持在可正常关仓区间 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 市价关仓 acceptablePrice、executionPrice、Position、short OI、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额 | 高杠杆空仓通过普通市价关仓后全平；不得误走 Liquidation；仓位与 OI 清零 | 高风险边界+方向状态迁移 | T90 `0x1928c42d1366d618DB7Fb5d687e83e4e314a18AB` |
| XT-MKT-CLOSE-010 / P0 | 市价关仓_initialCollateral=C_eff 的仓位_100% 关仓后仓位全平且无保证金残值 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-COL-010 开仓断言通过；边界保证金仓位仍可正常关仓 | 100% Position size；初始抵押=C_eff | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | remainingCollateral、realized PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、Position、OI、钱包/Vault 余额 | 边界保证金仓位全平；费用瀑布和抵押返还正确；不得留下 dust Position 或未归属余额 | 最小有效抵押边界+资金守恒 | T91 `0x485aDeB62C1FC53d8907d121d09089A25Aba6248` |
| XT-MKT-CLOSE-011 / P0 | 市价关仓_initialCollateral=C_eff+1 的仓位_100% 关仓后仓位全平且最小单位账本正确 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-COL-011 开仓断言通过；仓位仍为 Open | 100% Position size；初始抵押=C_eff+1 最小原始单位 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 原始精度抵押、remainingCollateral、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、Position、OI、余额 | 全平后多出的 1 最小原始单位按公式进入返还或费用；不得被页面舍入吞掉；账本守恒 | 相邻边界+精度守恒 | T92 `0x279034F80D88b6ACa42Ab1D63320b3BF4a1f4F10` |
| FT-MKT-CLOSE-012 / P1 | 市价关仓_Max 开仓形成的仓位_页面 100% 市价关仓可提交且最终状态正确 | 前端层 | 页面 | FT-MKT-COL-012 的标准交易开仓成功；页面已显示仓位 | 100%/Max 市价关仓；沿用开仓方向 | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 输入值、按钮状态、钱包请求、Toast、仓位列表、History、Est. Receive 与实际显示 | 页面使用完整仓位规模提交市价关仓；成功后仓位消失、History 类型为市价关仓；不得因余额预留或舍入导致残仓 | 前端关仓基本流+最大值边界 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-MKT-CLOSE-013 / P0 | 市价关仓_long_仅设置 TP 的仓位_100% 关仓后仓位全平且 TP 自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-TP-013 开仓断言通过；long Position 与 TP 均为 Open；无 SL | 100% Position size；TP orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease` 与 TP 事件、Position、long OI、TP Store/History、autoCancel 列表、Execution Fee 退款、余额 | long 仓位全平；唯一 TP 同流程自动取消；autoCancel 列表清空；TP 退款正确且不能再次执行 | 全平级联取消+状态迁移 | T93 `0x65E78AFA02850D9343A1AcAb244810bE4736949f` |
| XT-MKT-CLOSE-014 / P0 | 市价关仓_long_仅设置 SL 的仓位_100% 关仓后仓位全平且 SL 自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-SL-014 开仓断言通过；long Position 与 SL 均为 Open；无 TP | 100% Position size；SL orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease` 与 SL 事件、Position、long OI、SL Store/History、autoCancel 列表、Execution Fee 退款、余额 | long 仓位全平；唯一 SL 同流程自动取消；autoCancel 列表清空；SL 退款正确且不能再次执行 | 全平级联取消+状态迁移 | T94 `0xb5d77121327deb6a096D627202C4bCBE23998ADC` |
| XT-MKT-CLOSE-015 / P0 | 市价关仓_long_同时设置 TP 和 SL 的仓位_100% 关仓后仓位全平且两笔保护单均自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-TPSL-015 开仓断言通过；long Position、TP、SL 均为 Open | 100% Position size；TP/SL orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease`/TP/SL 事件、Position、long OI、两单 Store/History、autoCancel 列表、两笔退款、余额 | long 仓位全平；TP、SL 均同流程自动取消；列表与 Store 无孤儿单；两单均不能再次执行 | 多保护单级联取消+状态迁移 | T15 `0x772e4264b1CD75CbdDd5C4E37b6a58Bb34E2cc46` |
| XT-MKT-CLOSE-016 / P0 | 市价关仓_short_同时设置 TP 和 SL 的仓位_100% 关仓后仓位全平且两笔保护单均自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-TPSL-016 开仓断言通过；short Position、TP、SL 均为 Open | 100% Position size；TP/SL orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease`/TP/SL 事件、Position、short OI、两单 Store/History、autoCancel 列表、两笔退款、余额 | short 仓位全平；TP、SL 均自动取消；不得因 short 方向漏清保护单；两单均不能再次执行 | 多保护单级联取消+方向状态迁移 | T66 `0x2b5E7a2D0d15Adf9Bf5227fCEe3fe0F9EC6AaBb4` |
| XT-MKT-CLOSE-020 / P0 | 市价关仓_short_仅设置 TP 的仓位_100% 关仓后仓位全平且 TP 自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-TP-020 开仓断言通过；short Position 与 TP 均为 Open；无 SL | 100% Position size；TP orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease` 与 TP 事件、Position、short OI、TP Store/History、autoCancel 列表、Execution Fee 退款、余额 | short 仓位全平；唯一 TP 自动取消；autoCancel 列表清空；TP 不能再次执行 | 全平级联取消+方向状态迁移 | T54 `0x627B1355AEBbE39C6a5774F3c5C57Ff88A106642` |
| XT-MKT-CLOSE-022 / P0 | 市价关仓_short_仅设置 SL 的仓位_100% 关仓后仓位全平且 SL 自动取消 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-SL-022 开仓断言通过；short Position 与 SL 均为 Open；无 TP | 100% Position size；SL orderKey；autoCancel=true | 确认关联 TP/SL 保护单为 `Open`；打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额；查询关联 TP/SL 保护单并重试执行 | `MarketDecrease` 与 SL 事件、Position、short OI、SL Store/History、autoCancel 列表、Execution Fee 退款、余额 | short 仓位全平；唯一 SL 自动取消；autoCancel 列表清空；SL 不能再次执行 | 全平级联取消+方向状态迁移 | T55 `0x2C60805D6485fE3ff7a744C9b137DD1E6d69a417` |
| XT-MKT-CLOSE-018 / P1 | 市价关仓_8 位 Mock Token 仓位_按 Reader 完整原始规模全平且无精度残仓 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-PREC-018 对应数据集开仓成功；8 位 Mock 市场已具备 | Reader 返回的完整 token size；8 位运行时 decimals | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 显示规模、原始 sizeDelta、isSizeDeltaUsd、Position、OI、余额最小单位 | 按运行时 8 位精度将仓位全平；不得因显示舍入少平或多平；无 dust Position | 精度边界+完整关仓归一化 | T52 `0x1647744222a8FB9e03AA751Ac7da4D05F4fA1161` |
| XT-MKT-CLOSE-019 / P1 | 市价关仓_18 位 Mock Token 仓位_按 Reader 完整原始规模全平且无 wei 残仓 | 合约层 + 前端层 | RPC + 页面 | XT-MKT-PREC-019 对应数据集开仓成功；18 位 Mock 市场可用 | Reader 返回的完整 token size；18 位运行时 decimals | 打开关仓面板；选择 100%；核对 Est. Receive；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 显示规模、原始 sizeDelta、isSizeDeltaUsd、Position、OI、余额 wei 差分 | 按运行时 18 位精度将仓位全平；不得复用 8 位换算；无 1 wei 残仓 | 精度边界+完整关仓归一化 | T32 `0x8Eb46e63424439E4a5CcA7b845d5F6ba9aedE4fC` |

#### B2. 市价加仓、杠杆/保证金调整、市价部分减仓与账本（`MarketIncrease` / `MarketDecrease`；34 条原子用例）

> **职责边界**：B2 只验证“已有仓位”上的加仓、提高/降低杠杆、增加保证金、部分减仓及其账本变化。普通 100% 市价关仓由 B1.2 统一覆盖，B2 不重复登记。`CT-MKT-REDUCE-011` 虽然最终全平，但其请求值小于整仓规模，验证的是 v0.3.2 向上取整后自动归一边界，不属于普通关仓重复。
>
> B2 的已有仓位由可复用 fixture 在同一 snapshot 内创建；fixture 只是前置数据，不重复计为 B2 开仓用例。每个数据集完成断言后恢复 snapshot，不为清理而新增重复关仓用例。

##### B2.1 市价加仓（`MarketIncrease`；12 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-MKT-ADD-001 / P0 | 市价加仓_long_不同执行价再次加仓_合并为单一多仓且加权开仓价正确 | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；记录 PositionKey、S1、T1、C1、E1、OI 与余额 | 追加 size S2=S1；执行价 E2>E1；合法 collateral；不带 TP/SL | 将 Mock Oracle 价格更新至 E2；读取市价加仓预览；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | PositionKey、sizeInUsd/sizeInTokens、collateralAmount、entryPrice、increasedAtTime、long OI、Position Fee、UI Fee、Funding Fee、Execution Fee、余额 | PositionKey 不变且只有一笔多仓；newSize=S1+S2；newToken=T1+T2；entryPrice=newSize/newToken；抵押品、OI、费用与余额按增量精确更新；页面刷新为链上结果 | 完成断言后恢复 snapshot | 场景法+公式核对；字段规则 §5、§17.2 | T19 `0x144039294C639C5FE4c361CA0b20dEd8C555a2E2` |
| XT-MKT-ADD-002 / P0 | 市价加仓_short_不同执行价再次加仓_合并为单一空仓且加权开仓价正确 | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；记录 PositionKey、S1、T1、C1、E1、OI 与余额 | 追加 size S2=S1；执行价 E2<E1；合法 collateral；不带 TP/SL | 将 Mock Oracle 价格更新至 E2；读取市价加仓预览；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI 与余额 | PositionKey、sizeInUsd/sizeInTokens、collateralAmount、entryPrice、increasedAtTime、short OI、Position Fee、UI Fee、Funding Fee、Execution Fee、余额 | PositionKey 不变且只有一笔空仓；newSize=S1+S2；newToken=T1+T2；entryPrice=newSize/newToken；short 价格、PnL 与 OI 方向正确；账本按增量精确更新 | 完成断言后恢复 snapshot | 场景法+方向等价类+公式核对；字段规则 §5、§17.2 | T20 `0xED962Cb460996Eb7E4F0E8CC8F0D34ae0016E550` |
| XT-MKT-ADD-003 / P0 | 市价加仓_long_已有 TP 和 SL 保护单_加仓后保护单规模与清算保护期均不重置 | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位及 `Open` TP/SL；两单 `autoCancel=true`；记录 orderKey、sizeDelta、graceStart/graceEnd | 追加 size=原仓位 50%；TP>P_ref；SL<P_ref；Oracle 不触发保护单 | 读取市价加仓预览；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与 TP/SL 保护单 | PositionKey、新旧仓位规模、TP/SL orderKey/sizeDelta/状态、`autoCancel` 列表、graceStart/graceEnd、页面 | 仓位正确合并；TP/SL 保护单仍为 `Open`，orderKey 与 sizeDelta 不变；`autoCancel` 关联不变；加仓不重置 graceStart/graceEnd；页面不得虚构已自动扩大的保护规模 | 完成断言后恢复 snapshot | 状态迁移+关联关系；`IncreaseOrderUtils` 仅在新建仓位时写 grace | T21 `0xfe2F474D0d1b8b0CA7718d4c08f1E1aBa98bF17b` |
| XT-MKT-ADD-004 / P0 | 市价加仓_short_已有 TP 和 SL 保护单_加仓后保护单规模与清算保护期均不重置 | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位及 `Open` TP/SL；两单 `autoCancel=true`；记录 orderKey、sizeDelta、graceStart/graceEnd | 追加 size=原仓位 50%；TP<P_ref；SL>P_ref；Oracle 不触发保护单 | 读取市价加仓预览；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与 TP/SL 保护单 | PositionKey、新旧仓位规模、TP/SL orderKey/sizeDelta/状态、`autoCancel` 列表、graceStart/graceEnd、short 页面 | 仓位正确合并；TP/SL 保护单仍为 `Open`，orderKey 与 sizeDelta 不变；`autoCancel` 关联不变；加仓不重置 graceStart/graceEnd；short 不得复用 long 价格方向 | 完成断言后恢复 snapshot | 状态迁移+方向等价类+关联关系 | T22 `0x92Fccf1a697D02eb8C86fDf7864BccE644776599` |
| FT-MKT-ADD-005 / P0 | 市价加仓_long_MAX_OPEN_INTEREST 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 long 仓位；硬 OI 是唯一最小容量来源；将页面余量 H 配置为两位小数且 H>0；单仓、软 OI 与 Reserve 充足 | long 同向加仓；sizeDelta=H、H+0.01 USD | 每个数据点独立打开加仓表单；填写 sizeDelta；读取 Reader、预览、CTA 与容量提示；H+0.01 时尝试提交；查询钱包/Relay 请求、payload、原仓位和 Order | long OI、H、sizeDelta、hardCapUsd、`INSUFFICIENT_MARKET_CAPACITY`、CTA、详情提示、Position/OI | H 点无容量错误且 CTA 可用（不提交）；H+0.01 显示“流动性不足”与“可用流动性不足，最多可开 $H”，禁止提交；原仓位、OI 和余额不变 | “无关仓动作” | 两点边界+加仓入口+公式隔离；页面 USD 输入精度为 0.01；`MAX_OPEN_INTEREST` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-006 / P0 | 市价加仓_short_MAX_POSITION_SIZE_USD 剩余额度边界_R 可输入且 R+0.01 USD 提示超过最大仓位 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 short 仓位 size=S；将单仓上限 C 和 R=C−S 配置为两位小数且 R>0；三种聚合容量充足 | short 同向加仓；sizeDelta=R、R+0.01 USD | 每个数据点独立填写加仓 sizeDelta；核对 nextSize=S+sizeDelta、CTA 和提示；R+0.01 时尝试提交；查询钱包/Relay 请求、payload、原仓位和 Order | S、R、nextSize、C、`MAX_POSITION_SIZE`、主/明细提示、CTA、Position/OI | R 点无 Cap 错误且 CTA 可用（不提交）；R+0.01 显示“超过最大仓位”和“仓位超过市场上限”，禁止提交；原仓位不变 | “无关仓动作” | 两点边界+合并仓位口径；页面 USD 输入精度为 0.01；`existingSize+sizeDelta` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-007 / P0 | 市价加仓_short_MAX_OPEN_INTEREST_FACTOR 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 short 仓位；软 OI 比例上限是唯一最小容量来源；H 为两位小数且 H>0；单仓、硬 OI 与 Reserve 充足 | short 同向加仓；sizeDelta=H、H+0.01 USD | 每个数据点独立打开加仓表单；填写 sizeDelta；读取 poolUsd、factor、Reader 与页面提示；H+0.01 时尝试提交 | poolUsd、factor、short OI、H、hardCapUsd、CTA、详情提示、钱包/Relay 请求、Order | H 点无容量错误且 CTA 可用（不提交）；H+0.01 显示“流动性不足”与按 H 格式化的“最多可开”提示；禁止提交且原仓位不变 | “无关仓动作” | 两点边界+加仓入口+公式隔离；`MAX_OPEN_INTEREST_FACTOR` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-008 / P0 | 市价加仓_long_RESERVE_FACTOR 剩余容量边界_H 可输入且 H+0.01 USD 提示流动性不足 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 long 仓位；Reserve 是唯一最小容量来源；H 为两位小数且 H>0；单仓与硬/软 OI 充足 | long 同向加仓；sizeDelta=H、H+0.01 USD | 每个数据点独立打开加仓表单；填写 sizeDelta；读取 poolUsd、双向 OI、Reader 与页面提示；H+0.01 时尝试提交 | reserveFactor、totalNominalOI、H、hardCapUsd、CTA、详情提示、钱包/Relay 请求、Order | H 点无容量错误且 CTA 可用（不提交）；H+0.01 显示“流动性不足”与按 H 格式化的“最多可开”提示；禁止提交且原仓位不变 | “无关仓动作” | 两点边界+加仓入口+组合容量；`RESERVE_FACTOR` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-009 / P0 | 市价加仓_long_MAX_POSITION_SIZE_USD=0_页面不限制加仓后总仓位 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 long 仓位 S；`MAX_POSITION_SIZE_USD=0`；三种聚合容量与风险抵押充足 | long 同向加仓；普通合法 sizeDelta=D>0 | 打开加仓表单；填写 D；核对 nextSize=S+D、CTA、提示与 payload 草稿；本行不实际提交 | S、D、nextSize、`MAX_POSITION_SIZE_USD`、CTA、主/明细提示、payload 草稿 | 0 表示不限制单仓；不出现最大仓位提示；其他校验满足时 CTA 可用且 payload 使用 D，不把 0 当成零额度 | “无关仓动作” | 零值语义+已有仓位交互；`MAX_POSITION_SIZE_USD=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-010 / P0 | 市价加仓_long_MAX_OPEN_INTEREST=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 long 仓位；当前方向 `MAX_OPEN_INTEREST=0`；软 OI、Reserve、单仓与抵押均不构成更早错误 | long 同向加仓；任意合法 sizeDelta=D>0 | 打开加仓表单；填写 D；读取 Reader availableLongUsd 和页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | `MAX_OPEN_INTEREST`、availableLongUsd、hardCapUsd、CTA、详情提示、Order、Position/OI | CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；禁止提交；原仓位与 OI 不变 | “无关仓动作” | 零值语义+加仓入口；`MAX_OPEN_INTEREST=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-011 / P0 | 市价加仓_short_MAX_OPEN_INTEREST_FACTOR=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 short 仓位；当前方向 `MAX_OPEN_INTEREST_FACTOR=0`；硬 OI、Reserve、单仓与抵押均不构成更早错误 | short 同向加仓；任意合法 sizeDelta=D>0 | 打开加仓表单；填写 D；读取 Reader availableShortUsd 和页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | factor、availableShortUsd、hardCapUsd、CTA、详情提示、Order、Position/OI | CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；禁止提交；原仓位与 OI 不变 | “无关仓动作” | 零值语义+加仓入口；`MAX_OPEN_INTEREST_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-MKT-ADD-012 / P0 | 市价加仓_long_RESERVE_FACTOR=0_页面提示市场容量不足且禁止提交 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有 long 仓位；`RESERVE_FACTOR=0`；硬/软 OI、单仓与抵押均不构成更早错误 | long 同向加仓；任意合法 sizeDelta=D>0 | 打开加仓表单；填写 D；读取 Reader availableLongUsd 和页面提示；尝试点击 CTA；查询钱包/Relay 请求和 Order | reserveFactor、totalNominalOI、availableLongUsd、hardCapUsd、CTA、详情提示、Order、Position/OI | CTA 显示“流动性不足”，详情显示“该交易量下市场容量不足”；禁止提交；原仓位与 OI 不变 | “无关仓动作” | 零值语义+加仓入口；`RESERVE_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |

##### B2.2 调整杠杆、增加保证金与容量交互（10 条原子用例）

> **当前前端交互口径**：提高杠杆时，页面先按市场综合剩余容量与 `MAX_POSITION_SIZE_USD` 计算 `maxAllowedTargetLeverage`，输入框和滑杆会把超出值限制到该上限；正常页面路径不会显示内部提交保护文案“该杠杆调整的市场容量不足”或“目标杠杆超过最大持仓规模”。B2.2 的提高杠杆页面用例据此验证“上限收紧且不会创建超额订单”，并把“未解释限幅原因”登记为前端提示 GAP，不能误写成当前已有提示。
>
> 降低杠杆是 `MarketDecrease`，不得被开仓 Cap 阻断。增加保证金是 `MarketIncrease` 但 `sizeDeltaUsd=0`，跳过三种聚合容量校验；合约仍会复核现有仓位是否超过非零 `MAX_POSITION_SIZE_USD`。`MAX_OPEN_INTEREST=0` 的提高杠杆路径和存量仓位 `S>C` 的增加保证金路径存在已知前后端预检缺口；可执行且与明确期望不一致时记 `FAIL`，能力/测试面尚未实现时另记 `GAP`，不得合写或把链上取消反向算作前端通过。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FT-LEV-CAP-001 / P0 | 提高杠杆_市场综合容量 H>0 限制最大可选杠杆_页面限幅且不得创建超额订单 | 前端层 | 页面 + RPC 只读 | 已有合规仓位；Reader 综合容量 H>0；风险抵押和单仓剩余上限充足；页面数据已刷新 | 推导最大可选杠杆 L_cap，使对应 sizeDeltaUsd≤H；尝试输入 L_over>L_cap | 打开调整杠杆面板；记录 L_cap；通过输入框和滑杆分别尝试 L_over；核对最终目标杠杆、预览与确认行为；查询钱包/Relay 请求和 Order | H、currentLeverage、L_cap、L_over、最终 targetLeverage、sizeDeltaUsd、CTA、Order | 输入框与滑杆均不能保留 L_over，最终目标值≤L_cap 且预览 sizeDeltaUsd≤H；不得创建 sizeDeltaUsd>H 的订单；正常页面路径不显示内部提交保护错误 | 记录 UI-GAP：页面只限幅，未说明上限由市场容量导致 | 边界限制+前端交互；综合容量 H>0 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-LEV-CAP-002 / P0 | 提高杠杆_MAX_POSITION_SIZE_USD 限制最大可选杠杆_页面限幅且目标仓位不得超过 C | 前端层 | 页面 + RPC 只读 | 已有合规仓位 size=S<C；三种聚合容量和风险抵押充足；单仓上限 C 已刷新 | 推导最大可选杠杆 L_cap，使 targetSize≤C；尝试输入 L_over>L_cap | 打开调整杠杆面板；记录 L_cap；通过输入框和滑杆分别尝试 L_over；核对最终目标杠杆、目标仓位预览与确认行为；查询 Order | S、C、L_cap、L_over、最终 targetLeverage、targetSize、CTA、钱包/Relay 请求、Order | 输入框与滑杆不能选择令 targetSize>C 的杠杆；最终 targetSize≤C；不得创建超过 C 的订单；正常页面路径不显示内部提交保护错误 | 记录 UI-GAP：页面只限幅，未说明上限由最大仓位导致 | 边界限制+单仓上限+前端交互 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-LEV-CAP-003 / P0 | 降低杠杆_市场容量已满_页面不误拦截且 MarketDecrease 执行成功 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已有合规仓位；将当前方向市场剩余容量设为 0；仓位可正常减少规模 | 降低目标杠杆；sizeDeltaUsd>0；collateralDeltaAmount=0 | 打开调整杠杆面板；降低目标杠杆；核对无 Cap 提示；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与页面 | 容量前后值、orderType、sizeDeltaUsd、collateralDeltaAmount、Position/OI、提示、History | 不显示任何开仓 Cap 提示；可提交并执行成功；仓位规模和同方向 OI 降低，抵押不变；页面显示降低杠杆而不是加仓 | 完成断言后恢复 snapshot | 反向交互+风险降低动作 | T3 `0x7486fbaB163A98549708cF75F475D9b01f33391B` |
| XT-MARGIN-CAP-004 / P0 | 增加保证金_三种市场综合容量已满_页面不误拦截且保证金增加成功 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已有合规仓位且 size≤`MAX_POSITION_SIZE_USD`；将硬 OI、软 OI 或 Reserve 剩余容量隔离为 0 | Deposit Margin；sizeDeltaUsd=0；collateralDeltaAmount=D>0 | 每种聚合 Cap 独立数据集；打开保证金面板；输入 D；核对无 Cap 提示；提交并签名；Keeper 执行 `MarketIncrease`；查询 Position、OI、余额和页面 | 三种 Cap 剩余量、sizeDeltaUsd、collateralDeltaAmount、Position size/collateral、OI、页面提示/History | 不显示市场容量不足；三组均可提交并执行成功；Position size 和 OI 不变，collateral 按 D 增加；不得显示为加仓 | 完成断言后恢复 snapshot | 反向交互；`sizeDeltaUsd=0` 跳过 OI/Reserve 校验 | T4 `0xBaD726C70dD52921a04b827a878D4A4Cd7DBA17c` |
| XT-LEV-CAP-005 / P0 | 提高杠杆_MAX_OPEN_INTEREST=0_前端可能按无限容量处理并与链上零容量语义不一致（已知缺口） | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 先建立合规仓位；再将当前方向 `MAX_OPEN_INTEREST` 设为 0；其他参数不构成更早错误 | 提高目标杠杆；sizeDeltaUsd>0；Reader availableUsd=0 | 打开调整杠杆面板；尝试选择高于当前值的杠杆；核对最大可选值与提示；若页面允许则提交并签名、由 Keeper 执行；查询 Order、Position 与 OI | `MAX_OPEN_INTEREST`、availableUsd、targetLeverage、sizeDeltaUsd、页面提示、钱包/Relay 请求、订单终态、Position/OI | 正确标准：0 表示零容量，页面应阻止提高杠杆；当前门控可能把 0 当作未配置，若页面放行则前端断言记 FAIL；已发订单应因容量错误取消且仓位/OI 不变，链上取消不能反向算作前端通过 | GAP 登记；完成后恢复 snapshot | 零值语义+前后端一致性+已知缺口 | T6 `0x5d721eCEc922372A4a9A6F1AfadbFde6F3b85f53` |
| XT-MARGIN-CAP-006 / P0 | 增加保证金_存量仓位 S>C_前端缺少 MPS 预检并可能创建必然取消的订单（已知缺口） | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已有仓位 size=S；治理将非零 `MAX_POSITION_SIZE_USD` 下调为 C<S；其他输入合法 | 增加保证金 D>0；sizeDeltaUsd=0；existingSize=S；C<S | 打开增加保证金面板；输入 D；核对提示与 CTA；若页面允许则提交并签名、由 Keeper 执行；查询 Order、Position、OI 与余额 | S、C、D、sizeDeltaUsd、页面提示、钱包/Relay 请求、`MaxPositionSize`、订单终态、Position/OI/余额 | 正确标准：页面应识别该订单执行时必然失败并阻止提交；当前前端没有该预检和已确认产品文案，若仍放行则前端断言记 FAIL；已发订单由 Keeper 执行时以 `MaxPositionSize` 取消，仓位、OI 和保证金不变 | GAP 登记；产品文案待确认；完成后恢复 snapshot | 存量仓位+参数下调+前后端一致性+已知缺口 | T12 `0x13C980b0E2409c7a79d5BD60eB5923E193740488` |
| FT-LEV-CAP-007 / P0 | 提高杠杆_MAX_OPEN_INTEREST_FACTOR=0_最大可选杠杆退回当前值且不创建订单 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有合规仓位；当前方向 `MAX_OPEN_INTEREST_FACTOR=0`；硬 OI、Reserve、单仓与抵押不构成更早限制 | 当前杠杆 L_current；尝试选择 L_target>L_current；Reader availableUsd=0 | 打开调整杠杆面板；读取最大可选杠杆；尝试拖动滑杆或输入 L_target；核对 CTA、提示、钱包/Relay 请求和 Order | factor、availableUsd、L_current、maxAllowedTargetLeverage、最终 targetLeverage、CTA、Order | 最大可选目标杠杆退回 L_current；无法保留更高输入；CTA 显示“无变化”且不创建订单；当前页面不显示容量原因 | 记录 UI-GAP：零容量只表现为不可提高，未解释限制原因 | 零值语义+前端交互；`MAX_OPEN_INTEREST_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| FT-LEV-CAP-008 / P0 | 提高杠杆_RESERVE_FACTOR=0_最大可选杠杆退回当前值且不创建订单 | 前端层 | 页面 + Admin RPC + RPC 只读 | 已有合规仓位；`RESERVE_FACTOR=0`；硬/软 OI、单仓与抵押不构成更早限制 | 当前杠杆 L_current；尝试选择 L_target>L_current；Reader availableUsd=0 | 打开调整杠杆面板；读取最大可选杠杆；尝试拖动滑杆或输入 L_target；核对 CTA、提示、钱包/Relay 请求和 Order | reserveFactor、availableUsd、L_current、maxAllowedTargetLeverage、最终 targetLeverage、CTA、Order | 最大可选目标杠杆退回 L_current；无法保留更高输入；CTA 显示“无变化”且不创建订单；当前页面不显示容量原因 | 记录 UI-GAP：零容量只表现为不可提高，未解释限制原因 | 零值语义+组合容量+前端交互；`RESERVE_FACTOR=0` | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-LEV-CAP-009 / P0 | 降低杠杆_治理将 MAX_POSITION_SIZE_USD 下调至存量仓位 S 以下_仍可执行 MarketDecrease | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已有合规仓位 size=S；治理将非零单仓上限下调为 C<S；仓位仍可安全减少规模 | 降低目标杠杆；sizeDeltaUsd>0；collateralDeltaAmount=0 | 打开调整杠杆面板；选择低于当前值的杠杆；核对无 Cap 阻断；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与页面 | S、C、orderType、sizeDeltaUsd、Position/OI、页面提示、History | 页面不显示开仓 Cap 错误；创建并执行 `MarketDecrease`；仓位规模和同方向 OI 降低；不得因执行前 S>C 阻断风险降低操作 | 完成断言后恢复 snapshot | 逃生路径+反向交互；MPS 参数下调 | T7 `0x31EC024aAF2De1009aDc195f868e3811Ea41B8C7` |
| XT-MARGIN-CAP-010 / P0 | 增加保证金_存量仓位 S=MAX_POSITION_SIZE_USD_等号边界执行成功 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已有仓位 size=S；将非零单仓上限设为 C=S；三种聚合容量和费用条件合法 | 增加保证金 D>0；sizeDeltaUsd=0；existingSize=S=C | 打开增加保证金面板；输入 D；核对无 Cap 提示；提交并签名；Keeper 执行 `MarketIncrease`；查询 Order、Position、OI、余额与页面 | S、C、D、sizeDeltaUsd、collateralDeltaAmount、Position size/collateral、OI、页面提示/History | 等号允许；订单执行成功；Position size 与 OI 不变，collateral 按 D 增加；不得误显示为加仓或市场容量不足 | 完成断言后恢复 snapshot | 等号边界+保证金调整；`S=C` | T8 `0x90eE74361732f98E16bFA7F17993D80b03F699D9` |

##### B2.3 市价部分减仓与账本（`MarketDecrease`；12 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-MKT-REDUCE-005 / P0 | 市价部分减仓_long_盈利状态减仓 50%_剩余多仓与已实现盈利账本正确 | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；无保护单；记录仓位、OI、Funding 快照与余额 | sizeDeltaUsd=50%×sizeInUsd；将 Oracle 价格设为使 long PnL>0 | 将 Mock Oracle 价格更新至盈利价；读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | sizeDeltaUsd/sizeDeltaInTokens、剩余仓位、entryPrice、realized PnL、long OI、Position Fee、UI Fee、Funding Fee、Execution Fee、输出与余额 | `MarketDecrease` 保持 Reduce-only；仅实现对应 token 比例的正 PnL；剩余仓位规模、OI、抵押品和费用精确；entryPrice 不因部分减仓重算 | 完成断言后恢复 snapshot | 场景法+方向等价类+资金守恒；字段规则 §7、§8 | T23 `0xA74b73b624d5B895C29070e64119dde0dA82f6dF` |
| XT-MKT-REDUCE-006 / P0 | 市价部分减仓_long_亏损状态减仓 50%_剩余多仓与已实现亏损账本正确 | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；抵押足以承担亏损与费用；无保护单 | sizeDeltaUsd=50%×sizeInUsd；将 Oracle 价格设为使 long PnL<0 且减仓后仓位仍合规 | 将 Mock Oracle 价格更新至亏损价；读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 负 realized PnL、remainingCollateral、剩余仓位、long OI、费用明细、输出、风险参数与余额 | 仅实现对应 token 比例的负 PnL；亏损、费用和输出按结算瀑布处理一次；剩余多仓、OI 与账本正确 | 完成断言后恢复 snapshot | 场景法+损益等价类+资金守恒；字段规则 §7、§8 | T24 `0xd5F260Da3155D227dacc200387430D7685ebe5Fb` |
| XT-MKT-REDUCE-007 / P0 | 市价部分减仓_short_盈利状态减仓 50%_剩余空仓与已实现盈利账本正确 | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；无保护单；记录仓位、OI、Funding 快照与余额 | sizeDeltaUsd=50%×sizeInUsd；将 Oracle 价格设为使 short PnL>0 | 将 Mock Oracle 价格更新至盈利价；读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | sizeDeltaUsd/sizeDeltaInTokens、剩余仓位、entryPrice、realized PnL、short OI、Position Fee、UI Fee、Funding Fee、Execution Fee、输出与余额 | `MarketDecrease` 保持 Reduce-only；short 盈利方向正确；仅实现对应 token 比例的正 PnL；剩余空仓、OI、费用与账本正确 | 完成断言后恢复 snapshot | 场景法+方向等价类+资金守恒；字段规则 §7、§8 | T25 `0x6521817E4e8018C7671377AafE503b8e0d1AF9b0` |
| XT-MKT-REDUCE-008 / P0 | 市价部分减仓_short_亏损状态减仓 50%_剩余空仓与已实现亏损账本正确 | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；抵押足以承担亏损与费用；无保护单 | sizeDeltaUsd=50%×sizeInUsd；将 Oracle 价格设为使 short PnL<0 且减仓后仓位仍合规 | 将 Mock Oracle 价格更新至亏损价；读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 负 realized PnL、remainingCollateral、剩余仓位、short OI、费用明细、输出、风险参数与余额 | short 亏损方向正确；亏损、费用和输出只结算一次；剩余空仓、OI 与账本正确 | 完成断言后恢复 snapshot | 场景法+方向等价类+损益等价类+资金守恒 | T26 `0xAA5ed684511bfDDc7f0da0cDA11a1a941C68383D` |
| XT-MKT-REDUCE-009 / P0 | 市价部分减仓_long_已有 TP 和 SL 保护单_剩余多仓与两笔保护单均保留 | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；TP/SL 均为 `Open`、`autoCancel=true`；两单 sizeDelta=减仓前整仓规模 | sizeDeltaUsd=50%×sizeInUsd；Oracle 不触发 TP/SL | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI、余额与 TP/SL 保护单 | 剩余仓位、TP/SL orderKey/sizeDelta/状态、`autoCancel` 列表、long OI、页面 Open Orders | 仅减少 50% 多仓；不触发全平 `autoCancel`；TP/SL 仍为 `Open` 且原 sizeDelta 不变；页面与链上数据一致，不得误报已取消或已自动改小 | 超额保护单执行由 C 节 XT-TPSL-CAP-LTP-020、XT-TPSL-CAP-LSL-021 验证；完成后恢复 snapshot | 状态迁移+关联关系；`ExecuteOrderUtils` 仅在仓位归零时清理 `autoCancel` | T27 `0x7C4f464A50DB42FaC614b96B0FA06D3e240E09d6` |
| XT-MKT-REDUCE-010 / P0 | 市价部分减仓_short_已有 TP 和 SL 保护单_剩余空仓与两笔保护单均保留 | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；TP/SL 均为 `Open`、`autoCancel=true`；两单 sizeDelta=减仓前整仓规模 | sizeDeltaUsd=50%×sizeInUsd；Oracle 不触发 TP/SL | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI、余额与 TP/SL 保护单 | 剩余仓位、TP/SL orderKey/sizeDelta/状态、`autoCancel` 列表、short OI、页面 Open Orders | 仅减少 50% 空仓；不触发全平 `autoCancel`；TP/SL 仍为 `Open` 且原 sizeDelta 不变；页面与链上数据一致，不得复用 long 方向状态 | 超额保护单执行由 C 节 XT-TPSL-CAP-STP-022、XT-TPSL-CAP-SSL-023 验证；完成后恢复 snapshot | 状态迁移+方向等价类+关联关系 | T28 `0xfE4b9709d89F428A4621E1770a995d196b2312E1` |
| CT-MKT-REDUCE-011 / P0 | 市价部分减仓_long_sizeDeltaUsd=仓位规模−1 最小单位且 Token 换算耗尽_自动归一为全平 | 合约层 | RPC | long 仓位 sizeInUsd=3e30、sizeInTokens=1e15；无保护单 | isSizeDeltaUsd=true；sizeDelta=3e30−1 | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | 请求/实际 sizeDeltaUsd、sizeDeltaInTokens、`OrderSizeDeltaAutoUpdated`、PnL、费用明细、long OI、Position | long 向上取整得到全部 1e15 Token；订单从 3e30−1 自动归一为 3e30 并发出事件；PnL、费用、OI 和事件统一使用整仓 USD 规模；USD/Token 均无残仓 | 本用例已因边界归一而全平；不再执行 B1.2 | 边界值+取整公式+状态迁移；05 §B32-7-01 | T29 `0x390b3A46a40F72EBB2BCAF01476e7DA0BBfdcC97` |
| CT-MKT-REDUCE-012 / P0 | 市价部分减仓_short_sizeDeltaUsd=仓位规模−1 最小单位_向下取整后保留最小剩余仓位 | 合约层 | RPC | short 仓位 sizeInUsd=3e30、sizeInTokens=1e15；无保护单 | isSizeDeltaUsd=true；sizeDelta=3e30−1 | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | sizeDeltaUsd/sizeDeltaInTokens、剩余 sizeInUsd/sizeInTokens、自动归一事件、short OI | short 向下取整为 1e15−1 Token；不发出 `OrderSizeDeltaAutoUpdated`；剩余 sizeInUsd=1、sizeInTokens=1；不得误归一为全平 | 完成断言后恢复 snapshot | 边界值+方向等价类+取整公式；05 §B32-7-02 | T30 `0xD3e02446273729C89a0bb6b6d790f3fB928CB9d3` |
| XT-MKT-MINOUT-013 / P0 | 市价部分减仓_minOutputAmount=实际输出 USD−1 最小单位_执行成功 | 合约层 + 前端层 | RPC + 页面 | 已有仓位；固定 Oracle 与减仓输入；用 Reader 得到 outputAmount | sizeDeltaUsd=50%×sizeInUsd；O_usd=outputAmount×collateralPrice.min；minOutputAmount=O_usd−1 | 读取市价部分减仓预览并记录 O_usd；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | payload.minOutputAmount、outputAmount、collateralPrice.min、outputUsd、订单终态、剩余仓位与余额 | payload 按 USD 1e30 传值；outputUsd>minOutputAmount；订单为 `Executed`；部分减仓与输出精确，不得将抵押品 Token 原始数量当作 USD | 完成断言后恢复 snapshot | 三点边界的下相邻点；05 §B32-6-01 | T56 `0xA16B9D4cC3a1354d511302777B501aA7b79e6255` |
| XT-MKT-MINOUT-014 / P0 | 市价部分减仓_minOutputAmount=实际输出 USD_等号执行成功 | 合约层 + 前端层 | RPC + 页面 | 与 XT-MKT-MINOUT-013 相同的独立仓位快照与 Oracle 数据 | sizeDeltaUsd=50%×sizeInUsd；minOutputAmount=O_usd | 读取市价部分减仓预览并记录 O_usd；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | payload.minOutputAmount、outputUsd、等号比较、订单终态、剩余仓位与余额 | 校验条件为 outputUsd<minOutputAmount 才拒绝；等号时订单为 `Executed`；部分减仓与输出精确 | 完成断言后恢复 snapshot | 三点边界的等号点；05 §B32-6-01 | T57 `0x8fD70bbE619B438C8Ed2f60FCC50eceb478645Bf` |
| XT-MKT-MINOUT-015 / P0 | 市价部分减仓_minOutputAmount=实际输出 USD+1 最小单位_订单取消且仓位与资金原子回滚 | 合约层 + 前端层 | RPC + 页面 | 与 XT-MKT-MINOUT-013 相同的独立仓位快照与 Oracle 数据 | sizeDeltaUsd=50%×sizeInUsd；minOutputAmount=O_usd+1 | 读取市价部分减仓预览并记录 O_usd；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | `InsufficientOutputAmount(outputUsd,minOutputAmount)`、`Cancelled`、Position/OI/抵押品前后值、Execution Fee 与退款 | outputUsd<minOutputAmount；市价减仓订单执行失败后为 `Cancelled`；Position、OI 和仓位抵押品不变；仅允许按取消规则处理 Execution Fee，不得产生额外资金损失 | 失败后无关仓动作；恢复 snapshot | 三点边界的上相邻点+原子性；05 §B32-6-01 | T58 `0x65354E3F8a66c5b0429C5fdE50a10E25Ebc16Bc7` |
| CT-MKT-LEDGER-016 / P0 | 市价部分减仓_抵押品 Token 金额与 USD 价值不同_剩余成本完成价格换算且账本守恒 | 合约层 | RPC | 已有仓位；构造 remainingCostInOutputToken>0；抵押品 Oracle 有 min/max 双价 | sizeDeltaUsd=50%×sizeInUsd；collateralPrice.min≠1 USD；Token decimals 按运行时读取 | 读取市价部分减仓预览；提交并签名；Keeper 执行 `MarketDecrease`；查询 Order、Position、OI 与余额 | remainingCostInOutputToken、collateralPrice.min、remainingCostUsd、Reader、PositionDecrease 事件、Vault/钱包差分 | remainingCostUsd=remainingCostInOutputToken×collateralPrice.min，并按 USD 1e30 精度记录；不得直接把 Token 原始数量当作 USD；Reader、事件、Position 与资金差分守恒 | 完成断言后恢复 snapshot | 单位等价类+公式核对+资金守恒；v0.3.2 R15，字段规则 §5.4 | T59 `0xEA22195Da8e6b76965031C07Da3DcDF851aeA146` |

#### B3. 限价开仓（`LimitIncrease`；16 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-LMT-OPEN-001 / P0 | 限价开仓_long_触发前保持 Open 且触发后建立多仓 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle | long；trigger 低于当前价；中间杠杆；无 TP/SL | 填写 `long` 限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | type=1、triggerPrice、validFrom、`Open` / `Executed`、Position | 触发前无仓；`price.max<=triggerPrice` 后执行并建立多仓 | 成交后执行“市价关仓标准操作” | 基本流+状态迁移 | T10 `0x53bf5f02907F476Aae9dDF02f301d4cc51FA56AB` |
| XT-LMT-OPEN-002 / P0 | 限价开仓_short_触发前保持 Open 且触发后建立空仓 | 合约层 + 前端层 | RPC + 页面 | 无仓位；可控 Oracle；余额和 Keeper 正常 | short；trigger 高于当前价；中间杠杆；无 TP/SL | 填写 `short` 限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | `price.min`、triggerPrice、isLong=false、`Open` / `Executed`、Position | 触发前无仓；`price.min>=triggerPrice` 后执行并建立空仓 | 成交后执行“市价关仓标准操作” | 基本流+方向等价类+状态迁移 | T11 `0x363172f48786c7aC6b5bdd6a7a23Ba0180e3931a` |
| XT-LMT-LEV-003 / P0 | 限价开仓_long_leverage=L_min_触发后开仓成功 | 合约层 + 前端层 | RPC + 页面 | long；L_min 来自前端配置（链上无最小杠杆参数）；合约层只断言链上不拒绝，L_min 等号规则在前端层判 | leverage=L_min；合法 trigger；C_eff 以上 | 填写 `long` 限价开仓参数及 leverage=L_min；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | 杠杆、Order、Position、费用后抵押 | 等号成功；限价开仓路径不得绕过或改变最小杠杆规则 | “市价关仓标准操作” | 最小边界 | T95 `0x8a0913BF339b76CE4652bd6e14e255386Ae20D03` |
| XT-LMT-LEV-004 / P0 | 限价开仓_long_leverage=L_max_触发后开仓成功 | 合约层 + 前端层 | RPC + 页面 | long；读取 L_max | leverage=L_max；合法 trigger；C_eff 以上 | 填写 `long` 限价开仓参数及 leverage=L_max；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | 杠杆、Order、Position、费用后抵押 | 等号成功；触发时仍满足 L_max 和抵押要求 | “市价关仓标准操作” | 最大边界 | T96 `0x82fD28F07C3F4Fc22A889B81fAE7d951610585A7` |
| XT-LMT-LEV-005 / P0 | 限价开仓_short_leverage=L_min_触发后开仓成功 | 合约层 + 前端层 | RPC + 页面 | short；L_min 来自前端配置（链上无最小杠杆参数）；合约层只断言链上不拒绝 | leverage=L_min；合法 trigger | 填写 `short` 限价开仓参数及 leverage=L_min；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | short Order/Position/清算价 | 最小杠杆 short 成功 | “市价关仓标准操作” | 方向+最小边界 | T97 `0x9Da4B6181ee7f0c43A1b520441cB4027871100F0` |
| XT-LMT-LEV-006 / P0 | 限价开仓_short_leverage=L_max_触发后开仓成功 | 合约层 + 前端层 | RPC + 页面 | short；读取 L_max | leverage=L_max；合法 trigger | 填写 `short` 限价开仓参数及 leverage=L_max；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | short Order/Position/清算价 | 最大杠杆 short 成功 | “市价关仓标准操作” | 方向+最大边界 | T98 `0x83C283EB122585a4692Dbe42901aa6778d3BC2Bf` |
| XT-LMT-TRG-007 / P0 | 限价开仓_long_price.max 在 triggerPrice 三点边界_仅等号与低一单位进入执行 | 合约层 + 前端层 | RPC + 页面 | long；tx-fork Mock Oracle 推价（min=max=feed），±1 为 price 原始单位（1e30 精度）的最小单位 | `price.max=triggerPrice+1`、`=triggerPrice`、`=triggerPrice−1` | 每个数据点独立填写 `long` 限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行订单；查询 Order、Position、OI 与余额 | price.max、triggerPrice、Order Store、Position | +1 保持 Open；等号/−1 进入执行校验并按有效参数建立多仓 | 成交数据集执行“市价关仓标准操作”；+1 数据集执行“取消订单标准操作” | 三点边界 | T69 `0x0d940f94CC4D6af8e873A13e218508b077eFf999` |
| XT-LMT-TRG-008 / P0 | 限价开仓_short_price.min 在 triggerPrice 三点边界_仅等号与高一单位进入执行 | 合约层 + 前端层 | RPC + 页面 | short；tx-fork Mock Oracle 推价（min=max=feed），±1 为 price 原始单位的最小单位 | `price.min=triggerPrice−1`、`=triggerPrice`、`=triggerPrice+1` | 每个数据点独立填写 `short` 限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行订单；查询 Order、Position、OI 与余额 | price.min、triggerPrice、Order Store、Position | −1 保持 Open；等号/+1 进入执行校验并按有效参数建立空仓 | 成交数据集执行“市价关仓标准操作”；−1 数据集执行“取消订单标准操作” | 三点边界+方向 | T99 `0xD7AAFcEbF14A31B166ded3020F3256d1F70eC10F` |
| XT-LMT-TP-009 / P0 | 限价开仓_long_设置 TP 和 SL_主单触发后两笔 TP/SL 保护单可用 | 合约层 + 前端层 | RPC + 页面 | 无仓位；`long` 限价开仓主单可触发；可控 Oracle | 限价开仓 long；TP>P_ref；SL<P_ref | 填写 `long` 限价开仓参数并设置 TP 和 SL；提交并签名创建 `LimitIncrease` 及两笔保护单；确认主单为 `Open`；将 Mock Oracle 价格更新至主单触发条件；Keeper 执行主单；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 三单创建时点、主单 `Open` / `Executed`、Position、TP/SL type、size、关联和 autoCancel | 主单未成交前 TP/SL 不得形成错误减仓；成交后两笔保护单保持 `Open` 且字段正确 | “市价关仓并校验保护单自动取消”；全平后 TP、SL 均自动取消 | 条件组合+状态迁移 | T16 `0xFe4607bfc13B2747c7f87D48f572F039Ff819a8D` |
| XT-LMT-TP-010 / P0 | 限价开仓_short_设置 TP 和 SL_主单触发后两笔 TP/SL 保护单方向正确 | 合约层 + 前端层 | RPC + 页面 | 无仓位；`short` 限价开仓主单可触发；可控 Oracle | 限价开仓 short；TP<P_ref；SL>P_ref | 填写 `short` 限价开仓参数并设置 TP 和 SL；提交并签名创建 `LimitIncrease` 及两笔保护单；确认主单为 `Open`；将 Mock Oracle 价格更新至主单触发条件；Keeper 执行主单；查询 Order、Position、OI、余额与关联 TP/SL 保护单 | 三单创建时点、short Position、TP/SL type、触发方向、acceptablePrice 哨兵和 autoCancel | 主单成交后 TP/SL 保持 `Open`；两笔保护单按 short 镜像方向配置 | “市价关仓并校验保护单自动取消”；全平后 TP、SL 均自动取消 | 方向组合+状态迁移 | T67 `0x5d2461EaAF78D49Ed99b3bEabD9edC5A7f1458cc` |
| FT-LMT-VAL-011 / P0 | 限价开仓_trigger 为空、0、负、超精度、非数字_每个非法类单独拦截 | 前端层 | 页面 | 页面限价开仓模式 | trigger 为空、0、负、超精度、非数字 | 填写指定无效值；点击“提交”；核对字段提示、提交按钮、钱包请求与订单列表 | 错误文案、按钮、payload/签名 | 每个非法类单独拦截；不创建订单；不静默变为市价开仓或条件开仓 | “无关仓动作” | 无效等价类+输入边界（空值、零值、负值、超精度、非数字） | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-LMT-TIME-012 / P0 | 限价开仓_validFrom=T_在 T−1、T、T+1 按时间边界决定是否执行 | 合约层 + 前端层 | RPC + 页面 | tx-fork 不允许改区块时间：T−1 用建单后立即执行（区块时间 < T）；T+1 用等待真实时间越过 T 并刷新 Mock Oracle 时间戳；「=T」在真实时间下打不准，该点标为需 time-fork（`evm_setNextBlockTimestamp`）执行 | validFrom=T；在 T−1、T+1 执行（tx-fork）；=T（time-fork） | 每个时间点独立填写限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；在 T−1、T、T+1 由 Keeper 尝试执行；查询 Order、Position、OI 与余额；另用独立数据集提交 validFrom≠0 的市价开仓订单 | timestamp、validFrom、Order Store、事件、revert 选择器、Position | T−1：执行 revert `OrderValidFromTimeNotReached(validFromTime, currentTimestamp)`，订单保持 Open；T/T+1：进入 Trigger/Acceptable 校验；市价单 validFrom≠0 在创建时被拒 | 成交数据集执行“市价关仓标准操作”；仍 Open 的数据集执行“取消订单标准操作”；被拒数据用“无关仓动作” | 时间三点边界 | T13 `0x8282DB469AE3fD9eAF975ACA1b34744C01087657` |
| XT-LMT-UPD-013 / P0 | 限价开仓_更新 triggerPrice 与 size_合法更新生效且跨现价不静默换型 | 合约层 + 前端层 | RPC + 页面 | 已有 `Open` 的 `LimitIncrease` 订单 | 更新 triggerPrice 但不跨现价；再跨现价；更新 size | 选择目标订单；更新 triggerPrice 与 size；点击“更新”并确认交易；查询更新前后字段与订单状态 | 更新前后 type、triggerPrice、size、acceptablePrice、updatedAtTime | 合法更新生效；跨价时不静默改成 `StopIncrease` / `MarketIncrease`；字段和页面一致 | 验证完成后执行“取消订单标准操作” | 状态迁移；第二批需新增 updateOrder 驱动 | T17 `0x66828A98a1f8272F07709AfF4490090e7e013b7b` |
| XT-LMT-CAN-014 / P0 | 限价开仓_未触发订单_取消订单后订单删除且退款正确 | 合约层 + 前端层 | RPC + 页面 | 已有未触发的 `LimitIncrease` 订单 | 取消订单；Standard 组本轮第二批执行（需新增 cancelOrder 驱动）；direct Relay 与 1CT 组等待各自驱动和准入 | 选择目标订单；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 | 取消交易、Order Store、Execution Fee/Relay Fee、抵押退款、History | 订单删除；退款正确；无仓位；页面显示 `Cancelled`；Relay 两组分别核对主钱包/session signer 与 Router | 取消订单已完成清理；确认无仓位和残单 | 状态迁移 | T18 `0xe4160214E982a6B8176e4BEC4667b26c653C4455` |
| XT-LMT-ACC-015 / P0 | 限价开仓_long/short_执行价相对 acceptablePrice 三点边界_等号和有利侧成交 | 合约层 + 前端层 | RPC + 页面 | Trigger 条件已满足；executionPrice 用 tx-fork Mock Oracle 推价控制；期望按 v0.3.2 部署参数同公式重算 | E 比 A 有利、E=A、E 最小不利越界；long/short | 每个数据点独立填写限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至指定 executionPrice；Keeper 尝试执行订单；查询 Order、Position、OI 与余额 | Trigger、A、E、Executed/Cancelled/Frozen、Position、余额 | Trigger 满足不代表成交；等号/有利成交，不利越界按规则失败且无错误仓位 | 成交数据集执行“市价关仓标准操作”；失败数据集执行“无关仓动作” | 判定表+三点边界 | T70 `0xCBc1dD8DbD1D0342e854D1eCb4126928f2286725` |
| XT-LMT-PREC-016 / P1 | 限价开仓_8 位与 18 位 Mock Token_同一经济 size 与 trigger 按运行时 decimals 换算 | 合约层 + 前端层 | RPC + 页面 | 需要 8 位与 18 位两个市场各一组；tx-fork 现有两市场均为 18 位口径，18 位组用 market 2，8 位组 BLOCKED 待 8 位 Mock 市场 | 同一经济 size 与 trigger 显示值 | 在 8 位和 18 位 Mock 市场分别填写相同经济规模的限价开仓参数；提交并签名创建 `LimitIncrease`；确认订单为 `Open`；将 Mock Oracle 价格更新至触发条件；Keeper 执行订单；查询 Order、Position、OI 与余额 | 页面显示、原始 size/trigger、Order、执行后 Position | 按运行时 decimals 换算；经济规模一致；不写死 BTC/1e8 假设 | 每个成功数据集执行“市价关仓标准操作”；BLOCKED 数据无清理 | 精度等价类 | T31 `0x66220dE6b77A4fA331b5B190Ef44e550c1FfDED7` |


#### B4. 开仓主单取消与 TP/SL 保护单级联取消（`MarketIncrease` / `LimitIncrease`；2 条原子用例）

> 本节只验证主开仓订单尚未执行时的取消行为。主单已经成交并形成仓位后的 TP/SL 取消订单、Oracle 触发、保护期和清算联动统一放在 C 节。
>
> 取消与级联清理逻辑不依赖仓位方向，因此本节用“市价开仓 + `long`”和“限价开仓 + `short`”做成对交叉覆盖，不重复生成四条同逻辑用例。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-MKT-CAN-021 / P0 | 市价开仓_long_超时取消主单并自动取消 TP/SL_动态读取 REQUEST_EXPIRATION_TIME | 合约层 + 前端层 | RPC + 页面 + Time RPC | `MarketIncrease` 主单及关联 TP/SL 已创建且 Keeper 未执行；运行时读取 T=`REQUEST_EXPIRATION_TIME`；各样本独立 | long；合法 TP>P_ref/SL<P_ref；requestAge=T−1/T/T+1；另有可随时取消的 Limit 对照 | 在三个时间点查取消按钮/倒计时并尝试取消；在 T 组确认主单取消后查三笔订单、退款、Position 与 `autoCancel`；改链上 T 后刷新倒计时；验证 Limit 对照 | T 链上原值、requestAge、按钮/倒计时、`RequestNotYetCancellable`、主单与 TP/SL 终态、autoCancel、抵押/Execution Fee 退款、Position/OI、事件 | T−1 前端禁用并显示动态倒计时，直调合约以 `RequestNotYetCancellable` 拒绝；T 等号和 T+1 按链上规则可取消；成功后主单不建仓、TP/SL 级联清理、退款一致；参数变更后页面不使用写死时间；Limit 行为不受影响 | 成功组已完成级联清理；OC-23 将较短 `MAX_ORACLE_PRICE_AGE` 推导为“中间死窗”不成立，Keeper 可在 T 内提交执行时的 fresh report | 三点时间边界+动态配置+级联状态；OC-23 Closed | T60 `0x328c8cdF88dfa9907Ff6fb310E44c0Eee5B08C65` |
| XT-LMT-CAN-017 / P0 | 限价开仓_short_主单关联 TP 和 SL_触发前取消主单并自动取消两笔保护单 | 合约层 + 前端层 | RPC + 页面 | `LimitIncrease` 主单及关联 TP/SL 保护单已创建且尚未触发 | short；triggerPrice 高于当前价；合法 TP<P_ref；合法 SL>P_ref | 创建关联 TP/SL 保护单的限价开仓主单；确认主单为 `Open`；选择主单；点击“取消”并确认交易；查询三笔订单状态、退款、Position 与 `autoCancel` 列表 | 三笔 orderKey、`Open` / `Cancelled` 状态、autoCancel、退款、Position/OI、页面 Open Orders/History | 主单由 `Open` 变为 `Cancelled`；关联 TP、SL 保护单同时自动取消；不建立仓位；页面、Reader、事件与余额一致 | 取消订单已完成清理；将 Mock Oracle 价格更新至越过 `triggerPrice` 后确认三单仍不可执行 | 状态迁移+级联关系 | T61 `0x16d0c093134c071a00e6eb3CC852785895f2dE3d` |

### C. 止盈/止损（TP/SL）、清算保护期与关闭并重新开仓（39 条原子用例）

> **职责边界**：B1/B3 验证开仓与关联 TP/SL 保护单的创建；B2 验证部分减仓时保护单保留，C1 继续验证已建立仓位后的 TP/SL 生命周期与清算保护期；C2 验证保护期结束后由前端发起的“关闭并重新开仓”。B1 的市价关仓共性断言可复用，但 C2 必须另行验证到期入口、两腿顺序、新保护期、失败隔离和前端反馈。TP/SL 创建前置可复用 B 节 helper，但不重复登记为开仓覆盖。合约保护期条件是 `block.timestamp < graceEnd` 时禁止创建清算订单；它不应阻止合法 TP/SL 的更新、取消或执行。

#### C1. TP/SL 与清算保护期（20 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| XT-TPSL-004 / P0 | 保护单（TP/SL）_已有保护单更新 triggerPrice 与 size_合法更新生效且不得静默改变订单类型 | 合约层 + 前端层 | RPC + 页面 | 已有仓位及一笔 `Open` TP 或 SL | 合法 trigger 更新；跨越 P_ref 的非法 trigger；不超过仓位的 size 更新 | 选择目标保护单；更新 triggerPrice 与 size；点击“更新”并确认交易；查询更新前后字段与订单状态；用独立数据集更新为跨越参考价的非法 triggerPrice 并查询原订单 | OrderUpdated、更新前后 orderType、triggerPrice、size、acceptablePrice、updatedAtTime、autoCancel 列表、页面显示 | 合法字段更新生效；非法跨价更新被拒且原单保持不变；不得把 TP/SL 静默转换为另一订单类型或破坏 acceptablePrice 哨兵 | 取消保护单后执行“市价关仓标准操作” | 状态迁移+有效/无效等价类 | T35 `0xf3fa588Cbc8C512b0924d59E2FAef6e11f8F696c` |
| FT-LIQ-005 / P1 | 清算保护期_保护中、保护到期与清算完成_页面状态和链上终态一致 | 前端层 | 页面 | 页面连接 Position、Order 与清算事件源 | 保护中；到达 graceEnd；清算完成 | 分别进入保护中、保护到期和清算完成三个独立状态；刷新页面；查询保护标识、仓位行、订单与 History | 倒计时、保护标识、可用操作、Toast、仓位行、History、事件对应关系 | 保护中显示剩余时间且不误报已清算；到期后保护标识消失；清算完成后仓位消失并显示 Liquidation，不得显示为普通市价关仓 | 每个数据集 snapshot/revert；不得把页面观察写成链上执行 PASS | 状态迁移+前端状态一致性 | T100 `0x7a23500e4807354d75652e2cC7ca2650B10115dB`（前端探针） |
| XT-PROT-LTP-006 / P0 | 清算保护期内_long_仓位设置 TP 和 SL_预言机（Oracle）达到 TP 后 TP 执行且 SL 自动取消 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | long 仓位已建立；`block.timestamp<graceEnd`；TP/SL 均为 `Open` | TP>P_ref；SL<P_ref；Oracle 仅达到 TP | 读取 graceEnd 并确认当前时间小于 graceEnd；将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、Oracle 原始价、TP/SL 状态、Position、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额、事件 | TP 成功执行并全平；SL 同流程自动取消；autoCancel 列表清空；不得因处于保护期而拒绝 TP 或随后发生清算 | 终结动作已清理仓位；确认 TP 不可重复执行 | 状态迁移+规则组合 | T36 `0x322074F81E6f48832cdaA6bC13dea35487c25625` |
| XT-PROT-LSL-007 / P0 | 清算保护期内_long_仓位设置 TP 和 SL_预言机（Oracle）达到 SL 后 SL 执行且 TP 自动取消 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | long 仓位已建立；`block.timestamp<graceEnd`；TP/SL 均为 `Open` | TP>P_ref；SL<P_ref；Oracle 仅达到 SL | 读取 graceEnd 并确认当前时间小于 graceEnd；将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、Oracle 原始价、executionPrice、TP/SL 状态、Position、损失、费用明细、余额、事件 | SL 成功执行并全平；TP 同流程自动取消；autoCancel 列表清空；即使仓位达到清算条件也不得先走 Liquidation | 终结动作已清理仓位；确认 SL 不可重复执行 | 状态迁移+规则组合 | T37 `0x8ad9145862C9bD61c8a476551F7B01B790fD405d` |
| XT-PROT-STP-008 / P0 | 清算保护期内_short_仓位设置 TP 和 SL_预言机（Oracle）达到 TP 后 TP 执行且 SL 自动取消 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | short 仓位已建立；`block.timestamp<graceEnd`；TP/SL 均为 `Open` | TP<P_ref；SL>P_ref；Oracle 仅达到 TP | 读取 graceEnd 并确认当前时间小于 graceEnd；将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、short 触发方向、acceptablePrice、TP/SL 状态、Position、PnL、费用明细、事件 | short TP 按有利方向执行并全平；SL 自动取消；autoCancel 列表清空；不得错误使用 long 条件 | 终结动作已清理仓位；确认 TP 不可重复执行 | 方向等价类+状态迁移 | T38 `0xf17Ed0E592C2B19F1f6BDbFC70F2F5d205FCDeD9` |
| XT-PROT-SSL-009 / P0 | 清算保护期内_short_仓位设置 TP 和 SL_预言机（Oracle）达到 SL 后 SL 执行且 TP 自动取消 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | short 仓位已建立；`block.timestamp<graceEnd`；TP/SL 均为 `Open` | TP<P_ref；SL>P_ref；Oracle 仅达到 SL | 读取 graceEnd 并确认当前时间小于 graceEnd；将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、short 触发方向、acceptablePrice 哨兵、TP/SL 状态、Position、损失、费用明细、事件 | short SL 按不利方向执行并全平；TP 自动取消；autoCancel 列表清空；即使仓位达到清算条件也不得先走 Liquidation | 终结动作已清理仓位；确认 SL 不可重复执行 | 方向等价类+状态迁移 | T39 `0x80D4fc9Fc5ffe5af9EF5DD8cE44738839C38cdd0` |
| XT-PROT-LIQ-010 / P0 | 清算保护期边界_long/short 的 TP 和 SL 均未触发_到期后清算且两笔保护单自动取消 | 合约层 + 前端层 | 页面 + Time RPC + Admin RPC + Keeper RPC | long、short 各独立数据集；仓位达到清算条件；TP/SL 均为 `Open` 且不满足触发条件 | `graceEnd−1`、`graceEnd`、`graceEnd+1` | 每个时间点使用独立数据集；将 Mock Oracle 价格更新至清算条件；Liquidation Keeper 执行清算；查询 Position、关联订单与账本 | timestamp/graceEnd、清算条件、Position、TP/SL 状态、`autoCancel` 列表、残值、费用明细、余额、事件 | T−1 清算被拒且 TP/SL 保持 `Open`；T、T+1 满足清算条件时清算成功；TP、SL 同流程自动取消且无孤儿订单；long/short 结果镜像一致 | 成功清算数据已清理；T−1 数据取消保护单并恢复 snapshot | 三点时间边界+方向等价类+级联清理 | T40 `0xa97209566D434Dc65720F59F2243Be166Ab52988` |
| XT-PROT-RACE-011 / P0 | 清算与 TP/SL 竞争_同一仓位接近同时执行_仅首个终结动作成功且只结算一次 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | `block.timestamp>=graceEnd`；仓位可清算且一笔 TP 或 SL 同时满足执行条件 | 顺序 A：先执行 TP/SL；顺序 B：先清算；各自独立 snapshot | 分别在独立数据集中按“TP/SL 保护单先执行”和“清算先执行”提交终结交易；再执行后到交易；查询 Position、关联订单与账本 | tx 顺序、成功/失败回执、revert/cancel 原因、Position、全部关联订单、PnL/费用明细、余额、事件 | 首个成功动作决定唯一终态；后到动作不得重复结算或复活仓位；全部关联订单进入终态；账本、OI 和余额只变化一次 | 两组数据均以唯一终态结束；确认无可执行残单 | 并发竞争+幂等性 | T41 `0x578d7D1D4df65C00517823dACbc1Fd4F485a8b06` |
| XT-TPSL-CAN-LTP-012 / P0 | 保护单（TP/SL）取消订单_long_仅有状态为 `Open` 的 TP_取消 TP 后仓位保持不变 | 合约层 + 前端层 | RPC + 页面 | long 仓位已建立；仅一笔 TP 为 Open | TP>P_ref；autoCancel=true | 选择目标 TP；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 | 取消交易、TP Order Store/History、Execution Fee 退款、Position、OI、余额、autoCancel 列表 | TP 由 `Open` 变为 `Cancelled`/从 Store 删除；退款符合规则；long 仓位、OI 和 PnL 不因取消订单改变；无 SL 被创建 | 取消订单断言后执行“市价关仓标准操作” | 合法状态迁移+方向等价类 | T42 `0x0f13F330f1b0d64Ba370496B51D05e6FA8999956` |
| XT-TPSL-CAN-LSL-013 / P0 | 保护单（TP/SL）取消订单_long_仅有状态为 `Open` 的 SL_取消 SL 后仓位保持不变 | 合约层 + 前端层 | RPC + 页面 | long 仓位已建立；仅一笔 SL 为 Open | SL<P_ref；autoCancel=true | 选择目标 SL；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 | 取消交易、SL Order Store/History、Execution Fee 退款、Position、OI、余额、autoCancel 列表 | SL 由 `Open` 变为 `Cancelled`/从 Store 删除；退款符合规则；long 仓位、OI 和 PnL 不因取消订单改变；无 TP 被创建 | 取消订单断言后执行“市价关仓标准操作” | 合法状态迁移+方向等价类 | T43 `0x85D0a6ad1e4c3A8C2E347706F3C27e4C8ce6aB46` |
| XT-TPSL-CAN-STP-014 / P0 | 保护单（TP/SL）取消订单_short_仅有状态为 `Open` 的 TP_取消 TP 后仓位保持不变 | 合约层 + 前端层 | RPC + 页面 | short 仓位已建立；仅一笔 TP 为 Open | TP<P_ref；autoCancel=true | 选择目标 TP；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 | 取消交易、TP Order Store/History、Execution Fee 退款、Position、OI、余额、autoCancel 列表 | TP 由 `Open` 变为 `Cancelled`/从 Store 删除；退款符合规则；short 仓位、OI 和 PnL 不因取消订单改变；无 SL 被创建 | 取消订单断言后执行“市价关仓标准操作” | 合法状态迁移+方向等价类 | T44 `0x83Ef8F5662dfbd8384307AFEa8eac0dFC19dDfa8` |
| XT-TPSL-CAN-SSL-015 / P0 | 保护单（TP/SL）取消订单_short_仅有状态为 `Open` 的 SL_取消 SL 后仓位保持不变 | 合约层 + 前端层 | RPC + 页面 | short 仓位已建立；仅一笔 SL 为 Open | SL>P_ref；autoCancel=true | 选择目标 SL；点击“取消”并确认交易；查询订单状态、退款、Position 与关联 TP/SL 保护单 | 取消交易、SL Order Store/History、Execution Fee 退款、Position、OI、余额、autoCancel 列表 | SL 由 `Open` 变为 `Cancelled`/从 Store 删除；退款符合规则；short 仓位、OI 和 PnL 不因取消订单改变；无 TP 被创建 | 取消订单断言后执行“市价关仓标准操作” | 合法状态迁移+方向等价类 | T45 `0x3D6AdC0863996c2b77E24b65104d595e2a427481` |
| XT-PROT-OUT-LTP-016 / P0 | 清算保护期外_long_仓位设置 TP 和 SL_预言机（Oracle）达到 TP 后 TP 执行且 SL 自动取消 | 合约层 + 前端层 | 页面 + Time RPC + Admin RPC + Keeper RPC | long 仓位已建立；`block.timestamp>=graceEnd`；TP/SL 均为 `Open`；仓位不可清算 | TP>P_ref；SL<P_ref；Oracle 仅达到 TP | 推进至 graceEnd 并确认当前时间不小于 graceEnd；将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、Oracle 原始价、TP/SL 状态、Position、PnL、Funding Fee、Position Fee、UI Fee、Execution Fee、余额、事件 | 保护期结束不改变 TP 规则；TP 全平成功；SL 自动取消；无 Liquidation 事件；账本一致 | 终结动作已清理仓位；确认 TP 不可重复执行 | 时间状态迁移+规则组合 | T46 `0x1dAe2B4F075D2D9B2E64ADB892969239a8A819e6` |
| XT-PROT-OUT-LSL-017 / P0 | 清算保护期外_long_仓位设置 TP 和 SL_预言机（Oracle）达到 SL 后 SL 执行且 TP 自动取消 | 合约层 + 前端层 | 页面 + Time RPC + Admin RPC + Keeper RPC | long 仓位已建立；`block.timestamp>=graceEnd`；TP/SL 均为 `Open`；SL 价位下仓位仍不可清算 | TP>P_ref；SL<P_ref；Oracle 仅达到 SL | 推进至 graceEnd 并确认当前时间不小于 graceEnd；将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、Oracle 原始价、executionPrice、TP/SL 状态、Position、损失、费用明细、事件 | SL 在清算条件成立前成功全平；TP 自动取消；无 Liquidation 事件；账本一致 | 终结动作已清理仓位；确认 SL 不可重复执行 | 时间状态迁移+风险隔离 | T47 `0xe4A2A631d6287ab8527451a0E89935D3A334D9B7` |
| XT-PROT-OUT-STP-018 / P0 | 清算保护期外_short_仓位设置 TP 和 SL_预言机（Oracle）达到 TP 后 TP 执行且 SL 自动取消 | 合约层 + 前端层 | 页面 + Time RPC + Admin RPC + Keeper RPC | short 仓位已建立；`block.timestamp>=graceEnd`；TP/SL 均为 `Open`；仓位不可清算 | TP<P_ref；SL>P_ref；Oracle 仅达到 TP | 推进至 graceEnd 并确认当前时间不小于 graceEnd；将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、short 触发方向、acceptablePrice、TP/SL 状态、Position、PnL、费用明细、事件 | short TP 全平成功；SL 自动取消；无 Liquidation 事件；不得复用 long 触发条件 | 终结动作已清理仓位；确认 TP 不可重复执行 | 时间状态迁移+方向等价类 | T48 `0xd57440587Ff428d7B1A23FBe804a47e84581A4c8` |
| XT-PROT-OUT-SSL-019 / P0 | 清算保护期外_short_仓位设置 TP 和 SL_预言机（Oracle）达到 SL 后 SL 执行且 TP 自动取消 | 合约层 + 前端层 | 页面 + Time RPC + Admin RPC + Keeper RPC | short 仓位已建立；`block.timestamp>=graceEnd`；TP/SL 均为 `Open`；SL 价位下仓位仍不可清算 | TP<P_ref；SL>P_ref；Oracle 仅达到 SL | 推进至 graceEnd 并确认当前时间不小于 graceEnd；将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | timestamp/graceEnd、short 触发方向、acceptablePrice 哨兵、TP/SL 状态、Position、损失、费用明细、事件 | short SL 在清算条件成立前成功全平；TP 自动取消；无 Liquidation 事件；账本一致 | 终结动作已清理仓位；确认 SL 不可重复执行 | 时间状态迁移+方向等价类+风险隔离 | T49 `0xaE93f28D40aBEaAb6e4936ebc4C0Bd7d3B06598F` |
| XT-TPSL-CAP-LTP-020 / P0 | 保护单（TP/SL）超额规模_long_部分减仓后 TP 规模大于剩余仓位_按剩余多仓封顶执行并全平 | 合约层 + 前端层 | RPC + 页面 + Keeper RPC | 已按 XT-MKT-REDUCE-009 完成 50% 市价部分减仓；原 TP/SL 仍为 `Open` 且 sizeDelta 大于剩余多仓 | long；Oracle 仅达到 TP；TP `autoCancel=true` | 将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | TP 原 sizeDelta、剩余 sizeInUsd/sizeInTokens、`OrderSizeDeltaAutoUpdated`、实际减仓规模、Position、long OI、关联 SL、费用明细与余额 | TP 按剩余多仓规模封顶并发出自动更新事件；仓位全平；SL 自动取消；不得因 TP 原规模超额而失败或多结算 | 终结动作已清理仓位；确认 TP/SL 均不可重复执行 | 边界值+关联状态迁移；`DecreasePositionUtils` 条件减仓封顶规则 | T62 `0x3b362F22F212b2E8Eb5D8Ac6F34826956B5BAAe6` |
| XT-TPSL-CAP-LSL-021 / P0 | 保护单（TP/SL）超额规模_long_部分减仓后 SL 规模大于剩余仓位_按剩余多仓封顶执行并全平 | 合约层 + 前端层 | RPC + 页面 + Keeper RPC | 已按 XT-MKT-REDUCE-009 完成 50% 市价部分减仓；原 TP/SL 仍为 `Open` 且 sizeDelta 大于剩余多仓 | long；Oracle 仅达到 SL；SL `autoCancel=true` | 将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | SL 原 sizeDelta、剩余 sizeInUsd/sizeInTokens、`OrderSizeDeltaAutoUpdated`、实际减仓规模、Position、long OI、关联 TP、费用明细与余额 | SL 按剩余多仓规模封顶并发出自动更新事件；仓位全平；TP 自动取消；不得因 SL 原规模超额而失败或多结算 | 终结动作已清理仓位；确认 TP/SL 均不可重复执行 | 边界值+关联状态迁移；`DecreasePositionUtils` 条件减仓封顶规则 | T63 `0x4f6a4DF4f0453D01DC5951Fc2a9909eCd21bb29f` |
| XT-TPSL-CAP-STP-022 / P0 | 保护单（TP/SL）超额规模_short_部分减仓后 TP 规模大于剩余仓位_按剩余空仓封顶执行并全平 | 合约层 + 前端层 | RPC + 页面 + Keeper RPC | 已按 XT-MKT-REDUCE-010 完成 50% 市价部分减仓；原 TP/SL 仍为 `Open` 且 sizeDelta 大于剩余空仓 | short；Oracle 仅达到 TP；TP `autoCancel=true` | 将 Mock Oracle 价格更新至 TP 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | TP 原 sizeDelta、剩余 sizeInUsd/sizeInTokens、`OrderSizeDeltaAutoUpdated`、实际减仓规模、Position、short OI、关联 SL、费用明细与余额 | TP 按剩余空仓规模封顶并发出自动更新事件；仓位全平；SL 自动取消；不得复用 long 方向或多结算 | 终结动作已清理仓位；确认 TP/SL 均不可重复执行 | 边界值+方向等价类+关联状态迁移 | T64 `0x718397288C089b5861b23a2ad1c1528339567D1E` |
| XT-TPSL-CAP-SSL-023 / P0 | 保护单（TP/SL）超额规模_short_部分减仓后 SL 规模大于剩余仓位_按剩余空仓封顶执行并全平 | 合约层 + 前端层 | RPC + 页面 + Keeper RPC | 已按 XT-MKT-REDUCE-010 完成 50% 市价部分减仓；原 TP/SL 仍为 `Open` 且 sizeDelta 大于剩余空仓 | short；Oracle 仅达到 SL；SL `autoCancel=true` | 将 Mock Oracle 价格更新至 SL 触发条件；Keeper 执行保护单；查询 Position、关联 TP/SL 保护单与账本 | SL 原 sizeDelta、剩余 sizeInUsd/sizeInTokens、`OrderSizeDeltaAutoUpdated`、实际减仓规模、Position、short OI、关联 TP、费用明细与余额 | SL 按剩余空仓规模封顶并发出自动更新事件；仓位全平；TP 自动取消；不得复用 long 方向或多结算 | 终结动作已清理仓位；确认 TP/SL 均不可重复执行 | 边界值+方向等价类+关联状态迁移 | T68 `0xb1C0FA68C0dF7Da7cD87D454e1Bed605f5854663` |

#### C2. 清算保护期结束后的关闭并重新开仓（Close & Reopen；19 条原子用例）

> **两腿与原子性**：该功能是前端编排的两个普通订单：先创建并执行 100% `MarketDecrease`，再创建并执行 `MarketIncrease`。两腿分别产生 orderKey、Execution Fee、Position/UI/Funding 账本变化和 Keeper 执行证据；它们之间没有合约级原子回滚或失败补偿。不得用“点击一次”将两腿记为一笔链上动作。
>
> **分层执行**：XT-PROT-ROLL-028 与 XT-PROT-ROLL-030、XT-PROT-ROLL-029 与 XT-PROT-ROLL-031 分别是同一次自动化的关仓腿和重开腿，共享账户、snapshot 与前后态证据。关仓腿未通过时不执行对应重开腿。重开只能以“本次 `MarketDecrease` 已 `Executed` 且旧 Position 已归零”作为门禁，不能仅凭 Position 消失判定。Standard 双腿成功在本节覆盖；1CT 的 session、Relay Fee、task 和 action count 只在 §5 C 的 FT/XT-FLASH-ROLL-009～013 覆盖，不重复造数。
>
> **新仓规则**：重开使用点击时冻结的 market、direction、collateralToken、leverage 和 Token 规模；Token 规模按该市场当前页面允许的小数位向下截断，不得向上放大仓位。重开快照不包含 TP/SL，旧 TP/SL 必须在全平时按 `autoCancel` 清理，新仓不得误继承旧保护单。只有重开 `MarketIncrease` 真正 `Executed` 并建立新 Position 后，才能展示新保护期；`graceEnd=graceStart+LIQUIDATION_GRACE_PERIOD_BASE(marketIndex)×LIQUIDATION_GRACE_PERIOD_TIER_MULTIPLIER(tier)/1e18`，不使用页面写死时长。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 关联用例 / 后续处理 | 设计方法 / 依据 | 测试账户 / 地址 |
|---|---|---|---|---|---|---|---|---|---|---|---|
| FT-PROT-ROLL-024 / P0 | 清算保护期边界_关闭并重新开仓入口_graceEnd−1 不显示且 graceEnd/graceEnd+1 可触发 | 前端层 | 页面 + Time RPC + RPC 只读 | 原 Position 仍存在且已选中；`graceStart/graceEnd` 非零；仓位未达清算条件；费用与交易门禁均正常 | 三个独立数据集：`now=graceEnd−1`、`=graceEnd`、`=graceEnd+1` | 在保护期页面选中仓位；分别推进到三个时间点；读取倒计时、到期状态、按钮可见性/可用性；不提交交易 | 链上 `graceEnd`、页面 now、倒计时、到期标识、按钮文案/disabled、钱包请求、task/tx/Order | T−1 仍在保护期且不显示关闭并重新开仓入口；T 等号和 T+1 显示到期且入口可用；时间迁移本身不弹钱包、不创建订单 | T/T+1 只验证入口；真实点击由 XT-PROT-ROLL-028/029 覆盖 | 三点时间边界+无副作用 | T71 `0xC8564779ba903De6daB75C4053E22C044Dec7683` |
| FT-PROT-ROLL-025 / P0 | 清算保护期已结束_刷新、重新进入 Trade 与重新选择仓位_关闭并重新开仓入口仍可达 | 前端层 | 页面 + Time RPC | 有一笔 `now>=graceEnd` 但仍存在的不可清算 Position；桌面端和移动端均可用 | D1 到期后整页刷新；D2 离开后重进 Trade；D3 桌面仓位行重新选择；D4 移动仓位卡重新选择 | 每个数据集独立到期；刷新/路由进出/取消后再选中目标仓位；打开保护期卡片并检查入口 | Position 列表、selectedPositionId、`graceEnd`、到期卡片、桌面/移动按钮、路由前后状态 | 到期不得使仍存在的仓位无法选中；四种重进路径都能重新看到对应到期状态与关闭并重新开仓入口；不得只在从 T−1 持续停留的页面中可见 | 完成可达性断言后恢复 snapshot | 状态持久性+多入口等价类 | T72 `0xEf4aE2b2a3d29AeE126bdCBe9C1C384d02e63451` |
| FT-PROT-ROLL-026 / P0 | 清算保护期已结束_clean、recovered、危险区与已清算_入口文案与实际动作一致 | 前端层 | 页面 + Admin RPC + Time RPC | 可独立构造保护期内未达清算线、触及后恢复、到期时仍在危险区及实际已清算四种状态 | D1 expired-clean 且 Position 存在；D2 expired-recovered 且 Position 存在；D3 expired-danger 且 Position 存在；D4 Liquidation 后 Position 不存在 | 每组独立打开到期卡片；核对主标题、风险提示、CTA 文案与按钮所绑定动作；D4 只观察不交易 | live Position、expiredOutcome、文案、CTA、绑定 action、钱包/task/Order | D1/D2 且原仓位仍存在时明确显示“关闭并重新开仓”；D3 若按钮仍绑定两腿动作，文案必须明确关仓与重开风险，不得用模糊的“立即交易”隐藏全平；D4 不显示关仓腿，只能作为新开仓入口 | D3 真实交易竞争由 XT-PROT-ROLL-039 覆盖 | 状态判定表+文案/动作一致性 | T73 `0x705b64Ff10177f9C77CEea439a7F11d8eaDfcB93` |
| FT-PROT-ROLL-027 / P0 | 关闭并重新开仓_快照缺少市场、抵押品、Token 精度、规模或杠杆_关仓前拦截且不裸平 | 前端层 | 页面 + 故障注入 RPC | 有已到期且仍存在的 Position；可逐项屏蔽 positionsInfo 中的必填字段 | 缺 posInfo、marketIndex、collateralToken、indexToken.decimals、sizeInTokens、leverage 各一组 | 每组刷新后点击“关闭并重新开仓”；观察页面反馈；查询钱包请求、task、tx、Order、Position 和保护单 | 缺失字段、本地化错误、roll lock、钱包/task/tx/Order、原 Position/TP/SL | 每一必填字段缺失都在第一腿前显示“仓位数据加载完成前无法关闭并重新开仓。您的仓位尚未关闭。”；无钱包请求、无任务、无订单；原仓位和 TP/SL 不变；lock 释放后可重试 | 不执行关仓；恢复数据源 | 无效等价类+失败原子性 | T74 `0xB4aB41BdeE6d3246b557936c8CFa2D592241eaC9` |
| XT-PROT-ROLL-028 / P0 | 关闭并重新开仓_Standard_long_关仓腿全平旧多仓并自动取消 TP 和 SL | 合约层 + 前端层 | 页面 + Time RPC + Keeper RPC | Standard；long Position 已到期且不可清算；TP/SL 均为 `Open`、`autoCancel=true`；两腿 Gas/Execution Fee 充足 | 记录旧 market、direction、sizeInUsd/sizeInTokens、collateral、leverage、grace、TP/SL orderKey | 点击“关闭并重新开仓”；确认第一次钱包交互；查询关仓 Order；Keeper 执行关仓；在第二腿签名前查询 Position、OI、TP/SL、账本和页面跟踪状态 | close orderKey/type/size/`autoCancel=false`、`OrderExecuted`、Position/OI、TP/SL `OrderCancelled`及 AUTO_CANCEL 原因、PnL、Funding/Position/UI/Execution Fee、余额、页面阶段 | 只创建一笔精确 100% `MarketDecrease`；关仓单自身 `autoCancel=false` 不影响旧保护单按 positionKey 清理；执行后旧多仓删除、long OI 准确下降，TP/SL 同流程自动取消且不可再执行；关仓账本与 B1.2 一致；页面不得在重开前宣告整个流程成功 | 不恢复 snapshot，继续执行 XT-PROT-ROLL-030 | 状态迁移+关联清理+账本对拍；复用 B1.2 共性 | T75 `0xA354a6A75BDfc43757c654b7E5BE9D4Cb93874bC` |
| XT-PROT-ROLL-029 / P0 | 关闭并重新开仓_Standard_short_关仓腿全平旧空仓并自动取消 TP 和 SL | 合约层 + 前端层 | 页面 + Time RPC + Keeper RPC | Standard；short Position 已到期且不可清算；TP/SL 均为 `Open`、`autoCancel=true`；两腿 Gas/Execution Fee 充足 | 记录旧 market、direction、sizeInUsd/sizeInTokens、collateral、leverage、grace、TP/SL orderKey | 点击“关闭并重新开仓”；确认第一次钱包交互；查询关仓 Order；Keeper 执行关仓；在第二腿签名前查询 Position、OI、TP/SL、账本和页面跟踪状态 | close orderKey/type/size/`autoCancel=false`、`OrderExecuted`、Position/OI、TP/SL `OrderCancelled`及 AUTO_CANCEL 原因、PnL、Funding/Position/UI/Execution Fee、余额、页面阶段 | 只创建一笔精确 100% `MarketDecrease`；关仓单自身 `autoCancel=false` 不影响旧保护单按 positionKey 清理；执行后旧空仓删除、short OI 准确下降，TP/SL 同流程自动取消且不可再执行；不得复用 long 方向价格/账本；页面不得在重开前宣告整个流程成功 | 不恢复 snapshot，继续执行 XT-PROT-ROLL-031 | 方向等价类+状态迁移+关联清理 | T76 `0xDdEe924E536C5c3db938534295dd6C23EdDc197E` |
| XT-PROT-ROLL-030 / P0 | 关闭并重新开仓_Standard_long_重开腿建立同向多仓并生成新清算保护期 | 合约层 + 前端层 | 页面 + Keeper RPC + RPC 只读 | 同一自动化的 XT-PROT-ROLL-028 已通过；前端保留点击时快照；新开仓容量与费用充足 | 旧仓 market/direction/collateral/leverage/size 快照；当前 tier 及保护期参数；Order Tracking=on | 只在关仓 orderKey 已 `Executed` 且旧 Position 删除后确认第二次钱包交互；捕获重开 payload/Order；Keeper 执行；查询新 Position、OI、费用、grace 和页面 | reopen orderKey/type/payload、创建/执行时间、新 Position 全字段、long OI、Funding 新快照、费用、TP/SL、倒计时/跟踪弹窗 | 只创建并执行一笔 `MarketIncrease`；新仓 market/direction/collateral/leverage 与冻结快照一致；新 `graceStart` 等于重开执行区块时间，`graceEnd` 按链上公式重算；新仓不含旧 TP/SL，页面明确提示需重新设置保护单；页面只在新 Position 出现后显示新倒计时 | 完成后市价关仓，不再登记为额外用例 | 两阶段状态迁移+公式核对 | T75 `0xA354a6A75BDfc43757c654b7E5BE9D4Cb93874bC` |
| XT-PROT-ROLL-031 / P0 | 关闭并重新开仓_Standard_short_重开腿建立同向空仓并生成新清算保护期 | 合约层 + 前端层 | 页面 + Keeper RPC + RPC 只读 | 同一自动化的 XT-PROT-ROLL-029 已通过；前端保留点击时快照；新开仓容量与费用充足 | 旧仓 market/direction/collateral/leverage/size 快照；当前 tier 及保护期参数；Order Tracking=off | 只在关仓 orderKey 已 `Executed` 且旧 Position 删除后确认第二次钱包交互；捕获重开 payload/Order；Keeper 执行；查询新 Position、OI、费用、grace 和页面 | reopen orderKey/type/payload、创建/执行时间、新 Position 全字段、short OI、Funding 新快照、费用、TP/SL、倒计时/toast | 只创建并执行一笔 `MarketIncrease`；新仓为 short 且其余快照字段一致；新 `graceStart/graceEnd` 按重开执行时点和链上参数重算；不得误建多仓或继承旧 TP/SL，页面明确提示需重新设置保护单；关闭 Order Tracking 时 toast 仍须完整跟踪两腿终态 | 完成后市价关仓，不再登记为额外用例 | 方向等价类+两阶段状态迁移 | T76 `0xDdEe924E536C5c3db938534295dd6C23EdDc197E` |
| XT-PROT-ROLL-032 / P0 | 关闭并重新开仓_旧仓 Token 规模超过市场页面小数位_重开向下截断且不得只平不开 | 合约层 + 前端层 | 页面 + Keeper RPC | 已到期 Position 的 `sizeInTokens` 按 18 位 Position 精度格式化后，含超过该 market 页面允许小数位的非零尾数；两腿其他条件正常 | 运行时读 `d_display`；`oldSizeToken` 的第 `d_display+1` 位后非零；覆盖不同 Token decimals/价格导出的展示精度 | 执行已到期的关闭并重新开仓；记录快照原始 size 字符串、表单预填值与重开 payload；执行两腿；查询新仓规模 | raw snapshot size、`d_display`、prefill/override size、两个 orderKey、新 sizeInTokens、错误/toast | `reopenInput=truncate(oldSizeToken,d_display)`，表单与实际 payload 字节一致；`newSize<=oldSize`且 `0<=oldSize−newSize<10^−d_display`；不向上取整；重开订单必须真实创建并执行，不得因快照 18 位精度被前端静默拒绝而留下裸平仓 | 新仓市价关仓 | 精度等价类+向风险侧取整+缺陷回归 | T77 `0x842C75bacA3eF89e2E8aE046aBB34fc276eDf4fa` |
| FT-PROT-ROLL-033 / P0 | 关闭并重新开仓_Standard 第一次钱包签名被用户拒绝_不创建关仓或重开订单 | 前端层 | 页面 | 已到期且仍存在的 Standard Position；关联 TP/SL 为 `Open` | 在关仓 `MarketDecrease` 钱包请求中选择 Reject | 点击“关闭并重新开仓”；拒绝第一次钱包请求；查询按钮/lock、Order、Position、TP/SL、余额和反馈 | wallet error、close/reopen orderKey、roll lock、Position/OI、TP/SL、余额、toast/modal | 用户拒绝后两腿均不创建；原仓位、OI、TP/SL 和余额不变；页面不显示关仓/续保成功；lock 及时释放，用户可重新发起 | 无关仓；恢复 snapshot | 用户中止+失败原子性 | T78 `0x417221E60E49389e1341082F2702571981A5B92a` |
| XT-PROT-ROLL-034 / P0 | 关闭并重新开仓_关仓 MarketDecrease 已创建但 Keeper 执行取消_原仓位保留且不提交重开 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 已到期但不可清算的 Position；可在创建关仓单后使 acceptablePrice 失效；未超过重开 TTL | 只使关仓 `MarketDecrease` 命中一个可取消的执行错误 | 点击并创建关仓单；变更 Oracle/执行条件；Keeper 执行；查询关仓 Order、Position和是否出现重开请求 | close `OrderCancelled` 原因、Position/OI/账本、reopen payload/Order、页面终态 | 关仓市价单为 `Cancelled` 而非 `Frozen`；原 Position/OI/账本不变；前端不得创建 `MarketIncrease`、不得显示新保护期，并须保留真实取消原因 | 取消原单或等 TTL 收敛后恢复 snapshot | 执行故障注入+状态迁移 | T79 `0x1F4459b0bB1d28615e20D7EBd5fE2cAc7F6405F1` |
| FT-PROT-ROLL-035 / P0 | 关闭并重新开仓_关仓腿未归零的 180000ms 边界_超时后迟到关仓不再自动重开 | 前端层 | 页面 + Time RPC + 故障注入 RPC | 已创建关仓 Order；可暂停 Keeper 与控制浏览器时钟；前端当前 `ROLL_REOPEN_TTL_MS=180000` | 独立数据集：request age=179999ms、180000ms、180001ms；超时后再执行原关仓 Order | 点击后暂停关仓执行；推进到三点并查询内存请求、lock 和页面反馈；超时后让原关仓执行；查询是否有重开 | request id/createdAt/age、timer、Position、close/reopen Order、lock、toast/modal | 179999ms 请求仍可等待但旧仓未平时不重开；180000ms 等号请求失效并显示“关闭并重新开仓已超时。请确认当前仓位状态后重试。”；180001ms 不重复报错；超时后迟到的关仓即使成功也不能使用已过期快照自动开仓 | 确认迟到关仓后为空仓；人工恢复 | 三点时间边界+过期幂等 | T80 `0x675936E17D5f617B6a4b0f8A75C226Cc01b40Dd2` |
| FT-PROT-ROLL-036 / P0 | 关闭并重新开仓_旧仓已平但重开所需市场或价格配置在 TTL 内未就绪_明确告知用户当前空仓 | 前端层 | 页面 + Keeper RPC + 故障注入 RPC | 关仓 `MarketDecrease` 已 `Executed`；可持续屏蔽重开 market token data 或 execution-price config 直至 TTL | closeLanded=true；重开依赖在 180000ms 前始终未就绪 | 点击并执行关仓；确认旧 Position 删除；保持重开依赖不可用到 TTL；观察提示、表单、lock、Order 和 Position | close/reopen orderKey、positionsInfoData、market/token/config readiness、prefill、toast/modal、lock | 不创建无配置的重开订单；TTL 到达时显示“仓位已平，但重新开仓的委托未能提交。请在下单面板中重新开仓。”；页面明确显示空仓而非已续保；表单保留冻结快照供人工重试；lock 释放 | 用户人工选择是否重开；恢复数据源 | 失败分支+不可逆两阶段流程 | T81 `0x9FDa29dae6DA4D4106818EAF1c83e872a7b2BA64` |
| FT-PROT-ROLL-037 / P0 | 关闭并重新开仓_Standard 余额仅足够关仓腿但不足重开腿_关仓前显示两腿费用并阻断 | 前端层 | 页面 + RPC 只读 | Standard；两腿均非 Execution Fee 补贴；可读实时 Gas、两腿 Execution Fee、钱包余额与开仓需求 | B1<关仓腿所需；B2=只够关仓腿但<两腿总需求；B3≥两腿总需求 | 每组独立到期；打开入口并记录两腿费用明细；核对 CTA 和提示；B1/B2 尝试点击；B3 只验证可用性不提交 | native 余额、两腿 Gas/Execution Fee、重开抵押品需求、CTA、wallet/tx/Order | B1/B2 均在任何关仓签名前显示精确不足项并禁止，不得在只能完成第一腿时先平仓；B3 才可发起；Standard 不显示 Relay Fee，但两腿 Execution Fee 与钱包 Gas 必须分别可见、不重复或漏算 | 本用例不创建订单 | 余额等价类+两阶段预检 | T82 `0x53f708F4D53C8c302C4A1bA31AF944618af55AD9` |
| XT-PROT-ROLL-038 / P0 | 关闭并重新开仓_重开 MarketIncrease 创建后容量被其他仓位占用_订单 Cancelled 且不显示新保护期 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 旧仓已由本流程全平；重开 `MarketIncrease` 已创建但未执行；创建时容量充足 | 重开 size=S；在 Keeper 执行前由其他账户占用目标 Cap，使剩余<S；只命中一个 Cap | 执行关仓并签名创建重开单；其他账户占用容量；Keeper 执行重开单；查询 Order、Position/OI、grace、费用与页面 | Cap 公式输入、`OrderCancelled`/reason、Position/OI、grace、Execution Fee/退款、toast/modal/倒计时 | `MarketIncrease` 遇履约错误为 `Cancelled`而非 `Frozen`；重开仓位不建立、OI 不增加、无新 grace；页面必须跟踪到 Keeper 终态并显示真实失败原因，不能在 OrderCreated/Relay task accepted 时提前显示续保成功 | 用户人工重开或保持空仓；恢复 Cap | 执行时状态漂移+市价单终态规则 | T83 `0x0A2FEb2C201e0446C5D2cb40De7bA7393EF9f22f` |
| XT-PROT-ROLL-039 / P0 | 关闭并重新开仓_清算先于本次 MarketDecrease 终结旧仓_关仓取消且绝不自动重开 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | `block.timestamp>=graceEnd`；原仓已达清算条件；用户的关仓 `MarketDecrease` 已创建但未执行；有 TP/SL | 执行顺序：Liquidation 先，用户 MarketDecrease 后 | 点击关闭并重新开仓以创建关仓单；Liquidation Keeper 先执行清算；再由 Order Keeper 执行用户关仓单；查询全部 Order、Position、TP/SL、OI、账本和页面 | 清算/关仓 tx 顺序、Liquidation 事件、close `OrderCancelled(EmptyPosition)`、PositionDecrease 次数、TP/SL、reopen Order、PnL/费用/OI | Liquidation 是唯一仓位终结与账本结算；TP/SL 按清算流程自动取消；后到的用户关仓单为 `Cancelled(EmptyPosition)`，不产生第二次 PositionDecrease/OI/费用；前端不得把“Position 因清算消失”误当本次关仓已执行，因此不得创建重开 `MarketIncrease` | 终态已清仓；确认无残单 | 并发竞争+因果关联+幂等性 | T84 `0xfA5E784906C06b2f73450CE3be80AAE20c46ffD2` |
| FT-PROT-ROLL-040 / P0 | 关闭并重新开仓_双击、回车与两个到期入口并发_全流程只生成一对订单 | 前端层 | 页面 + 故障注入 RPC | 已到期且仍存在的 Position；图表覆盖层和订单面板两个入口可见；可延迟钱包/tx/Keeper | 同步双击；Enter+点击；两入口同时触发；分别在关仓签名、关仓等待、重开等待阶段触发 | 分别延迟每个阶段；在延迟期间按数据集发起并发操作；完成首次两腿；统计 handler、wallet、task/tx/Order 和终态 | roll lock 时间线、handler 调用次数、钱包请求、close/reopen payload/orderKey、Position/OI | 首次触发后按钮立即进入在途状态；全过程只有一个冻结快照、一笔全量 `MarketDecrease` 和一笔 `MarketIncrease`；其他触发被原子拒绝；终态后 lock 释放，不得多平、多开或重复扣费 | 新仓市价关仓 | 并发+幂等+多入口竞争 | T85 `0x4b4D783D61DD4F12e597aA3F906dEF37fdec5813` |
| FT-PROT-ROLL-041 / P0 | 关闭并重新开仓_提交关仓后切换市场和修改下单表单_重开仍使用点击时冻结快照 | 前端层 | 页面 + Keeper RPC | 已到期 Position；两个市场可用；可在关仓等待期间编辑普通开仓表单 | 快照 A=market/direction/collateral/leverage/size；关仓后页面改为完全不同的表单 B | 点击关闭并重新开仓并记录快照 A；在旧仓归零前切换市场、方向、杠杆、抵押品和规模为 B；执行关仓并捕获自动重开 payload | 快照 A、表单 B、reopen overrides/payload/orderKey、新 Position、误提交数 | 重开必须且仅使用冻结快照 A；B 不能污染重开 market/direction/collateral/leverage/size；表单可被快照重置为当前重开值，但不得额外提交 B；全流程仍只有一对订单 | 新仓市价关仓 | 快照隔离+状态污染反证 | T86 `0xdCDc6C10A88aAEBdcE5e7FDffeA331da9cf666bd` |
| XT-PROT-ROLL-042 / P0 | 关闭并重新开仓_多仓位同时在保护期且到期前选中第二笔_第二笔先到期后只关闭并重开第二笔 | 合约层 + 前端层 | 页面 + Time RPC + Keeper RPC | Standard；同一账户有 2 笔或 3 笔使用不同 market/direction 组合的 Position，positionKey 均可唯一识别；全部仍在保护期且不可清算；P2 最先到期；每笔均有可区分的 `Open` TP/SL；两腿费用与容量充足 | D1：N=2；D2：N=3；P2 的 `graceEnd=T`，P1/P3 的 `graceEnd>T`；在 `T−1` 选中 P2；记录每笔 Position、TP/SL、OI 与余额基线 | D1/D2 分别执行：构造并记录全部仓位与保护单；在 `T−1` 点击第二笔仓位并确认 selectedPositionId/positionKey=P2；推进至 T，确认 P1/P3 仍在保护期且页面未切换选择；点击 P2 的“关闭并重新开仓”；依次执行 P2 的全量 `MarketDecrease` 与 `MarketIncrease`；在两腿后分别查询所有 Position、TP/SL、OI、订单、费用与页面选择状态 | 每笔 positionKey、market/direction、selectedPositionId、倒计时/到期状态、CTA 绑定对象；两腿 payload/orderKey；全部 Position 字段及 TP/SL 状态；分市场/方向 OI、余额与费用差分 | 从 T−1 到 T，选中对象始终为 P2；P2 到期时仅其卡片显示可用入口，页面不得自动切到仍在保护期的 P1/P3 或混用其数据；全流程只为 P2 创建一笔 100% `MarketDecrease` 和一笔 `MarketIncrease`，且两腿参数均来自 P2 冻结快照；P2 旧 TP/SL 随全平自动取消，新 P2 Position 建立新保护期且不继承旧保护单；P1/P3 的 Position、grace、TP/SL、OI、余额和费用均不变，也不得产生钱包请求或订单；重开成功后页面仍指向新的 P2 仓位上下文 | 两腿共性断言复用 XT-PROT-ROLL-028～031；完成后市价关仓 P2 新仓并恢复 P1/P3 snapshot | 最小多对象边界（2/3）+状态迁移+选择持久性+对象隔离 | T87 `0x9ae0b9e06bb17b00682e284957eCe229B7b74B91` |

## 4. 订单类型主用例（16 条原子用例）

> 本节只登记“前端业务类型—合约 OrderType—触发条件—可接受价—订单终态”的映射断言。凡已在 B、C 或第 5 节设计的交易动作均直接引用原子用例证据，不再独立执行或重复造数；仅缺少来源原子的订单类型在本节单独执行。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| XT-ORD-MI-001 / P0 | 市价开仓_`MarketIncrease`_long/short_各建立一笔正确方向仓位 | 合约层 + 前端层 | 复用关联用例证据 | 无仓位；Oracle/Keeper 正常；余额充足 | `MarketIncrease`；long、short；固定 USD size/collateral | 复用 B1.1 XT-MKT-OPEN-001/002 的执行证据；核对前端业务类型与链上 `MarketIncrease` 映射 | payload 全字段、OrderCreated/Executed、Position、OI、余额 | long/short 各只建立一笔正确方向仓位；页面类型与链上 type=0 一致；账本守恒 | 等价类；XT-MKT-OPEN-001/002 |
| XT-ORD-MI-002 / P0 | 市价开仓_`MarketIncrease`_long/short_acceptablePrice 等号成交 | 合约层 + 前端层 | 复用关联用例证据 | 可精确控制 Oracle 与价格影响 | long/short；executionPrice 分别为 A−1、A、A+1（按各方向有利/不利定义） | 复用 XT-PRICE-005 的市价开仓数据；核对 `acceptablePrice` 三点边界与 `MarketIncrease` 终态 | acceptablePrice A、E、订单终态、取消/冻结原因 | 等号成交；有利侧成交；最小不利越界按合约规则不成交且无仓位/资金损失 | 边界值+判定表 |
| XT-ORD-LI-001 / P0 | 限价开仓_`LimitIncrease`_long_price.max>triggerPrice 时保持 Open | 合约层 + 前端层 | 复用关联用例证据 | 可控 Oracle；无仓位 | `LimitIncrease long`；P−1、P、P+1 | 复用 B3 XT-LMT-TRG-007 的执行证据；核对前端限价开仓与链上 `LimitIncrease` 映射 | Oracle max、triggerPrice、validFrom、Order Store、Position | max>trigger 时保持 Open；max=trigger 与 max<trigger 时进入执行校验；成功后建立多仓 | 三点边界 |
| XT-ORD-LI-002 / P0 | 限价开仓_`LimitIncrease`_short_price.min<triggerPrice 时保持 Open | 合约层 + 前端层 | 复用关联用例证据 | 可控 Oracle；无仓位 | `LimitIncrease short`；P−1、P、P+1 | 复用 B3 XT-LMT-TRG-008 的执行证据；核对前端限价开仓与链上 `LimitIncrease` 映射 | Oracle min、triggerPrice、终态 | min<trigger 时保持 Open；min=trigger 与 min>trigger 时进入执行校验；成功后建立空仓 | 三点边界 |
| XT-ORD-SI-001 / P0 | 条件开仓_`StopIncrease`_long_price.max<triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 可控 Oracle；无仓位 | `StopIncrease long`；P−1、P、P+1 | 每个数据点独立创建 `StopIncrease`；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle max、triggerPrice、页面 Type、链上 type | max<trigger 保持 Open；等号/越过触发；页面不得标为限价开仓 | 判定表；BUG-STOPTYPE-016 |
| XT-ORD-SI-002 / P0 | 条件开仓_`StopIncrease`_short_price.min>triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 可控 Oracle；无仓位 | `StopIncrease short`；P−1、P、P+1 | 每个数据点独立创建 `StopIncrease`；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle min、triggerPrice、页面 Type、链上 type | min>trigger 保持 Open；等号/向下越过触发；页面不得标为限价开仓 | 判定表；BUG-STOPTYPE-016 |
| XT-ORD-MD-001 / P0 | 市价减仓_`MarketDecrease`_部分减仓与市价关仓均保持 Reduce-only | 合约层 + 前端层 | 复用关联用例证据 | 已有 long、short 仓位 | `MarketDecrease`；25%、50%、100%；USD/Token 两种 size 口径 | 复用 B2 的市价部分减仓证据与 B1.2 的市价关仓证据；核对前端业务类型与链上 `MarketDecrease` 映射，不独立重复执行 | type=2、sizeDelta、isSizeDeltaUsd、PnL、费用明细、剩余仓位 | Reduce-only；部分减仓按指定规模；100% 后仓位清零；页面与账本一致 | 等价类+边界；关联 B1.2、B2 |
| XT-ORD-LD-001 / P0 | 止盈_`LimitDecrease`_long_price.min<triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；可控 Oracle | `LimitDecrease` 多头 TP；P−1、P、P+1 | 每个数据点独立创建 TP（`LimitDecrease`）；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle min、trigger、acceptablePrice、PnL、终态 | min<trigger 保持 Open；等号/上穿触发；盈利与 type=3 显示正确 | 三点边界 |
| XT-ORD-LD-002 / P0 | 止盈_`LimitDecrease`_short_price.max>triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；可控 Oracle | `LimitDecrease` 空头 TP；P−1、P、P+1 | 每个数据点独立创建 TP（`LimitDecrease`）；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle max、trigger、acceptablePrice、终态 | max>trigger 保持 Open；等号/下穿触发；空仓减仓方向和 acceptablePrice 正确 | 判定表 |
| XT-ORD-SL-001 / P0 | 止损_`StopLossDecrease`_long_price.min>triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 已有 long 仓位；可控 Oracle | `StopLossDecrease` 多头 SL；P−1、P、P+1 | 每个数据点独立创建 SL（`StopLossDecrease`）；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle min、trigger、acceptablePrice、Frozen/Executed | min>trigger 保持 Open；等号/下穿触发；损失方向和 type=4 正确 | 三点边界 |
| XT-ORD-SL-002 / P0 | 止损_`StopLossDecrease`_short_price.max<triggerPrice 时保持 Open | 合约层 + 前端层 | RPC + 页面 | 已有 short 仓位；可控 Oracle | `StopLossDecrease` 空头 SL；P−1、P、P+1 | 每个数据点独立创建 SL（`StopLossDecrease`）；将 Mock Oracle 价格更新至指定价格；Keeper 尝试执行；查询 Order 与 Position | Oracle max、trigger、acceptablePrice、哨兵、终态 | max<trigger 保持 Open；等号/上穿触发；平空哨兵和 type=4 正确 | 判定表；BUG-ACCEPTSENTINEL-018 |
| XT-LIQ-001 / P0 | 清算_`Liquidation`_未达、等于、越过清算条件_未达条件拒绝 | 合约层 + 前端层 | RPC + 页面 | long/short 危险仓；Liquidation Keeper 可用；点差 fixture 的 configured MIN≤0、MAX≥0 | 未达、等于、越过清算条件；balanceWasImproved true/false | 每个数据点独立将 Mock Oracle 价格更新至指定清算条件；Liquidation Keeper 尝试清算；查询 Position、关联订单与账本 | type=5、executionPrice、dynamicSpread、费用档位、残值、事件 | 未达条件拒绝；满足条件清算；该 fixture 的 effectiveMin=0，负 raw 的 final=0；费档与账本正确 | 边界+判定表；XT-LIQ-003 |
| XT-ADL-001 / P0 | 自动减仓_ADL_不满足处置条件时拒绝 | 合约层 + 前端层 | RPC + 页面 | long/short 仓位；满足/不满足 ADL 两组；configured MIN≤0、MAX≥0 | `SecondaryOrderType.Adl`；raw spread 负值 | 分别准备满足与不满足 ADL 条件的独立仓位；ADL Keeper 尝试执行；查询 Position、OI 与事件 | secondary type、选仓、executionPrice、OI/PnL/事件 | 不满足条件拒绝；满足时按 ADL 处置；effectiveMin=0 时负 raw 的 final=0；前端不显示成用户市价关仓 | 等价类；CT-ADL-004 |
| XT-ORD-LIFE-001 / P0 | 订单生命周期_非市价订单_validFrom=T_T−1 不执行 | 合约层 + 前端层 | RPC + 页面 | 可控 Oracle/Keeper | 任一非市价单；validFrom=T；T−1/T/T+1 | 创建 validFrom=T 的非市价订单；在 T−1、T、T+1 独立数据集由 Keeper 尝试执行；查询 Order 与事件 | block timestamp、validFrom、Order Store、事件 | T−1 不执行；T/T+1 进入价格校验；页面不提前显示 Executed | 边界值 |
| XT-ORD-LIFE-002 / P0 | 订单生命周期_Frozen 后 Keeper 重试、更新解除冻结与用户取消_三种恢复路径分别符合状态规则 | 合约层 + 前端层 | 复用关联用例证据 | 已有容量漂移导致的 `Frozen` 条件订单 | Keeper 重试、更新订单、取消订单三组独立数据 | 分别复用 XT-FRZ-RETRY-005、XT-FRZ-UPD-006 和 XT-FRZ-CAN-007 的执行证据；只核对前端业务类型与链上状态迁移映射 | `Frozen→Executed`、`Frozen→Updated/Open→Executed`、`Frozen→Cancelled`；executionFee、托管抵押、Position/OI 与页面状态 | 三种恢复路径分别有唯一终态；本行不重复发送业务交易，不将三个独立用例合并为一次执行 | 状态迁移；关联 XT-FRZ-RETRY-005 / UPD-006 / CAN-007 |
| XT-ORD-MODE-001 / P0 | 交易模式_Standard 与 1CT 公开切换_手填/派生字段分流且切换本身无业务副作用 | 合约层 + 前端层 | 复用关联用例证据 | 两种公开模式的功能开关、链与 Router 可用；页面已填写合法 Market/Limit 表单及 TP/SL | Standard→1CT→Standard；手填金额/Max；历史 localStorage=`flash`；另有 1CT task pending 和 Order `Open` | 复用 FT-RELAY-SWITCH-022、FT-RELAY-MODE-023、FT-RELAY-LEGACY-024、FT-RELAY-MAX-025/026 与 FT-RELAY-QUOTE-027 的页面证据；pending 组额外记录切换前的 amountRaw、signed `maxFeeAmount`、session signer、Router、taskId/txHash/orderKey，切换后只为下一笔草稿重新计算 | 模式偏好、有效模式、选中卡、手填/派生字段；在途 payload/signer/Router/taskId/txHash/orderKey；下一笔钱包请求、task、tx、Order/Position/余额 | 公开 UI 只有 Standard/1CT；切换动作本身无签名、task、交易或订单；非派生字段和手填金额保持，Max/百分比按当前模式重算；已提交的 1CT task 与 `Open` Order 必须继续按原 payload、session signer 和 Router 跟踪，不得因切到 Standard 被改路由、重签、取消或重发，只有下一次用户明确提交才使用 Standard；历史 `flash` 由 FT-RELAY-LEGACY-024 阻止 zombie 提交 | 证据复用+状态转换+在途对象隔离；不重复发送已覆盖交易 |

## 5. 其他 P0/P1 跨域用例（68 条原子用例）

### A. 动态点差、成交价与可接受价（6 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| CT-PRICE-001 / P0 | 交易定价_普通市价开仓或市价减仓改善 OI 失衡并形成负 raw spread_允许价格改善 | 合约层 | RPC | 可控 OI 和 Oracle | 改善失衡的普通市价开仓/市价减仓，构造负 raw spread | 读取操作前 OI/深度/配置并独立推导 raw；提交并签名指定市价订单；Keeper 执行订单；查询 Position/OI、事件 final spread 与 executionPrice | 独立推导的 raw、事件/Reader final spread、executionPrice、balanceWasImproved | 普通订单允许负点差；成交价按有符号值计算 | B32-1-06 |
| CT-PRICE-002 / P0 | 交易定价_raw spread 分别低于、等于、高于 MIN_final=max(raw, MIN)，等号不产生额外偏差 | 合约层 | Admin RPC + RPC | CONFIG_KEEPER 可写 | raw spread 分别低于、等于、高于 MIN | 每个数据点独立设置 MIN 与 OI；按前态配置/OI 独立推导 raw；提交并签名同方向市价订单；Keeper 执行订单；读取事件/Reader final spread 与 executionPrice | MIN、独立推导 raw、公开 final、executionPrice | final=max(raw, MIN)，等号不产生额外偏差 | B32-1-03；Foundry 单元层已覆盖，E2E 链路仍需覆盖 |
| CT-PRICE-003 / P0 | 交易定价_raw spread 分别低于、等于、高于 MAX_final=min(raw, MAX) | 合约层 | Admin RPC + RPC | CONFIG_KEEPER 可写 MIN/MAX_DYNAMIC_SPREAD | raw spread 分别低于、等于、高于 MAX | 每个数据点独立设置 MAX 与 OI；按前态配置/OI 独立推导 raw；提交并签名同方向市价订单；Keeper 执行订单；读取事件/Reader final spread 与 executionPrice | MAX、独立推导 raw、公开 final、executionPrice | final=min(raw, MAX) | B32-1-04；Foundry 单元层已覆盖，E2E 链路仍需覆盖 |
| CT-PRICE-004 / P0 | 交易定价_指数输入到封顶前、等于、越过_封顶行为精确且无溢出 | 合约层 | RPC | 可制造极大 size/OI | 指数输入到封顶前、等于、越过 | 按 size/parameter/depth 独立推导指数；报价并执行或验证拒绝 | 独立推导的 exp 输入/priceImpact，事件/Reader final spread、gas/revert | 封顶行为精确且无溢出；订单终态可解释 | B32-2 |
| XT-PRICE-005 / P0 | 交易定价_市价开仓与市价关仓的 long/short 四象限_executionPrice=acceptablePrice 时成交 | 合约层 + 前端层 | RPC + 页面 | Oracle 可精确控价 | 市价开仓 long、市价开仓 short、市价关仓 long、市价关仓 short；E=A 与不利越界一步 | 每个方向独立提交并执行 executionPrice=acceptablePrice 及最小不利越界数据；查询订单终态与取消原因 | acceptablePrice A、executionPrice E、订单事件/取消原因 | E=A 成交；越过最小不利步长取消；页面显示正确原因 | 已有 8 数据集脚本 |
| FT-PRICE-006 / P0 | 交易定价_正 spread、负 spread、clamp 与 token 精度_页面保留符号并使用合约舍入口径 | 前端层 | 页面 | 前端接入 v0.3.2 SDK；8 位市场未部署前对应数据集 BLOCKED | 正 spread、负 spread、clamp；8/18 位 index token 经济等价组 | 分别读取各组市价开仓/市价关仓预览；提交并签名；核对 payload 与最终展示；8/18 位组比较 token 原始值、OI/skew 和执行价 | 页面执行价、价格影响符号、Pay/Receive、payload、token decimals 与原始整数 | 负点差不得被当成 uint、0 或绝对值；展示与合约同舍入口径；不得把 index token 固定按 18 位换算。当前 `executionPrice.ts` / `configureOracle.ts` 的 18 位硬编码先记静态 GAP，不因 8 位环境 BLOCKED 而消失 | v0.3.2 新增前端适配；精度等价类 |

### B. 资金费、清算与自动减仓（6 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| CT-FUND-001 / P0 | 资金费_成功订单执行前推进连续 EMA 与四指数_不遍历无关仓位结算 | 合约层 | Time RPC + Admin RPC + Keeper RPC | v0.3.2 time-fork；可控 long/short OI、Oracle 与成功订单；另有零 OI/前态 EMA 非零数据集 | 单边/平衡/零 OI；dt=0/1/3599/3600/3601/>41×3600 | 每点独立 snapshot；记录 OI、旧 EMA/指数/timestamp 与无关仓位；通过一笔最小合法订单触发执行链内 `updateFundingState`；读取事件和后态 | raw skew、连续衰减 EMA、四 per-size、timestamp、`Funding`/per-size 事件、无关 Position/余额 | 3600 是时间常数而非离散开关；非零 OI 按公式推进但不遍历结算无关仓位；零 OI 时 per-size 不变、timestamp 更新、EMA 写零并发 `Funding(0,0,0)`；后续订单失败则更新整体回滚 | 时间边界+状态迁移；无独立 Funding Keeper worker |
| CT-FUND-002 / P0 | 资金费_加仓、保证金动作、部分减仓、全平、清算与 ADL_按动作前全仓快照差结算 | 合约层 | Time RPC + Admin RPC + Keeper RPC | 有累计 Funding 的 long/short 仓位；可控 collateral min/max；协议 FeeHandler/FEE_RECEIVER 可读 | negative/positive/相等；最小舍入；部分/full；足额；普通动作不足；full Liquidation/full ADL 不足；Reader long/short 不对称 | 每组独立执行对应仓位动作；记录市场指数、Position 快照、理论与 actual Funding；核对 claimable、Market Vault、PositionVault、FeeHandler/receiver 与不足事件/回滚 | negative/positive amount、快照、`PositionFees*`、`InsufficientFundingFeePayment`、claimable/Vault/receiver/用户余额、Reader preview、全状态前后值 | 已有仓位每次按动作前整个 Position 结清旧累计；应付 ceil@min、应收 floor@max；negative 净额形成协议 claimable，positive 净额由 Market Vault→PositionVault；领取终点由 FEE_RECEIVER 决定；普通 Increase/Decrease（含普通全平）资金不足时整体回滚且不留不足事件，只有 full Liquidation/full ADL 可成功早退并留下 expected/actual/step；Reader long/short 当前均按 GAP 对照实际；无虚构坏账 ledger | 等价类+取整+资金守恒；需要 v0.3.2 time-fork |
| XT-LIQ-003 / P0 | 清算_balanceWasImproved=true/false_最终 spread 最低为 0 | 合约层 + 前端层 | 页面 + Admin RPC + Keeper RPC | 可控危险仓位、价格与 configured MIN≤0、MAX≥0 | balanceWasImproved true/false | 将 Mock Oracle 价格更新至清算条件；Liquidation Keeper 执行清算；查询 Position、关联订单与账本 | executionPrice、spread、费用档位、remaining collateral、事件 | 该 fixture 的 effectiveMin=0，负 raw 的 final spread=0；手续费档位正确；账本守恒 | UT-R5B02；E2E 链路仍需覆盖 |
| CT-LIQ-FEE-005 / P0 | 清算费_偿付充分的 full Liquidation_LIQUIDATION_FEE_TYPE 从计算到最终接收完整守恒 | 合约层 | Admin RPC + Keeper RPC | 可清算且偿付充分的 full-close 仓位；可控 liquidationFeeFactor、receiverFactor；FeeHandler、FEE_KEEPER、FEE_RECEIVER 可读 | factor=0/普通值；receiverFactor=0/中间值/1e30；claimable=0/正值；重复 claim；无/正确 FEE_KEEPER withdraw | 每组独立执行清算并按整数公式复算 Liquidation Fee；核对 pool 与 claimable；调用 `FeeHandler.claimFees`；先由无权限账户再由 FEE_KEEPER withdraw；重复 claim/withdraw | size、collateral price.min、fee factor、receiver factor、PositionVault/Market Vault、LIQUIDATION_FEE_TYPE claimable、FeeHandler available、FEE_RECEIVER/reward-splitter、事件与余额 | 判定阶段不计 Liquidation Fee；执行按 floor/ceil/floor 顺序；余数归 pool；claimable 清零并等额进入 available，只有 FEE_KEEPER 可 withdraw，最终 receiver 等额收到；零值/重复调用不多领；全链资金守恒 | 公式边界+状态迁移+权限+幂等 |
| CT-LIQ-REF-006 / P1 | 清算 Referral_偿付充分且 affiliate reward 非零_累计并从 PositionVault 领取 | 合约层 | Admin RPC + Keeper RPC | 可清算且偿付充分的仓位；trader 绑定 referral code/affiliate；Close Fee 与折扣可精确复算；affiliate claim 入口可用 | 无 referral/有 referral；reward=0/1/普通值；领取 receiver；重复领取；insolvent empty-fees 对照 | 每组独立执行 full Liquidation；读取 PositionFees/referral 与 affiliateReward key/pool 汇总；affiliate 调 `claimAffiliateRewards` 后重复领取；另跑 Fee-step insolvent 对照 | trader discount、affiliateRewardAmount、affiliateReward key/pool、PositionVault、receiver、AffiliateRewardUpdated/Claimed、PositionFeesInfo/Collected | Liquidation 仍执行 `handleReferral`；偿付充分时按实际 fees 累计 reward，领取清零账户 key 并递减 pool、从 PositionVault 等额转 receiver；重复不多领；Fee-step empty-fees 不虚增 reward；不得与 LIQUIDATION_FEE_TYPE FeeHandler 路径混算 | 等价类+领取生命周期+资金守恒 |
| CT-ADL-004 / P0 | 自动减仓_ADL_raw spread 负/正与 effective MAX 边界_final 按特殊订单区间 clamp | 合约层 | Admin RPC + Keeper RPC | 可构造 ADL 条件；configured MIN≤0；可分别写 MAX=0 与 MAX>0 | raw<0、raw=0、0<raw≤MAX、raw>MAX；MAX=0/>0 | 每组独立构造 ADL 条件与指定 raw/MAX；ADL Keeper 执行；查询 Position、OI、执行价与事件 | configured/effective MIN/MAX、独立 raw、final spread、execution price、PnL/OI/事件 | effectiveMin=0；MAX=0 时负/零/正 raw 均 final=0；MAX>0 时负/零 final=0，区间内正 raw 保留，越界钳到正 MAX | 边界值+场景法 |


### C. Relay、Flash One-Click、代付费、子账户与兼容性（42 条原子用例）

> 执行口径：Relay/Gasless 使用主钱包逐动作 EIP-712 和 `RelayRouter`；Flash One-Click/1CT 使用 session action signature 和 `SubaccountRelayRouter`。CURRENT 固定前端仍缺 v0.3.2 `slot` schema，这是功能 `GAP`；目标 tx-fork 前端/Relay 未准入是环境 `BLOCKED`，两层结果必须分开登记。直接 Relay 设置卡已按产品决定隐藏，direct 路径只从 Relay RPC/SDK 覆盖，不得将其作为第三张公开模式卡的通过标准。历史 ID 中的 `FLASH` 仅作稳定编号，不代表可以省略具体路径。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| CT-RELAY-001 / P0 | 代付费（Relay Fee）_全局抵押品 Token 地址为零_返回 EmptyToken 且不创建订单或扣费 | 合约层 | Relay RPC | v0.3.2 Relay 可调用；可读写全局 COLLATERAL_TOKEN | R1=全局键为零；R2=全局键为合法抵押品 | 每个数据点独立设置全局抵押品 Token；生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询配置、业务订单、`RelayFeePaid` 与相关余额 | 配置读值、revert、Order Store、账户余额 | R1 `EmptyToken`，不创建订单、不扣费；R2 进入后续校验 | 边界/故障注入；B32-3 |
| CT-RELAY-002 / P0 | 代付费（Relay Fee）_feeToken 分别为抵押品、其他 Token 和零地址_仅抵押品 Token 通过 | 合约层 | Relay RPC | COLLATERAL_TOKEN 已配置 | feeToken=抵押品、其他 Token、零地址 | 每个 feeToken 数据点独立生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | feeToken、revert selector、Order、余额 | 仅抵押品 Token 通过；其他 Token/零地址均返回 `UnexpectedRelayFeeToken`；失败原子回滚 | 等价类 |
| CT-RELAY-003 / P0 | 代付费（Relay Fee）_maxFeeAmount=F−1、F、F+1_F−1 时业务动作回滚 | 合约层 | Relay RPC | 可固定 gas/Oracle 报价并算出实际 Relay Fee F | maxFeeAmount=F−1、F、F+1 | 每个 maxFeeAmount 数据点独立生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | 实际 F、上限、RelayFeePaid、双方余额 | F−1 返回 `InsufficientRelayFee` 且业务动作回滚；F/F+1 成功且只扣实际 F | 三点边界 |
| CT-RELAY-004 / P1 | 代付费（Relay Fee）_同一 Relay sender 分别为 excluded=false 和 true_仅 false 扣费 | 合约层 | Admin RPC + Relay RPC | CONTROLLER 可配置 Relay Fee excluded | 同一 Relay sender：excluded=false/true | 每个 excluded 状态独立设置配置；生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | feeAmount、事件、用户/Relay 余额、Order | false 正常扣费；true 不扣 Relay Fee，但订单动作仍成功且经济结果除 Relay Fee 外一致 | 开关判定表 |
| CT-RELAY-005 / P1 | 代付费（Relay Fee）_nativeFee=0_事件记录 feeAmount=0 且无 Token 转账 | 合约层 | Relay RPC | 可构造 nativeFee=0 或测试桩等效条件 | nativeFee=0 | 设置 nativeFee=0；生成 EIP-712 签名；通过 Relay 提交业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | RelayFeePaid、转账日志、余额 | 事件记录 feeAmount=0；无 Token 转账；订单动作成功 | 零值等价类 |
| CT-RELAY-006 / P1 | 代付费（Relay Fee）_multiplier=0、1e30、1.2e30_零值按默认倍率计算 | 合约层 | Admin RPC + Relay RPC | CONFIG_KEEPER 可写倍率；其他条件固定 | multiplier=0、1e30、1.2e30 | 每个 multiplier 数据点独立设置倍率；生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | gasUsed、relayGasLimit、nativeFee、feeAmount | 0 按默认 1e30；1e30 同基准；1.2e30 按配置放大并向上取整 | 判定表+公式复算 |
| CT-RELAY-007 / P0 | 代付费（Relay Fee）_cap=0、N−1、N_零值不限制且等号成功 | 合约层 | Admin RPC + Relay RPC | 可写 MAX_RELAY_SWAP_WNT_CAP；先算 nativeFee N | cap=0、N−1、N | 每个 cap 数据点独立设置上限；生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | nativeFee、cap、revert、余额、Order | cap=0 不限；N−1 返回 `MaxRelaySwapWntCapExceeded`；N 等号成功 | 三点边界 |
| CT-RELAY-008 / P0 | 代付费（Relay Fee）_calldata 长度为 49,999、50,000、50,001 bytes_仅超上限回退 | 合约层 | Relay RPC | 可构造不同 calldata 长度 | 49,999、50,000、50,001 bytes | 每个 calldata 长度数据点独立生成 EIP-712 签名；通过 Relay 提交等价批量请求；查询业务订单、`RelayFeePaid` 与相关余额 | calldataLength、calldata gas、revert、业务原子性 | 49,999 与 50,000 bytes 进入执行；50,001 bytes 回退；不得部分创建、更新或取消订单 | 边界值+原子性 |
| CT-RELAY-009 / P0 | 代付费（Relay Fee）_WNT 与 feeToken 双价_按 max/min 换算并向上取整 | 合约层 | Relay RPC | 可控 WNT/feeToken secondary price | 单价；min<max 双价；价格最小单位变化 | 每个价格数据点独立生成 EIP-712 签名；通过 Relay 提交同一业务请求；查询业务订单、`RelayFeePaid` 与相关余额 | WNT price.max、feeToken price.min、向上取整、feeAmount | 严格按 max/min 换算并向上取整；事件和余额差分精确等于公式值 | 公式+边界；Oracle 联动 |
| XT-RELAY-010 / P0 | 代付费（Relay Fee）_三条技术提交路径使用相同业务参数_身份、路由与资金流可解释 | 合约层 + 前端层 | 页面 + Relay RPC | Standard 和 1CT 页面路径已准入；direct Relay 可由 Relay RPC/SDK 构造；Relayer 有 WNT 与 Router allowance | 同市场、方向、订单类型、规模、价格；用户 native/WNT=0；非补贴/补贴 update | 页面执行 Standard/1CT，Relay RPC 执行 direct Relay；查 signer、Router、Order/Position、Relayer WNT→OrderVault、用户 feeToken→Relayer、execute/cancel/update 退款 | Order 字段、Position、Execution Fee、Relay Fee、signer、`tx.from/to`、WNT/feeToken/native 余额、refundReceiver | 三路业务结果一致；direct Relay/1CT 收 Relay Fee，Standard 不收；Relayer 先垫 WNT、用户以 feeToken 偿付，正常余额退款以 native 归用户 receiver，补贴 update 按合约路径退 WNT；不重复扣费 | 三路成组比较+双币守恒；CMB-A-006 使用本版 override |
| XT-FLASH-002 / P0 | Flash One-Click/1CT_`string slot` 创建、替换、删除与异常输入_合约最多 4 个 active slot | 合约层 + 前端层 | RPC + 页面 | v0.3.2 合约可调；页面层须先补齐 `slot` schema/ABI；各样本独立 snapshot | 空/超长/大小写不同 slot；zero/重复 subaccount；第五槽；同名替换；过期；removeSubaccount/removeSlot 存在与不存在；旧地址复用 | 先用 RPC 逐个固定合约语义，再用页面核对 typed data、slot 管理和 Reader；记录移除前后 expiry/max/actionCount | approval signer、typed data、slot 原字符串、subaccount、isActive、active count、nonce、Reader、事件、历史权限字段 | CURRENT 合约：4 个 active slot；空/超长 slot 不被长度或非空规则拒绝，zero subaccount 也可占位但不能通过动作校验；第五槽 revert；`removeSubaccount` 不释放槽且不清历史字段，`removeSlot` 才释放；移除不存在项的事件差异按实现记录；页面无完整多槽管理则记 GAP，不伪造拒绝规则 | 状态迁移+边界+地址复用；DEC-TRADE-006 |
| FT-FLASH-003 / P0 | Flash One-Click/1CT_使用 v0.3.1 无 slot 格式签名_旧授权不得被识别为有效 | 前端层 | 页面 | 旧授权或旧 typed data | v0.3.1 格式签名 | 在页面尝试恢复并使用旧授权；查询提示、签名 schema 与链上结果 | 页面提示、typed data、Relay task、链上结果 | 不误认为有效；引导按 v0.3.2 重新授权；不发送已知不兼容请求且不得无限 pending | v0.3.2 迁移专项 |
| XT-FLASH-004 / P0 | 三条技术提交路径_相同市场、方向和规模_身份与路由不同但经济结果一致 | 合约层 + 前端层 | 页面 + Relay RPC | Standard/1CT 公开模式与 direct Relay 技术路径均已准入 | 同市场、方向、订单类型、规模、价格 | 页面执行 Standard/1CT，Relay RPC 执行 direct Relay；恢复签名者并查询 `tx.from/to`、payload、task、Order、Position、费用、余额与计数 | main/session signer、`tx.from/to`、account、Relay Fee、task、action count、终态账本 | Standard 为主钱包→ExchangeRouter；direct Relay 为主钱包动作签名+Relayer→RelayRouter；1CT 为 session 动作签名+Relayer→SubaccountRelayRouter；1CT 外层 `tx.from` 是 Relayer 而非 subaccount；经济 account 与账本一致 | 三路等价性；CMB-A-006 使用本版 override |
| FT-RELAY-011 / P0 | 代付费（Relay Fee）_前端估算的 maxFeeAmount 授权上限_取消/更新限价单不得因授权不足回滚 | 前端层 | 页面 | 1CT 公开路径可用；前端部署含目标修复；账户有两笔独立远价待成交限价单 | D1 余额充足；D2 采当次 quote 后使余额精确等于 computed `maxFeeAmount`；D3 余额为 computed `maxFeeAmount` 减 1 个原始单位 | 每档独立 snapshot；D1 用订单 A cancel、订单 B update；D2/D3 先取当次 fresh quote 再构造余额；提交时核对实际 signed max 与预计算值一致，并采 display/actual fee 与终态 | 动作独立 computed/signed maxFeeAmount、displayFee、RelayFeePaid.feeAmount、订单 A/B 终态、USDC 差分、失败参数 | D1 的 Cancel/Update 分别成功；D2 等号通过，仅当 computed cap 命中 0.5 floor 时才可简写为 0.50 USDC；D3 提交前拦截；actual fee 按实时公式复算且不超 signed max；direct Relay 仅可做 API/CT 对照，不伪装 FT 入口 | 动态三点边界；缺陷回归 Notion 3cc3d787…d8a0；详见 [cases/FT-RELAY-011.md](cases/FT-RELAY-011.md) |
| CT-RELAY-012 / P0 | 1CT 安全_子账户专属 Relay Fee USD cap_恶意 session 自任 Relayer 时必须受独立上限约束 | 合约层 | Admin RPC + Relay RPC | 隔离 fork；主账户已授权测试 session；可控 gas price、全局 WNT cap、子账户 USD cap 与余额 | 全局 WNT cap=0/非 0；子账户 USD cap 的 C−1/C/C+1；高 gas price；单次/重复调用 | session 作为调用者签请求并自任 Relayer；设置较大 maxFeeAmount；各配置独立执行 fee-only 或最小业务动作；查询 revert、事件、Order 与双方余额 | 两类 cap 原始值、nativeFee、feeAmount、maxFeeAmount、`RelayFeePaid`、主账户/调用者余额、Order 原子性 | 安全标准：子账户专属 USD cap 必须独立强制，C 等号通过、C+1 拒绝，失败不扣费不建单，重复调用不能绕过；CURRENT v0.3.2 未读取该键，预期暴露 R8-B02，修复前不得把 1CT 资金权限判安全 | 安全负向+三点边界+重复调用；R8-B02 |
| CT-RELAY-013 / P1 | 1CT 安全_Relay create/update 的 callback execution fee cap_非空 callback 不得套取主账户资金 | 合约层 | Admin RPC + Relay RPC | 隔离 fork；主账户已授权测试 session；攻击 callback 可记录退款；execution fee 与 cap 可控 | create/update/batch；callback=0/攻击合约；executionFee=max−1/max/max+1/极大值 | session 自任 Relayer；分别构造 create/update/batch payload；执行后核对 OrderVault、callback 退款、主账户、Relayer 与攻击合约余额 | `shouldCapMaxExecutionFee` 实际路径、executionFee、cap/refund、callback 事件、各方余额、Order 原子性 | 安全标准：非空 callback 的子账户订单必须封顶，越界拒绝或安全裁剪，不能把主账户资金退款到攻击 callback；CURRENT Relay 路径固定 false，执行安全断言时预期得到 `FAIL`，未实现封顶能力在准入层另记 `GAP` | 安全负向+三点边界+资金守恒；R8-B21 |
| CT-RELAY-AUTH-014 / P0 | Relay 调用者权限_任意 caller 持合法 payload 可提交并成为 Relay Fee 收款人_CURRENT 无 Keeper ACL | 合约层 | Relay RPC | 隔离 fork；可用主钱包/session；两个不同 caller；可控 chainId/deadline/nonce | 正常 caller、非 Keeper caller、signer/domain/chainId/deadline 错误、完全相同 digest 重放 | 分别向 RelayRouter 和 SubaccountRelayRouter 提交；恢复 signer；查 caller、fee recipient、digest、Order 和余额 | `tx.from`、recovered signer、EIP-712 domain、deadline、digest 消耗、`RelayFeePaid` recipient、Order/余额 | CURRENT 事实：非 Keeper caller 持合法 payload 也成功且获得 Relay Fee；签名/domain/chain/deadline/重放无效时原子拒绝。本结果只证明 permissionless 实现，不代表安全验收通过 | 权限等价类+重放；DEC-TRADE-002 |
| FT-RELAY-USDC-015 / P0 | Relay/1CT Increase Max_两级预留与确定性零值死区_严格覆盖 C+U 边界 | 前端层 | 页面 + state/component instrumentation + RPC 只读 | 6 位 USDC 同时为 pay/fee token，`U=1 USDC=1_000_000 raw`；fresh quote 可控且 `C=maxFeeAmount≥0.5U`；wallet 是唯一约束；Add 组已有同向 Position；1CT 准入按层登记 | Market/Limit/Stop Increase；open/add；long/short；pay/gross-up/size；桌面/移动；`B=C+U−1/C+U/C+U+1 raw`、1.48/1.50/1.500001U（C=0.5U）与普通大余额；Standard 对照；index token 6/8/18 位 | 每 variant 独立 snapshot；注入 fresh `C` 和精确 B；点击 Max 并采 input string、state/raw budget、校验状态；0 结果前分别用空输入和旧输入；用 builder instrumentation 检查全组合，Market/Limit/Stop 各至少一组真实提交捕获 payload；hidden direct Relay 与 1CT 各取一组技术路径证据 | B、C、U、spendable、MaxBudget、input/raw/payload、舍入 dust、按钮/提示、订单类型/方向/前态、Router/task/Order | CURRENT 唯一公式 `B>C+U ? B−C−U : 0`；−1 与等号为 0，+1 为 1 raw；C=0.5U 时 B≤1.5U 全为 0。Pay/gross-up/size 从同一 budget 推导且不超余额；index token decimals 只影响 size 换算。当前 0 时 handler 直接 return、不清旧值、不置灰、不提示，执行 UI 断言应暴露并登记 GAP；Standard 当前仍减 U，作为作用域 GAP 留证 | 三点 raw 边界+判定表+状态残留；v0.3.2 Max 裁决 |
| FT-RELAY-USDC-016 / P0 | Relay/1CT 手工金额_业务余额与 signed cap 准入边界_显式切 Standard 后再确认 | 前端层 | 页面 + RPC 只读 | fresh quote 得到 `M=maxFeeAmount>1 raw`；固定手工业务扣款 `T`，Pay 已是 gross debit 时不重复加 Position Fee；表单完整 | Market/Limit open/add、long/short；`B=T−1/T/T+M−1/T+M/T+M+1 raw`；订单类型、方向、手填金额、杠杆、限价、滑点 | 每档独立设置余额并尝试 CTA；记录第一错误和有无 Switch；在 `T≤B<T+M` 点击 Switch，比较字段并查签名/task/tx/Order；再由用户点击 Standard CTA 并等 Order 创建 | T、M、B、blockReason、提示原文、全部手填字段、钱包请求、task/tx、`tx.from/to`、RelayFeePaid、Order 数 | `B<T` 为业务余额不足；`T≤B<T+M` 精确显示 `Insufficient USDC for Relay Fee` 与 `Switch to Standard`；`B=T+M` 和 +1 通过本余额门。切换保持手填值及其他字段，零签名/task/tx/Order；再次点击后仅主钱包→ExchangeRouter、零 Relay task/RelayFeePaid、恰好一笔 Order | 分类树+五点边界+无副作用；v0.3.2 signed-cap gate |
| CT-RELAY-BATCH-017 / P0 | Relay batch_空批、混合批、失败回滚_固定 create→update→cancel 且只收一次 Relay Fee | 合约层 + 前端层 | Relay RPC + 页面 | 可构造 pending Order；可控任一腿失败；1CT 已授权 | 空 batch；create/update/cancel 各 1；中间腿 revert；Cancel All；开仓附 TP/SL | 签并提交 batch；查 Router 处理顺序、task/tx、所有 Order、RelayFeePaid 次数、action count 差分；失败组独立 snapshot | batch 数组长度、动作顺序、Order 前后、事件、task/tx、Relay Fee 次数、actionCount | 空 batch 按 `RelayEmptyBatch` 拒绝；混合批按 create→update→cancel 原子执行；任一腿失败全部回滚且不扣费；成功批只有一个 task、一笔外层 tx 和一次 Relay Fee；1CT actionCount 增量=所有动作腿数，不是 1 | 批量原子性+计数守恒 |
| FT-RELAY-MAX-018 / P0 | Adjust Margin Deposit Max_钱包上限与风险上限_两级预留、百分比和手填边界分离 | 前端层 | 页面 + state/component instrumentation + RPC 只读 | 已有 long/short Position；6 位 USDC collateral/fee token，`U=1 USDC=1_000_000 raw`；fresh `C`；可构造 riskMax≥B 与 riskMax<B；1CT 准入按层登记 | wallet-bound/risk-bound；Standard/1CT；`B=C+U−1/C+U/C+U+1 raw` 与普通余额；Max、25/50/75%、手填 `B−C` 等号；已有 Position 的 size/collateral/leverage 固定 | 每组记录 Position 风险输入、B/C/U；点击百分比/Max 或手填；采 amountRaw、selectedPercent、校验与提示；代表组真实签名提交并执行 MarketIncrease，查询 Position/余额；切模式/刷新 quote 后观察选中值重算 | riskMax 推导、depositMaxRaw/depositWalletMaxRaw、amount raw/display、selectedPercent、blockReason、payload、Position collateral/size、余额/task/tx | 1CT Max=`min(riskMax,max(B−C−U,0))`，严格边界同 015；wallet/risk 较小者生效。手填最多到 `min(riskMax,B−C)`，25/50/75% 按未扣额外 U 的 depositMaxRaw 计算，故额外 `U` 只属于 Max；Deposit 增抵押不改 size，执行后 collateral 增量与 amount 一致。Standard 100% 当前仍扣 U，作为作用域 GAP 留证 | 决策表+风险/钱包双边界+状态迁移 |
| FT-RELAY-MAX-019 / P0 | 市价关仓_100%_不适用钱包 U reserve_仅验证 fee-only signed-cap 门 | 前端层 | 页面 + RPC 只读 | 已有 long/short Position；可创建全量 `MarketDecrease`；fresh `M=maxFeeAmount` | 100% sizeInUsd/sizeInTokens；1CT feeToken 余额 `B=M−1/M/M+1 raw`；Standard 对照；index token runtime decimals | 每组独立打开关仓面板并选择 100%；对拍 Position exact size；1CT 三档余额分别尝试 CTA，合法组捕获 payload/task；Standard 组捕获交易；查询订单与仓位 | Position exact bigint、B/M、输入/payload、blockReason、模式、签名/task/tx/orderKey、Position/OI/余额 | 100% 必须等于链上仓位全量，不从 size 额外扣钱包 `U`；1CT 仅执行 fee-only gate，M−1 阻断，M 等号与 M+1 通过；Standard 不显示 Relay Fee、不创建 Relay task；执行后均全平且不得把 index token 8/18 位误当 USDC reserve 精度 | 负适用性+三点边界+方向等价类；Withdraw Max 独立见 FT-RELAY-MAX-028 |
| FT-RELAY-QUOTE-020 / P0 | Relay Fee quote 生命周期_动作 consumer 与签后 fresh-cap 复查_异常不得混成余额不足 | 前端层 | 页面 + state/component instrumentation + 故障注入 RPC | 可控制 quote/balance 请求、chain/token 切换和签名至发送间隔；有 Increase、Update、Deposit、Close/Withdraw 代表动作 | inactive；loading→ready；balance loading/unknown；quote unavailable；wrong chain/token stale quote；fresh required=`M−1/M/M+1`；fresh read error | 每状态逐动作打开表单并尝试 CTA；切 chain/token 验证旧 quote 不复用；签后改变 gas/Oracle/config 或注入 fresh read；查是否发给 Relay API、task/tx/Order；记录 consumer 使用 `blocked` 还是 only-insufficient | quote key/status、display/M、balance、blockReason/CTA、Max 重算、fresh required、错误原文、API/task/tx/Order | Increase/Update/Protection 全 blocked；CURRENT Deposit、Close/Withdraw 等在 loading/unavailable 只做 UI fail-open，必须记录动作差异，且 submit helper 在 fresh quote 不可得或 required>M 时于 Keeper 发送前拒绝。required≤M 通过，M+1/read error 阻断；所有异常均不得伪装为 `Insufficient USDC for Relay Fee` | 状态机+故障注入+签名前后竞态 |
| FT-RELAY-CAP-021 / P0 | Relay Fee 页面报价_floor 与 25 USDC cap_−1、等号、+1 的显示和错误分类 | 前端层 | 页面 + fee estimator instrumentation | 可注入未应用 floor/cap 的 computed display/max；动作 gas profile 固定；USDC 6 位 | display=`9,999/10,000/10,001 raw`；computed max=`499,999/500,000/500,001 raw`；max=`24,999,999/25,000,000/25,000,001 raw` | 每档刷新报价；记录 display 与 signed M；在合法组捕获 EIP-712，越界组检查 CTA/错误/Switch 和零副作用；再执行一组 actual fee 对拍 | computed/output display、computed/signed M、error code/text、Switch、payload、RelayFeePaid、task/tx/Order | display 前两档钳到 0.01U、+1 保留；signed max 前两档钳到 0.5U、+1 保留；25U−1 与 25U 可报价，25U+1 拒绝。CURRENT +1 被折叠为 generic unavailable 且无 Switch，作为错误分类 GAP 留证；actual fee 仍只需≤signed M，不把 display 当硬顶 | 三点边界+错误分类；DEC-TRADE-005 |
| FT-RELAY-SWITCH-022 / P0 | Relay/1CT 切 Standard_手填值保持而 Max/百分比派生值重算_切换零副作用 | 前端层 | 页面 + RPC 只读 | 1CT 表单数据完整；6 位 USDC `U=1 USDC=1_000_000 raw`；wallet-bound Increase 与 Deposit；另有 Close/Withdraw；fresh C>0；可监听钱包/Relay/链 | 手填 vs Max/25/50/75%；Market/Limit/Stop open/add；Deposit；Close 100%/Withdraw Max；long/short | 记录切换前所有 raw/display 字段和选择态；点击 Switch；等待 quote/mode effect 稳定；再次采集；确认无签名/task/tx/Order；最后由用户显式点击一组 Standard CTA | mode、C 前后、selectedPercent/openMaxMode、金额 raw/display、type/direction/unit/leverage/price/TP-SL/slippage、钱包/API/task/tx/Order/RelayFeePaid | 非派生字段及手填金额保持；Increase Max 从 `B−C−U` 重算为 CURRENT Standard 的 `B−U`，Deposit 百分比/Max 也按 C=0 重算；Close/Withdraw 不因 `U` 规则变化。切换本身零副作用；第二次 CTA 才产生主钱包→ExchangeRouter、零 RelayFeePaid 的一笔目标 Order | 状态迁移+派生/手填对照+无副作用 |
| FT-RELAY-MODE-023 / P0 | 交易模式联合准入_1CT 任一依赖缺失_页面明确不可用且不得混用 Standard 与 Relay 状态 | 前端层 | 页面 + 故障注入 RPC + RPC 只读 | Standard 路径可用；可逐项控制前端功能开关、支持链、`RelayRouter`、`SubaccountRelayRouter`、Relay API/Relayer、v0.3.2 `slot` schema 与 session；本地偏好为 `1ct` | A0=全部就绪；A1～A6 每次只缺一项：feature flag、supported chain、`RelayRouter`、`SubaccountRelayRouter`、Relay API/Relayer、兼容 `slot` schema；合法手工 `MarketIncrease` 表单 | 每个数据集独立进入 Settings 与 Trade；记录模式卡、高亮、CTA、Relay Fee 行和有效路径；尝试提交；若页面显示 Standard 为有效模式，再由用户明确点击一次 CTA；A0 使用 ready session 提交代表组 | 持久化偏好、页面高亮模式、effective mode、CTA/错误、钱包或 session signer、`tx.to`、task、`RelayFeePaid`、action count、Order | 页面只显示 Standard 与 Flash One-Click/1CT；仅 A0 可显示 1CT ready 并走 session→`SubaccountRelayRouter`。任一依赖缺失时必须在提交前显示 1CT 不可用：若仍保留 1CT 高亮则阻断，若回到 Standard 则页面必须同步明确显示 Standard；不得出现“1CT 高亮但静默走 `ExchangeRouter`/direct Relay”。用户看到并明确执行 Standard CTA 前无业务请求；CURRENT 未联合检查的依赖按实际登记 GAP | 联合准入判定表+显示/路由一致性+禁止静默降级 |
| FT-RELAY-LEGACY-024 / P0 | 隐藏 direct Relay_遗留 mode=flash 恢复页面_提交前必须选择明确的公开交易模式 | 前端层 | 页面 + state instrumentation + RPC 只读 | direct Relay 卡片按产品要求隐藏；Relay 基础设施可用；可注入 localStorage | `fx100:flash:mode="flash"`；合法手工 `MarketIncrease`；随后分别明确选择 Standard、1CT | 注入遗留值并刷新 Trade/Settings；检查两张公开卡、高亮与 CTA；未重新选择时尝试提交；再分别明确选择 Standard 与 1CT 并检查下一步，不需要重复完成业务成交 | localStorage/effective mode、选中卡数量、CTA/错误、main/session signer 请求、task、Router、Order、Relay Fee | 页面只显示 Standard 与 Flash One-Click；遗留 `flash` 不得形成“无卡高亮但实际 direct Relay”的 zombie 提交。DEC-TRADE-001 未裁决迁移目标前必须阻断并要求用户选择公开模式，零 EIP-712、task、tx、Order 和扣费；明确选 Standard 后才可走 `ExchangeRouter`，选 1CT 后必须先通过 session/setup 门禁；direct Relay 的合法技术路径仅复用 XT-RELAY-010/XT-FLASH-004 | 遗留状态迁移+产品入口隔离+零副作用 |
| FT-RELAY-MAX-025 / P0 | 市价开仓_Max 在 Standard 为有效值且切换 1CT 后变为零_不得保留旧金额或允许提交 | 前端层 | 页面 + state/component instrumentation + RPC 只读 | 1CT session ready；6 位 USDC；fresh `C=maxFeeAmount>0`；`U=1_000_000 raw`；钱包是唯一限制 | `B=C+U`；Market、long、Pay Max；Standard 独立公式得到 `S_std>0`，1CT Max=`0`；反向切换用独立 snapshot | Standard 点击 Max 并记录 `S_std`；切到 1CT 并等待 mode/quote 稳定；尝试 CTA；独立执行 1CT 零值→Standard；全程不确认业务交易 | mode、openMaxMode、B/C/U、input/display/raw、校验错误、CTA、钱包/task/tx/Order | Standard→1CT 后必须把旧 `S_std` 清为空或零并显示可判定的 Max/余额不足原因，CTA 不可提交；不得沿用 Standard 旧值。1CT→Standard 后按 Standard 公式重新得到 `S_std`；两次切换均零签名、零 task、零 tx、零 Order。Standard 是否错误预留 `U` 仍由 FT-MKT-COL-012/FT-RELAY-USDC-015 单独判定，本条不以调整余额掩盖该 GAP | 精确零值边界+双向状态迁移+旧值污染反证 |
| FT-RELAY-MAX-026 / P0 | 增加保证金_Max 在 Standard 为有效值且切换 1CT 后变为零_不得保留旧增保金额 | 前端层 | 页面 + state/component instrumentation + RPC 只读 | 已有不可清算 Position；`riskMax≥B`，只命中钱包上限；其余同 FT-RELAY-MAX-025 | `B=C+U`；Deposit Margin；`selectedPercent=100%`；Standard `S_std>0`，1CT Max=`0`；双向切换 | Standard 选择 100%/Max 并记录金额；切到 1CT 等待重算并尝试 CTA；独立从 1CT 零值切回 Standard；全程不确认交易 | depositWalletMaxRaw、depositMaxRaw、selectedPercent、B/C/U、输入/错误/CTA、钱包/task/tx、Position 全字段 | Standard→1CT 后旧增保金额必须清零并明确阻断，不得继续使用旧值；1CT→Standard 后重新计算 `S_std`；切换过程不改变 Position 的 size、collateral、leverage、grace 或余额，不产生签名/task/tx/Order。wallet-bound/risk-bound 与实际增保继续由 FT-RELAY-MAX-018 覆盖 | 精确零值边界+派生草稿状态迁移+仓位无副作用 |
| FT-RELAY-QUOTE-027 / P0 | 交易模式切换期间 Relay Fee quote 延迟返回_旧 1CT 报价不得污染 Standard Max | 前端层 | 页面 + state/component instrumentation + 故障注入 RPC | 1CT ready；可挂起并按顺序释放报价；Market Increase 已选 Max；钱包余额充足 | 1CT 初始 quote=`C1`；刷新中的 quote=`C2≠C1`；切回 1CT 后当前上下文 quote=`C3`；Standard Max=`S_std` | 1CT 用 C1 计算 Max；发起 C2 刷新并挂起；响应返回前切到 Standard；释放 C2；观察金额与 Relay Fee 行；再切回 1CT 并取得当前 chain/token/action 的 C3；不提交业务交易 | quote request key/sequence/status、effective mode、C1/C2/C3、Max raw、Relay Fee 行、CTA/错误、签名/task/tx/Order | 切到 Standard 后只按 Standard 规则显示 `S_std` 并隐藏 Relay Fee；迟到 C2 不得重新启用 Relay、改写 Standard Max 或显示 Relay 余额不足。切回 1CT 后必须使用当前上下文 fresh C3 计算 `B−C3−U`，不得复用 C1/C2；全过程零签名、零 task、零 tx、零 Order | 异步竞态+上下文键隔离+状态迁移 |
| FT-RELAY-MAX-028 / P0 | 提取保证金_Max 使用 25% 清算余量与最大杠杆双上限_Standard 和 1CT 显示同一风险金额 | 前端层 | 页面 + state/component instrumentation + RPC 只读 | 已有 long/short Position；可读 size、equity、settled margin、`minCollateralFactorForLiquidation`、`minCollateralUsd`、action threshold 与 UI max leverage；fresh `M`；可控 Oracle | factor floor 主导、absolute floor 主导；maintenance-bound、leverage-bound、两 ceiling 等号；25% 余量向上取整前/等号/后 1 raw；Standard/1CT；1CT feeToken `B=M−1/M/M+1 raw` | 每组独立读取链上与 Reader 快照；链下按 raw 整数重算 buffered liquidation floor、action required、maintenance ceiling、leverage ceiling 与最终 withdraw Max；分别在 Standard/1CT 点击 Max 并对拍；切换模式确认草稿不变；代表组提交并执行 sizeDelta=0 的 `MarketDecrease` | 原始 factor/absolute floor、25% ceil、action threshold、equity/settled margin、maxLeverageBps、两 ceiling/binding branch、withdrawMaxUsd/raw、输入/payload、B/M、task/tx/Order、后态 Position/余额 | liquidation floor 的 factor/absolute 两来源均纳入 `floor+ceil(floor×25%)`，再与 action threshold 取较高 required；maintenance ceiling=`max(equity−required,0)`，leverage ceiling=`max(settledMargin−ceil(sizeInUsd×10000/maxLeverageBps),0)`，Withdraw Max 取两者较小并按 collateral decimals 向风险侧换算。相同仓位快照下 Standard/1CT 的 Max 金额完全一致且切换不重算、不自动提交；1CT 只额外执行 fee-only 门：M−1 阻断、M/M+1 通过。成功后剩余仓位同时高于缓冲清算线且不超过最大杠杆 | 双约束判定表+25% 向上取整边界+模式等价性；实现依据 `MAX_FILL_LIQ_HEADROOM_BPS=2500` |
| FT-RELAY-SUBMIT-029 / P0 | Flash One-Click/1CT 开仓_首次点击后至 Relay 交易取得 txHash 前锁定开仓按钮_快速重复操作仅提交一次 | 前端层 | 页面 + Relay API/状态注入 + RPC 只读 | 1CT session ready；MarketIncrease 表单合法；可分别挂起 preflight、Permit/EIP-712 签名、Relay API 返回及 task→txHash；可监听桌面和移动端 CTA | long；不带 TP/SL；首次点击后同一事件循环双击、连续点击 10 次、按 Enter、移动端连续 tap；分别停在 preflight、签名中、API pending、task accepted 且无 txHash | 每个阶段独立重置；第一次点击“开多”后立即注入重复操作；记录按钮状态、文案与 handler/request 次数；依次释放到 task accepted，仍重复操作；最后为同一 submission 返回 txHash 并查询订单 | CTA `disabled`/pending 文案、同步 lock、submissionId、userNonce、EIP-712/Permit 次数、Relay API POST、taskId、txHash、orderKey、`RelayFeePaid`、Order 数 | 第一次点击须同步取得提交锁，React 重渲染前的第二次输入也被拒绝；按钮立即禁用并显示提交中，从 preflight、签名、API pending 到 task accepted 但尚无 txHash 均不可再次触发。全程只有一个 submissionId/userNonce、至多一次所需 Permit、一次业务签名、一次 Relay 请求、一个 task、一笔 tx 和一笔主订单；同一 submission 的 txHash 可追踪后才允许退出锁定并进入订单跟踪。不得用后端幂等或链上重放失败替代前端禁用 | 状态转换+同事件循环竞态+at-most-once；CURRENT `useSubmissionLock`/`submissionLock.ts` |
| FT-RELAY-SUBMIT-030 / P0 | Flash One-Click/1CT 开仓_提交状态弹窗重复触发提交_不得产生第二笔交易 | 前端层 | 页面（提交状态弹窗）+ state/component instrumentation | 1CT 开仓已进入提交状态且弹窗可见；可控制 submission/task 状态并监听弹窗事件 | 弹窗按钮重复点击、Retry 重复触发、关闭后重开、重复状态回调；具体可达入口待页面流程复核 | 保留专项用例位；正式细化时先枚举弹窗内所有可触发提交或重试的入口，再按 pending、明确失败、成功三态分别注入重复操作；行为不一致时拆为独立原子用例 | active submissionId、弹窗状态、事件 handler/Retry 次数、签名、Relay API POST、taskId、txHash、orderKey、Order 数 | 同一进行中的 submission 不得因弹窗重复点击、重挂载或重复回调创建第二笔交易；只有原提交明确失败并完成状态清理后才可发起一次新重试。具体按钮禁用、关闭/重开与状态恢复断言待弹窗实际流程复核后补齐 | 高风险用例占位；待弹窗交互核对后原子化 |
| FT-FLASH-SESSION-005 / P0 | 1CT session 建立与续期_随机 key、90 天、+1,000,000 次、无 1h 锁且 setup 不自动下单 | 前端层 | 页面 + RPC 只读 | 全新 owner；可切 Stay connected；可控制时间和链上 actionCount | Stay connected on/off；新建/续期；剩余 3 天边界；后台 1h、刷新、重启、切 owner/chain | 完成 setup，记录 key 生成时钱包弹窗、approval 数值、存储位置；模拟时间/重载；续期后检查是否下单 | key 随机性、钱包弹窗类型、expiresAt、onchain/local max count、storage、readiness、task/tx/Order | key 由浏览器随机生成且该步无钱包签名；默认 90 天、maxAllowedCount=onChainActionCount+1,000,000；不采用 1h 锁；Stay connected 默认开存 localStorage，关时存 sessionStorage；建立/续期后不自动提交；自动续签不是已实现验收项 | 状态迁移+时间边界；2026-07-23/DEC-TRADE-007 |
| FT-FLASH-CONSENT-006 / P0 | 1CT Consent_Terms/Privacy 与风险同意分层_owner+版本缓存且 Settings/Trade readiness 一致 | 前端层 | 页面 | 全新/已同意 owner；可变更 consent version；Settings 和 Trade 可达 | 未同意、同 owner+同 version、新 version、换 owner；setup/renew；Settings/Trade 两入口 | 先连钱包完成 Terms/Privacy；再开 1CT 签风险 `personal_sign`；分别续期、升版、换 owner；从 Settings 创建后回 Trade | checkbox/personal_sign 内容与次数、owner、version、缓存键、approval/task/Order、两处 readiness | Terms/Privacy 只属钱包连接披露，1CT modal 只处理风险 consent；同 owner+同 version 续期不重签，换 owner/版本升级要重签；Settings 不得绕过 consent 显示假 ready。CURRENT 重复 Terms/Privacy 及 Settings 漂移均记 GAP | 状态表+两入口一致；2026-07-28 Closed |
| FT-FLASH-RISK-007 / P0 | 1CT 风险披露_安全文案不得承诺资金绝对无法转移_与当前权限边界一致 | 前端层 | 页面 + 复用关联用例证据 | R8-B02/R8-B21 未修或修复状态可知；可打开所有 consent/Settings/Trade 文案 | 当前文案 `can never withdraw or move your funds`；正常与恶意 session 证据 | 检查 setup、Settings、tooltip 和帮助文案；引用 CT-RELAY-012/013 的资金差分；不重复执行攻击 | 用户可见文案、实际授权 action scope、fee/callback 上限、修复状态 | 有可复现反例时必须删除“绝不可移动资金”类保证，如实说明泄漏 key、Relay Fee/callback、长期授权和撤销方式；安全缺口和误导文案任一存在时 1CT 不得判发布通过 | 安全文案反例；R8-B02/R8-B21 |
| CT-FLASH-SCOPE-008 / P0 | 1CT actionType 权限_同一 `SUBACCOUNT_ORDER_ACTION` 对 create/update/cancel/batch 整包生效_CURRENT 无最小动作授权 | 合约层 | Relay RPC | 隔离 fork；主账户授权一个 session；有可更新/取消订单 | 统一 Trade Order action；自造 CREATE-only/UPDATE-only actionType；create/update/cancel/batch | 对同一 approval 逐类提交动作；再用非合约识别的细分 actionType 尝试；查权限校验、count、Order 与余额 | approval actionType、动作类型、revert、actionCount、Order/账本 | CURRENT 事实：一个统一 actionType 同时允许 create/update/cancel/batch，自造细分权限不被识别；计数按实际动作腿增加。本结果只固定当前权限边界，是否接受整包权限在 DEC-TRADE-003 裁决前记 GAP | 权限矩阵+最小权限反证；DEC-TRADE-003 |
| FT-FLASH-ROLL-009 / P0 | 一键交易_关闭并重新开仓_未建立 session_关仓前进入 setup 且不回退为直接 Relay | 前端层 | 页面 | 已选 Flash One-Click/1CT；有已到期且仍存在的 Position；owner 尚未创建 session | `subaccountInfo=null`；Relay Fee 余额其他方面充足 | 打开到期卡片；点击关闭并重新开仓；观察建连流程；查询 signer、Router、task/tx/Order、Position 和费用 | setup modal、consent/授权步骤、signer、`tx.to`、task、close/reopen Order、余额 | 在第一腿前打开共享 1CT setup；setup 未完成前不发关仓请求、不扣费；不得静默降级为主钱包逐动作签名的直接 Relay；setup 成功也不自动开始两腿，由用户再次明确点击 | 状态门禁+禁止静默降级；复用 FT-FLASH-SESSION-005 |
| FT-FLASH-ROLL-010 / P0 | 一键交易_关闭并重新开仓_session 已过期_关仓前要求续期且不使用失效 key | 前端层 | 页面 + Time RPC + RPC 只读 | 1CT 已建立但 `now>=expiresAt`；有已到期 Position | `expiresAt−1`、`expiresAt`、`expiresAt+1`；三组独立会话 | 分别在三个时间点点击关闭并重新开仓；核对 session readiness、renew modal、签名、task/Order 和原 Position | expiresAt/now、renew UI、session signer、task/tx/Order、Position/TP/SL、余额 | 过期前仅在其他门禁正常时可发起；等号和过期后必须在关仓前要求续期，无 close/reopen task、订单或扣费；不得先平仓再在重开腿才报 session 过期 | 三点时间边界+两腿前置门禁 |
| FT-FLASH-ROLL-011 / P0 | 一键交易_关闭并重新开仓_session action count 已用尽_关仓前要求续期且不部分执行 | 前端层 | 页面 + RPC 只读 | 1CT session 未过期；有已到期 Position；可读 `actionCount/maxAllowedCount` | C=maxAllowedCount−actionCount；C=0、C=1、C=2；关闭并重新开仓需两个 order action | 每组打开到期入口；点击并检查是否先进入续期；查询 action count、task/Order、Position 与费用 | 剩余 count、renew UI、close/reopen task/orderKey、actionCount 差分、Position | C=0 和 C=1 都不足以安全完成两腿，必须在任何业务动作前续期；不得在 C=1 时只消耗一次并留下裸平仓；C≥2 才通过本门禁；失败组 actionCount、订单、仓位和余额均不变 | 计数边界+两腿容量预检 |
| XT-FLASH-ROLL-012 / P0 | 一键交易_关闭并重新开仓_有效 session 使用两个 Relay task 完成两腿且 action count 增加 2 | 合约层 + 前端层 | 页面 + Relay RPC + Keeper RPC | v0.3.2 兼容的 1CT session 已 ready；剩余 action count≥2；Relay Fee/抵押品充足；已到期 short Position 含 TP/SL | 使用与 XT-PROT-ROLL-029/031 等价的 market/direction/size/leverage；Order Tracking on/off 各一组 | 点击一次；跟踪 close relay task 到关仓 Order `Executed`；确认旧仓归零后跟踪 reopen relay task 到重开 Order `Executed`；查询两个 task、两个 orderKey、signer/Router、action count、Relay Fee、Position 和 grace | taskId/txHash/orderKey 两组映射、session signer、`SubaccountRelayRouter`、actionCount 前后、两笔 RelayFeePaid/maxFeeAmount/Execution Fee、TP/SL、新 Position/grace | 两腿均使用同一有效 session 经 `SubaccountRelayRouter` 提交，不逐腿弹主钱包；两个独立 task 与两个 orderKey 顺序唯一，actionCount 总共 +2；两笔 Relay Fee/Execution Fee 分开记录；业务结果与 XT-PROT-ROLL-029/031 一致，新仓获得链上实际保护期 | 两阶段跨路由一致性+计数/费用守恒；复用 XT-PROT-ROLL-029/031 |
| FT-FLASH-ROLL-013 / P0 | 一键交易_关闭并重新开仓_Relay Fee 报价未就绪或余额不足_关仓前阻断且显式切换 Standard | 前端层 | 页面 + 故障注入 RPC + RPC 只读 | 1CT session 有效且剩余 count≥2；已到期 Position；可独立报价两个 Relay task | Q1=loading；Q2=unavailable；两腿 signed cap 分别为 `Mclose/Mreopen`，目标预检总额 `Mtotal=Mclose+Mreopen`；feeToken 余额=`Mtotal−1/Mtotal/Mtotal+1 raw` | 每组打开到期卡片；核对 Relay Fee 行与 CTA；不足组尝试点击；点击“Switch to Standard”并查是否自动发单；等号/高一单位只查可用性 | close/reopen 分腿 quote、signed max、actual fee，Mtotal、feeToken 余额、CTA/guard 文案、mode、task/tx/Order | loading/unavailable 与 `Mtotal−1` 时必须在第一腿前阻断且准确分类；`Mtotal` 等号与 +1 通过本余额预检。若 CURRENT 未汇总两腿 signed cap 而只逐腿 gate，则登记 GAP，不以 actual fee 合计 F 替代签名前 cap；切换 Standard 只改变模式并保留当前仓位/快照，不自动发起两腿，用户再次确认后才提交 | 两腿 cap 预检+三点边界+显式模式迁移 |

### D. 前端状态、错误与事件适配（7 条原子用例）

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| FT-STATE-001 / P0 | 订单状态_`Created`→`Executed`、`Cancelled`、`Frozen`_三态语义可区分 | 前端层 | 页面 | 可控制 Keeper | `Created`→`Executed`、`Cancelled`、`Frozen` | 分别制造三类终态 | Toast、Open Orders、History、余额、刷新后状态 | 三态语义可区分；刷新后不回退为旧状态 | 场景法 |
| FT-STATE-002 / P0 | 订单状态_市价部分减仓与市价关仓_索引和页面不依赖已删除字段 | 前端层 | 页面 | 前端使用 v0.3.2 ABI | 执行市价部分减仓/市价关仓 | 分别执行市价部分减仓与市价关仓；比对事件解析、Reader 与页面 History | 删除的 secondary output 字段、主输出、状态 | 索引/页面不依赖已删除字段；金额和状态正确 | v0.3.2 R17；缺专项自动化 |
| FT-COMPAT-003 / P0 | 前端兼容_检查页面、SDK、后台调用_不再显示或调用已移除 claimCollateral 机制 | 前端层 | 页面 | 前端构建可运行 | 检查页面、SDK、后台调用 | 搜索并实际访问相关入口 | claimCollateral UI/API、旧配置键、网络调用 | 不再显示或调用已移除 claimCollateral 机制 | v0.3.2 兼容专项 |
| XT-ERROR-004 / P0 | 异常恢复_用户拒签、RPC、Oracle、Keeper 超时、Indexer 延迟_不把依赖故障显示为成功 | 合约层 + 前端层 | 页面 + 故障注入 RPC | 可注入失败 | 用户拒签、RPC、Oracle、Keeper 超时、Indexer 延迟 | 逐类触发并恢复 | 是否发单、订单终态、退款、重试、页面提示 | 不把依赖故障显示为成功；可恢复；不重复下单 | 异常场景法+恢复测试+幂等性（客户端、RPC、Oracle、Keeper、Indexer 分层故障） |
| FT-ORD-TRACK-005 / P0 | Order Tracking_新用户默认关闭、历史开启值保留_两种展示均追踪完整终态 | 前端层 | 页面 | 可清空/注入 Order Tracking localStorage；Standard 与 1CT 可用 | 无历史值、false、true；Market/Limit；Standard/1CT；成功/失败 | 分别启动新会话与历史 true 会话；下单并观察 Modal/toast；刷新后继续追踪 | 偏好值、Modal 显示、toast 更新、taskId/txHash/orderKey、Order 终态、刷新后状态 | 新用户默认关闭 Modal；历史 true 不被强行重置；关闭时同一条 toast 仍完整显示 signing/task/tx/order 终态，开启时 Modal 与 toast 指向同一证据；不把 Relay task executed 当 Order filled | 状态组合+持久化；OC-24/OC-25 Closed |
| FT-ORD-CANCEL-006 / P0 | 取消订单_Standard 与 1CT 从 Submitted 收敛到同一终态_慢签名/失败不提前消失 | 前端层 | 页面 + Time RPC | 两种公开模式可用；有可取消 Limit 和 Market 订单；可控钱包延迟/回执失败 | Standard/1CT；成功/未到 Market 取消时间/用户拒签/慢签名超 8 秒 | 分别取消；记录同一 toast 从 submitted 到 canceled/error；制造慢签名和回执 revert；比较警告/真错误视觉级别 | toast id、时间线、receipt、OrderCancelled/revert、Order Store、按钮文案、警告与错误色 | Standard 不停在 `Cancel Submitted`，receipt 后同条 toast 升级为 Canceled 或准确错误；1CT 终态语义与视觉一致；签名/等待超 8 秒不静默消失；普通警告与真错误可区分；不得在 receipt 前报 Canceled | 双路状态对拍+延迟/错误注入；OC-26 Closed/High |
| FT-ORDER-DEDUP-007 / P0 | 下单防重_双击、键盘+点击、Relay 无 txHash_同一逻辑提交只生成一张单 | 前端层 | 页面 + 故障注入 RPC | Standard/1CT 可用；可延迟 txHash 或返回 error；可抓取 payload/task/tx | 同步双击、Enter+点击；Standard/1CT；快链小于 1500ms；Relay accepted 无 txHash；30s backstop；error 后重试 | 对同市场同方向表单连续触发；在各阶段观察 lock；失败/超时后再显式点一次 | lock 时间、handler 调用次数、签名/payload/task/tx/orderKey 数量、解锁原因、重试结果 | 同一在途提交只产生一份 payload、一个 task/tx/order；lock 至少 1500ms，Standard/Relay 在 txHash 或 error 后解锁，Relay 无 txHash 最长 30s 恢复；明确失败后只有用户再点才可新建；不需等 Keeper fill 才解锁 | 并发+幂等+时间边界；2026-08-06 Closed/High |

### E. 触发时容量漂移、`Frozen` 与前端恢复（7 条原子用例）

> 本节固定前端可见的完整 `Frozen` 链路，不替代各功能节中对具体错误公式和方向的合约原子用例。四条容量漂移用例采用成对组合，同时覆盖四个 Cap、`LimitIncrease` / `StopIncrease` 与 `long` / `short`；后续拆分功能时，仍须按 §2.5 对每个适用的 `Frozen` 原因登记独立用例。

| 用例 ID / 优先级 | 测试标题 | 层 | 入口 | 前置条件 | 测试数据 | 操作步骤 | 核对数据 | 期望结果 | 设计方法 / 依据 |
|---|---|---|---|---|---|---|---|---|---|
| XT-FRZ-CAP-001 / P0 | 限价开仓_long_创建时容量充足_触发前同方向仓位占满 MAX_OPEN_INTEREST_触发执行后订单 Frozen | 合约层 + 前端层 | 页面 + RPC + Admin RPC + Keeper RPC | 可控 Oracle；独立 snapshot；硬上限低于其他容量上限；订单创建时完整 size 可履约 | `LimitIncrease long`；目标 size=S；创建时剩余容量 H≥S；其他账户开 long 后 H'<S | 在页面创建订单并确认为 `Open`；其他账户开仓占用同方向 OI；将 Mock Oracle 价格更新至限价触发条件；Keeper 执行；查询 Order、Position、OI、OrderVault 与页面 | 创建时/执行时 OI 与剩余容量、`MaxOpenInterestExceeded(openInterest,maxOpenInterest)`、`OrderFrozen`、`isFrozen`、executionFee、初始抵押、前端状态/原因/操作 | 触发前保持 `Open`；触发后为 `Frozen`且不建仓；订单仍在 Store，executionFee=0，初始抵押仍托管；页面显示“已冻结”、准确的 OI 上限原因及更新/取消入口 | 执行时状态漂移+状态迁移；`MAX_OPEN_INTEREST` |
| XT-FRZ-CAP-002 / P0 | 限价开仓_short_创建时容量充足_触发前同方向仓位占满 MAX_OPEN_INTEREST_FACTOR_触发执行后订单 Frozen | 合约层 + 前端层 | 页面 + RPC + Admin RPC + Keeper RPC | 可控 Oracle；独立 snapshot；软上限低于硬上限和 Reserve 上限 | `LimitIncrease short`；S≤创建时 `poolUsd×factor−short OI`；其他账户开 short 后剩余量<S | 在页面创建订单并确认为 `Open`；其他账户开仓占用 short OI；将 Mock Oracle 价格更新至限价触发条件；Keeper 执行；查询公式输入、Order 与页面 | `poolUsd`、factor、short OI、软上限、`MaxOpenInterestExceeded(openInterest,poolUsd×factor)`、Frozen 事件/字段、资金回滚、页面原因 | 只命中比例上限；订单由 `Open` 变为 `Frozen`；不建立空仓；页面不得显示成交/取消或泛化为未知错误 | 执行时状态漂移+公式隔离；`MAX_OPEN_INTEREST_FACTOR` |
| XT-FRZ-CAP-003 / P0 | 条件开仓_long_创建时容量充足_触发前其他方向仓位占用组合 RESERVE_FACTOR_触发执行后订单 Frozen | 合约层 + 前端层 | 页面 + RPC + Admin RPC + Keeper RPC | 可控 Oracle；独立 snapshot；Reserve 上限低于同方向硬/软上限 | `StopIncrease long`；创建时 long+short 名义 OI 留有余量；其他账户增加 short 名义 OI | 在页面创建订单并确认为 `Open`；其他账户开 short 占用组合 Reserve；将 Mock Oracle 价格更新至条件开仓触发条件；Keeper 执行；查询 Order、双向 OI、价格与页面 | long/short OI in tokens、indexPrice.max、poolUsd、reserveFactor、`InsufficientReserveForOpenInterest(totalNominalOI,maxAllowed)`、Frozen 事件/字段、页面原因 | 只命中组合 Reserve 上限；条件开仓触发前保持 `Open`；触发后为 `Frozen`且不建立多仓；页面明确显示 Reserve 容量原因 | 执行时状态漂移+双向组合；`RESERVE_FACTOR` |
| XT-FRZ-CAP-004 / P0 | 条件开仓_short_创建时容量充足_触发前同一目标仓位被加仓超过 MAX_POSITION_SIZE_USD_触发执行后订单 Frozen | 合约层 + 前端层 | 页面 + RPC + Admin RPC + Keeper RPC | 已有 short 仓位 S0；可控 Oracle；其他市场容量上限充足 | `StopIncrease short` size=S1；创建时 S0+S1≤cap；触发前市价加仓 Δ；S0+Δ+S1>cap | 在页面创建订单并确认为 `Open`；对同一 PositionKey 执行市价加仓；将 Mock Oracle 价格更新至条件开仓触发条件；Keeper 执行；查询 Position、Order 与页面 | S0、Δ、S1、cap、PositionKey、`MaxPositionSize(positionSize,maxPositionSize)`、Frozen 事件/字段、原仓位与页面原因 | 触发失败只冻结条件开仓订单；已完成的市价加仓保留，本次触发不再增加仓位/OI；页面显示准确的单仓上限原因 | 执行时状态漂移+单仓边界；`MAX_POSITION_SIZE_USD` |
| XT-FRZ-RETRY-005 / P0 | Frozen_释放容量_FROZEN_ORDER_KEEPER 重试后执行成功 | 合约层 + 前端层 | RPC + Keeper RPC + 页面 | 复用一笔容量原因的 `Frozen` 订单；`FROZEN_ORDER_KEEPER` 可用；当前 Oracle 仍满足触发条件 | 取消/减少占用容量的其他仓位；释放量使本订单可履约 | 释放容量；核对 Reader 剩余量；由 `FROZEN_ORDER_KEEPER` 使用最新价格重试执行；查询 Order、Position、OI、余额与页面 | Keeper 角色、重试前 `isFrozen`、最新 Oracle 价格、OrderExecuted、Position/OI、页面终态 | 普通 Order Keeper 不得执行已冻结订单；容量恢复后 Frozen Keeper 重试成功；订单移除、仓位建立/增加一次；页面不得仍显示“已冻结” | 角色等价类+状态恢复+幂等性 |
| XT-FRZ-UPD-006 / P0 | Frozen_更新订单减小 size 并补足 Execution Fee_解除冻结后执行成功 | 合约层 + 前端层 | 页面 + RPC + Keeper RPC | 一笔因容量不足而 `Frozen` 的非补贴订单；原 executionFee 已在冻结时支付 | 更新后 size≤当前剩余容量；补足当前所需 Execution Fee；保留合法 trigger/acceptablePrice | 在页面打开更新订单；减小 size；核对并补足 Execution Fee；提交 `updateOrder`；确认订单重新为 `Open`；Keeper 执行；查询订单、仓位、费用与页面 | 更新前后 size、isFrozen、executionFee、OrderUpdated/OrderExecuted、钱包扣费、Position/OI、页面状态 | 页面明确显示所需补费；更新后 `isFrozen=false`、executionFee 满足执行要求；仅按新 size 执行一次；不得因冻结时 executionFee 已归零而在更新阶段无提示回退 | 状态迁移+费用边界+恢复测试 |
| XT-FRZ-CAN-007 / P0 | Frozen_用户手动取消_订单移除且托管抵押退回 | 合约层 + 前端层 | 页面 + RPC | 一笔 `Frozen` 的 Increase 条件订单；记录 OrderVault、钱包和冻结前后 Execution Fee | 初始抵押 C；冻结时已支付 Keeper 的 Execution Fee F | 在页面选择冻结订单；点击“取消”并确认交易；查询 Order Store、OrderVault、钱包、Open Orders、History；再由 Keeper 尝试执行 | OrderCancelled、Order Store、C 的托管/退回差分、F 的支付记录、Position/OI、页面列表/历史/刷新后状态 | 用户可取消冻结订单；订单从 Store/Open Orders 移除并记录为 `Cancelled`；托管抵押 C 按规则退回；已支付 Keeper 的 F 不重复退还；不建仓且不可再执行 | 状态迁移+资金守恒+幂等性 |

## 6. 建议执行批次

1. **批次 0：环境与配置**——CT-BASE-001～008、XT-BASE-009。
2. **批次 1：开仓、关仓与主单取消**——执行 B1 市价开仓/关仓、B3 限价开仓、B4 开仓主单及关联 TP/SL 保护单级联取消。
3. **批次 2：市价加仓、杠杆/保证金调整、部分减仓与账本**——执行 B2 的 XT/FT-MKT-ADD-*、FT/XT-LEV-CAP-*、XT/FT-MARGIN-CAP-*、XT/CT-MKT-REDUCE-*、XT-MKT-MINOUT-*、CT-MKT-LEDGER-*。
4. **批次 3：TP/SL、清算保护期与关闭并重新开仓**——执行 C1 的 XT-TPSL、XT-PROT、FT-LIQ-005 及 C2 的 FT/XT-PROT-ROLL-024～042，包括部分减仓后的超额保护单封顶、到期入口、Standard 两腿、多仓位选择隔离、失败隔离和清算竞争。
5. **批次 4：订单类型**——执行 §4 订单类型全组；已有关联原子用例的行只复用证据，不重复执行交易动作。
6. **批次 5：v0.3.2 定价变更**——执行 CT/XT-PRICE 全组及 XT-LIQ-003。
7. **批次 6：Relay、1CT 与代付费**——先执行 FT-RELAY-MODE-023 与 FT-RELAY-LEGACY-024 固定联合准入和公开模式边界；再执行 CT-RELAY-001～009、XT-RELAY-010、FT-RELAY-011、CT-RELAY-012～014、FT-RELAY-USDC-015～016、CT-RELAY-BATCH-017、FT-RELAY-MAX-018/019/025/026/028、FT-RELAY-QUOTE-020/027、FT-RELAY-CAP-021、FT-RELAY-SWITCH-022、FT-RELAY-SUBMIT-029/030 和 XT/FT/CT-FLASH 全组，其中 FT/XT-FLASH-ROLL-009～013 复用 C2 业务证据专门验证关闭并重新开仓的 session、两个 task、action count 和两腿 Relay Fee。Max raw 边界先在 state/component 层执行，页面与目标链层再按准入状态登记；Withdraw Max 必须包含 25% 清算余量、maintenance/leverage 双上限和模式等价性；开仓提交锁必须从第一次点击同步生效并保持到同一 submission 的 txHash 可追踪；FT-RELAY-SUBMIT-030 在弹窗实际交互核对后再完成原子化；R8-B02/R8-B21 安全负向与 permissionless/action-scope 边界只在隔离 fork 执行。
8. **批次 7：资金费**——待 v0.3.2 time-fork 后执行 CT-FUND-001/002。
9. **批次 8：Frozen、前端兼容与异常恢复**——执行 XT-FRZ、FT-STATE、FT-COMPAT、XT-ERROR、FT-ORD-TRACK-005、FT-ORD-CANCEL-006 与 FT-ORDER-DEDUP-007。

## 7. 执行能力与环境缺口

- tx-fork 已登记 v0.3.2 部署；2026-09-02 起 `CURRENT.primary.admissionScope["tx-fork:contract"]` = READY_FOR_SYSTEM_TEST，非时间类合约用例可开始执行并正式登记。总开关 `admission` 仍为 NOT_READY，表示全环境全层；FT/XT 与 Relay 层等待 `tx-fork:frontend` 准入。
- oracle-fork 与 time-fork 当前仍登记为 `@v0.3.1 对比基线`，不能作为 v0.3.2 精确 Oracle/Funding 证据。
- R15 剩余成本 USD 单位、R17 事件字段适配、负点差前端展示目前没有完整专项自动化。
- v0.3.2 的执行结论必须基于本矩阵用例在目标环境中的独立结果，不沿用旧版本结果。
