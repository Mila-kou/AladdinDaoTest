# 晨间审核报告 — 2026-09-05

> 读法：**第一部分**是我已改的、请你验；**第二部分**是我没改但查出来的、需要你定夺；**第三部分**是还在进行中的。

---

# 一、我已经改了什么（请审这部分）

每一条我都自己打开源码验过，没有直接采信 agent 结论。下面「依据」列的行号都是我亲自读过的。

## 1. `FX100-前端代码公式.md`（我上一轮新建的，本轮修 5 处）

| # | 改动 | 依据（我亲验） |
|---|---|---|
| 1 | **§0 价格标度**：原写「价格 = `USD × 10^(30−tokenDecimals)`」，改为区分两层——合约/oracle 是 `10^(30−d)`，**SDK 的 `TokenPrices.minPrice/maxPrice` 是恒定 `1e30`/每整枚 token，与 decimals 无关** | `packages/sdk/src/utils/tokens.ts:21` `parseUsdPrice = parseUnits(v, 30)`；`:17` `parseContractPrice = price × 10^tokenDecimals` 就是两层间的换算器 |
| 2 | **§5.4 算术**：`9055/4.0922 ≈ 2212.9` → `≈ 2212.75` | 手工验算，正确值 2212.7462 |
| 3 | **§6 对照值**：`差 ≈ 13.6 USD（0.62%）` → `13.44 USD（0.61%）`，并补出 `= 55/4.0922` 的来源 | 同上 |
| 4 | **§6 新增 Path A/B 界面归属表**：原文把「调保证金/杠杆预览」都归为 Path B，**实际是分裂的** | `PositionMarginDialog.tsx:521` 调 SDK `getLiquidationPrice`（Path A）；`PositionLeverageDialog.tsx:435` 调 `useLiqPriceEstimate`（Path B）；`OrderPreview.tsx:345` 调 `estimateNewLiqPrice`（Path B） |
| 5 | **§7 重写**：原文标题写「三条线」但只列了两条；补齐第三条，并补 `isLong` 分支 | `trade/decrease.ts:445` `getMinCollateralUsdForLeverage = applyFactor(sizeInUsd, minCollateralFactor)`——**没有 `max(…, minCollateralUsd)` 那层**，与前两条差的是地板不是因子 |

## 2. `FX100-Keeper代码公式.md`（我上一轮新建的，本轮修 5 处）

| # | 改动 | 依据（我亲验） |
|---|---|---|
| 6 | **§7 加显著警告**：`evaluateTrigger` 是**遗留路径，提交路径不再调用**；现役实现在 §1.2 | keeper 自家 `IMPLEMENTATION.md:705` 原文 *"is the legacy single-slot wrapper retained for its tests; the submit path no longer calls it."*；全仓 grep 确认除自身定义与测试外无调用点 |
| 7 | **§4.1 更正来源**：`effectiveShortfallUsd` **不是 Reader 返回字段**，是 Keeper 自减 | `chain/marketState/store.ts:1155` `effectiveShortfallUsd: minCollateralUsdForLeverage - remainingCollateralUsd`；Reader 的 `info` 只有三个字段 |
| 8 | **§5/§10 更正 ADL**：并列扩展只在中间阶段，最终被硬截 | `strategies/adl/auto.ts:53` `topM = maxPerTick × multiplier`，`:93` `selected.slice(0, maxPerTick)` |
| 9 | **§8 锚点**：「同上模块」指向错 | `minMaxPriceSelector.ts` 只 import `useMaxForSide`，文件头明确要求其它函数直接从 SDK import |
| 10 | **§5 差异项**：原文说合约 PnL 含费用/funding，实际合约 `_getPositionPnlUsd` 本身也不含 | 差异其实在「用中间价 vs 含点差执行价」和「有无盈利封顶」两条 |

## 3. `FX100-功能用例公式与核对依据索引.md`

- 修 3 处失效章节锚点。规则坑：GitHub slug 里 **`、` 是删除不是转连字符**（`Pay、Margin、Size` → `paymarginsize`）；另一处是我把 §3.1 的编号和 §3 的标题拼在了一起。
- 已写脚本全量校验：**本目录所有相对链接与章节锚点现在都可解析**。

## 4. `FX100-项目模块与资金方向总结.md`

- Holding/Insurance 那行改为实际的两个用途，并补「穿仓不向任何地址转账、缺口由 LP 担」。
- 依据：`HOLDING_ADDRESS` 在 v0.3.2 只有两处用途——`TokenUtils.sol:69-79`（转账失败兜底）、`GasUtils.sol:268-274`（超额执行费沉淀）。

## 5. `FX100-核心字段计算公式.md` / `README.md` / `CURRENT.json`

- **核心公式文档的公式本体一个字没动**，只加了：文首指向三层、§0.2 来源优先级插入「前端/Keeper 代码实现」一档、§24 补两份新文档入口。
- `CURRENT.json`：`frontend` 补 `head`/`headAt`/`subpaths(sdk/app/keeper)`。理由：`develop` 是移动分支，前端与 Keeper 的公式锚点必须可 pin，否则行号随 rebase 失效。

---

# 二、查出来但**没动**的问题（需要你定夺）

## ⚠️ A. 最严重：深度核对的结论**不参与用例判定**

`tests/B/xt-mkt.spec.ts:86-94` 的 `assertAllChecksPassed` 只遍历 `result.checks`（runner 内联断言），**完全不看 `result.reconciliationReport.checks[].verdict`**。

后果：`market-open.ts`（20+ 条）与 `fee-claimable.ts`（4 条）两个核对包里的断言，**包括 blocking 级判 FAIL，都不会让 Playwright 用例变红**——只在看板上多一行。真正拦人的 runner 内联断言明显更松（trader USDC Δ 用 ±1 raw 容差、执行价只断言单侧不等式）。

**这意味着：这两个包里公式即使写错，也不产生 FAIL 信号。**

## ⚠️ B. funding 双锚点是「先看链上实际值再选期望值」——已真实发生

`execution-evidence.ts:1255-1257`：先算 v0.3.2 锚，不中就试 v0.3.1 锚，**命中任一即 PASS**。

我在 `TestCode/artifacts/latest/results.json` 里直接验到（原文照录）：

```json
"id": "tx2-execute-open-claimable-funding",
"status": "PASS",
"delta": "+282573 raw (0.282573 USDC)",
"formula": "claimableFundingAfter = before + v0.3.1 Σmax(positionPaysLp, 0)…",
"note": "…两口径取整可差 1 raw。链上 Actual 精确命中其一才 PASS…",
"verification": "计算复算"
```

四件事同时成立：
1. 批次标着 v0.3.2，**命中的却是 v0.3.1 锚**（该文件里共 33 处 v0.3.1 锚字样）
2. 差额 **282573 raw ≈ 0.28 USDC**，而 note 说「两口径取整可差 1 raw」——**被实测证伪，差了 6 个数量级**
3. 却标着 **`verification: 计算复算`**（FULL_RECOMPUTE，最高证据等级）
4. 状态 **PASS**

另有 `execution-evidence.ts:378` 的 `lpToPositionVault` 是**纯 v0.3.1 单锚点，代码里根本没有 v0.3.2 分支**。

**由此引出一个超出文档范围的怀疑**：tx-fork 上跑的可能不是 v0.3.2 的 funding 语义，尽管 `CURRENT.json` 登记 `forkOf: tx-fork@v0.3.2`。佐证：`playwright-reporter.ts:377-380` 注释写明 `run.targetRelease = CURRENT.json primary`——**`releaseMismatch` 比的是标签 vs 登记，不是链上实况 vs 登记**，链上从未被校验。

这直接冲击 `admissionScope.tx-fork:contract = READY_FOR_SYSTEM_TEST` 的成立基础。

> **按既定分工，环境类问题归看板同事的 session**，我没有动任何环境文件，也没有尝试连 fork。建议把这条转给那边做一次决定性检查：读 tx-fork 上部署的 `MarketUtils` 是否存在 `settleFundingFees`。

## ⚠️ C. 公式文档根本没进跑批链路

看板 2535 条证据的 `formulaBasis` 实际指向：

| 次数 | 指向 |
|---:|---|
| 1441 | `src/scenarios/scn-009-runner.ts` ← **代码自己**（用被测代码解释被测结果） |
| 514 | `Docs/…/FX100-十大功能领域需求文档.md` ← 需求文档 |
| 466 | `Docs/…/FX100-页面字段计算公式.md` ← 需求文档 |
| 101 | `TestCode/docs/07` |
| **0** | **`ContractCodeSummary/v0.3.2/FX100-核心字段计算公式.md`** |

而索引文档 §4 的 `formulaBasis` 规范要求 `sourcePath` 指向核心公式文档。**规范与实际完全脱节**——这也解释了那份文档为什么能烂这么久没被发现。

顺带一提：这也说明**你们的系统其实已经在拿需求文档当公式依据了**（980 条），这正是我昨晚提醒过的风险形态。

## D. 好消息：公式本身基本没被污染

7 条经复核确认安全，其中值得记的：
- 文档 §3.1 那个最严重的错（`indexPrice` 当最终除数）**没有传染到代码**——`execution-evidence.ts` 把「预备量」与「最终值」分得很干净
- 事件字段重排**结构性传不进来**——解码按链上自带的 key 名取值，字段怎么挪都不影响
- 单仓应付/应收 Funding 两行是**真独立重算**，与合约逐项一致，是这段代码里证据最扎实的部分
- `claimCollateral` 等已删机制在 TestCode 里 **0 处引用**

---

# 三、进行中

- **核心公式文档 12 组重推**：改写已完成（约 3400 行、200 处修正、102 条需求差异台账），但**首轮复核全部死于用量限额，等于零验证**。已重新发起复核（改写走缓存）。**在复核通过前我不会合并任何一段进主文件。**
- skill `formulas.md` 修订：待复核结论出来一并做
- `TestCase/E2E/versions/v0.3.2/` 审计：10 个 agent 全部死于限额、零产出，需整轮重跑。**该目录我一个字都没动。**

---

# 四、需要你拍板的

1. **A/B 两条要不要立缺陷单？** 它们不是文档问题，是跑批有效性问题——按 05 标准「无证据的勾选不算通过」，双锚点 + 结论不进判定这两条，性质上属于"批次绿灯不可信"。
2. **C 条怎么收口？** 要么把 `formulaBasis` 改成真的指向公式文档，要么承认现状、把规范改成实际做法。现在这样两头不靠。
3. **tx-fork 的 funding 语义**要不要转给看板 session 做那次决定性检查？

---

# 五、需求差异台账初稿（103 条，**未经复核，全部属候选**）

重推的 12 组各自产出了「实现 vs 需求规范」差异，合计 103 条。定性分布：**设计已变更未回写文档 19 条 · 缺陷候选 8 类 · 需求表述模糊约 10 条**，其余为说明性条目。

> ⚠️ **这批全部未经独立复核**。产出它们的 agent 在部分条目上自己就标了「配置值未核实」「未在 fork 上做反证实验」。**不要据此直接立缺陷单**，需要先按下面的顺序各自验证一次。

## 值得优先验证的 8 条

| # | 差异 | 若成立的影响 | 验证方法 |
|---|---|---|---|
| 1 | **清算费基数**：需求按保证金，实现按名义仓位规模 | 10x 杠杆实收 ≈ 需求意图的 10 倍（3 万名义/3 千保证金 → 90 而非 9 USDC）；50x 达保证金 15%，加深穿仓与坏账 | 读 `PositionPricingUtils::getLiquidationFees` 的基数，对照费用体系设计规范 G-01 |
| 2 | **清算费余额腿去向**：需求给清算人，实现给 LP Vault | Keeper 清算收益只剩执行费，清算激励弱于设计 | 同上函数的分账去向 |
| 3 | **UI Fee 加减方向**：需求内扣，实现外加 | 用户实付 = 手续费 + UI Fee，高于需求口径；总额公式符号依赖此点 | `getUiFees` 与 §9 总额公式 |
| 4 | **funding 资金路由不对称**：仓位净付→协议费用账本，仓位净收→LPVault 出 | LP 单向出钱，承担负向敞口却无正向收益；前端若展示为「LP 收益」属误导。**注：v0.3.1 同样不对称，非 v0.3.2 引入** | `MarketUtils::settleFundingFees` 两分支的收款方 |
| 5 | **funding factor 边界校验失效**：`validateRange` 校验 uint 存储，实际读取 int 存储 | 费率上下界可配成任意值；`min > max` 时 `Calc.clamp` 返回值可超出 max，费率失控 | `ConfigUtils::validateRange` 的键 vs funding 读取路径的键 |
| 6 | **Reader 资金费预览翻倍 + 空头取错侧**（沿用 GMX 双 token 折半假设 `multiplier = 2`） | 前端 Margin/Net Value/杠杆分母系统性偏大；空头在常见 skew 下被预览成「应付」。**两版皆有的实现缺陷，不是版本滞后** | Reader 的 funding 预览函数 vs `MarketUtils::getFundingAmount` |
| 7 | **`MIN/MAX_DYNAMIC_SPREAD` 无链上护栏** | 漏配→该市场该方向点差恒 0（协议永久少收、偏斜调节失效）；写反（max<min）→静默变成恒定点差。均不 revert 不报警 | 需求 08-18 方案要求的 `require` 是否落地；`defaults.ts` 与 `environment-initialization-profiles.json` 的实配 |
| 8 | **破产清算费用清零 + 被扣抵押品滞留** | 穿仓时手续费/清算费/UI Fee/返佣全收不到（全有或全无）；已扣的 token 留在 `positionVault` 无账面归属，ΣΔ 守恒核对会出现无法解释的残差 | `DecreasePositionCollateralUtils` 的 `isInsolventCloseAllowed` 分支 |

## 其它值得注意的（非缺陷但影响用例设计）

- **零 OI 时 funding 不是 no-op**：`fundingUpdatedAt` 照刷、skewEMA 被整体清零、事件照发。跨「清仓→再开仓」的 E2E 不能沿用旧 EMA 快照算期望值。且可被用于低成本重置 skew EMA。
- **`Reader.getExecutionPrice` 不复现减仓的尺寸闸门与 acceptablePrice 校验**：超仓/残仓过小/保证金临界三类场景，Reader 预览与链上不一致，E2E 用 Reader 值做期望值会对不上。
- **`MIN_COLLATERAL_FACTOR > MIN_COLLATERAL_FACTOR_FOR_LIQUIDATION` 只写在注释里，合约不校验**：两者若配成相等，开仓校验线 == 清算线，安全垫为零。（与我记忆里的「全市场动作线=清算线」现象吻合，值得优先验。）
- **`MAX_RELAY_FEE_SWAP_USD_FOR_SUBACCOUNT` 是无人读取的死配置**：名字像风控上限，实际不生效。
- **v0.3.2 修掉了一个 v0.3.1 的真实资金缺陷**：`VaultBase._withdrawAssets` 缺口路径在 v0.3.1 下 LP 会少拿 `min(balance, assets)`，差额永久留在 vault。v0.3.2 已修正并加严格相等校验。

## 我的建议

这 103 条不要一次性处理。按这个顺序：
1. 先验上表 8 条中的 **1/2/3/6**（纯读源码即可判定，不需要环境），成立的立 `FX100-BUG-NNN`
2. **5/7/8** 需要读链上实配或构造用例，交给能动环境的 session
3. 剩下的「设计已变更未回写文档」19 条，本质是需求文档该更新，不是缺陷，可批量处理

---

# 六、重推复核结果（凌晨更新）

## 复核跑通了，而且 12 组**全部**有实质错误

第一轮复核死于限额、等于零验证；重跑后 36/36 全通、0 错误，结论是 **12 组没有一组可以直接合并**。这次是真的抓到东西，不是没跑。举几个复核抓到的：

| 组 | 复核抓到的问题 |
|---|---|
| u04 | §5.2 公式漏了 `/1e30`，**会把分支判反** |
| u03 | 把资金费的 skew 说成「成本口径」——与源码相反，且与本文 §10.1/§10.2 自相矛盾 |
| u05 | §8.6 事件名映射整段写错（`PositionFeesInfo` / `PositionFeesCollected` 的归属反了） |
| u10 | §16.4 触发单哨兵值方向写反（只对减仓成立，增仓完全相反） |
| u11 | `marginToken` 被重定义成净额，与 §21、§16.2 同名不同数 |
| u12 | 4 处源码锚点指向**已删除或已注释掉**的函数 |

**所以我一段都没合并。** 已启动修正轮：每组读自己的复核意见、逐条回源码核实后修正，再做一轮独立把关，通过才允许合并。修正 agent 被明确要求——**复核意见本身也可能错，核实后若源码支持原写法就驳回，不许照单全收**。

## 顺带纠正了一个长期错误说法（已改 skill）

复核 u06 时挖出一条：文档与 skill 都写着

> `fee = ⌊⌊sizeUsd × factor / 1e30⌋ / colPrice.min⌋` ← 两次独立 floor；**合并分母一次除会差 1 wei**

**这个说法是错的。** 对正整数 m、n 有恒等式 `⌊⌊x/m⌋/n⌋ = ⌊x/(m·n)⌋`，此处 m=1e30、n=colPrice.min 都是正整数，两式**恒等**。我用 50 万组随机值暴力验证：**0 组不一致**。

已修 `fx100-verify-handbook/references/formulas.md` §6：保留两步写法（忠实对应源码结构），但删掉"会差 1 wei"的错误论断，并补上说明——**真正会差 1 wei 的是重排乘除顺序（先除后乘），那才是「同序复刻」要防的东西**。

这条值得单独记：它是个流传已久、听起来很专业但数学上站不住的说法，可能已经影响过容差设计。


---

# 七、`versions/v0.3.2/` 审计结果（凌晨更新）——结论和预想相反

153/156 agent 跑通。**这批文档质量明显高于核心公式文档**，三份深度文档的复核评语一致：

- `dynamicSpread-(v0.3.2).md`：「真为 v0.3.2 写的，**没有发现 v0.3.1 残留**」，并列出 8 处核心公式文档缺失、应吸收的内容
- `Funding-(v0.3.2).md`：「真为 v0.3.2 重写」「**总体质量明显高于核心公式文档**」
- `Liquidation-(v0.3.2).md`：「**精度明显高于核心公式文档**」

## 查出 10 处「核心公式文档错、这批对」，实为 5 个问题

| 问题 | 独立撞到的 agent 数 |
|---|---|
| **§19.2 `positionPaysLp` 结算口径**——A 停在 v0.3.1；且 A 自己 §10.6 已写对，**A 内部自相矛盾** | **4** |
| **§19.3/§10.2 零 OI 与 EMA**——A 说「整体 no-op」，实际是 per-size no-op **+ 三处副作用**（`fundingUpdatedAt` 照刷、skewEMA 被清零、发 `Funding(0,0,0)`）；且 A 的「一小时采样间隔」措辞会被读成离散分桶，**实际 3600 是指数衰减时间常数** | 3 |
| §10.6 减仓/清算/ADL 路径传的是**实付额**，A 写成应付额 | 1 |
| §3.1 `indexPrice` vs `executionPrice`（印证前两轮） | 1 |
| §21 速查写死 `/1e18`，应运行时读 decimals（8 位 index token 市场会全线错量级） | 1 |

「一小时采样间隔」那条尤其致命：按离散分桶理解，`dt < 3600` 的用例会算出 0 增量、跨小时用例会用错 skew，**Funding 全部数值断言失准**。正确表述是连续衰减递推 `alpha = exp(-dt/3600)`、`emaBeforeNew = lastValue×(1-alpha) + lastEmaValue×alpha`、本次 factor 用 `emaBeforeNew` 而新 rawSkew 只影响下一段、`dt > 41×3600` 收敛到旧 `lastValue`。

## ⚠️ 附带查出 TestCode 里的一个真 bug（我已亲验）

`TestCode/src/reconciliation/formulas.ts:288` 的 `composeDynamicSpread`：

```js
// 注释自称蓝本：PositionPricingUtils.sol:152-175，「≤0 夹到 0」
const spread = sum <= 0n ? 0n : sum;        // ← v0.3.1 的 floor-at-0
```

v0.3.2 实际（`PositionPricingUtils.sol:154-186`）：

```solidity
return (Calc.clamp(dynamicSpread, minDynamicSpread, maxDynamicSpread), balanceWasImproved);
// min/max 是按 (marketIndex, isLong) 配置的有符号值；!allowNegativeSpread 时才把负 min 抬到 0
```

这个函数**连 `min/maxDynamicSpread` 入参都没有**——v0.3.2 的招牌特性「点差可为负」在它这儿根本不可表达。

**同时要更正我自己的一个操作失误**：我在重推工作流里让 agent 把 `formulas.ts` 当作「更可能正确的交叉佐证」。在 dynamicSpread 这一项上**这个假设是错的**。所幸核心公式文档 §4 经核对本来就是对的（不属 14 章照搬区），且重推 agent 被要求以源码为准，影响面有限——但我会在合并复核时逐条重新确认涉及点差的段落。

## 对合并计划的影响

原计划是「按源码重推核心公式文档」。现在看应改成 **「重推 + 吸收 versions/v0.3.2 已写对的内容」**——尤其 Funding 与 dynamicSpread 两域，B 的表述更完整且已经过源码验证。我会在合并时逐条对照，不再让核心文档另起一套说法。

**这批文档我仍然一个字没动。**


---

# 八、修正轮进展（凌晨更新）

## 第一轮修正：4/12 通过

36/36 agent 跑通、0 错误、**0 条复核意见被驳回**（说明上一轮复核提的都成立）。

| 状态 | 组 |
|---|---|
| ✅ 可合并 | `u03` §4 动态点差 · `u07` §10 Funding · `u08` §11 清算判定+§12 ADL · `u09` §13 LP Vault+§14 执行费 |
| ⏳ 第二轮 | `u01` `u02` `u04` `u05` `u06` `u10` `u11` `u12` |

8 组的阻塞点都是**具体的一行错**（复核者原话：「一行可改」「改一句话即可」），已启动第二轮。举两例：
- `u04` 新引入一句可证伪的绝对断言（「26/26 市场两个 minCF 相等 ⇒ 部分平仓必然 revert」），复核给出了反例
- `u06` 新增的门控段落漏了一个决定订单成败的前置条件，按它套算普通平仓会得出「成功、费用全 0」，链上实际 revert

## 重要验证：重推稿独立覆盖了 `versions/v0.3.2` 的关键结论

第七节说 B（versions 文档）有 10 处比 A 正确。我逐条查了重推稿是否已独立写对——**是的**：

- `u07` §10：EMA 写的是 `alpha = exp(-e)` **连续衰减递推**（不是被批评的「一小时采样间隔」离散分桶）；零 OI 明写「**这不是完全 no-op**…抹掉已有的 skew EMA 历史」
- `u11` §19.2：完整描述 `settleFundingFees`；**明确区分减仓传实付额 `amountPaidInCollateralToken` / 加仓传应付额**；并直接声明 v0.3.1 旧行为「在 v0.3.2 全部删除，不得再作为断言依据」

所以重推不需要靠 B 兜底，两条独立路径得出同一结论——这比任何一条单独成立都更可信。第二轮我仍把 B 作为可选交叉参考传给了 agent，但明确「冲突时以源码为准」。


---

# 九、合并前必须你拍板的一件事：**文档会膨胀 5 倍**

## 数据

空跑合并脚本（仅 8 组已通过的）：**1394 行 → 3945 行**。13 组全合完预计 **7000+ 行**。

构成分析（抽 3 组）：

| 组 | 总行 | 表格 | 公式代码块 | 标题 | 空行 | 散文 |
|---|---:|---:|---:|---:|---:|---:|
| u04 §5 | 399 | 30 | 93 | 10 | 106 | 160 |
| u07 §10 | 306 | 18 | 64 | 10 | 76 | 138 |
| u12 §21–23 | 249 | 144 | 2 | 15 | 34 | 54 |

空行占 ~27%（markdown 块间距），真正承载内容的约 70%。

## 这些行数是挣来的吗？——基本是

以 §5 为例：原文 65 行覆盖 6 个子节，而审计查出它**漏了两条 material**（`payForCost` 的「先扣 output 再动抵押品」优先级、全平时 `initialCollateralDeltaAmount` 被强制置 0）。补齐分支、加源码锚点、加可验算的数值例子，行数必然涨。

但也要如实说：密度已经从**「精粹卡」变成「参考手册」**，与你说过的「公式越简洁可读性越高越好」有张力。你当时同时要了三样——**简洁 + 多条件 + 有例子**——这三者本身互相拉扯，重推稿选择了完整性。

## 三个选项（我不替你决定）

| 选项 | 结果 | 代价 |
|---|---|---|
| **A. 按现状合并** | 7000+ 行完整参考手册，分支全、锚点全、例子可验算 | 查阅要靠搜索；通读成本高 |
| **B. 合并后做一轮压缩** | 保留全部分支/锚点/例子，只压散文与重复表述，估计能压到 4500–5000 行 | 多一轮工作；压缩本身可能误删 |
| **C. 拆成两层** | 主文档回到精粹卡（每节 30–50 行，只留公式+锚点+取整方向），把完整分支与例子拆进 `<章节名>-详解.md` | 改动结构最大；但最贴合你「简洁」的要求，且与 skill 的精粹版定位一致 |

**我的倾向是 B**：先把正确性落地（这是三轮对抗复核挣来的，不该为了短而丢），再单独做一轮压缩。C 虽然最合你的口味，但拆分会让本来就没有版本控制的这批文档更难维护。

**在你拍板前我不合并。** 合并脚本已写好并空跑验证通过（章节映射无冲突、无遗漏、无越界），随时可执行。


---

# 十、修正轮最终状态：12/12 就绪

| 轮次 | 通过 | 累计 |
|---|---|---|
| 第一轮 | u03 u07 u08 u09 | 4/12 |
| 第二轮 | u04 u06 u10 u12 | 8/12 |
| 第三轮 | u01 u02 u05 | 11/12 |
| 我手工修 | u11 | **12/12** |

三轮共 96 个 agent、**0 错误**。

## 两个值得记的过程细节

**1. 修正 agent 驳回了复核者的论断，并自证**

`u02` 出现本次唯一的 `blocker-itself-wrong`：复核者称「减仓取整偏差的符号由方向决定（多头略亏、空头略赚）」，修正 agent 指出复核者对空头沿用了多头的 PnL 符号约定，换回交易者自身符号后**两侧同号、恒对交易者不利**，并跑 40 万组随机整数对拍验证「正偏差 0 次」。这是我要的对抗行为，不是照单全收。

顺带纠正了该节一个原有的错误论断：原稿说「减仓取整不改变即时净值」——不成立，因为合约按 token 比例分摊整仓 PnL（`mulDiv(R, t, T, R<0)`）而非 `t×P − d`，所以「已实现 + 剩余未实现 = 整仓 PnL」**不是恒等式，不能当 wei 级守恒/ΣΔ 不变量**。

**2. u11 最后一条我自己改的（并已对源码核实）**

复核指出 `decreasePosition` 的订单自动改写是**七处不是六处**，漏的第 7 处是 `DecreasePositionUtils.sol:238-241` 全平时 `setInitialCollateralDeltaAmount(0)`，而且**不发任何事件**。我核了源码属实，已补写并加了两条断言警告：

- 全平场景事件与 Reader 里的 `initialCollateralDeltaAmount` **恒为 0**，不能当"本次提取保证金"——真正还给用户的是 `output.outputAmount`。按它建模会**整整少算一份本金**。
- 因为不发事件，**无法靠事件流发现这次改写**。断言"全平时该字段等于创建时的值"会必然 FAIL，而那不是缺陷。

这条与 u04 §5.4、以及需求差异台账里的同一条互相印证，是三条独立路径撞到同一个坑。

## 还差 §18

补齐 §18 的那一组仍在跑（它要与前端公式文档的 Path A/B 表、u08 §11、`Liquidation-(v0.3.2).md` 三方对齐）。落地后我会做全量空跑，然后**停在这里等你对第九节的膨胀问题拍板**，不擅自合并。


---

# 十一、全部就绪：13/13，等你拍板

## §18 补齐结果

补出来的内容比预想多——§18 原本 73 行，重推后 294 行，查出的都是**两版都缺**的东西：

- **§18 从来没有「同币种 / 异币种抵押」两套分母**（v0.3.1/v0.3.2 皆无）。异币种公式套同币种仓位会算出偏低的清算价。
- **Path A 的 `priceImpactDeltaUsd` 建立在 v0.3.2 死代码上**：`MAX_POSITION_IMPACT_FACTOR_FOR_LIQUIDATIONS` / `POSITION_IMPACT_FACTOR` / `POSITION_IMPACT_EXPONENT_FACTOR` 的 getter 在 `src/` 无任何调用方，`Position.Numbers` 也没有 `pendingImpactAmount` 字段（SDK 恒读 `0n`）。**若测试环境把这些遗留键配成非零，会出现「页面动而链上不动」。**
- **SDK 的 `pendingBorrowingFeesUsd` 在 v0.3.2 ABI 上恒为 `0n`**（`PositionFees` 结构无 borrowing 字段）——算了一项合约根本不结算的费。
- **前端 `DEFAULT_LIQUIDATION_PROTECTION_SECONDS = 15*60` 是兜底常量**，`graceEnd <= 0` 时窗口为 `null`，**不是默认 15 分钟**。
- **页面 Liquidatable 徽标是本地估算**（`netValue <= 0` 或 mark 越过本地清算价），不是 Reader 的 bool，且保护期内被抑制。
- **「Net Value 为正而徽标已亮」不是渲染 bug**：判定用 `posInfo.netValue`、显示用 `getPositionPanelNetValueUsd`，两个量差一项正 Funding，且 closeFee 档位来源也不同。

## ⚠️ 我又改错了一处，已订正（前端文档 §6）

复核 §18 时交叉发现：我先前写的 `FX100-前端代码公式.md` §6「下单预览 / 杠杆弹窗走 Path B」**是错的**。亲验源码：

```js
// OrderPreview.tsx:332-348
const liqRaw = preview?.liquidationPrice;   // ← SDK Path A
if (liqRaw != null) { … return liqNum; }     // ← 优先返回
return estimateNewLiqPrice({…});             // ← 仅 A 为 null 时兜底

// PositionLeverageDialog.tsx:491-495
displayLiqPrice = hasUserChangedTarget ? sdkEstimatedLiq ?? estimatedLiq.newLiqPrice : …
```

三个界面**全部是 Path A 优先、Path B 兜底**。我的错法是：**找到了 `estimateNewLiqPrice` 的调用点，就断定该界面「走 Path B」，没有再看一层它是主路径还是兜底**——和我在 Keeper 文档里抓出的 `evaluateTrigger` 是同一类错。已订正并写入更正记录与通用防法。

## 最终状态

| 项 | 状态 |
|---|---|
| 13 组重推稿 | **全部通过复核**（三轮修正 + 我手工修 u11、u13 共 4 处） |
| 合并脚本 | 已写并全量空跑通过：**23 节替换、3 节保留（§0/§24/§25）**，`1394 → 5642 行` |
| 需求差异台账 | 13 组共 **116 条**（原 103 + u13 的 13） |
| 已独立落地的修改 | 前端公式文档 6 处 · Keeper 公式文档 5 处 · 索引锚点 3 处 · 项目模块总结 1 处 · skill `formulas.md` 1 处 · `CURRENT.json` pin |

## 就差你一句话

**第九节的三个选项**（A 按现状合并 5642 行 / B 合并后压一轮到 ~4500 / C 拆成精粹卡 + 详解两层）。我倾向 **B**。

合并命令已就绪，你说一声我就执行：

```bash
python3 /private/tmp/claude-501/-Users-milakou-Documents-FX100/7d295c95-2c3e-4fee-a63f-707b9b23b26f/scratchpad/merge.py u01 u02 u03 u04 u05 u06 u07 u08 u09 u10 u11 u12 u13 --apply
```

**在你回复前，主文档一个字未动。**


---

# 十二、已执行（2026-09-05 中午，按你拍板的 A）

| 项 | 结果 |
|---|---|
| 核心公式文档合并 | **1394 → 5639 行**，23 节替换、§0/§24/§25 保留；26 节编号完整；135 个跨文档链接 0 失效；`baseline:lint` 在 ContractCodeSummary 下 0 命中 |
| 需求差异台账 | `FX100-需求与实现差异台账.md` **124 条**（候选、未复核），README 已登记 |
| skill 汇总层 | 4 个文件拷入 `fx100-verify-handbook` 真身（软链同 inode）：SKILL.md 43→53、formulas.md 127→150、traps.md 80→101、**新建** v032-facts.md 70；**253 个 `→ §` 指针全部解析通过** |
| 数据恢复事故 | 临时目录被清空丢失 13 份复核稿，已从 agent 转录回放 + Read 快照重建，逐节行数校验一致后才合并；重推稿已持久化在 `重推稿/` |
| `versions/v0.3.2/` | 备份后有 10 个文件被**另一会话**改动（证据：本会话全部子 agent 对该目录只有读操作）；**未还原**，见 MANIFEST |

**权威模型（已定）**：源码定数值 → 详解层（版本化文档）→ 汇总层（skill）单向同步；需求规范只定语义，实现≠设计进台账。到 v0.3.3 时只重推源码 diff 涉及的章节，skill 加一张 `v033-facts` 卡。

**待你决定**：执行器（`npm run verify:tx -- --tx 0x…`）落 `TestCode/tools/`、skill 只指路；前提是先修引擎三处（`composeDynamicSpread` 仍 v0.3.1、funding 双锚点、`lpToPositionVault` 无 v0.3.2 分支）。你点头我就开做。
