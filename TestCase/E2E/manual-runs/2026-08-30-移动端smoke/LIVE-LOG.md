# LIVE-LOG · 2026-08-30 移动端 smoke（实时观察底稿）

> 追加式底稿，正式结论以 RUN-SHEET / OPERATION-LOG 为准。

## 2026-08-30 ~14:4x 记录员仿真视口预扫（内置浏览器模拟，非真机；对应 M-P2-02 前置）

**环境**：Claude 内置浏览器，移动仿真（Android Chrome UA + 触控），fx100-dev /trade?market=BTCUSDC，未连接钱包。

1. **375×812 / 390×844 竖屏**：移动端专属布局正常出现（顶栏+跑马灯 ticker+市场头+统计格+图表区+市价/限价下单区+保护面板+持仓标签区+页脚），`scrollWidth == innerWidth`，**无横向溢出** ✓；弹性堆叠无遮挡。
2. **底部标签行**（持仓/当前订单/交易历史/资金费率历史/订单历史）为横向滚动式，390 宽下"订单历史"裁切在屏边、无滚动提示 —— 真机核可达性（M-P2-02 观察点）。
3. **812×375 横屏**：落在桌面断点（≥768），渲染桌面布局；`scrollWidth=814 vs innerWidth=812` 有 **2px 轻微横向溢出**。真机横屏（мобile UA）预计同样拿到桌面布局，触控目标偏小 —— E2E-TRD-053 横屏项以真机为准。
4. **⚠ K 线蜡烛不绘制（待真机裁决）**：移动 375×812 与桌面视口下，TradingView 控件均初始化完成（工具栏/图例出现，图例 OHLC **实时更新**，`/api/prices/candles` 返回正常数据），但主图 pane canvas 逐像素采样为**纯背景色(28,28,28)**，价格轴/时间轴无刻度文本，切 1H 不改善。桌面视口同样复现 → **疑内置浏览器渲染特性而非站点缺陷**（web 轮真实 Chrome 三轮未报"图表全空白"）。真机若复现则属行情/K线域缺陷（该域 web 轮本就是盲区 + 已有 PRICEFREEZE-015 前科），需开新单。
5. 仿真下 computer 事件注入超时（面板隐藏态），记录员改用 JS 驱动 + 截图观察；不影响真机执行。

## 2026-08-30 18:15~18:30 真机首批执行（iOS MetaMask 内置浏览器）

- 真机截图 5 张（18:15~18:20）：K 线蜡烛正常渲染 → **关闭仿真预扫疑点④**（"K线不绘制"确认为记录员仿真浏览器特性，非站点缺陷）。
- 链上事件流（MLOG-001~006）：LINK 100x 市价开多（全字段核对 PASS）→ ENA 市价开空（零点差, balanceWasImproved）→ WETH Buy-Limit 真实成交 → ENA TP/SL 同 tx 双单（Max 哨兵规范）→ ENA 市价平仓单 → ENA 再开空。
- 记录员观察哨：`mobile-watch.mjs` 后台轮询中（45s 间隔），事件落 `mobile-watch.jsonl`。
- 关注中：LINK 100x 仓位（uPnL −22%，强平价 11.329）保护 18:32:28 到期，可能自然清算——保护到期行为观察点。
- 显示级留观：持仓卡 uPnL 与"价值"两字段隐含标记价差 0.0007（刷新时点不同步），非缺陷级。

## 19:4x 余额对账（BUG-021 资金锁定实证）

- 执行人报告可用余额 656.06；链上流水推算 ~656.1（吻合）。反证若"存 10"已退应 ~666 → **10 USDC 锁在僵尸存款单金库**，已补进 BUG-EXEC-MARGINSTUCK-021 影响①。
- 当前敞口：仅 **ETH 100x 多仓**存续（抵押 187.49 在仓内，19:10 时净值 197.44、uPnL 转正）；LINK/ENA 已清零。
- 待验：僵尸单手动取消（成功应退 10 U，余额 656.06→666.06）。
