# 06 · 策略下单全矩阵与用例映射（tx-fork）

状态：设计初稿，待评审 ｜ 2026-08-13 起草 ｜ 功能场景：**tx-fork 真实链路策略下单全矩阵**

## 为什么用矩阵文档而不是连续编号

策略下单矩阵的组合（市价/限价 × long/short × 开/加/减/关 × 盈/亏）落在 S02、S03、S07 的既有 SCN 上，编号天然不连续。SCN 编号被总控清单、需求追踪矩阵、看板覆盖层与历史运行结果全链路引用，**不重排编号**（工作区纪律：不创建脱离设计文档的新编号）。功能场景的"覆盖设计视图"由本文档承担——一张矩阵一眼看全：每格写死 `SCN 编号 + 数据集 ID + 自动化状态`，执行层再用看板按本清单建批次。

三层记录方式：

1. **本矩阵文档** = 设计视图与覆盖清单（对齐 04/05 文档惯例）；
2. **数据集 ID** = 同一 SCN 内的方向/盈亏变体，报告保留独立结果（SCN-070 的 8 数据集模式）；ID 规范：`<SCN>/<方向>-<动作>[-<盈亏>]`，如 `SCN-023/long-decrease-profit`；
3. **看板批次** = 按本清单勾选 SCN 一键执行（后续可加"功能场景"标签一键筛选）。

## 矩阵 A ｜ 市价单（MarketIncrease 0 / MarketDecrease 2）

| 动作 | Long | Short | 数据集 | 状态 |
|---|---|---|---|---|
| 开仓 | **SCN-009**（快速退出含全平） | **SCN-065**（补齐 MarketIncrease short） | — | ✅ 009 ｜ ⬜ 065 |
| 加仓 | **SCN-025** | SCN-025/short-increase | `long-increase` / `short-increase` | ⬜ |
| 减仓·盈利 | **SCN-023**（分批兑现） | **SCN-030**（空头部分平仓，兼验 Funding 回归） | `long-decrease-profit` / `short-decrease-profit` | ⬜ |
| 减仓·亏损 | SCN-023/long-decrease-loss | SCN-030/short-decrease-loss | 归属待评审（亦可归 SCN-022） | ⬜ |
| 全平·盈利 | **SCN-022**（毛 PnL 与实际可得差异，盈利侧最有信息量） | SCN-022/short-close-profit | `long-close-profit` / `short-close-profit` | ⬜ |
| 全平·亏损 | **SCN-024**（紧急全平，亏损语境） | SCN-024/short-close-loss | `long-close-loss` / `short-close-loss` | ⬜ |

> SCN-009 的全平是"快速退出"（微亏 = spread + fee），与 SCN-022/024 的显著盈亏数据集不重复。

## 矩阵 B ｜ 限价与触发单（LimitIncrease 1 / StopIncrease 6 / LimitDecrease 3 / StopLossDecrease 4）

| 意图 | Long | Short | 状态 |
|---|---|---|---|
| 限价开（顺势低吸/高抛，LimitIncrease） | **SCN-010**（P−10% 低吸） | **SCN-011**（P+10% 高抛） | ✅ 010 ｜ ⬜ 011 |
| 突破追单（StopIncrease） | **SCN-012**（突破追多） | **SCN-013**（破位追空，含生效时间） | ⬜ |
| 止盈关（LimitDecrease/TP） | **SCN-015**（括号单 TP 先触发） | **SCN-066**（空头下跌止盈） | ⬜ |
| 止损关（StopLossDecrease） | **SCN-016**（括号单 SL 限制损失） | **SCN-067**（空头上涨止损） | ⬜ |

## 矩阵 C ｜ 订单管理与失败路径（矩阵 A/B 的横向补充）

| 场景 | SCN | 状态 |
|---|---|---|
| 改单追踪市场 | SCN-017 | ⬜ |
| 主动撤单并确认全额退款 | SCN-018 | ⬜ |
| 市价滑点不达时拒绝坏价成交 | SCN-019 | ⬜ |
| 触发单失败区分 Freeze 与 Oracle Revert | SCN-020 | ⬜ |

> 边界精度类（触发价 ±1、四象限最差可接受价、reduce-only/autoCancel 矩阵）留在 oracle-fork 的 S07（SCN-069/070/071/072），与本矩阵"真实链路流程闭环"目的互补，不重复。

## 执行方式（一个 tx-fork 跑全）

- 全部用例 `targetProject=tx-fork`（差异经看板覆盖层调整，不改 md 基线）；市场资源统一 `default-mock`；
- 每数据集独立 `evm_snapshot`/`evm_revert`：推价用的 `setMockPrice` 也在快照内，回滚即自动复位价格；
- Inline Keeper（避免排队延迟触发 60s 价格年龄、避免第三方交易污染核对窗口）；
- 价格构造统一三步：开仓于 P（set min=max 精确单价）→ 推价 ±x% 构造盈/亏 → 减/平仓；限价触发组：挂单 → 推价至触发 → Keeper 执行（SCN-010 已验证）；
- 每数据集开始刷新 Oracle 时间戳（runner 已内置自愈）。

## 放量前置（顺序执行）

1. P1 共享构造器方向守卫修复（short 开仓 ⌈⌉、部分平 ⌈⌉、盈亏 PnL 期望、funding 按 orderKey 锚定 + Σ 聚合）——short/减仓/亏损数据集全踩在这些潜伏错误上；
2. 共享 harness 抽取（SCN-070 纯 viem + Model 分离为范本；spec 五件套模板 + openPosition/movePrice/closePosition 三个 flow + 统一 check 与 evidence schema）；
3. 首条对偶用例 **SCN-065（市价开空）** 实证 short 路径；
4. 矩阵 A（市价组）→ 矩阵 B（限价/触发组）→ 矩阵 C（管理/失败路径，需 OrderCancelled/reasonBytes 解码下沉共享模块）。

## 回写约定

- 各 SCN 的"必做数据组"表评审后回写进 `scenarios/S02/S03` 对应用例文本（对齐 S07/S08 惯例）；
- 本文档状态列随自动化落地更新（✅/⬜），执行结果仍以看板与 SCENARIO-CHECKLIST 为准，本文不重复记录执行状态明细。
