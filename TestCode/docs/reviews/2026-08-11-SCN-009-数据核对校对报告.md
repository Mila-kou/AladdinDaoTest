# SCN-009 数据核对内容与公式校对报告

> 2026-08-11 · 6 维度并行审查 + 分组对抗性复核（9 agent，全部发现经独立复核确认，0 条被驳回）。
> 判据：fx100-verify-handbook（formulas/traps/v031-facts）+ `Github/fx100-contracts@release-v0.3.1` 源码实际实现 + 真实证据重放。
> 重放基准：`artifacts/runs/2026-08-06T152538-445Z`（SCN-009 PASS，93 行 reconciliation）。

## 一、总体结论

- **多头市价开平这条已实测路径上，公式实现与合约行为 bit 级一致**：五方守恒 ΣΔ（open/close/wholeFlow）、开/平执行价（含 dynamicSpread 分解）、Position Fee 两次 floor、5e29 分账、sizeInTokens 取整、全平支付瀑布 ΔTrader（0 容差对钱包实测差）、PnL、claimable 入账、funding 金额与 factor、Grace——全部独立重放吻合（见 §四）。
- **但发现 1 个高危新问题**：五方守恒（L1 零和总闸）在 runner 里只存 observations 从未断言，守恒 FAIL 用例仍绿。
- 另有一批**潜伏公式错误**（空头/部分平路径取整、funding 事件聚合）和覆盖缺口，当前多头全平用例不触发，但用例迁移放量后必然踩中。

## 二、确认发现（44 条，按严重度）

### [高|新发现] runner-01-conservation-not-asserted — 七组守恒结果只写进 observations，从未断言——L1 零和总闸 FAIL 时 SCN-009 仍然绿

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:775`｜类型：coverage-gap
- **问题**：runner 对 createOpen/executeOpen/createClose/executeClose/open/close/wholeFlow 七组 delta 都调用了 deps.checkConservation（L770-782），但结果只存进 observations，没有任何一条经过 check()；spec（tests/S02/scn-009.spec.ts）也只把 evidence 附出、不检查 observations。正确做法是每步守恒 status==='PASS' 都进 check()（UNVERIFIABLE 按 BLOCKED/FAIL 处理，不许当通过），legacy scenario.mjs 的 checkFiveWayConservation 就是现成模板。
- **依据**：formulas.md §1（零和守恒是'总闸，对取整免疫——期望值算错它也能兜底'）；ledger.mjs checkConservation L134-142 返回 PASS/FAIL/UNVERIFIABLE 三态；scn-009-runner.ts L770-782 仅存 observations；scn-009.spec.ts L49-81 无任何对 observations/conservation 的 expect。
- **失败情形**：合约资金流出现真实缺口（如某笔费用被重复划转，ΣΔ ≠ 0），checkConservation 返回 FAIL，但 runner 17 项 check 全过、spec 注解写'链上 17 项核对通过'，用例判 PASS——最强的独立核对手段完全不参与判定。推导层守恒行 FAIL 也不阻断（§10 已知质量闸门问题），两层叠加后守恒实际零门槛。
- **建议修复**：在 return 前对七组（至少 wholeFlow + 四个单步）逐一 check(assertions, '…五方守恒', cons.status === 'PASS', cons.sum, 0n)；UNVERIFIABLE 单独判 blocked 抛错。
- **复核修正**：runner 对七组 delta 调 checkConservation（:770-782）但结果只存 observations，没有任何一条进 check()；spec 亦无对 observations 的 expect。合约资金真缺口时 17 项 check 全过、用例判 PASS。注：推导层守恒行 FAIL 会把 executions 页 executionStatus 置 FAIL（FAIL 行确实计入 :1367 闸门，§10 豁免的只是 NOT_VERIFIED），但该状态不回写测试判定——最强核对手段在自动化判定链上零门槛的结论不变。修复：至少对 wholeFlow+四个单步逐一 check(status==='PASS')，UNVERIFIABLE 按 blocked 处理。
- <details><summary>复核推理</summary>复核 scn-009-runner.ts:770-782：七组 checkConservation 结果只进 observations，check() 清单（:458-684）无任何守恒断言；spec（tests/S02/scn-009.spec.ts:49-81）只写附件与注解、对 evidence 内容零 expect，:78-81 注解按 assertions.length 写『核对通过』。ledger.mjs :134-141 三态确认。唯一表述偏差：推导层守恒行 FAIL 会翻转 executionStatus=FAIL（:92-94 FAIL 行计入 :1367 闸门，§10 ⬜ 只豁免 NOT_VERIFIED）——但 executionStatus 仅是报告页字段，除 legacyPartialCoverage 分支（:2187-2198）外不改写 result.status，更不参与 playwright 判定。核心结论成立：L1 零和总闸 FAIL 时用例仍绿。</details>

### [高|已知 ⬜] cov-04 — after 快照与参数快照取 latest 而非执行块/execBlock−1，且报告标注为'执行区块'，无第三方交易污染检测

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:502`｜类型：snapshot-quality
- **问题**：afterOpen（502 行）与 afterClose（575 行）用 takeSnapshot 不传块号→eth_blockNumber latest；readTradeParameterSnapshot 用 afterOpen.blockNumber（504-512 行）而非真实执行块；execution-evidence.ts parameterBlockNote（182-186 行）却宣称'参数来自执行区块 #N 的 DataStore 实际值'。traps §4 要求 Before=execBlock−1 / After=execBlock、funding 累加器必须 execBlock−1，且 fork 上有 Service Keeper 抢跑需'同块无第三方交易'检测。
- **依据**：traps §10 ⬜ 'after-execute 快照取 latest 非执行块、参数快照标注不实'；traps §4；runner 已拿到执行事件 blockNumber（openExecutionEvent.blockNumber）却未回填快照
- **失败情形**：执行块与 latest 之间被第三方交易（Service Keeper 吃别的单、他人转账）插入时，TX2/TX4 全部 Δ 行、funding EMA、fee factor 读数被污染，可能假 FAIL 也可能互相抵消假 PASS；证据页'执行区块'字样给出不实的证据等级。
- **建议修复**：用 openExecutionEvent/closeExecutionEvent 的 blockNumber 重钉 afterOpen/afterClose 与参数快照（Before 用 execBlock−1），并加同块第三方交易检测，命中标 POLLUTED。
- <details><summary>复核推理</summary>known:true 项（traps §10 ⬜ 'after-execute 快照取 latest 非执行块、参数快照标注不实'），核实存在且影响 SCN-009：runner 502/575 行 takeSnapshot 不传块号→396-407 行走 eth_blockNumber latest；504-512/576-584 行 readTradeParameterSnapshot 用 afterOpen/afterClose.blockNumber（即 latest）；execution-evidence.ts 182-186 行 parameterBlockNote 对 source==='chain-at-block' 一律宣称『参数来自执行区块 #N 的 DataStore 实际值』——latest≠execBlock 时标注不实。TX2 的 Before 是 afterCreateOpen（钉创建块 478 行）而非 execBlock−1，创建→执行间隔内第三方交易可污染；执行事件 blockNumber 在 594 行后才取得、未回填重钉；无同块第三方交易检测。traps §4 三条要求全部未满足，Tenderly 共享 fork 上 Service Keeper 抢跑真实存在，假 FAIL/假 PASS 与证据等级虚标均成立，high 合理。</details>

### [高|已知 ⬜] feefund-01 — 参数快照钉的是 latest 轮询块而非执行交易块，fee/funding 行的"执行区块参数"标注不实

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:502`｜类型：snapshot-quality
- **问题**：afterOpen/afterClose 快照不传 blockNumber，走 eth_blockNumber latest（takeSnapshot L396-407）；readTradeParameterSnapshot 又以该 latest 块为 blockNumber（L511/L583）。buildFeeAndFundingRows 的 position-fee-factor / position-fee / fee-receiver / pool-fee 行用的 POSITION_FEE_FACTOR(bool)、positionFeeReceiverFactor，以及 funding factor 行用的 fundingEma.lastEmaValue，全部来自这个块；parameterBlockNote 却声称"参数来自执行区块 #N 的 DataStore 实际值"。正确做法是以 OrderExecuted 回执 blockNumber 为准钉块。
- **依据**：traps §4/§10 ⬜（after-execute 快照取 latest 非执行块、参数快照标注不实）；EMA 正确性依赖执行块末态：MarketUtils.getNextFundingAmountPerSize L636-649 + ExponentialMovingAverage.saveValue/emaValue L64-90 表明只有执行块末态 lastEmaValue == 合约算 factor 用的 skew。抽查证据 artifacts/latest/attachments/SCN-009-tx-fork-r0-0-scn-009-evidence.json 中 latest 恰好==执行块（45042780/45042782），属巧合而非保证。
- **失败情形**：共享 fork 上 Service Keeper 在执行块与轮询 latest 块之间执行了同市场其他订单：fundingEma.lastEmaValue 被改写 → funding factor 行 expected 错 → 错误 FAIL；若治理/脚本恰在其间改费率键，fee 复算行同样错。即使数值巧合相同，行 note 的溯源声明也是不实的。
- **建议修复**：readTradeParameterSnapshot 的 blockNumber 改用 openExecutionEvent/closeExecutionEvent 的回执块号；快照同理钉执行块（before 用 execBlock−1）。
- <details><summary>复核推理</summary>独立复核：scn-009-runner.ts L502/L575 afterOpen/afterClose 调 takeSnapshot 不传 blockNumber，L396-407 走 eth_blockNumber latest；L504-511、L576-584 readTradeParameterSnapshot 的 blockNumber 就是这个 latest 块。execution-evidence.ts L182-186 parameterBlockNote 确实声称『参数来自执行区块 #N 的 DataStore 实际值』，被 position-fee-factor/position-fee/fee-receiver/pool-fee/funding-factor 行引用。EMA 论证成立：MarketUtils.updateFundingState L580 saveValue 后 L649 emaValue()（ExponentialMovingAverage.sol L64-88：saveValue 后 lastTime==block.timestamp → emaValue 返回刚存的 lastEmaValue），即执行块末态 lastEmaValue 恰为合约算 factor 的 skew；latest 块间若有同市场 Funding 更新即被覆写。抽查证据 JSON 证实本次 latest==执行块（afterOpen 45042780==open feesCollected 块，afterClose 45042782==close feesCollected 块），属巧合。traps §10 ⬜ 明列此项（after-execute 快照取 latest、参数快照标注不实），known:true 成立且确实影响 SCN-009 全部 fee/funding/grace 参数行。</details>

### [中|新发现] cov-06 — 订单创建参数无回读核对：orderType/sizeDeltaUsd 原值/acceptablePrice/triggerPrice 全靠构造侧自信

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:611`｜类型：coverage-gap
- **问题**：SCN-009 未抓 OrderCreated 事件也未读回 OrderStore：fetchExecutionEvents 的 extraNames 只有 PositionIncrease/Decrease（611-624 行）。orderType==MarketIncrease(0)/MarketDecrease(2)、订单 sizeDeltaUsd===50e30 原值侧（formulas §2 '订单原值侧===rawSizeDelta'）、acceptablePrice、市价单 triggerPrice==0 均无断言；SCN-070 有整套 readOrder 核对（订单类型/方向/triggerPrice=0/acceptablePrice），SCN-009 没有复用。
- **依据**：formulas §2；traps §2（orderType 与 GMX 不同序，凭直觉写常量必错）；execution-evidence.ts SCN-010 有 limit-order-type 行（1505-1510）而 SCN-009 无对应行
- **失败情形**：buildIncreaseMulticall 若构造错 orderType/acceptablePrice（如枚举错序仍是合法可执行类型），订单照样执行，现有核对只能靠下游数值间接发现，acceptablePrice 完全无检；用例'市价单'语义未被证据锚定。
- **建议修复**：extraNames 加 OrderCreated（key 字段名为 key），断言 orderType、sizeDeltaUsd 原值、triggerPrice==0，并记录 acceptablePrice。
- **复核修正**：SCN-009 未抓 OrderCreated 也未读回 OrderStore（fetchExecutionEvents extraNames 仅 PositionIncrease/Decrease，runner 611-624 行），orderType、acceptablePrice、市价单 triggerPrice==0 均无断言，'市价单'语义未被证据锚定；SCN-070 有整套 readOrder 核对（orderType 0/2、triggerPrice=0、acceptablePrice）而 SCN-009 未复用，SCN-010 有 limit-order-type 行（1504-1510）且其 orderType 直接取自已采集的 PositionIncrease 事件 uint——SCN-009 的 openIncreaseUint 里同字段现成可用、加行零采集成本。注意：sizeDeltaUsd 原值侧已被间接但确定性覆盖（position-open-size 行断言 position.sizeInUsd === before+50e30 精确原值，1256-1263 行），不宜列为无检项。
- <details><summary>复核推理</summary>独立复核：runner 611-624 行 extraNames 确仅 ['PositionIncrease']/['PositionDecrease']；scenario.mjs fetchExecutionEvents（463-481 行）默认只抓 PositionFeesCollected+Funding，OrderCreated 需显式传入（key 字段名为 key，476 行注释确认）。flows.mjs 34-36 行 ORDER_TYPE 枚举与 27-33 行注释证实枚举错序曾真实发生过（写成 {2,4} 把增仓发成 MarketDecrease）；acceptablePrice 默认 acceptAnyPrice 哨兵（108/149 行）且全链路无断言。deriveScn009Evidence 全部行过目无 orderType/acceptablePrice/triggerPrice 行。驳回尝试：sizeDeltaUsd 原值侧其实已由 position sizeInUsd 精确断言覆盖——claim 该项表述过宽，已在 correctedClaim 修正；orderType/acceptablePrice/triggerPrice 缺口驳不倒，且 SCN-010 的实现证明该行取自已采集事件、纯推导层补齐即可。</details>

### [中|新发现] cov-07 — 执行价核对独立度停留在 IDENTITY：dynamicSpread 的 priceImpact/depth 项（LogExpMath）未独立复算

- **位置**：`TestCode/src/reporting/execution-evidence.ts:677`｜类型：coverage-gap
- **问题**：执行价三处核对（overview 开/平 + pricing 行）输入全取自被核对事件（oracle 价+dynamicSpread），已如实标注【恒等式】；Dynamic Spread 总值行如实 NOT_VERIFIED（677-684 行）。skewImpact 分量有参数复算但仅 CALCULATED 展示；priceImpactSpread 的 exp(⌊orderSize×param/depth⌋) 项（formulas §3，需 Balancer LogExpMath 逐位 BigInt 移植）零实现，导致整条执行价链路没有 FULL_RECOMPUTE 级证据。
- **依据**：formulas §3 'exp = LogExpMath 逐位移植……独立算执行价的最大实现件'；traps §7 '执行价核对的输入若全取自被核对事件本身……该 PASS 建立在未验证字段上'；标注本身诚实（§10 ✅ 已复核）
- **失败情形**：合约 dynamicSpread 计算有 bug（如 depth 参数读错键静默为 0）时，事件里的 spread 与执行价自洽，恒等式行照样 PASS，独立复算缺位漏检。
- **建议修复**：移植 LogExpMath（魔数+12 项泰勒同序截断），用执行区块 depth/param/constant 参数把 Dynamic Spread 行升级为独立复算，执行价随之升为 EVENT_ANCHORED→FULL_RECOMPUTE。
- <details><summary>复核推理</summary>独立复核：execution-evidence.ts 677-684 行 Dynamic Spread 总值行确为 NOT_VERIFIED（'当前未复制 LogExpMath，因此不伪记 PASS'）；691/1283/1303 行执行价三处均已诚实标注【恒等式】（oracle 价+dynamicSpread 取自被核对事件本身）；668-676 行 skewImpact 仅 CALCULATED 展示无对照值；全文 grep LogExpMath 仅出现在字符串标注中，src/reconciliation/formulas.ts 无 exp 移植。formulas §3 明示 depth 项需 Balancer LogExpMath 逐位移植且是'独立算执行价的最大实现件'；traps §7 明示该 PASS 建立在未验证字段上。failureScenario 成立：合约 depth 键读 0 类 bug 下事件 spread 与执行价自洽，恒等式行照样 PASS。标注诚实、claim 是覆盖缺口而非虚标，与 §10 ✅ 不冲突，known:false 定性正确。</details>

### [中|新发现] feefund-02 — Funding 行锚定 fundingEvents[0]（区间第一条），不是本单执行交易的 Funding 事件

- **位置**：`TestCode/src/reporting/execution-evidence.ts:737`｜类型：label-or-basis-error
- **问题**：buildFeeAndFundingRows（L737、L879-880）与 buildLedgerDeltaExpectations（L242）都取 fundingEvents[0] 当"本单"的 Funding 事件读 positionPaysLp 与 long/shortFundingFactorPerSecond。但 fetchExecutionEvents 抓的是 [create 块, latest 块] 区间内本市场全部 Funding 事件（Funding 事件无 orderKey，无法按单过滤），其注释明说"范围内可能有别的仓位操作也触发 funding 更新"。正确做法：factor 行按执行交易块号/txHash 精确匹配那一条；LP Claimable Funding 行的 expected 应对区间内全部事件的 max(positionPaysLp,0) 求和（与 after−before 的区间口径对齐）。
- **依据**：TestCode/tools/fx100-legacy/integration/lib/scenario.mjs L452-481（fundingEvents=区间全部；同文件 checkFundingMarketLoop L516-525 就是对全部事件求和的正确示范）；合约 ExecuteOrderUtils.sol L52 每单执行都推进 updateFundingState 并 emit Funding。
- **失败情形**：fork 上第三方订单在 [create 块, latest 块] 内先于本单执行（traps §4 Service Keeper 抢跑）：fundingEvents[0] 是别人的事件 → funding factor 行与 LP Claimable Funding 行拿错锚 → 错误 FAIL 或错锚 PASS；区间内两条以上 Funding 时，claimable Δ（区间总账）≠ 第一条的 paysLp，恒 FAIL 噪声。
- **建议修复**：fundingEvents 按 blockNumber===执行块（或 txHash）选本单事件供 factor/net 行；claimable-funding 行 expected 改为 Σ max(positionPaysLp,0)；找不到本单事件时输出 NOT_VERIFIED 行。
- <details><summary>复核推理</summary>独立复核：scenario.mjs fetchExecutionEvents L463-480 的 fundingEvents 是 [fromBlock,toBlock] 区间本市场全部 Funding 事件（注释 L455-456 明说不能只看本单），且 Funding 事件无 orderKey 不做过滤；runner 传的区间是 [create 块, latest 轮询块]（scn-009-runner.ts L611-624）。execution-evidence.ts L242/L288-290（ledger claimableFunding 期望）、L737/L772、L879-880（factor 行）全取 [0]。合约 ExecuteOrderUtils.sol L52 每次 executeOrder 都 updateFundingState 并 emitFunding（MarketUtils L597-603），故区间内第三方同市场订单会插入额外 Funding 事件抢占 [0] 位。同文件 checkFundingMarketLoop L516-525 的 Σmax(paysLp,0) 是正确口径而未被推导层采用。隔离 mock 市场下 [0] 恰为本单（证据 JSON 每相位恰 1 条），但 deployed-market/共享 fork 下失效场景成立。与 feefund-01 非重复：一个是锚错事件，一个是参数钉错块。</details>

### [中|新发现] feefund-03 — PositionFeesCollected/Funding 事件缺失时整组 Fee/Funding 行静默消失，无 NOT_VERIFIED 兜底

- **位置**：`TestCode/src/reporting/execution-evidence.ts:736`｜类型：coverage-gap
- **问题**：L736 `if (Object.keys(fees).length === 0) return [];`：feesCollected 没抓到时 12 条 Fee/Funding 行整组不输出；L887 的 gate 同理——Funding 事件缺失时 factor 行整段省略（L918 的 NOT_VERIFIED else 只覆盖"事件在、参数缺"）。runner 侧也没有任何断言要求 PositionFeesCollected 存在（scn-009-runner.ts 只硬性要求 OrderExecuted 与 PositionIncrease/Decrease）。2026-08-07 已给 Grace 修了同类问题（缺参数改输出 NOT_VERIFIED 行），fee/funding 未同步。
- **依据**：traps §3（indexed string topic 错=静默抓不到事件）、§10 ✅ Grace NOT_VERIFIED 修复先例、§10 ⬜ NOT_VERIFIED 不阻断整体 PASS；对照 buildGraceRows L515-531 的兜底写法。
- **失败情形**：事件名哈希/topic1 过滤或 RPC 分页问题导致 PositionFeesCollected 静默抓空：executions 页没有任何 fee/funding 行，reconciliations 里无 FAIL，executionStatus 仍 PASS——手续费与资金费维度零核对却全绿。
- **建议修复**：feesCollected 为空时输出一条 NOT_VERIFIED 占位行（"未捕获 PositionFeesCollected，Fee/Funding 全组未核对"）；funding 事件缺失同理；runner 增加"执行交易含 PositionFeesCollected"硬断言。
- **复核修正**：L736 `if (Object.keys(fees).length === 0) return [];` 使 PositionFeesCollected 缺失时约 11-13 条 Fee/Funding 行整组静默消失；L887 gate 使 Funding 事件缺失时 factor 行整段省略（L917-925 的 NOT_VERIFIED 只覆盖『事件在、参数缺』）；runner 无 feesCollected 硬断言——均属实。但失败形态需修正：feesCollected 缺失时 buildLedgerDeltaExpectations 会以 fees={} 计算（feeForPool/feeReceiverAmount=0），实际费用非零时 posVault/claimable/仓位 Margin 行会 FAIL 且 executionStatus 判 FAIL（deriveScn009Evidence L1367），并非全绿——但 FAIL 被误归因为账本不符而非采集失败，根因不可判读；真正『零核对仍全绿』只发生在实际费用/资金费恰为 0 时（如开仓相位 positionPaysLp=0 时 Funding 事件缺失即完全无痕）。
- <details><summary>复核推理</summary>逐行核实 L736/L887/L917-925 三个 gate 与 Grace 兜底先例（L513-531）；grep 确认 scn-009-runner.ts 无任何 feesCollected 断言（仅硬断 OrderExecuted 与经由执行价检查间接硬断 PositionIncrease/Decrease）。驳回尝试：追查 executionStatus 聚合（execution-evidence.ts L1367-1371、render-executions.ts L223），发现费用非零时账本行会 FAIL，故原 failureScenario 的『reconciliations 无 FAIL 仍 PASS』表述过强；但资金费=0 的静默全绿路径真实存在（本次证据开仓相位 paysLp=0），且与 Grace NOT_VERIFIED 标准不一致的覆盖缺口本身成立。维持 medium。</details>

### [中|新发现] formula-01 — 共享推导器 size↔token 取整矩阵只覆盖多头格：空头开仓应 ceil、多头部分平应 ceil，代码统一 floor

- **位置**：`TestCode/src/reporting/execution-evidence.ts:283`｜类型：formula-error
- **问题**：增仓 OI 期望 sizeInTokens = sizeUsd / executionPrice（283 行）对多空都用 floor，但合约空头分支是 Calc.roundUpDivision（PositionUtils.getExecutionPriceForIncrease，src/position/PositionUtils.sol:640）；减仓 decreaseTokens = oldSizeInTokens × sizeUsd / oldSizeUsd（365 行）对多空都用 floor，但合约多头部分平是 roundUpDivision（PositionUtils.getDecreaseOrderSize，PositionUtils.sol:531）。SCN-009（多头开仓 floor 正确 + 全平走恒等分支 tokens×dUSD/dUSD 精确）恰好不受影响，但该构造器是 SCN-009/010 共享，未来空头或部分平用例即错。
- **依据**：PositionUtils.sol:636（长侧 floor）/:640（短侧 roundUp）/:524（全平恒等分支）/:531（多头部分平 roundUp）/:535（空头部分平 floor）；formulas §4 四格矩阵
- **失败情形**：空头市价开仓或多头部分平仓用例复用本构造器时，OI token 行 expected 与链上差 1 wei→系统性假 FAIL；若有人因此放宽容差则反向漏检。
- **建议修复**：buildLedgerDeltaExpectations 按 isLong 分支：增仓空头用 ceilDiv；减仓多头部分平用 ceilDiv，并显式实现全平恒等分支（dUSD==oldSizeUsd 时直接取 oldSizeInTokens）。
- <details><summary>复核推理</summary>独立复核合约四格矩阵：PositionUtils.sol getExecutionPriceForIncrease USD 模式 L636 多头 floor、L640 空头 Calc.roundUpDivision；getDecreaseOrderSize L524-525 全平恒等分支直接返回 sizeInTokens、L531 多头部分平 roundUp、L535 空头部分平 floor——与 formulas.md §4 一致。execution-evidence.ts L283 增仓 sizeInTokens=sizeUsd/executionPrice（BigInt 除即 floor）不分多空；L365 decreaseTokens=oldSizeInTokens×sizeUsd/oldSizeUsd 同样统一 floor 且未实现全平恒等显式分支。SCN-009 豁免验证成立：多头开仓 floor 恰对；全平时 sizeUsd(=margin×leverage=50e30)===oldSizeUsd（runner L520 断言），先乘后除精确恒等。SCN-010 亦为 long-only 全平（scn-010-runner.ts L780-803），当前零影响，但 buildTradePhaseRows 被两场景共享（L1173/L1436 等），空头开仓或多头部分平复用即差 1 wei——latent formula error 成立。</details>

### [中|新发现] ledger-02 — 开仓阶段 sizeInTokens 期望对空头也用 floor，合约空头开仓用 roundUp

- **位置**：`TestCode/src/reporting/execution-evidence.ts:283`｜类型：formula-error
- **问题**：sizeInTokens = sizeUsd / executionPrice（floor）被同时用于 ΔLong OI 和 ΔShort OI 期望；合约 USD 模式开仓多头 floor（PositionUtils.sol:627 cache.sizeDeltaUsd / cache.executionPrice）、空头 Calc.roundUpDivision（PositionUtils.sol:640）。正确应按 isLong 分向：多 ⌊⌋ / 空 ⌈⌉。
- **依据**：Github/fx100-contracts@release-v0.3.1/src/position/PositionUtils.sol getExecutionPriceForIncrease（:627 多头 floor、:640 空头 roundUpDivision）；formulas.md §4 开仓行。SCN-009 多头实测 floor(50e30/60366307925829368)=828276595306007 与事件一致。
- **失败情形**：空头开仓场景（USD 模式、除法有余数）：合约 OI 增 ⌈⌉，模型期望 ⌊⌋，ΔShort OI 行差 1 wei 假 FAIL。当前 SCN-009/010 均为多头故潜伏。
- **建议修复**：sizeInTokens = isLong ? sizeUsd/executionPrice : ceilDiv(sizeUsd, executionPrice)。
- <details><summary>复核推理</summary>复核 PositionUtils.getExecutionPriceForIncrease：USD 模式多头 cache.sizeDeltaInTokens = sizeDeltaUsd / executionPrice（floor，约 :637），空头 Calc.roundUpDivision(sizeDeltaUsd, executionPrice)（:640-641）。execution-evidence.ts:283 sizeInTokens = sizeUsd / executionPrice 无条件 floor，并同时用于 ΔLong/ΔShort OI 期望（:338-344）。空头 USD 开仓且除法有余数时期望差 1 wei 假 FAIL。SCN-009/010 均多头故潜伏，与 formulas.md §4 开仓行（多⌊⌋/空⌈⌉）一致。驳回失败。</details>

### [中|新发现] ledger-03 — funding 期望只取 fundingEvents[0]，未按区间 Σ 聚合

- **位置**：`TestCode/src/reporting/execution-evidence.ts:242`｜类型：formula-error
- **问题**：buildLedgerDeltaExpectations（:242,:288-290）与 buildFeeAndFundingRows（:737,:772,:879）都只读 fundingEvents[0].positionPaysLp；而 fetchExecutionEvents 返回的是区间内本市场全部 Funding 事件（scenario.mjs:455 注释明言'市场 LP 闭环要的是区间总账'），vendor 参考实现 checkFundingMarketLoop 用 Σmax(positionPaysLp,0) 与 Σmax(−positionPaysLp,0)。正确应对区间内全部事件求和后再算 claimableFunding 与 lpToPositionVault。
- **依据**：TestCode/tools/fx100-legacy/integration/lib/scenario.mjs:516-525 checkFundingMarketLoop（Σ 口径）；MarketUtils.sol:581-595（每次订单执行都推进 funding 并产生一条 Funding 事件）。快照窗口为 [创建块, latest 轮询块]，deployed-market/service-keeper 模式下同市场他人订单会在窗口内追加 Funding 事件。
- **失败情形**：E2E_MARKET_MODE=deployed-market 且窗口内同市场发生第二笔订单执行：实际 Δclaimable Funding=Σ 而期望=首条事件值 → 待领取 Funding、ΔPositionVault、ΔLPVault 三行同时假 FAIL；若首条恰是他单事件，期望值整体错基。inline-keeper 私有 mock 市场（如实测 run 单事件 positionPaysLp=19）不触发。
- **建议修复**：对 fundingEvents 求 Σmax(positionPaysLp,0) / Σmax(−positionPaysLp,0) 后代入各期望，与 vendor 闭环②同口径。
- <details><summary>复核推理</summary>复核 scenario.mjs fetchExecutionEvents（:463-480）：fundingEvents = 区间内本市场全部 Funding 事件（topic1 按 marketIndex 过滤、不按 orderKey 过滤），注释明言市场 LP 闭环要区间总账；checkFundingMarketLoop（:516-533）用 Σmax(positionPaysLp,0)/Σmax(−positionPaysLp,0)。execution-evidence.ts :242/:288-290 与 :737/:772/:871-875/:879 均只读 fundingEvents[0]。runner 支持 E2E_MARKET_MODE=deployed-market（scn-009-runner.ts:417），共享市场窗口内他单执行会追加 Funding 事件 → 待领取 Funding、ΔPositionVault、ΔLPVault 期望错基。inline-keeper 私有 mock 市场单事件不触发，故为环境依赖的潜伏公式错。驳回失败。</details>

### [中|新发现] position-01 — position-open-exists 行 formula 声称核对 isLong=true，status 实际只核 exists，方向核对在核对行层缺位

- **位置**：`TestCode/src/reporting/execution-evidence.ts:1249`｜类型：label-or-basis-error
- **问题**：SCN-009 顶层行 formula 写 'afterOpen.position.exists = true 且 isLong = true'，但 status 仅 pass(boolValue(openPosition.exists), true)，isLong 未参与判定；buildPositionRows 字段表也不含 isLong。同文件 SCN-010 对应行（:1545）status 为 exists && isLong，证明遗漏而非有意。
- **依据**：execution-evidence.ts:1249 vs :1545-1546；runner 侧 scn-009-runner.ts:519 有『开仓后为多头』断言（openedPosition.isLong），方向核对仅存在于 assertions 层，不进 reconciliations 与 executionStatus（:1367-1371 只看 reconciliation FAIL + 交易状态）。
- **失败情形**：假设方向类缺陷（建成空仓但其余字段自洽）：executions 页该行仍 PASS 且 formula 文本让评审者误信方向已核；对历史证据 JSON 重放推导层时 runner 断言是冻结数据，核对页整体 PASS 不受方向错误影响——违反 05 标准『无证据的勾选不算通过』。
- **建议修复**：status 改为 pass(boolValue(openPosition.exists) && boolValue(openPosition.isLong), true)（与 SCN-010 对齐），或在 buildPositionRows 增加 isLong 行（expected == testData.isLong 订单输入，属 FULL_RECOMPUTE 独立期望）。
- **复核修正**：position-open-exists 行（:1249）formula 声称核对 isLong=true 但 status 只核 exists，与 SCN-010（:1545 exists && isLong）不一致，属遗漏；不过 runner 层 :519 已有 isLong 硬断言且 fail-fast，活跑中方向缺陷不会产出绿报告——缺口仅在推导层对历史证据重放时该行仍 PASS 且 formula 文本失实。修复：status 与 SCN-010 对齐或在 buildPositionRows 增加 isLong 行。
- <details><summary>复核推理</summary>复核 execution-evidence.ts:1248-1253：status = pass(boolValue(openPosition.exists), true)，isLong 不参与判定，而 formula 写『exists = true 且 isLong = true』；SCN-010 对应行 :1544-1546 status 为 exists && isLong，证明遗漏非有意；buildPositionRows 字段表（:468-474）确无 isLong。但注意 runner :519 有『开仓后为多头』check（passed=openedPosition.isLong）且失败即抛，方向缺陷在活跑中会被 runner 拦住、绿报告不会产出——暴露面仅限对冻结证据 JSON 的推导层重放与 formula 文本失实，实际风险低于 medium。</details>

### [中|新发现] runner-02-after-snapshot-errors-unchecked — 只有 before 快照断言了 errors 为空；afterClose 读数缺失时『全平后仓位已移除』会空过，traderUsdcDelta 用 ?? 0n 把缺读数当 0

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:585`｜类型：snapshot-quality
- **问题**：L458 只对 before 检查 errors.length===0，afterCreateOpen/afterOpen/afterCreateClose/afterClose 四份快照的 errors 全部无人检查。后果：(a) afterClose 的 getPosition 读数出错时 values.position=null → positionOf 返回 undefined → L585『全平后仓位已移除』在仓位实际还在的情况下通过（vacuous pass）；(b) L696 traderUsdcDelta = (afterClose.values.traderUsdc ?? 0n) − (before.values.traderUsdc ?? 0n)，null 读数被 ?? 0n 兜成 0，直接违反 ledger.mjs L91 的『读不到就是读不到——绝不当作 0』铁律；(c) 缺读数槽位 diff 为 null → 守恒退化 UNVERIFIABLE，而按 runner-01 它本来就不阻断。legacy scenario.mjs 的 takeSnapshot（L47-51）遇 errors 直接抛 blocked，runner 自己的 takeSnapshot 没有这层。
- **依据**：ledger.mjs L89-98（错误槽位置 null 并记入 errors）；scn-009-runner.ts L396-407（takeSnapshot 不检查 errors）、L458（仅 before）、L585、L696；对照 scenario.mjs takeSnapshot L39-53。
- **失败情形**：afterClose 快照时 Reader.getPosition 因 RPC 瞬时故障/限流返回 error：仓位实际未删（平仓被静默取消）也会通过『全平后仓位已移除』；同时 traderUsdc 读失败时 observations 里的 traderUsdcDelta 是编造的 0 基线数字，进入证据 JSON 误导人工复核。
- **建议修复**：四份 after 快照复用与 before 相同的 errors===[] 断言（或让 takeSnapshot 直接抛 blocked）；traderUsdcDelta 去掉 ?? 0n，缺读数置 null。
- <details><summary>复核推理</summary>复核：(a) :458 仅 before 断言 errors===[]，四份 after 快照无人检查；ledger.mjs :89-98 错误槽位置 null；positionOf（:296-299）对 values.position=null 返回 undefined → :585『全平后仓位已移除』（期望 false）空过成立。(b) :696 traderUsdcDelta 用 ?? 0n，null ?? 0n === 0n，把缺读数当 0 写进 observations/证据 JSON，违反 ledger.mjs :91 铁律。(c) diff（:119-127）缺读数槽 null → checkConservation UNVERIFIABLE，叠加 runner-01 不阻断。对照 scenario.mjs takeSnapshot :39-53 遇 errors 抛 blocked，runner 自己的 takeSnapshot（:396-407）无此层。驳回失败。</details>

### [中|新发现] runner-07-no-window-purity-detection — 无『同块/同窗无第三方交易』检测，也不清账户在挂订单与空头方向——共享 fork 上账本窗口纯净度无人把关

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:457`｜类型：coverage-gap
- **问题**：traps §4 要求 fork 上加『同块无第三方交易』检测，命中标 NOT_VERIFIED/POLLUTED；runner 完全没有：before 快照取发单前 latest（与创建块之间可隔多块），四个窗口内任何第三方/keeper 交易都会静默混入市场级槽位（OI、claimable、lpVaultAssets）。前置也只查『执行前无 ETH 多仓』（L459）——同账户空头仓位、遗留在挂订单（可能被 keeper 在窗口内执行）都不检查，而 legacy 已有 ensureCleanSlate/drainPendingOrders 模板未复用。
- **依据**：traps.md §4『fork 上有 Service Keeper 抢跑现象：加同块无第三方交易检测』；scn-009-runner.ts L457-459（before 取 latest、仅查多头）；scenario.mjs ensureCleanSlate L346-380。
- **失败情形**：窗口内 Service Keeper 执行了他人订单：closeDelta 的 lpVaultAssets/claimable 槽位混入他人资金流，推导层逐槽位期望假 FAIL（误报）或与本单误差对冲假 PASS（漏报）；本账户残留挂单被执行时甚至污染 trader 自身槽位。
- **建议修复**：对每个执行块用 eth_getBlockByNumber 校验块内交易集合仅含预期 tx（否则窗口标 POLLUTED 进 evidence）；开跑前查双方向仓位与 ACCOUNT_ORDER_LIST 并清场。
- <details><summary>复核推理</summary>复核：before 快照取发单前 latest（:457），与创建块之间可隔多块；四个窗口均无『同块/同窗无第三方交易』检测（traps §4 明确要求 fork 上加检测、命中标 NOT_VERIFIED/POLLUTED）；前置只查执行前无 ETH 多仓（:459），不查同账户空头与遗留挂单；scenario.mjs 有现成 ensureCleanSlate（:346-380）与 drainPendingOrders（:306）未复用。与 runner-03 不重复：runner-03 是快照钉块（即便钉块，同块第三方交易仍需检测），本条是污染检测+账户清场，修复手段不同。市场级槽位（OI/claimable/lpVaultAssets）混入他人资金流的失败情形成立。</details>

### [中|新发现] runner-08-missing-money-hard-assertions — runner 层缺关键金额硬断言：OI 双账本增量、平仓 output===ΔtraderUsdc、仓位结算闭环——金额正确性全押在不阻断的推导层展示行上

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:686`｜类型：coverage-gap
- **问题**：check() 清单里唯一的金额类断言是 sizeInUsd===SIZE_USD 与 cumulativeOpenCostsLong 回到原值。缺失且判据明确要求的硬断言：(a) formulas §2 订单原值侧——open 步 ΔcumulativeOpenCostsLong === SIZE_USD、双账本一致 ΔopenInterestInTokensLong === openedPosition.sizeInTokens（close 步对称清零）；(b) formulas §7——平仓 output === trader 钱包 USDC 实测差（0 容差），现在 traderUsdcDelta 只是 observation；(c) §〇.8 仓位结算闭环 collateralΔ 公式（scenario.mjs checkSettlementFormula 现成但 runner 未用）。推导层虽产出这些 expected 行，但其 FAIL/NOT_VERIFIED 不阻断（§10 已知），故这些量当前无测试门槛。
- **依据**：formulas.md §2/§7；scenario.mjs checkSettlementFormula L490-507（现成实现未接入）；scn-009-runner.ts check() 全清单（L458-684）中无上述任何一条。
- **失败情形**：开仓手续费被重复扣一次（trader 实际损失）或 OI 记账翻倍：sizeInUsd、grace、执行价不等式、四笔回执全部照常通过，SCN-009 判 PASS。
- **建议修复**：open/close 两步各加 OI 双账本增量断言；close 后用 PositionDecrease/PositionFeesCollected 事件值断言 output===ΔtraderUsdc 与结算闭环。
- <details><summary>复核推理</summary>复核 check() 全清单（:458-684）：金额类仅 sizeInUsd===SIZE_USD（:520）与 cumulativeOpenCostsLong 回原值（:586-592）。确缺：formulas §2 的 ΔcumulativeOpenCostsLong===SIZE_USD（open 步）与 OI 双账本 ΔopenInterestInTokens===position.sizeInTokens；§7 的 output===ΔtraderUsdc 0 容差断言（traderUsdcDelta 只是 observation，:696）；§〇.8 结算闭环（scenario.mjs checkSettlementFormula :490-507 现成未接入）。推导层虽有这些 expected 行且 FAIL 会翻 executions 页状态，但如 runner-01 所核，该状态不参与测试判定；NOT_VERIFIED 更不阻断。开仓费重复扣/OI 翻倍时 17 项断言照过的失败情形成立。</details>

### [中|已知 ⬜] cov-01 — 页面 T3 核对点、浏览器钱包签名链路、涨/平/跌三组价格对照全部未自动化（用例文本三项核心期望仅覆盖链上侧）

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:716`｜类型：coverage-gap
- **问题**：S02 SCN-009 行的核对数据要求'三组最终余额方向为涨>平>跌'与 README §7 的'页面 T3 与 Reader T0 分两个核对点'，当前只自动化了 T0/Reader 侧单组价格；三组价格对照、页面历史逐条核对、页面 Entry/Exit 显示舍入、杠杆显示公式 sizeInUsd/(collateral−pendingFunding) 均无任何行或断言。
- **依据**：runner coverage.pending 716-720 行明示三项未做；TestCase/E2E/scenarios/S02-策略下单与订单管理.md:18 核对列；TestCase/E2E/README.md §7（84-91 行）
- **失败情形**：涨>平>跌方向性（价格因果）完全未验证——若费用/PnL 符号接反但守恒仍闭合，单组价格用例发现不了；页面显示舍入/单位错误零检出。
- **建议修复**：先补三组 mock 价格对照（纯链上即可，比页面链路成本低）：同一 fork 三次运行分别推价 +x/0/−x，断言 traderUsdcDelta(涨)>Δ(平)>Δ(跌)。页面链路保持 pending 标注。
- <details><summary>复核推理</summary>known:true 项，核实存在且影响 SCN-009：runner coverage.pending 717-719 行确列『涨/平/跌三组可控价格对照』『浏览器钱包内从页面点击并签名』『页面历史与链上事件逐条核对』三项未做，completeScenario:false；S02-策略下单与订单管理.md:18 核对列确要求『三组最终余额方向为涨>平>跌』与 Entry/Exit 不利侧；TestCase/E2E/README.md §7（85-91 行）确要求页面 T3 与 Reader T0 分两个核对点、杠杆=sizeInUsd/(collateral−pendingFunding)。execution-evidence.ts 全文 grep leverage 仅有 size 推导用的 leverageTimes，无持仓面板杠杆行、无页面侧任何行。三组价格方向性（费用/PnL 符号因果）确实零覆盖——单组价格下符号接反而守恒仍闭合时无检出，failureScenario 成立。</details>

### [中|已知 ⬜] cov-02 — executionFee（ETH/WNT 侧）零覆盖：两笔订单各付 2e13 wei 执行费，无 ETH 账本、无退款/结余核对

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:163`｜类型：coverage-gap
- **问题**：EXECUTION_FEE=20_000_000_000_000n 在开/平两单各支付一次（471、545 行），但快照体系（ledger.mjs CONSERVATION_SLOTS 只有 5 个 USDC 槽）没有任何 ETH/WNT 余额读数，keeper 执行费结算与多余部分退款完全不核。
- **依据**：traps §10 ⬜ 'executionFee/WNT 零覆盖'；ledger.mjs:13 五槽全为 USDC；v031-facts §5 '执行费是 ETH/WNT 侧，另行核对'
- **失败情形**：执行费被多扣、退款丢失或路由错账户时，所有 USDC 守恒与断言仍全绿。
- **建议修复**：快照增加 trader/orderVault 的 ETH（或 WNT）余额槽，按 GasUtils 逻辑核 executionFee 扣除与退款；至少先加 ΔETH(trader) ≈ −gas − executionFee ± refund 的方向性断言。
- <details><summary>复核推理</summary>known:true 项（traps §10 ⬜ 'executionFee/WNT 零覆盖'），核实存在且影响 SCN-009：runner 163 行 EXECUTION_FEE=2e13，471 与 545 行开/平两单各付一次；ledger.mjs:13 CONSERVATION_SLOTS 五槽全为 USDC（trader/orderVault/posVault/lpVault/feeReceiver），snapshot() plan（52-83 行）无任何 ETH/WNT 余额读数；v031-facts §5 确认执行费是 ETH/WNT 侧另行核对且当前未核。执行费多扣/退款丢失时 USDC 侧全绿，failureScenario 成立。</details>

### [中|已知 ⬜] cov-03 — 静默取消判别不完整：只轮询订单离队 + 只抓 OrderExecuted，无 OrderCancelled/OrderFrozen/reasonBytes 解码

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:594`｜类型：coverage-gap
- **问题**：594-609 行只 fetch OrderExecuted 并断言恰好一条；486-500 行的判定是 orderIsPending 为 false（'离队'）。traps §5 要求按 orderKey 分计 Executed/Cancelled/Frozen 并解码 reasonBytes；legacy scenario.mjs:199-207 已有 OrderCancelled 抓取实现但未接入 SCN-009。
- **依据**：traps §10 ⬜ 'SCN-009/010 无 OrderCancelled/reasonBytes 解码（SCN-070 的实现宜下沉公共模块）'；tools/fx100-legacy/integration/lib/scenario.mjs:199-207
- **失败情形**：订单被静默取消时用例会失败（requireValue 仓位不存在 + OrderExecuted=0 条），vacuous-pass 防线存在，但失败原因（Panic vs 风控 revert）不可诊断，排障需人工重查链上。
- **建议修复**：把 SCN-070 的取消/冻结判别与 reasonBytes 解码下沉公共模块，SCN-009 离队后先三分类再走成功分支。
- <details><summary>复核推理</summary>known:true 项（traps §10 ⬜），核实存在且影响 SCN-009：runner 594-609 行仅 fetchEmitterEvents('OrderExecuted') 并断言恰一条；486-500 行离队判定仅 orderIsPending==false；无 OrderCancelled/OrderFrozen 抓取、无 reasonBytes 解码。legacy scenario.mjs findSettlementTx（196-211 行，OrderCancelled 抓取在 198-201）确有现成实现且 SCN-009 未接入。但降 severity 至 low：静默取消的 vacuous-pass 防线已完整（503 行 requireValue 仓位必须存在 + 608 行恰一条 OrderExecuted），取消场景必然 FAIL 不会假绿，缺的只是失败归因（Cancelled vs Frozen vs Panic vs 风控），是诊断链缺口而非正确性漏洞——与同题 cancel-01 的 low 评级一致。</details>

### [中|已知 ⬜] runner-03-after-snapshot-latest-not-execblock — afterOpen/afterClose 快照取轮询后的 latest 而非执行块，执行步 before 也不是 execBlock−1；参数快照标注『执行区块』不实（§10 ⬜ 已核实仍在）

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:502`｜类型：snapshot-quality
- **问题**：traps §4 要求 Before=execBlock−1、After=execBlock、禁 latest。现状：afterOpen（L502）与 afterClose（L575）都走 takeSnapshot 无块号分支 → eth_blockNumber 取 latest；执行步 delta 的 before 用的是创建交易块快照（afterCreateOpen/afterCreateClose），不是 execBlock−1。执行块其实可得：inline 模式 sendKeeperExecution 返回的 execution.blockNumber（L481-484）被丢弃，OrderExecuted 事件的 blockNumber（events.mjs L99）也没回填快照。openParameterSnapshot 钉在 afterOpen.blockNumber（latest），但 L523 断言名写『首次开仓 Grace 按执行区块当前参数复算』——标注不实；grace 的 tier/multiplier、fundingEma 同样读的是 latest 块。快照内部单块一致性没有问题（rpc.readMany 全部钉同一 blockNumber）。
- **依据**：traps.md §4、§10 ⬜『after-execute 快照取 latest 非执行块、参数快照标注不实』；scn-009-runner.ts L396-407/L481-484/L502/L575/L504-512/L523；events.mjs L98-102（事件带 blockNumber 可用作执行块）。
- **失败情形**：执行块与 latest 之间被第三方交易（fork 上的 Service Keeper 抢跑、他人订单）插入：executeOpenDelta/closeDelta 把无关资金流并进本单账本，逐槽位期望值假 FAIL 或对冲假 PASS；若未来做 funding 独立复算，dt 也无法按 execBlock−1 取 fundingUpdatedAt。
- **建议修复**：先抓 OrderExecuted 拿执行块（或用 inline 的 execution.blockNumber），afterOpen=snapshot(execBlock)、执行步 before=snapshot(execBlock−1)；参数快照同钉执行块，断言名与实际块号对齐。
- <details><summary>复核推理</summary>known:true 项，逐点核实均在当前工作树成立：:502/:575 无块号 → :404-406 latest；执行步 delta 的 before 是创建块快照（:686-689 diff(afterCreateOpen, afterOpen)），非 execBlock−1（traps §4 要求）；inline sendKeeperExecution 返回的 execution.blockNumber（:288）在 :481-484 被丢弃；OrderExecuted 事件带 blockNumber（events.mjs :102-104）未用于钉块；openParameterSnapshot 钉 afterOpen.blockNumber（:511）而 :523 断言名与 parameterBlockNote（execution-evidence.ts:184）都写『执行区块』——标注不实；快照内部单块一致性无问题（ledger.mjs readMany 同块）。确实影响 SCN-009 账本/pricing/grace/funding 全维度。保留为本问题正主（ledger-07、snapshot-01 首末两条归并至此）。</details>

### [中|已知 ⬜] runner-04-no-cancel-reason-decoding — SCN-009 无 OrderCancelled/OrderFrozen 分计与 reasonBytes 解码（§10 ⬜ 已核实仍在）；但『订单离队≠成功』的硬断言是有的

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:594`｜类型：coverage-gap
- **问题**：runner 只按 orderKey 抓 OrderExecuted（L594-609）与 PositionIncrease/PositionDecrease（L611-624），不抓 OrderCancelled/OrderFrozen、不解码 reasonBytes——尽管 events.mjs 已支持 bytes/string 族解码、scenario.mjs findSettlementTx 有现成两族事件模板。静默取消不会被当成功：开仓侧 requireValue(positionOf(afterOpen))（L503）、平仓侧『全平后仓位已移除』（L585）和『恰好一条执行证据』（L608-609）都会拦住（traps §5 的 vacuous pass 已防）；缺的是判别——被取消时只抛通用错误，看不到被哪道闸门拦、是 Panic 还是风控。
- **依据**：traps.md §5、§10 ⬜『SCN-009/010 无 OrderCancelled/reasonBytes 解码』；events.mjs L44-54（bytes/string 已可解）；scenario.mjs L196-211（findSettlementTx 两族事件先例）；合约 OrderEventUtils.sol（取消事件字段名 key）。
- **失败情形**：keeper 静默取消开仓单（如 MaxPriceAgeExceeded）：用例报错『开仓订单离队，但多头仓位不存在（可能被静默取消）』，配合 fail-fast（runner-06）零附件，排障只能去翻 keeper 日志——事件里现成的 reason/reasonBytes 一个都没进证据。
- **建议修复**：订单离队后按 orderKey 同窗抓 OrderExecuted/OrderCancelled/OrderFrozen 三族，取消时把 reason/reasonBytes 解码进错误消息与 evidence（下沉 SCN-070 的实现为公共模块）。
- <details><summary>复核推理</summary>known:true 项，核实仍在：runner 只抓 OrderExecuted（:594-599）与 PositionIncrease/Decrease（:611-624），无 OrderCancelled/OrderFrozen 与 reasonBytes 解码；events.mjs 已支持 bytes/string 族解码（MEMBERS 表含动态类型偏移解法，注释明言就是为 OrderCancelled 的 reason/reasonBytes）；scenario.mjs findSettlementTx（:196-211）有两族事件先例。claim 的限定也准确：静默取消不会被当成功（:503 requireValue、:585、:608-609 硬断言在），缺的是判别与证据——影响 SCN-009 的排障维度而非正确性。</details>

### [中|已知 ⬜] runner-06-failfast-breaks-evidence — check() 首败即抛，assertions/快照/交易证据全部丢失——spec 只在成功路径写附件（§10 ⬜ 已核实仍在）

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:309`｜类型：coverage-gap
- **问题**：check() 在 push 后立即 throw（L301-310），runScn009 抛错时局部的 assertions 数组、五份快照、deltas、事件全部随栈丢失；spec 侧 evidence 附件（scn-009-evidence.json / tx-and-receipt.json / reader-ledger-before-after.json）只在 runScn009 正常返回后写（spec L49-72），catch 分支只加 service-keeper blocked 注解。任何一条中途断言失败 → 测试红但零附件。
- **依据**：traps.md §10 ⬜『runner check() fail-fast 断证据流』；scn-009-runner.ts L301-310；scn-009.spec.ts L36-72。
- **失败情形**：『开仓恰好一条执行证据』失败（如 fork 抢跑重放）时，报告里只有一行错误消息：之前已通过的 10+ 项断言、before/afterOpen 快照、四笔交易回执全部不落盘，无法离线复盘。
- **建议修复**：把 evidence 组装改为渐进式（错误对象携带已收集的部分 evidence，或 runner 内 try/finally 落盘 partial evidence），spec 的 catch 分支照样附出。
- <details><summary>复核推理</summary>known:true 项，核实仍在：check() push 后立即 throw（:301-310）；runScn009 抛错时局部 assertions/五份快照/deltas/事件全部随栈丢失（均为函数局部变量，仅在 :698 正常返回时组装）；spec :49-72 三个附件只在成功路径写，catch 分支（:39-48）只加 service-keeper blocked 注解后 rethrow。中途断言失败 → 测试红但零附件成立。traps §10 ⬜ 对应项。</details>

### [低|新发现] conserve-01 — 守恒行信任证据里预存的 sum/status，推导层未独立重算 Σterms

- **位置**：`TestCode/src/reporting/execution-evidence.ts:78`｜类型：coverage-gap
- **问题**：buildConservationRow 的 PASS 判定为 observation.status==='PASS' && 存储的 sum===0 && 五项 terms 齐全，但不重算 Σterms 是否等于 sum；求和逻辑单点依赖 ledger.mjs checkConservation（同一来源产生 terms 和 sum）。推导层重算五项之和成本为零且能提供真正的双录独立性。
- **依据**：TestCode/src/reporting/execution-evidence.ts:78-99；TestCode/tools/fx100-legacy/tool/onchain-tx/lib/ledger.mjs:134-141。本次重放独立求和 open/close/wholeFlow 三组 ΣΔ 均 =0，与存储 sum 一致。
- **失败情形**：若采集层写入 terms 与 sum 不一致的证据（如序列化/裁剪 bug 导致某 term 值损坏但 sum 仍为旧值 0），守恒行仍 PASS 且展示自相矛盾的展开式，漏检账本损坏。
- **建议修复**：在 buildConservationRow 内用 BigInt 重算五项之和并与存储 sum 双验，二者不符标 FAIL 并在 note 说明。
- <details><summary>复核推理</summary>独立复核代码：buildConservationRow（execution-evidence.ts 71-109 行）status 判定为 stringValue(observation.status)==='PASS' && sum===0n && hasAllTerms（92-94 行），terms 仅检存在性并拼展示串（80-86 行），确不重算 Σterms 与 sum 双验；ledger.mjs checkConservation（134-142 行）terms 与 sum 同源同函数产出，采集层单点。failureScenario 成立：证据 JSON 中某 term 被序列化/裁剪损坏而 status/sum 保持旧值时，行仍 PASS 且展开式自相矛盾。推导层五项 BigInt 求和成本为零、构成真双录，suggestedFix 合理。low 恰当（当前无实际影响，属防御纵深缺口）。</details>

### [低|新发现] cov-05 — 旁观市场双侧 OI 前后不变未核对：快照只读被交易 marketIndex

- **位置**：`TestCode/tools/fx100-legacy/tool/onchain-tx/lib/ledger.mjs:59`｜类型：coverage-gap
- **问题**：formulas §2 要求'旁观市场双侧 OI 前后不变（不是==0）'；snapshot() 的 cumulativeOpenCosts/openInterestInTokens 键只取 ctx.marketIndex（59-62 行），部署另一市场（deployed market #2 与 mock market 并存）的 OI/开仓成本无任何读数与断言。
- **依据**：formulas §2；ledger.mjs plan 数组无第二市场槽位
- **失败情形**：合约按 marketIndex 写错槽位（key 派生 bug 串市场）时，被测市场数值可能仍自洽，旁观市场被污染零检出。
- **建议修复**：快照加一组旁观市场（非交易 marketIndex）的 OI 双账本槽位，期望 Δ 恒 0。
- <details><summary>复核推理</summary>独立复核：ledger.mjs snapshot() 59-62 行 cumulativeOpenCosts/openInterestInTokens 四键均只取 ctx.marketIndex，plan 数组（52-83 行）无第二市场槽位；formulas §2 确要求'旁观市场双侧 OI 前后不变（不是==0）'。旁观市场真实存在：SCN-009 默认走 mock market（runner 434 行 marketIndex 取 mockResource），deployed market #2 并存（160 行 DEPLOYED_MARKET_INDEX=2，v031-facts LPVault 两市场共用）。合约按 marketIndex 串槽位类 bug 下被测市场自洽、旁观市场污染零检出，failureScenario 成立。low 合理。</details>

### [低|新发现] cov-08 — positionPaysLp 金额恒等式未复算：LP Claimable Funding 入账行的输入直接采 Funding 事件值

- **位置**：`TestCode/src/reporting/execution-evidence.ts:869`｜类型：coverage-gap
- **问题**：claimable-funding 行（869-877 行）expected = before + max(positionPaysLp,0)，positionPaysLp 取自 Funding 事件未复算；formulas §5 的 floor funding 金额恒等式 positionPaysLp = trunc[(trunc(多OI·f_l·dt)+trunc(空OI·f_s·dt))/colMid] 无对应实现（factor 本身已有 calculateFundingFactors 复算，缺的是 factor×OI×dt→金额 一跳；dt 需 execBlock−1 的 fundingUpdatedAt，当前快照体系不采，与 cov-04 相关）。行内也未标注 EVENT_ANCHORED 证据等级。
- **依据**：formulas §5 floor funding 式与 'dt 必须从 execBlock−1 读 fundingUpdatedAt'；traps §7 独立度分级要求
- **失败情形**：合约 positionPaysLp 计算有 bug 时，事件值与 DataStore claimable 同源同错，本行照样 PASS。
- **建议修复**：补采 execBlock−1 的 fundingUpdatedAt 与成交前 OI，用事件 factor 复算 positionPaysLp 金额；至少先给本行标注证据等级（事件↔状态交叉，非独立）。
- <details><summary>复核推理</summary>独立复核：execution-evidence.ts 869-877 行 claimable-funding 行 expected=before+max(positionPaysLp,0)，positionPaysLp 取自 Funding 事件（288 行 bigintValue(funding.positionPaysLp)）未复算，行内无【恒等式】/EVENT_ANCHORED 类标注（对比 691/806/858 行同类标注惯例）；formulas §5 的 floor funding 金额恒等式无实现——calculateFundingFactors 确已存在且用于 factor 行（890-916 行），缺的正是 factor×OI×dt→金额一跳；chain-parameter-snapshot.ts grep 证实采 FUNDING_SKEW_EMA 但不采 fundingUpdatedAt，且快照体系无 execBlock−1 读数（与 cov-04 关联属实）。合约 positionPaysLp 有 bug 时事件与 DataStore claimable 同 tx 同源写入、同错双 PASS，failureScenario 成立。</details>

### [低|新发现] cov-09 — Entry/Exit 均价口径无断言：entryPrice 仅是 observations 里的整除展示值

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:693`｜类型：coverage-gap
- **问题**：用例文本要求 'Entry/Exit 使用对用户不利侧执行价'，方向断言有（执行价≥max/≤min），但 Entry=sizeInUsd/sizeInTokens 应≈开仓执行价（formulas §9：entry 用均价口径，用 index 价必偏）没有任何行或断言——693-695 行的 entryPrice 只进 observations 且为 BigInt 整除，无容差声明；Exit 侧同样无均价核对行。
- **依据**：formulas §9 'entry = sizeInUsd/sizeInTokens = 平均执行价'；S02:18 核对列；runner observations.entryPrice 无消费者
- **失败情形**：sizeInTokens 落账错误但 OI 行恰因同源派生同错时，Entry 口径核对本可兜住这类错，现在缺位。
- **建议修复**：加一行：|sizeInUsd − sizeInTokens×执行价| < 执行价（即 entry 与事件执行价差在 1 token-wei 取整范围内），声明容差来源。
- **复核修正**：Entry/Exit 均价口径无断言：runner 693-695 行 entryPrice=sizeInUsd/sizeInTokens（BigInt 整除、无容差声明）只进 observations，execution-evidence.ts 全文 grep 无消费者；Exit 侧无均价行。formulas §9 要求 entry 用均价口径。需修正 failureScenario：所提议的 |sizeInUsd − sizeInTokens×execPrice| < execPrice 在数学上等价于现有 ΔLong OI 行的 expected 模型（⌊USD/execPrice⌋），'OI 行同源同错'情形（executionPrice 被一致性错发）会同时骗过两者；该行的真实增量价值在于它是纯仓位字段核对、对市场级 OI 快照污染（第三方同市场交易、cov-04 的 latest 快照）免疫，且补上 Exit 侧与容差声明。
- <details><summary>复核推理</summary>独立复核：runner 693-695 行与 grep 确认 entryPrice 无任何消费者，Entry/Exit 均价行确实缺位，S02:18 与 formulas §9 依据属实，方向断言（631-644 行 execPrice≥max/≤min）确已存在。驳回尝试部分成功：所述 failureScenario 站不住——若 position.sizeInTokens 落账错而 executionPrice 正确，现有 OI 行 expected=⌊USD/execPrice⌋ 会 FAIL（并非同源同错）；若 executionPrice 一致性错发则提议的 Entry 行也同样通过。但覆盖缺口本身（无行、无容差、Exit 缺失、对快照污染免疫的仓位级交叉检缺位）驳不倒，故 CONFIRMED 并修正表述。</details>

### [低|新发现] cov-10 — 发单资金前置校验缺失：无 USDC/ETH 余额预检

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:457`｜类型：coverage-gap
- **问题**：手册核心事实 6 / traps §6 要求把 'collateral + 开仓费(+关仓费预算) < USDC 余额（严格小于留余量）' 写进用例前置；runner 前置只有快照无缺读与无仓位两条（457-459 行），SCN-070 有 '测试账户抵押币余额充足' 前置而 SCN-009 没有，executionFee 的 ETH 余额同样未预检。
- **依据**：traps §6；v031-facts §5；scn-070 断言 meta '测试账户抵押币余额充足'（execution-evidence.ts:1715-1719）
- **失败情形**：余额不足时 createOrder transferFrom revert，产生'无订单/无事件'空转用例，失败归因混入环境噪音。
- **建议修复**：before 快照后断言 traderUsdc ≥ collateral + 开仓费预算 且 trader ETH ≥ 2×executionFee + gas 余量。
- <details><summary>复核推理</summary>独立复核：runner 前置仅 458-459 两条（before 无缺读数 + 无 ETH 多仓），无 USDC/ETH 余额预检；手册核心事实 6 与 traps §6 确要求把余额预检写进用例前置；SCN-070 确有'测试账户抵押币余额充足'断言（execution-evidence.ts 1714-1719 行 meta：collateralBalance(trader) ≥ 10 USDC）而 SCN-009 无对应；executionFee 的 ETH 侧同样未预检（与 cov-02 相邻但不重复——本条是前置校验、cov-02 是结算核对）。余额不足时 createOrder transferFrom revert 产生'无订单/无事件'空转、归因混入环境噪音，failureScenario 与 traps §6 描述一致。low 合理。</details>

### [低|新发现] coverage-01 — 手册 §2 的 oiΔ === positionΔ 双账本恒等断言未作为独立行/断言存在（仅链式间接覆盖）

- **位置**：`TestCode/src/reporting/execution-evidence.ts:338`｜类型：coverage-gap
- **问题**：现状：ΔLong OI 行 expected=⌊USD/execPrice⌋（事件锚定复算）、position sizeInTokens 行 expected=事件同名字段，二者各自 PASS 时 oiΔ==positionΔ 只能传递推出；手册 §2 要求的直接恒等断言（合约同 tx 同 cache 写两账本，恒真、对期望模型误差与快照污染免疫）在 runner 与行集均缺失。
- **依据**：formulas.md §2『断言：oiΔ === positionΔ』；scn-009-runner.ts 全文无 openInterest 相关断言（仅 cumulativeOpenCostsLong 回退，:586-592）；execution-evidence.ts 无跨行恒等式。
- **失败情形**：当 expected 模型本身有误（如 ledger-01 的取整错）且快照被污染时，两条独立行可能同时 FAIL 却无法区分『模型错』还是『账本真不守恒』；恒等断言可作免疫哨兵定位问题层。
- **建议修复**：在市场/OI 分组加一行【恒等式】：ΔopenInterestInTokens(方向侧) === Δposition.sizeInTokens（快照差对快照差），标注 IDENTITY 独立度。
- <details><summary>复核推理</summary>独立复核：formulas.md §2 确有『断言：oiΔ === positionΔ』。scn-009-runner.ts 全部 check() 断言逐条过目（458-683 行），无任何 openInterest 断言，仅 587-592 行的 cumulativeOpenCostsLong 回退；execution-evidence.ts 中 ΔLong OI 行 expected=sizeUsd/executionPrice（283 行 BigInt 整除=floor，338-341 行），position sizeInTokens 行 expected=PositionIncrease 事件同名字段（1179-1180 行 positionExpected: openIncreaseUint），buildSnapshotRows/buildPositionRows 均无跨行『快照差==快照差』恒等式。两行各自 PASS 时 oiΔ==positionΔ 确需经 event.sizeInTokens==⌊USD/execPrice⌋ 传递推出，直接恒等断言（对期望模型误差免疫）缺失。快照同时含 openInterestInTokensLong 与 position.sizeInTokens，补行成本为零，claim 成立。</details>

### [低|新发现] feefund-04 — funding no-op 判定用 before 快照 OI，与合约执行时点的判定可能不一致

- **位置**：`TestCode/src/reporting/execution-evidence.ts:896`｜类型：snapshot-quality
- **问题**：calculateFundingFactors 的 hasOpenInterest 取 input.before.openInterestInTokensLong/Short（阶段前快照）>0；合约的 no-op 判据是执行时刻（本单增减仓生效前）totalOpenInterest==0（MarketUtils.getNextFundingAmountPerSize L628-633）。两时点之间若有第三方改变市场 OI（首仓开立或清空），复算走错分支。
- **依据**：MarketUtils.sol L618-633；traps §4 fork 第三方交易污染、无"同块无第三方交易"检测。
- **失败情形**：before 快照时市场 OI=0，第三方在本单执行前开了首仓：合约 factor 非零、复算 expected 0/0 → 错误 FAIL；反向（第三方清空）同理。
- **建议修复**：与 feefund-01/02 一并修：用执行块−1 的 OI 读数判定，并加同区间第三方交易检测，命中标 NOT_VERIFIED/POLLUTED。
- <details><summary>复核推理</summary>独立复核：execution-evidence.ts L896-897 hasOpenInterest 取 input.before（execute-increase 相位 before=afterCreateOpen，钉 TX1 创建块）的 OI；formulas.ts calculateFundingFactors L70-77 在 hasOpenInterest=false 时期望 0/0。合约 MarketUtils.getNextFundingAmountPerSize L618-633 的 no-op 判据是执行 tx 内、本单 OI 变更落账前的 totalOpenInterest==0（updateFundingState 先于仓位更新，ExecuteOrderUtils L52）。创建块与执行块之间第三方开首仓/清空即令两个判定分叉：合约走非 no-op 分支（L640-644 初始化 EMA 后 factor 可非零）而复算期望 0/0，反向同理。SCN-009 隔离 mock 市场下两端一致（开仓时双双判 0、平仓时双双非零），失败场景仅在共享市场出现，low 恰当。与 feefund-01 非重复（before 快照 OI vs 参数快照块号，不同输入不同分支）。</details>

### [低|新发现] feefund-05 — uiFeeAmount==0 从未被独立断言（Total Cost 恒等式取事件值，独立度缺口）

- **位置**：`TestCode/src/reporting/execution-evidence.ts:853`｜类型：coverage-gap
- **问题**：Total Cost 行把 uiFeeAmount 事件值直接加进 expected（不是假设 0，恒等式本身没错），但没有任何行验证"本用例 order.uiFeeReceiver==address(0) ⇒ uiFeeAmount 应为 0"（合约 PositionPricingUtils.getUiFees L413-415）。uiFee 是 Total Cost 三分量中唯一无独立复核的项。
- **依据**：PositionPricingUtils.sol L405-422；traps §7 独立度分级——事件值直接入恒等式属 IDENTITY 级。
- **失败情形**：若订单构造被改动误带 uiFeeReceiver（或合约侧异常记了 uiFee），Total Cost 恒等式仍自洽 PASS，多扣的 uiFee 只能靠守恒行间接暴露，fee 维度无行指认。
- **建议修复**：增加一行：SCN-009 订单 uiFeeReceiver=0 ⇒ pass(uiFeeAmount, 0) 且 uiFeeReceiverFactor=0。
- <details><summary>复核推理</summary>独立复核前提：flows.mjs orderParamsTuple L70 地址组为 (account,account,ZERO,ZERO)，对照 IBaseOrderUtils.sol L29-34 struct 第四位即 uiFeeReceiver=address(0)；合约 PositionPricingUtils.getUiFees L413-415 uiFeeReceiver==0 时返回全零。核查 buildFeeAndFundingRows：uiFee（L770）只进 Total Cost 恒等式（L771、L853-859），全文件无任何行断言 uiFeeAmount==0 或 uiFeeReceiverFactor==0。证据 JSON 两相位 uiFee=0 属实但无行验证之。若异常出现非零 uiFee，Total Cost 行仍自洽 PASS，只能靠账本/守恒行间接偏差暴露且无法指认——独立度缺口成立，low 恰当。</details>

### [低|新发现] feefund-06 — 手册 §10 "分账切桶余数归协议"与合约 receiver/pool 切分方向相反（手册措辞问题，代码是对的）

- **位置**：`.claude/skills/fx100-verify-handbook/references/formulas.md:102`｜类型：label-or-basis-error
- **问题**：合约 PositionPricingUtils.getPositionFeesAfterReferral L531-532：feeReceiverAmount(协议侧)=floor(protocolFee×factor/1e30)，positionFeeAmountForPool=protocolFee−receiver——余数归 LP Pool，不归协议（referral/discount 桶的余数才归协议 L528）。execution-evidence.ts 的 expectedReceiver/expectedPool（L752-753）与合约一致且经实测验证（5005→2502/2503）；但手册速查"余数归协议"一刀切表述会误导后续核对者把 receiver 改成 ceil。
- **依据**：PositionPricingUtils.sol L525-532；实测证据 SCN-009-tx-fork-r0-0-scn-009-evidence.json close 相位 protocolFee=5005 → receiver=2502（floor）、pool=2503（得余数）。
- **失败情形**：按手册字面复算 receiver=ceil(5005×0.5)=2503 → 与链上 2502 相差 1 wei，误判合约或核对代码有错。
- **建议修复**：手册 §10 改为："referral/discount 桶⌊⌋余数归协议；receiver/pool 切分 receiver⌊⌋余数归 LP Pool"。
- <details><summary>复核推理</summary>独立复核：formulas.md L102 原文『分账切桶全⌊⌋余数归协议』一刀切。合约 PositionPricingUtils.getPositionFeesAfterReferral L531-532：feeReceiverAmount=applyFactor(protocolFee, factor)（floor），positionFeeAmountForPool=protocolFee−receiver——余数归 LP Pool；而 L518-528 affiliateReward/discount 桶 floor 后余数留在 protocolFeeAmount（归协议）。证据 JSON 实测：close 相位 protocolFee=5005 → receiver=2502（floor）、pool=2503（吃余数），按手册字面算 receiver=⌈5005/2⌉=2503 即差 1 wei 误判。execution-evidence.ts L752-753 与合约一致（expectedReceiver=floor、expectedPool=差值），代码没错、手册措辞误导，claim 定性准确。</details>

### [低|新发现] formula-02 — balanceWasImproved 期望值口径与合约不符：用 after 快照最终 OI 重算，合约用预备 tokenDelta（裸 index 价）算 next OI

- **位置**：`TestCode/src/reporting/execution-evidence.ts:601`｜类型：formula-error
- **问题**：buildPricingRows 的 expectedImproved = |after 快照失衡| < |before 快照失衡|（599-601 行），after 用的是事件最终 sizeDeltaInTokens 落账后的 OI；合约判定用 getNextOpenInterestParams 的 usdDelta = 预备 tokenDelta × midPrice（PositionPricingUtils.sol:288），增仓预备值来自 getIncreaseOrderSize 裸 index 价（PositionUtils.sol:503-507，多头 ⌊USD/index.max⌋），与最终 ⌊USD/execPrice⌋ 不同。traps §8 明示此坑（'开仓 tokenDelta 用预备值非事件最终值'）。
- **依据**：PositionPricingUtils.sol:165/288/302-325；PositionUtils.sol:599（getExecutionPriceForIncrease 先取预备 size 再算 spread）；traps §8
- **失败情形**：双边 OI 接近平衡、预备与最终 token 数横跨改善阈值时，balance-improved 行误判（假 FAIL 或错 PASS），并连带 Position Fee Factor 档位行的解释口径；SCN-009 空市场开/全平两端恰好结果一致，风险在后续多仓并存场景。
- **建议修复**：增仓场景按预备值口径重算 nextOI（裸 index 价换算 tokenDelta×midPrice），平仓无此分裂可保持现算法。
- <details><summary>复核推理</summary>独立复核：合约 PositionPricingUtils getDynamicSpread L164 以 getNextOpenInterest(params,true) 判 balanceWasImproved（L239-243 严格 <），其中 usdDelta=tokenDelta×midPrice（L288），增仓 tokenDelta 来自 getIncreaseOrderSize 预备值（PositionUtils L599 先于 dynamicSpread/execPrice 计算，多头 ⌊USD/index.max⌋，L503-504），最终落账 tokens 则是 L636 ⌊USD/execPrice⌋（execPrice≥index.max 故 ≤ 预备值）。execution-evidence.ts L592-601 的 expectedImproved 用 before/after 快照 OI（after 已含最终 tokens）重算，口径与合约不同：预备值≠最终值且失衡横跨阈值时误判。traps §8 明示此坑而代码未遵循。SCN-009 两端豁免验证：开仓前空市场（0→正，两口径同判 false）、全平（正→0，同判 true）。平仓 tokenDelta 无预备/最终分裂（getDecreaseOrderSize 比例值即落账值），claim 的范围限定准确。low 恰当。</details>

### [低|新发现] ledger-04 — 守恒行把 UNVERIFIABLE 判成 FAIL，且不复核 Σterms==sum

- **位置**：`TestCode/src/reporting/execution-evidence.ts:92`｜类型：label-or-basis-error
- **问题**：buildConservationRow 的 status 只有 PASS/FAIL 两态：ledger.mjs checkConservation 返回 UNVERIFIABLE（缺读数，:135-138）时行状态显示 FAIL（语义应为 NOT_VERIFIED——评分体系里已有该状态并在 Grace/OI 行使用）；且行内展示的 'ΔTrader(x)+…=sum' 等式从未被复核，status 直接信 evidence 里存的 status/sum 字段，未验证 Σterms===sum。
- **依据**：execution-evidence.ts:78-98（status 逻辑，sum/terms 均直读 observation）；ledger.mjs:134-142（UNVERIFIABLE 分支）；对照 Grace 行 NOT_VERIFIED 处理（:515-531）。
- **失败情形**：某一槽位 RPC 读取失败 → runner 记 UNVERIFIABLE → 看板显示'守恒 FAIL'，误导为资金不闭合（实为不可核）；另若证据 JSON 的 terms 与 sum 不一致（生成端 bug/手改），行仍按 sum 判 PASS，展示的等式为假。方向上不会漏判（不产生假 PASS 于真实破恒），属标注/自证问题。
- **建议修复**：status==='UNVERIFIABLE' → NOT_VERIFIED；PASS 前加 Σterms===sum 复核。
- <details><summary>复核推理</summary>复核 execution-evidence.ts:71-108：status = observation.status==='PASS' && sum===0n && hasAllTerms ? 'PASS' : 'FAIL'，只有两态；ledger.mjs checkConservation :134-141 确实返回三态（UNVERIFIABLE 于缺读数时）。UNVERIFIABLE → 行显示 FAIL，语义应为 NOT_VERIFIED（该状态已在 Grace :518 / OI :440 使用）。且 sum 与 terms 均直读 evidence，无 Σterms===sum 复核——若生成端 bug 使 terms 与 sum 不一致仍按 sum 判。方向上不产生假 PASS（守恒真破必 FAIL），属标注/自证问题，low 恰当。驳回失败。</details>

### [低|新发现] ledger-05 — claimable 槽位缺 UI_POSITION_FEE_TYPE（与 affiliate reward）覆盖

- **位置**：`TestCode/tools/fx100-legacy/tool/onchain-tx/lib/ledger.mjs:63`｜类型：coverage-gap
- **问题**：快照 claimable 三槽只覆盖 POSITION/FUNDING/LIQUIDATION 三个 feeType；v0.3.1 路由还有 UI_POSITION_FEE_TYPE（增/减仓都调 incrementClaimableUiFeeAmount）与 referral affiliate reward claimable，钱同样物理留在 PositionVault、只记 DataStore 应收。13 槽表（execution-evidence.ts:203-217）随之无此两行。
- **依据**：IncreasePositionUtils.sol:229-238 / DecreasePositionCollateralUtils.sol:242-250 incrementClaimableUiFeeAmount；v031-facts §3 feeType 四常量含 UI_POSITION_FEE_TYPE。
- **失败情形**：带 uiFeeReceiver 或 referral 的场景：uiFee/affiliate 份额入应收账本但无任何行核对（五方 ERC20 守恒不受影响，属应收账本完整性缺口）。SCN-009 实测 uiFeeAmount=0、无 referral，当前无影响。
- **建议修复**：扩展快照与 LEDGER_FIELDS 增加 claimableUiFee（及需要时 affiliateReward）槽位，非 UI 场景期望 0。
- <details><summary>复核推理</summary>复核 ledger.mjs :63-70：claimable 槽只有 POSITION/FUNDING/LIQUIDATION 三个 feeType。合约 IncreasePositionUtils:229-238 与 DecreasePositionCollateralUtils:239 都调 FeeUtils.incrementClaimableUiFeeAmount，写独立的 claimableUiFeeAmountKey（FeeUtils.sol:67-69，非 claimableFeeAmountKey）；affiliate reward 亦存在（PositionUtils.sol:582 调 ReferralUtils.incrementAffiliateReward 写 affiliateRewardKey）。两者钱物理留 PositionVault、只记 DataStore 应收，快照与 LEDGER_FIELDS 13 槽均无对应行。SCN-009 实测 uiFeeAmount=0、无 referral，当前无影响——应收账本完整性缺口成立，low 恰当。</details>

### [低|新发现] ledger-06 — execute-close 的 ΔPositionVault 期望是姊妹行推导值，未标注推论性质

- **位置**：`TestCode/src/reporting/execution-evidence.ts:362`｜类型：independence-mislabel
- **问题**：平仓阶段 posVaultUsdc 期望 = −TraderOutput − ΔLPVault（:362,:374-377），数值上与合约直接流向和（−output −feeForPool −negativePnl +positivePnl +lpToPos）恒等，本身正确；但它是 trader/lp 两个期望的线性推导，在守恒行成立的前提下不提供独立信息，却与其余行同格式呈现、无类似 trader-usdc-final 的【守恒推论】标注（traps §7 要求标独立度）。
- **依据**：DecreasePositionCollateralUtils.sol:117-217 各 transferOut 点直接求和 = −out−lpDelta（本次审查代数核实并实测 −9987468 吻合）；对照 trader-usdc-final 行已有【守恒推论】注（execution-evidence.ts:1331）。
- **失败情形**：评审者把该行计为 PositionVault 流向的独立核对；若 trader 与 lp 模型出现同源错误，该行的'通过'不构成额外证据（不产生假 PASS 于余额本身，因仍与实测余额比对，属独立度标注问题）。
- **建议修复**：在 note 补'由 TraderOutput 与 ΔLPVault 推导（守恒变形），非第三个独立模型'。
- **复核修正**：平仓阶段 posVaultUsdc 期望 = −TraderOutput − ΔLPVault（:362,:374-377），数值正确且 formula 字符串已如实写出推导式；但与 trader-usdc-final 的【守恒推论】标注（:1331）不对称，note 未声明本行是 trader/lp 两模型的线性推导、在守恒行成立前提下不提供第三个独立信息（traps §7 独立度纪律）。宜在 note 补『由 TraderOutput 与 ΔLPVault 推导（守恒变形），非独立模型』。
- <details><summary>复核推理</summary>复核 execution-evidence.ts:361-362：lpVaultDelta 与 traderOutput 先算，positionVaultDelta = −traderOutput − lpVaultDelta，:374-377 行以同格式呈现。代数上与 DecreasePositionCollateralUtils 各 transferOut 直接流向和恒等（本次核对 §7 瀑布 + :217 feeForPool 转出 + :124 LPVault→PosVault 盈利垫付确认），数值无错。对照 trader-usdc-final 行已有【守恒推论】标注（:1331），本行 note 无独立度声明——但 formula 字符串本身已写明『= −TraderOutput − ΔLPVault』，推导关系并非完全隐藏。缺的是 traps §7 要求的显式非独立标注。</details>

### [低|新发现] pnl-01 — Uncapped Base PnL 行公式只对全平成立，共享构造器无全平守卫（部分平会与合约按整仓折算+取整的结果不等）

- **位置**：`TestCode/src/reporting/execution-evidence.ts:698`｜类型：formula-error
- **问题**：行公式 expectedUncappedPnl = sizeDeltaInTokens×execPrice − sizeDeltaUsd；合约先算整仓 totalPnl = S_tok×P − S_usd 再 mulDiv(totalPnl, dTok, S_tok, 亏损⌈⌉) 折算（PositionUtils.sol:208-245）。仅当 dTok==S_tok（全平）二者恒等；部分平时 dTok=⌈S_tok×dUsd/S_usd⌉（多头），两式一般不等。buildPricingRows 对任何 PositionDecrease 都套用此式，无全平判断。
- **依据**：PositionUtils._getPositionPnlUsd:208-245；真实证据全平重放：828276595306007×60000000000000000−50e30 = −303404281639580000000000000000 与事件 uncappedBasePnlUsd/basePnlUsd 逐位一致（SCN-009 当前正确，且正确使用事件执行价而非 oracle 价，符合 §7）。
- **失败情形**：未来部分平仓场景复用 buildPricingRows：折算取整与两式差异使 expected 与事件差 1 wei 至更多 → false FAIL；或在 formula 栏误导评审者以为 delta 口径公式是合约通式。
- **建议修复**：增加全平判断（sizeDeltaUsd == positionBefore.sizeInUsd 时用现式），部分平时改为 mulDiv(S_tok×P−S_usd, dTok, S_tok, 亏损向上) 同序复刻，或部分平时降级 NOT_VERIFIED。
- <details><summary>复核推理</summary>复核 PositionUtils._getPositionPnlUsd（:196-249）：先算整仓 totalPositionPnl = sizeInTokens×execPrice − sizeInUsd（多头），再 Precision.mulDiv(totalPnl, sizeDeltaInTokens, sizeInTokens, totalPnl<0)（亏损幅度⌈⌉）折算。execution-evidence.ts:696-700 expectedUncappedPnl = sizeDeltaInTokens×execPrice − sizeDeltaUsd 仅在 dTok==S_tok 且 dUsd==S_usd（全平恒等分支）时与合约逐位相等；buildPricingRows 对任何 PositionDecrease 都套用（!isIncrease 即入，:694），无全平守卫。部分平时 dTok=⌈S_tok×dUsd/S_usd⌉（多头）使两式一般不等 → 复用即假 FAIL。SCN-009 全平当前正确（且正确用事件执行价，符合 formulas §7）。潜伏缺陷成立，low 恰当。</details>

### [低|新发现] pnl-02 — 减仓期望公式隐含『多头+全平』假设：uncapped PnL 用 delta 直算、traderOutput 加回全部剩余押金，部分平仓会偏

- **位置**：`TestCode/src/reporting/execution-evidence.ts:360`｜类型：formula-error
- **问题**：decrease 分支 traderOutput = oldMargin + … 把全部剩余押金计入回款（行 360，合约部分平时是 min(请求提取额, remColl)，DecreasePositionCollateralUtils.processCollateral），uncapped PnL 行用 sizeDeltaInTokens × execPrice − sizeDeltaUsd（行 698-700，合约是先算全仓 totalPnl = position.sizeInTokens × execPrice − position.sizeInUsd，再 mulDiv(totalPnl, dTokens, sizeInTokens, 亏损时向上取整幅度)，PositionUtils.sol getPositionPnlUsd）。两式仅在全平时逐 wei 相等。
- **依据**：合约 Github/fx100-contracts@release-v0.3.1/src/position/PositionUtils.sol:208-246（totalPositionPnl 与 Precision.mulDiv(…, totalPositionPnl < 0)）与 DecreasePositionCollateralUtils.processCollateral 支付瀑布；手册 formulas.md §7。本次全平重放：−303404281639580000000000000000 与事件 basePnlUsd/uncappedBasePnlUsd 逐 wei 一致（掩盖了部分平仓分支缺失）。
- **失败情形**：未来部分平仓用例复用该共享构造器时：uncapped PnL 行与 ΔTrader 行的 Expected 与合约 mulDiv 取整/瀑布 min() 语义偏差 1 wei 级至押金级 → 假 FAIL；若继续沿用会迫使放宽容差。
- **建议修复**：部分平仓分支单独建模：totalPnl 全仓口径 + mulDiv 同序复刻（亏损 ceil 幅度）；traderOutput 用 min(请求提取额, remColl)；或在 phaseKind 上显式限定 full-close 并对 partial 输出 NOT_VERIFIED。
- **复核修正**：decrease 分支的 traderOutput = oldMargin + …（:360）隐含全平假设：合约部分平时 output += min(请求提取额, remColl)（DecreasePositionCollateralUtils.processCollateral :259-273），仅全平时全部剩余押金进回款（DecreasePositionUtils :256-258）；部分平仓复用会假 FAIL。（其 uncapped PnL 一半与 pnl-01 为同一发现，以 pnl-01 为准。）修复：部分平仓单独建模或 phaseKind 显式限定 full-close、partial 输出 NOT_VERIFIED。
- <details><summary>复核推理</summary>两半分别核：(1) traderOutput 部分成立且独立——execution-evidence.ts:359-360 traderOutput = oldMargin + …恒把全部剩余押金计入回款；合约仅全平时如此（DecreasePositionUtils :256-258 sizeInUsd==0 时 output += 全部 collateralAmount；部分平走 processCollateral :259-273 output += min(请求提取额, remainingCollateral)，全平时 :220 还会把请求额清零）。部分平仓复用该构造器时 ΔTrader 期望差可达押金级。(2) uncapped PnL 部分（:698-700 delta 直算 vs 合约整仓折算 mulDiv）与 pnl-01 完全重合，锚点相同（PositionUtils:208-246）。SCN-009 全平当前逐 wei 正确属实。</details>

### [低|新发现] pricing-02 — 平多执行价两处行的 formula 文本未写 floor（开仓行写了 ceil），文档不对称

- **位置**：`TestCode/src/reporting/execution-evidence.ts:1301`｜类型：label-or-basis-error
- **问题**：close-execution-price 行 formula 'indexPrice.min × (1e18 − dynamicSpread) / 1e18' 与 calculateExecutionPrice 非 adverseUp 分支的 expanded 文本（formulas.ts:42）都不含 floor 标注；代码实现（BigInt 正数截断）与合约 PositionUtils.sol:709-710（Solidity 截断除法=floor）一致，计算无错，但期望值推导文档缺取整方向。
- **依据**：PositionUtils.sol:708-710；formulas.md §3 '否则: execPrice = ⌊…⌋'；对照开仓行 formula 明确写 'ceil(...)'（:1281）。本次证据 close spread=0 未行使 floor 分支。
- **失败情形**：评审者按 formula 文本用有理数/浮点重推非整除样本时得到 +1 差异，误判核对代码有错；违反用例标准『期望值必须附推导过程』的完整性（取整方向是推导的一部分）。
- **建议修复**：两处文本改为 'floor(indexPrice.min × (1e18 − dynamicSpread) / 1e18)'，calculateExecutionPrice expanded 同步加 floor(...)。
- <details><summary>复核推理</summary>复核三处：execution-evidence.ts:1301 close-execution-price 行 formula 'indexPrice.min × (1e18 − dynamicSpread) / 1e18' 无 floor；formulas.ts calculateExecutionPrice 非 adverseUp 分支 expanded（:41-43）同样无 floor；对照开仓行 :1281 明确写 ceil(...)。计算实现（BigInt 正数截断）与合约 PositionUtils :708-710（Solidity 截断除法）一致，无数值错。formulas.md §3 明写『否则: execPrice = ⌊…⌋』，文档不对称成立——期望值推导缺取整方向，评审者浮点重推非整除样本会误判。纯文档问题，low 恰当。</details>

### [低|新发现] runner-09-grace-anchor-unverified — Grace 复算的锚点 graceStart 直接取自仓位状态，未对照执行块 timestamp——锚点错则断言照过

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:514`｜类型：coverage-gap
- **问题**：grace 断言（L514-534）用 openedPosition.graceStart（合约输出）+ 链上 base/tierMultiplier 复算 graceEnd，公式本身与 IncreaseOrderUtils.sol L46-55（graceEnd = block.timestamp + base×multiplier/1e18）逐位一致；但合约规定 graceStart==执行块 block.timestamp（开仓即计时，traps §8），runner 从未验证这一点——只验证了 end−start 的差。执行块 timestamp 可从 OrderExecuted 事件块取得。
- **依据**：IncreaseOrderUtils.sol L46（setGraceStart(block.timestamp)）；traps §8『宽限期开仓即计时』；scn-009-runner.ts L514-534 仅核 graceEnd−graceStart 关系。
- **失败情形**：若合约把 graceStart 写成订单创建时间（或任何错误时间戳），graceEnd=graceStart+effective 的关系仍成立，断言通过，宽限期整体前移/后移的缺陷漏检。
- **建议修复**：取开仓执行块的 block.timestamp，增加 check『graceStart === 执行块时间戳』。
- **复核修正**：grace 断言（:514-534）用 openedPosition.graceStart 复算 graceEnd，公式与 IncreaseOrderUtils.sol:46-56 一致，但从未验证 graceStart===开仓执行块 block.timestamp（traps §8 开仓即计时）。注意：因合约 graceEnd 独立锚定 block.timestamp，仅 graceStart 单独写错会破坏 end−start 关系而被现有断言捕获；真正漏检的是 graceStart/graceEnd 同源偏移类缺陷（两者一起锚到错误时间戳，宽限期整体前移/后移）。修复：取 OrderExecuted 所在块的 timestamp 增加『graceStart===执行块时间戳』check。
- <details><summary>复核推理</summary>复核 IncreaseOrderUtils.sol :46-56：新仓 setGraceStart(block.timestamp)，setGraceEnd(block.timestamp + base×multiplier/1e18)，两者同锚。runner :514-534 只用 openedPosition.graceStart（合约输出）复算 graceEnd 并断言相等，从未对照执行块 timestamp——graceStart 未独立验证属实。但失败情形需收窄：合约 graceEnd 独立由 block.timestamp 算出，若缺陷仅使 graceStart 写错（如订单创建时间），end−start 关系被破坏、现有断言反而会 FAIL；漏检的是 start/end 同源整体偏移类缺陷（如重构后两者都锚到 order.updatedAtTime）。coverage 缺口成立，low 恰当。</details>

### [低|新发现] runner-10-signature-check-mode-basis — 『四笔交易均包含非零签名字段』在 impersonation 模式下依据不成立：要么恒 FAIL，要么拿 fork 合成签名冒充真实签名证据

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:671`｜类型：label-or-basis-error
- **问题**：hasSignature/『真实签名』三连断言（L671-684）不区分 signingMode：runMode 明确支持 tx-fork-impersonation（免签名 eth_sendTransaction），此时创建交易没有用户私钥签名——fork 返回的 r/s 若为 0 则断言恒红（模式不可用），若是节点合成的非零值则断言绿但证据语义（'用户真实签名'）为假。该检查只在 private-key 模式下才是有效证据，应按模式门控或改标注。
- **依据**：scn-009-runner.ts L334-339（hasSignature）、L446-448（两种 broadcaster 分叉）、L671-677；actions.mjs impersonateBroadcaster（免签名发送）。
- **失败情形**：impersonation 模式运行：若 fork 合成非零 r/s，报告写『四笔交易均包含非零签名字段=PASS』，人工复核据此认定走了真实签名链路——证据标注失实（traps §7 独立度纪律）。
- **建议修复**：signingMode==='impersonation' 时跳过或把断言名改为『交易字段完整（fork 免签模式，不构成签名证据）』。
- <details><summary>复核推理</summary>复核：:671-677『四笔交易均包含非零签名字段』无 signingMode 门控；runMode 明确支持 tx-fork-impersonation（:700），此时 broadcaster 走 actions.mjs impersonateBroadcaster（:55-79，注释明言 eth_sendTransaction 免签名），创建交易无用户私钥签名。fork 节点合成 r/s 的具体值无法在本环境实测，但 claim 的析取结构成立：r/s 为 0 则该模式恒红（断言不可用），非零则 hasSignature 通过、且 buildTransaction.signatureVerified（:1103-1104）与报告『真实签名』语义在该模式下失实（traps §7 证据独立度）。private-key 模式无问题。label/basis 缺陷成立，low 恰当。</details>

### [低|新发现] spec-11-finally-masks-root-error — spec finally 里的 expect(reverted).toBe(true) 会覆盖 runScn009 的原始错误

- **位置**：`TestCode/tests/S02/scn-009.spec.ts:85`｜类型：other
- **问题**：finally 块中 evm_revert 后直接 expect(reverted).toBe(true)（L83-86）：当 runScn009 已抛错且 revert 又失败（或 rawRpc 抛错）时，finally 的异常按 JS 语义取代原始异常，测试报告只显示 revert 问题，真正的断言失败原因被吞。
- **依据**：scn-009.spec.ts L82-87；JS try/finally 异常覆盖语义。
- **失败情形**：开仓断言失败 + fork 管理端瞬时不可用：报告只见『evm_revert 失败』，叠加 runner-06 的零附件，根因完全不可见。
- **建议修复**：finally 里 revert 失败只 console.warn/annotation，不用 expect 抛断言。
- <details><summary>复核推理</summary>复核 scn-009.spec.ts:82-87：finally 内先 rawRpc('evm_revert')（其 :17 对 RPC error 直接 throw），再 expect(reverted).toBe(true)。JS 语义下 finally 中抛出的异常取代 try 块原始异常——runScn009 断言失败 + revert 异常/返回 false 时，报告只见 revert 问题，叠加 runner-06 零附件根因不可见。失败情形成立，low 恰当。</details>

### [低|已知 ⬜] failfast-01 — runner check() 失败即抛错，中断后续证据采集（已知）

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:309`｜类型：coverage-gap
- **问题**：check() 在 passed=false 时 push 断言后立即 throw（行 301-309），首个失败断言之后的快照/事件/守恒证据全部不落盘，失败运行只有残缺证据（8-04 至 8-06 的多个 FAIL run 均无 executionEvidence 即此故）。
- **依据**：traps.md §10 ⬜『runner check() fail-fast 断证据流』；scn-009-runner.ts:309；artifacts/runs 扫描：所有 SCN-009 FAIL run 的 results.json 均无 executionEvidence。
- **失败情形**：任何早期断言失败（如创建交易 revert）后，无法用同一 run 的证据做失败根因核对，必须重跑。
- **建议修复**：check() 记录不抛错，流程末尾统一汇总失败并尽量完成证据组装；不可继续的硬前置才 throw。
- <details><summary>复核推理</summary>known:true 项按纪律只核实存在性与 SCN-009 影响：scn-009-runner.ts L301-310 check() 在 passed=false 时 push 后立即 throw（L309），而证据对象在 L698 才组装返回，任何断言失败即整个 runScn009 抛出、无 executionEvidence。traps §10 ⬜ 明列『runner check() fail-fast 断证据流』。实测验证 artifacts/runs 全量扫描：SCN-009 共 19 个 FAIL run 全部 NO-EVIDENCE、3 个 BLOCKED 无证据、5 个 PASS 均有证据——与 claim 的证据陈述完全吻合。存在且影响 SCN-009 失败运行的根因核对能力，成立。</details>

### [低|已知 ⬜] ledger-08 — 已知未修：NOT_VERIFIED 不阻断整体 PASS，账本组含常驻 NOT_VERIFIED 行

- **位置**：`TestCode/src/reporting/execution-evidence.ts:1367`｜类型：coverage-gap
- **问题**：executionStatus 只在存在 FAIL 时才不判 PASS（:1367-1371）；账本组的 positionImpactPoolAmount（execute 阶段恒 NOT_VERIFIED，:346-348/:408-410）与 executionPrice=0 时的 OI 行不计入闸门。核实确认该 ⬜ 项影响 SCN-009：每次 run 至少 2 行账本 NOT_VERIFIED 仍整体 PASS。
- **依据**：traps.md §10 ⬜ 'NOT_VERIFIED 不阻断整体 PASS（质量闸门）'；execution-evidence.ts:1367。
- **失败情形**：未来某槽位因证据缺失批量降级 NOT_VERIFIED（如参数快照缺失）时整体仍绿，核对覆盖静默收缩。
- <details><summary>复核推理</summary>known:true 项，核实确实存在且影响 SCN-009：execution-evidence.ts:1367-1371 executionStatus 只排除 FAIL 行；positionImpactPoolAmount 期望恒缺（:346-348/:408-410 无 value → buildSnapshotRows :440 判 NOT_VERIFIED），执行价=0 时 OI 行同理，每次 run 至少 2 行账本 NOT_VERIFIED 仍整体 PASS。与 traps §10 ⬜『NOT_VERIFIED 不阻断整体 PASS』一致。本条保留为该已知问题的正主（gate-01 三次出现均判 DUPLICATE 归并至此）。</details>

### [低|已知 ⬜] manifest-01 — TestCode 部署 manifest 仍未登记 feeHandler/revenuePool/treasury（已知遗留；SCN-009 实际地址来自合约仓产物，未受影响）

- **位置**：`TestCode/config/deployments/base-sepolia-v0.3.1-260729.json`｜类型：coverage-gap
- **问题**：manifest 的 contracts/additionalContracts 无 feeHandler/revenuePool/treasury 条目；账本实际经 tools/fx100-legacy/.../deployment.mjs 从合约仓 deployed_addresses.json 读 Fx100Execution#FeeHandler=0x009559c1B221243939f4DfbBB036fC87883c452c（与 v031-facts 一致），故 SCN-009 五方守恒的 FeeHandler 槽地址正确；但 TS 侧未来若改从 manifest 取址会踩空。
- **依据**：v031-facts.md §2 与 traps.md §10 ✅ 项括注『manifest 仍未登记（待补）』；实测 manifest 无这些键、deployed_addresses.json 地址与手册一致。
- **失败情形**：后续核对代码改用 TestCode manifest 取 feeHandler/revenuePool 地址时读 undefined → balanceOf 报错或静默跳槽位，claim/withdraw 场景守恒圈缺账户。
- **建议修复**：在 manifest additionalContracts 补 feeHandler/revenuePool/protocolTreasury/insuranceTreasury 并与 deployed_addresses.json 交叉校验。
- <details><summary>复核推理</summary>known:true 项，核实：TestCode/config/deployments/base-sepolia-v0.3.1-260729.json 的 contracts（11 键）与 additionalContracts（11 键）均无 feeHandler/revenuePool/treasury；账本实际经 tools/fx100-legacy/tool/onchain-tx/lib/deployment.mjs:31 以 'Fx100Execution#FeeHandler' 从合约仓 deployed_addresses.json 取址，与 v031-facts §4 FeeHandler=0x009559…452c 一致，故 SCN-009 五方守恒槽地址正确、当前无影响；风险仅在未来 TS 侧改从 manifest 取址。与 traps §10 ✅ 项括注『manifest 仍未登记（待补）』一致，low 恰当。</details>

### [低|已知 ⬜] raw-01 — pass() 仅字符串比较，行数据无 raw bigint/difference 字段（已知）

- **位置**：`TestCode/src/reporting/execution-evidence.ts:115`｜类型：other
- **问题**：pass(actual, expected) 为 String(a)===String(e)（行 115-117），Reconciliation 行只有格式化字符串，FAIL 时无 raw 差值字段，逐 wei 排障需人工反解析。
- **依据**：traps.md §10 ⬜；execution-evidence.ts:115-117。本次重放中所有 PASS 行数值与独立复算一致，字符串比较未引入误判（bigint→string 无歧义）。
- **失败情形**：FAIL 时报告只显示两串长数字，缺 difference=after−expected，1 wei 级偏差定位成本高；未来若 expected 混入非规范化字符串（前导零/正号）会假 FAIL。
- **建议修复**：行结构增加 rawBefore/rawAfter/rawExpected/difference（string 化的 bigint），pass 改为 BigInt 比较。
- <details><summary>复核推理</summary>known:true 项，核实仍在：pass()（execution-evidence.ts:115-117）为 String(a)===String(e)；Reconciliation 行的 before/after/delta/expected 均为格式化字符串（rawAndUnit 产物），无 rawBefore/rawExpected/difference 字段，FAIL 时逐 wei 排障需人工反解析。当前数据源均为 bigint.toString() 规范形，字符串比较未引入实际误判——claim 对此的限定准确。traps §10 ⬜ 对应项，low 恰当。</details>

### [低|已知 ⬜] runner-05-executionfee-wnt-zero-coverage — executionFee/WNT 侧零覆盖（§10 ⬜ 已核实仍在）；relayFee 经核实不适用本用例

- **位置**：`TestCode/src/scenarios/scn-009-runner.ts:163`｜类型：coverage-gap
- **问题**：两笔单各随 value 发 EXECUTION_FEE=2e13 wei（0.00002 ETH，testData 标注一致），经 sendWnt 进 OrderVault（flows.mjs L114/L155），但账本快照没有任何 WNT/ETH 槽位（ledger.mjs plan L52-83 全是 USDC/DataStore），runner 也无任何执行费消耗/退款断言——多收/漏退执行费完全检不出。relayFee 不适用：SCN-009 走普通 multicall[sendWnt,sendTokens,createOrder] 直发 ExchangeRouter，非 Flash/relay 模式（traps §6 的 relayFee 项只针对 relay 模式）。
- **依据**：traps.md §10 ⬜『executionFee/WNT 零覆盖』、§6；formulas.md §1『executionFee 走 ETH/WNT，不污染 USDC 账本』；flows.mjs buildIncreaseMulticall/buildDecreaseOrder；ledger.mjs 槽位表。
- **失败情形**：执行费被全额吞掉不退（或重复扣）时，USDC 五方守恒与全部 17 项断言照常通过，ETH 侧损失无人知晓。
- **建议修复**：快照加 trader ETH 余额与 OrderVault WNT 余额槽位，按『预付 − keeper 实耗 − 退款』做双侧核对（至少断言退款事件存在）。
- <details><summary>复核推理</summary>known:true 项，核实仍在：EXECUTION_FEE=2e13（runner:163）随 multicall sendWnt 进 OrderVault（flows.mjs:114/:155，label 显示普通 multicall[sendWnt,sendTokens,createOrder] 直发 ExchangeRouter——relayFee 不适用的判断正确）；ledger.mjs plan（:52-83）全部槽位为 USDC balanceOf / DataStore getUint，无任何 WNT/ETH 槽；runner 无执行费消耗/退款断言。executionFee 多收/漏退完全检不出，与 formulas §1『executionFee 走 ETH/WNT 不污染 USDC 账本』的 scope 声明一致但自身账无人核。保留为本问题正主（wnt-01 归并至此）。</details>

## 三、重复项归并（14 条）

- `ledger-01`：（第二条 ledger-01，标题『共享账本 Δ 构造器的 OI 取整忽略四格矩阵』）内容 = 首条 ledger-01（部分平多头 ⌈⌉）+ ledger-02（空头开仓 ⌈⌉）的合并版，合约锚点（:636/:640/:524-526/:531/:535）与两条已确认发现完全一致，无新增信息。保留拆分的 ledger-01 与 ledger-02。
- `ledger-07`：与 runner-03-after-snapshot-latest-not-execblock 实质相同（traps §10 同一 ⬜ 项：after-execute 快照取 latest 非执行块）。已核实该问题在当前工作树真实存在且影响 SCN-009：scn-009-runner.ts:502/:575 无块号 → :404 eth_blockNumber(latest)，TX2/TX4 全部账本 Δ 行与守恒窗口以 latest 为界。保留 runner-03（覆盖面最全：含执行步 before 非 execBlock−1、inline execution.blockNumber 被丢弃、参数快照标注不实、grace/fundingEma 同源）。
- `pricing-01`：（第二条 pricing-01，标题『balance-improved/skewRef 复算用事件最终 sizeDeltaInTokens 口径』）与首条 pricing-01 实质相同：同一代码位置（buildPricingRows :592-601）、同一合约依据（getIncreaseOrderSize 预备值 → getNextOpenInterest usdDelta=tokenDelta×midPrice）、同一失败情形（balance-improved 边界翻转、skewRef 偏离）。其引用手册章节写 traps §8（首条写 formulas §3 陷阱2，两处内容相同）。保留首条 pricing-01。
- `ledger-01`：（第二条 ledger-01，标题『共享账本 Δ 构造器的 OI 取整忽略四格矩阵』）内容 = 首条 ledger-01（部分平多头 ⌈⌉）+ ledger-02（空头开仓 ⌈⌉）的合并版，合约锚点（:636/:640/:524-526/:531/:535）与两条已确认发现完全一致，无新增信息。保留拆分的 ledger-01 与 ledger-02。
- `snapshot-01`：（第三条 snapshot-01，标题『after-execute 快照取 latest…含 funding EMA 读数』）与 runner-03 实质相同（同一 known ⬜ 项、同一代码位置 :502/:575/:511/:583）；其 fundingEma 依赖钉块的补充观察是同一根因的又一消费方，runner-03 已覆盖 fundingEma 同锚 latest 的事实。保留 runner-03。
- `gate-01`：（第三条 gate-01，标题『NOT_VERIFIED 行不阻断整体 PASS，质量闸门缺失』）与 ledger-08 实质相同（execution-evidence.ts:1367-1371、traps §10 同一 ⬜ 项）；其补充观察（本次证据 5 行 NOT_VERIFIED 与 PASS 并存、coverageStatus 固定 'PARTIAL' 与明细无联动——后者已从 :1377 核实为硬编码）不改变发现本体。保留 ledger-08。
- `gate-01`：（第三条 gate-01，标题『NOT_VERIFIED 行不阻断整体 PASS，质量闸门缺失』）与 ledger-08 实质相同（execution-evidence.ts:1367-1371、traps §10 同一 ⬜ 项）；其补充观察（本次证据 5 行 NOT_VERIFIED 与 PASS 并存、coverageStatus 固定 'PARTIAL' 与明细无联动——后者已从 :1377 核实为硬编码）不改变发现本体。保留 ledger-08。
- `snapshot-01`：（第三条 snapshot-01，标题『after-execute 快照取 latest…含 funding EMA 读数』）与 runner-03 实质相同（同一 known ⬜ 项、同一代码位置 :502/:575/:511/:583）；其 fundingEma 依赖钉块的补充观察是同一根因的又一消费方，runner-03 已覆盖 fundingEma 同锚 latest 的事实。保留 runner-03。
- `matrix-01`：内容 = ledger-01（部分平多头 ⌈⌉、全平恒等分支）+ ledger-02（空头开仓 ⌈⌉）的又一合并复述，合约锚点与失败情形一致（其重放验证 828276595306007=⌊50e30/60366307925829368⌋ 与两条已确认发现相同）。保留 ledger-01 与 ledger-02。
- `pricing-01`：（第二条 pricing-01，标题『balance-improved/skewRef 复算用事件最终 sizeDeltaInTokens 口径』）与首条 pricing-01 实质相同：同一代码位置（buildPricingRows :592-601）、同一合约依据（getIncreaseOrderSize 预备值 → getNextOpenInterest usdDelta=tokenDelta×midPrice）、同一失败情形（balance-improved 边界翻转、skewRef 偏离）。其引用手册章节写 traps §8（首条写 formulas §3 陷阱2，两处内容相同）。保留首条 pricing-01。
- `snapshot-01`：（第三条 snapshot-01，标题『after-execute 快照取 latest…含 funding EMA 读数』）与 runner-03 实质相同（同一 known ⬜ 项、同一代码位置 :502/:575/:511/:583）；其 fundingEma 依赖钉块的补充观察是同一根因的又一消费方，runner-03 已覆盖 fundingEma 同锚 latest 的事实。保留 runner-03。
- `cancel-01`：与 cov-03 实质相同：同一文件同一锚点（scn-009-runner.ts:594-609），同一缺口（无 OrderCancelled/OrderFrozen 抓取、无 reasonBytes 解码、traps §5 判别模板未下沉），同引 traps §10 ⬜，同为 known:true，suggestedFix 同为下沉 SCN-070 实现做三分类。cov-03 信息更全（含 486-500 行离队判定与 legacy scenario.mjs findSettlementTx 现成实现指认），保留 cov-03（severity 已按本条的 low 口径修正）；本条唯一增量'对本次 PASS 运行无数值影响'已并入 cov-03 复核结论。
- `wnt-01`：与 runner-05-executionfee-wnt-zero-coverage 实质相同（同一 known ⬜ 项：ledger.mjs :52-83 无 WNT/ETH 槽、executionFee 零核对），失败情形一致。已核实真实存在。保留 runner-05（额外核清了 relayFee 不适用的边界）。
- `gate-01`：（第三条 gate-01，标题『NOT_VERIFIED 行不阻断整体 PASS，质量闸门缺失』）与 ledger-08 实质相同（execution-evidence.ts:1367-1371、traps §10 同一 ⬜ 项）；其补充观察（本次证据 5 行 NOT_VERIFIED 与 PASS 并存、coverageStatus 固定 'PARTIAL' 与明细无联动——后者已从 :1377 核实为硬编码）不改变发现本体。保留 ledger-08。

## 四、正面确认清单（87 条：核对过且正确的关键点）

- create-order 阶段 ±margin 期望正确：flows.mjs:115 sendTokens(collateralToken, orderVault, collateral) 恰转 COLLATERAL=10_000_000（=decimalToRaw('10',6)），其余 11 槽零期望与'订单未执行'语义一致；实测 run ΔTrader=−10000000 吻合
- execute-open ΔPositionVault=margin−feeForPool+lpToPos、ΔLPVault=feeForPool−lpToPos 与合约一致：IncreaseOrderUtils.sol:27-31 OrderVault→PositionVault 整额、IncreasePositionUtils.processCollateral 里 feeReceiverAmount 只记 claimable（FeeUtils.incrementClaimableFeeAmount POSITION_FEE_TYPE）而 feeAmountForPool 物理 transferOut→LPVault、MarketUtils.sol:581-595 funding 双向（>0 记 claimable FUNDING_FEE_TYPE / <0 LPVault 物理转 PositionVault）；实测 9994995/5005/claimable 5005 全吻合
- execute-close ΔTrader 线性瀑布公式在偿付型全平下与 DecreasePositionCollateralUtils.processCollateral+payForCost 同值：positivePnl=⌊basePnl/pnlTokenPrice.max⌋ 且 pnlToken==collateralToken（DecreasePositionUtils.sol:224-225，故用事件 collateralTokenPrice.max 正确）、negativePnl=roundUp(−basePnl/colMin)（payForCost 实证）、funding/费用 ×min 再 ⌈/min⌉ 恒等消去、全平 output+=剩余抵押（DecreasePositionUtils.sol:257-259）；实测 9989990+0+0−20−303708−5005=9681257 吻合
- execute-close ΔLPVault=feeForPool+negativePnl−positivePnl−lpToPos 与合约转账点一一对应（:117-125 盈利 LP→PosVault、:188-193 亏损 PosVault→LP、:215-217 feeForPool PosVault→LP、MarketUtils else 分支 LP→PosVault）；实测 2503+303708=306211 吻合；ΔPositionVault=−9987468 吻合，且仓位付 20/市场 positionPaysLp 19 的 1 wei 取整尘埃被模型隐式留在 PositionVault（wholeFlow posVault +7527 = 7507+19+1 逐 wei 闭合）
- claimable 三槽期望正确：ΔClaimable Position Fee=feeReceiverAmount 与增仓（IncreasePositionUtils）及减仓（DecreasePositionCollateralUtils:222-229，非清算时 liquidationFeeAmountForFeeReceiver=0 故等于全额）一致，ΔClaimable Funding=max(positionPaysLp,0)、非清算 ΔClaimable Liquidation=0；实测 5005/2502/19 吻合（2502=⌊5005×5e29/1e30⌋ 亦独立验证 applyFactor floor）
- cumulativeOpenCosts ±inputSizeUsd 期望是订单原值侧输入锚定（USD 模式 sizeDeltaUsd===rawSizeDelta，formulas §2 的正确独立方向），非事件回抄；实测 ±50e30 吻合
- 五方守恒圈与手册 §1/v031-facts §2 完全一致：CONSERVATION_TERM_FIELDS 与 ledger.mjs CONSERVATION_SLOTS 同序同员，LP 槽读 lpVault.totalAssets()（ledger.mjs:57）非 balanceOf，第五方读 balanceOf(feeHandler)（ledger.mjs:56）且显示名已改 ΔFeeHandler、note 讲明哨兵语义与 RevenuePool 不在圈内；claimable 与 ERC20 两套账在任何行中均未相加
- buildSnapshotRows 的 expectedAfter=before+Δ 不是恒等式：Δ 来自 buildLedgerDeltaExpectations 的事件/输入锚定模型而非 diff 实测值，status 与链上 after 实测读数比对，实测 Actual Δ 在 note 中如实披露；expectation 缺值时如实 NOT_VERIFIED 不伪造
- LEDGER_FIELDS 13 槽精度/单位标注全部正确：五个余额槽+三 claimable 槽 USDC 1e6（与实测 collateral 价 1e24 刻度自洽）、open costs USD 1e30（$50→50e30 实证）、OI/impact pool token 槽走 tokenDecimals=18 参数化，与 ledger.mjs UNIT 表一致
- trader-usdc-final 行公式与标注正确：expected=before−Σ其余四方 wholeFlow 实测 Δ，是五方守恒式变形，已标【守恒推论】并指回 TX2/TX4 的独立 ΔTrader 行；实测 10000000000−318743=9999681257 吻合
- 开/平执行价期望公式与合约同序同取整：开多 ceilDiv(max×(1e18+spread),1e18) 对应 Precision.mulDiv(...,true)，平多 floor(min×(1e18−spread)/1e18)；BaseOrderUtils.getExecutionPriceForIncrease 的 acceptablePrice 检查只会原样返回或 revert，不改写价格，事件价与折 token 用价必同值；实测 60366307925829368 吻合
- claimable key 派生按 v0.3.1 三参签名（keys.mjs claimableFeeAmountKey 含 feeType，marketIndex+token+feeType 与 FX100Keys 一致），清算费独立 LIQUIDATION_FEE_TYPE 槽已单列（ledger.mjs:63-69），不存在 traps §1 的旧签名静默读 0 问题
- traps §10 ✅ 五项修复在当前工作树确认落地：ΔFeeHandler 改名（execution-evidence.ts:65-68）、守恒行独立分组'守恒'、【恒等式】标注（执行价 ×3 + Protocol Fee 分账 + Total Cost）、CALCULATED 行 expected 改'—（派生展示行…）'、Grace 缺参数输出 NOT_VERIFIED 行（:515-531）
- Position Fee 独立复算公式正确：calculatePositionFee（formulas.ts L46-60）的 tradeSizeUsd×factor/1e30/colMin 逐步 BigInt 截断 == 两次独立 floor == 合约 PositionPricingUtils.getPositionFeesAfterReferral L461-462（Precision.applyFactor floor 后再 / colPrice.min floor）；实测抽算 open 50e30×2e26→10010、close 50e30×1e26→5005 与事件逐 wei 一致，合并分母也未产生偏差的边界得到确认
- factor 档位独立取参正确：feeFactorLabel=POSITION_FEE_FACTOR(true/false) 的 key 派生（default-mock-market.ts keccak256(base‖abi.encode(mi,bool))）== FX100Keys.positionFeeFactorKey L1363-1365；expected 用链上参数快照值而非抄事件 factor，且 position-fee-factor 行把事件 factor 与链上值互核（事件 balanceWasImproved=false→2e26、true→1e26 与快照一致）
- tradeSizeUsd 口径正确：事件 tradeSizeUsd == 合约喂给费率公式的 sizeDeltaUsd（增仓 IncreasePositionUtils L170 cache.sizeDeltaUsd；减仓 DecreasePositionUtils L311 cache.sizeDeltaUsd，即 DecreasePositionCollateralUtils L91/L111 的 values.sizeDeltaUsd）
- Protocol Fee 分账行【恒等式】标注准确（protocolFee==feeReceiverAmount+positionFeeAmountForPool 即合约 L531-532 恒等式，note 已声明不构成独立重算）；相邻 Fee Receiver / LP Pool 行确实用执行区块 positionFeeReceiverFactor(全局键，abiStringKey==FX100Keys.POSITION_FEE_RECEIVER_FACTOR)独立复算，floor 方向与余数归属（receiver⌊⌋、余数归 pool）与合约一致，实测 5005→2502/2503 逐 wei 吻合
- Funding 应付/应收取整与价格边正确：expectedNegative=ceilDiv(Δneg×size,1e30×colMin)、expectedPositive=floor(Δpos×size/(1e30×colMax)) == MarketUtils.getFundingAmount L829-855（roundUpMagnitude→min 价+Math.Rounding.Ceil；roundDown→max 价+floor）；实测 close Δ=2.39e40×8.28e14/9.99e53=19.803→ceil 20 == 事件 negativeFundingFeeAmount=20（floor 会得 19，证明取整方向 load-bearing 且实现正确）
- Δ(perSize) 基准语义正确：仓位侧 perSize 存量在两次触碰之间不变，positionBefore（createClose/afterOpen 快照）读数 == 合约 getFundingFees L386-400 用的 position.negativeFundingFeePerSize；latest 侧取自事件（EVENT_ANCHORED，合约 L361-367 事件即权威）；新仓 size=0→expected 0 与合约一致；全平按全仓 sizeInTokens 先结清与手册 §5 一致——traps §4 的 execBlock−1 要求针对市场累加器 dt 推导，本组行不重放 dt，不受影响
- FX100 自动结算语义正确：行没有出现"仓位领取 claimable funding"的错误语义；负 funding 进 totalCostAmount 扣保证金、正 funding 单独加回保证金（IncreasePositionUtils L239-240），LP Claimable Funding 行只记 positionPaysLp>0 → FUNDING_FEE_TYPE claimable（updateFundingState L582-590），<0 → LPVault transferOut 到 PositionVault（L592-595），两方向账本去向分配正确（ledger 期望的 lpToPositionVault=max(−paysLp,0)、claimableFunding=max(paysLp,0)）
- Total Cost 行组成与合约 PositionFees 结构一致：totalCostAmount = positionFeeAmount + liquidationFeeAmount + uiFeeAmount − totalDiscountAmount + negativeFundingFeeAmount（PositionPricingUtils L373-376）；SCN-009 非清算（isLiquidation=false→liquidation 全零）且无 referral/pro（事件无 referral.*/pro.* 动态字段），行公式 positionFee+uiFee+negFunding 成立且已在 formula 文本声明前提；若前提破坏会 loud FAIL 而非漏检；实测 5005+0+20=5025==事件
- Claimable Position Fee 行 key 口径与金额正确：ledger.mjs L63-69 用 v0.3.1 四参 key keccak256(abi.encode(CLAIMABLE_FEE_AMOUNT, marketIndex, token, feeType))（keys.mjs L87-93 == FX100Keys L1033-1039），POSITION/FUNDING/LIQUIDATION 三 feeType 分槽不混计；金额 expected=before+feeReceiverAmount 与增仓 L220-227（全额）、减仓 L219-226（SCN-009 无清算费时等于全额）一致；实测 claimPos 0→5005→7507、claimFund 0→0→19 逐 wei 吻合
- Funding Factor 行公式正确：clamp(floor+base×ema/1e18, min, max) / clamp(−base×ema/1e18, min, max) == MarketUtils L665-674，JS BigInt 与 Solidity int 除法同为向零截断；EMA 取执行后端态 lastEmaValue 恰等于合约 saveValue 后 emaValue() 所用值（ExponentialMovingAverage L64-90，含首样本分支）；totalOI==0 no-op 分支（L631-633）有对应处理；实测 close ema=1e18 → long=clamp(4.61e22→max 3.06e22)、short=clamp(−4.27e22→min −4.25e21) 均与事件逐 wei 一致（clamp 两端都被真实触发）
- 参数 key 派生全部核对无误：POSITION_FEE_RECEIVER_FACTOR/MAX_PRICE_IMPACT_SPREAD（abi-string 全局键）、FUNDING_SKEW_EMA(marketIndex)（bytes32+uint256）、FUNDING_* 四键 valueType=int 走 getInt——与 FX100Keys.sol 及 traps §1 双重编码规则一致，无静默读 0 风险
- 开多执行价行公式与取整正确：expectedOpenExecution=ceilDiv(max×(1e18+spread),1e18)（execution-evidence.ts:1160-1164）与合约 PositionUtils.getExecutionPriceForIncrease:630-635（Precision.mulDiv roundUpMagnitude=true=⌈⌉）逐位一致；真实证据重放 ceil→60366307925829368==事件值，floor 会得 …367——ceil 是 load-bearing 且方向正确
- 平多执行价行计算正确：expectedCloseExecution=min×(1e18−spread)/1e18 的 BigInt 正数截断==合约 PositionUtils.getExecutionPriceForDecrease:709-710 的 Solidity 截断除法（floor）；证据重放 spread=0 → 60000000000000000==事件值（floor 分支本份证据未行使，但代码语义与合约同序）
- buildPricingRows 的不利侧选择矩阵 adverseUp=(isLong&&isIncrease)||(!isLong&&!isIncrease) 与合约四分支完全一致（增多⌈max⌉/增空⌊min⌋:638-640/减多⌊min⌋:708-710/减空⌈max⌉:712-714）；BaseOrderUtils.getExecutionPriceForIncrease/Decrease:197-267 只校验 acceptablePrice、不改价，忽略它不影响期望值
- 三处执行价行（open-execution-price:1283、close-execution-price:1303、pricing-execution-price:691）均已标【恒等式】并注明 dynamicSpread 独立复算未实现；pricing-dynamic-spread NOT_VERIFIED 行存在（:677-684）且不伪记 PASS——traps §10 前三条 ✅ 修复在本维度落实
- skew 的 OI 口径正确用盯市：oiUsd=openInterestInTokens×mid（:570-572,592-595）==MarketUtils.getOpenInterestInTokensInUsd:889-898（纯乘法无除）；mid=(min+max)/2 向下取整==Price.midPrice:25-27
- calculateSkewImpact 与合约 getSkewImpact（PositionPricingUtils.sol:202-219）一致：factor×skewRef/1e18 向零截断、改善严格<时先取负、后 SignedMath.max(min,min(max,·)) clamp；calculateSignedSkew 截断后取绝对值==合约 skewAbs 的 mulDiv floor；skewRef=(|before|+|after|)/2 同式（:221-237）
- 真实证据 dynamicSpread=5100032065091026 手动分解完全自洽：= CONSTANT_PRICE_SPREAD 1e14 + skewImpact 5e15（1e16×5e17/1e18=5e15 恰触 MAX_SKEW_IMPACT clamp，与 calculateSkewImpact 复算一致）+ priceImpact 32065091026（=linear 分支 (50e30×1e18/askDepth)/100 精确吻合，exp 分支 x=1603254551330<linear 不当选）——getDynamicSpread:152-175 的三段式与 clamp≤0→0 均验证
- balanceWasImproved 判定语义正确：行公式严格 < 与 isBalanceWasImproved:239-243 一致；本份证据 open=false（0→失衡）/close=true（失衡→0）双双复算相符
- ΔLong OI（开仓）期望 ⌊sizeUsd/execPrice⌋（:283）对多头方向正确==PositionUtils:636；证据重放 ⌊50e30/60366307925829368⌋=828276595306007==事件 sizeDeltaInTokens==ΔOI 快照差；全平 decrease 恒等分支（getDecreaseOrderSize:524-526）下 :365 公式退化为精确 oldSizeInTokens——SCN-009 双向 OI 行零误差
- cumulativeOpenCosts 成本口径正确：开仓行 +inputSizeUsd（margin×leverage=50e30==配置值，USD 模式订单 isSizeDeltaUsd=true 见 flows.mjs:75，合约 applyDeltaToCumulativeOpenCosts:501-521 加 sizeDeltaUsd 原值）；全平回退行 expected==beforeValues（:1314-1322）且 runner :586-592 双重断言
- PnL 用事件执行价而非 oracle 价（§7 合规）：uncapped-pnl 行以事件 executionPrice 计算，全平重放 828276595306007×60000000000000000−50e30=−303404281639580000000000000000 与事件 uncappedBasePnlUsd/basePnlUsd 逐位一致；base-pnl Cap 行如实 NOT_VERIFIED 不冒充
- 全平 ΔTrader（钱包实测差 0 容差）期望模型与 §7 支付瀑布一致：oldMargin+positiveFunding+⌊basePnl/colMax⌋−negativeFunding−⌈|basePnl|/colMin⌉−(totalCost−negativeFunding)（:352-372），偿付型全平下与瀑布顺序无关的求和恒等成立；positivePnl floor/negativePnl ceilDiv 取整边与价格边（max/min）配对正确
- CALCULATED 展示行（Long/Short OI USD、Raw Skew、Skew Impact）expected 列均为『—（派生展示行，无独立期望值）』占位，不回显 actual（traps §10 ✅ 项已落实）；skew-ref 行 expected 是派生平均值并有 note 说明，非自证回显
- buildPositionRows 字段映射经真实事件验证：PositionIncrease.uint 确含 sizeInUsd/sizeInTokens/collateralAmount/negativeFundingFeePerSize/positiveFundingFeePerSize 同名字段，无静默读 0；basis 如实标注『事件与 Reader 仓位快照交叉核对』；仓位规模独立期望存在（position-open-size expected=margin×leverage 订单输入，:1256-1264）
- trader-usdc-final 行已标【守恒推论】且公式为五方守恒变形（:1324-1332）；expectedFinalUsdc=before−Σ其他四方 wholeFlow Δ 符号推导正确
- check() 清单逐条盘点（service 模式 17 条 / inline 模式 19 条）：期望值来源分类为——独立输入：sizeInUsd===SIZE_USD（订单入参）、trader/keeper 地址（env 独立）、四笔回执 status、发单成功；恒等式/事件回读：两条执行价不等式（两输入均取自同一 PositionIncrease/Decrease 事件）；状态+独立参数：grace 公式（graceStart 取状态、base/tier 取 DataStore）。没有『expected 直接回显 actual』式的伪核对（『开仓后为多头』因 posKey 已按 isLong=true 派生而近于同义反复，但 expected 是字面量 true，无害）
- 快照内部单块一致性成立：ledger.mjs snapshot 把全部读数（5 个余额 + 8 个 DataStore uint + Reader.getPosition）经 rpc.readMany(calls, blockNumber) 用 Multicall3 钉同一 blockNumber（rpc.mjs #multicallRead tag=块号；退化路径 batchCall 亦带块号），traps §4 第一条满足
- afterCreateOpen/afterCreateClose 正确钉在创建交易所在块（runner L478/L551），避免了 Service Keeper 抢跑丢挂单态的竞态；orderIsPending 轮询用 latest 仅作等待判定不作断言，口径可接受
- 事件 topic 口径正确：fetchEmitterEvents 用 keccak256Utf8(eventName)（keccak.mjs L116，raw UTF-8 = ethers id() 语义），符合 traps §3；自带测试向量（空串/abc）校验过 keccak 实现
- orderKey 匹配字段兼容两族命名：eventKey 先 orderKey 后 key；源码核实 OrderEventUtils.sol 各事件用 "key"、PositionEventUtils.sol PositionIncrease/Decrease 用 "orderKey"（L70/L120），两族都盖住，无静默失配
- 『恰好一条 OrderExecuted』按 orderKey 过滤、窗口 before+1..afterClose（runner L594-609），能防同 key 重复执行/重放计数；静默取消也会被它与『仓位确实创建/已移除』硬断言拦住（traps §5 的 vacuous pass 已防，缺的只是 reason 判别，见 runner-04）
- 执行价不等式方向与合约一致（源码核实 PositionUtils.sol）：开多 getExecutionPriceForIncrease L629-635 = mulDiv(index.max, 1e18+dynSpread, 1e18, ceil) ⇒ execPrice ≥ oracle max（dynSpread 为 uint ≥0），runner 用 ≥ 正确；平多 getExecutionPriceForDecrease L708-710 = index.min×(1e18−dynSpread)/1e18 floor ⇒ ≤ oracle min，runner 用 ≤ 正确。不做精确复算的原因成立：dynSpread 需 LogExpMath 逐位移植 + 执行块盯市 skew 输入，runner 层未实现，推导层已按 §10 ✅ 显式标注【恒等式】
- Grace 公式与合约逐位一致（源码核实 IncreaseOrderUtils.sol L38-56）：仅新仓写入 graceStart=block.timestamp、graceEnd=block.timestamp+(base×tierMultiplier)/1e18（整除截断），calculateGrace（formulas.ts L15-27）同序截断；tier 来源 referralStorage.referrerTiers(account) 与合约同源
- Grace 两级 key 派生正确（traps §1 双重编码陷阱已避开）：LIQUIDATION_GRACE_PERIOD_BASE 用 packed-string（default-mock.ts L70，对应 FX100Keys.sol L7 keccak256(裸串)）再 abi.encode(bytes32,uint256) 拼 marketIndex（FX100Keys.sol L1626）；tierMultiplierKey 在 chain-parameter-snapshot.ts L134-137 同口径（raw keccak + uintDimensionKey），与 FX100Keys.sol L1630 一致
- claimableFeeAmountKey 用 v0.3.1 三参签名 (marketIndex, token, feeType)（keys.mjs L87-93），LIQUIDATION_FEE_TYPE 独立槽位已补进快照（ledger.mjs L69），traps §1 的跨版本恒 0 陷阱已避开
- ledger.mjs 守恒语义正确：CONSERVATION_SLOTS 五方含 lpVaultAssets=totalAssets()（非 balanceOf，traps §9）；checkConservation 缺读数判 UNVERIFIABLE 不判 PASS（L134-142）；diff 缺读数置 null 不参与守恒；CSV match 列无期望时留空不写 TRUE——快照/对账工具本身的『不冒充』纪律到位（问题只在 runner 未消费 status，见 runner-01）
- waitForExecution 结局字符串核实：flows.mjs L238-245 成功返回 {outcome:'settled'}，runner 比较 !== 'settled' 口径一致，不存在恒抛分支
- executionFee 数值自洽：EXECUTION_FEE=2e13 wei=0.00002 ETH 与 testData.executionFeeEth 标注一致；且经 flows.mjs 确认 SCN-009 走普通 multicall（sendWnt+sendTokens+createOrder），非 relay 模式，relayFee 前置校验不适用本用例
- spec 附件协议基本完整（成功路径）：evidence 全量 JSON（含 parameters/events/assertions/coverage）、tx-and-receipt、reader-ledger-before-after 三份附件 + coverage-gap/check-result 两条注解，bigint 经 stringifyEvidence 安全序列化；Service Keeper 未就绪走 blocked 注解不误记协议 FAIL；非持久模式 evm_snapshot/evm_revert 清场对称
- 零和守恒覆盖齐全且不空过：runner 对 createOpen/executeOpen/createClose/executeClose/open/close/wholeFlow 七组 Δ 均跑 checkConservation（scn-009-runner.ts:775-781），推导层按 TX1-TX4+全流程出守恒行；ledger.mjs:134-142 缺任一槽位判 UNVERIFIABLE，buildConservationRow:92-94 要求 status==PASS 且 sum===0n 且五项俱全才 PASS——缺读数不会冒充守恒成立（formulas §1）
- 五方账户圈与 LP 槽口径正确：CONSERVATION_SLOTS 为 Trader/OrderVault/PositionVault/LPVault/FeeHandler，LP 槽读 totalAssets() 非 balanceOf（ledger.mjs:57，符合 traps §9 与 VaultBase.totalAssets）；第五方实读 FeeHandler 余额且显示名已改 ΔFeeHandler（execution-evidence.ts:60-69，§10 ✅ 项在工作树确认），守恒行 note 正确说明 claimable 与 ERC20 两套账不相加
- claimable 三槽用 v0.3.1 带 feeType 的 key 签名独立采读（ledger.mjs:63-69 引 claimableFeeAmountKey(mi, token, feeType)，符合 FX100Keys.sol:1033-1038 与 traps §1 跨版本陷阱），LIQUIDATION 独立槽已补齐避免空过
- OI 双账本 USD 原值侧覆盖到位：cumulativeOpenCosts Δ=±inputSizeUsd（margin×leverage 独立推导，非抄事件）、全平回退断言（runner:585-592 + restored 行）、对侧 short OI/开仓成本 Δ=0 行均在（formulas §2 的原值侧与对侧不变要求）
- 执行价不利侧方向断言双端齐全：开多 executionPrice≥oracle.max、平多 ≤oracle.min（runner:631-644 + evidence 行）；执行价公式行（ceil(max×(1e18+spread)/1e18) / floor(min×(1e18−spread)/1e18)）与 PositionUtils.getExecutionPriceForIncrease/BaseOrderUtils 的 mulDiv(roundUp)/floor 取整方向一致，且三处均已如实标注【恒等式】不冒充独立重算（§10 ✅ 复核确认）
- 多头开仓 size→token 复算与合约一致：OI 行 sizeUsd/executionPrice 向下取整 == PositionUtils.sol:636 长侧分支；全平 decreaseTokens = oldSizeInTokens×dUSD/oldSizeUsd 在 dUSD==oldSizeUsd 时精确等于恒等分支（PositionUtils.sol:524），SCN-009 两段均属正确格
- 手续费独立复算链路完整：positionFee 两次独立 floor 同序（formulas.ts calculatePositionFee: 先 ×factor/1e30 再 /colMin，符合 formulas §6 '合并分母会差 1 wei'）；factor 档位按 balanceWasImproved 对执行区块 DataStore 值核对；receiver/pool 分账用链上 positionFeeReceiverFactor(5e29) 独立复算；claimable position fee 入账与 DataStore 快照交叉核对；protocol-fee 与 total-cost 两行已标注【恒等式】（§10 ✅）
- Funding 单仓结算取整方向正确：应付 ceilDiv(Δneg×sizeInTokens, 1e30×colPrice.min)、应收 floor(Δpos×sizeInTokens/(1e30×colPrice.max))（execution-evidence.ts:763-768），与 formulas §5 付⌈/min⌉收⌊/max⌋一致；funding factor 复算含 floor/base/clamp 与零 OI no-op 分支（calculateFundingFactors），EMA 值取链上 FUNDING_SKEW_EMA 快照而非离线重放，符合声明的 scope 边界
- 全平支付瀑布 ΔTrader 复算完整且 0 容差对钱包实测差：oldMargin + positiveFunding + ⌊盈利/colMax⌋ − negativeFunding − ⌈亏损/colMin⌉ − 费用exFunding（execution-evidence.ts:352-371），盈亏两处取整方向与 formulas §7 唯一两处真实舍入一致，Expected After 与 trader USDC 实际余额逐 wei 比对
- uncappedPnl 用事件执行价独立重算（sizeDeltaInTokens×executionPrice−sizeDeltaUsd，非 oracle 原价，符合 traps §8），PnL Cap 行因缺 poolPnl/maxPnlFactor 如实 NOT_VERIFIED 不倒推 PASS
- 静默取消的 vacuous-pass 防线存在：开仓后 requireValue(仓位存在) 硬断言 + before+1..afterClose 全窗口内'开/平各恰好一条 OrderExecuted'唯一性断言（runner:503、594-609），满足 traps §5 '预期成功用例必须带仓位确实创建的硬断言'；缺的只是取消/冻结分诊（见 cov-03）
- Grace 核对双层齐全：runner 用执行区块参数快照独立复算 graceEnd=graceStart+graceBase×tier/1e18 并断言（runner:513-534）；推导层缺参数时输出 NOT_VERIFIED 行而非省略或伪造（§10 ✅ 确认），首仓即计时语义被'首次开仓 Grace'断言覆盖
- 交易真实性证据完备：4 笔交易 from 归属（trader/keeper）、r/s 非零签名、回执 status==1 全部断言（runner:655-684），推导层 buildTransaction 再独立校验一遍格式与签名并纳入 executionStatus 门槛
- CALCULATED 展示行（Long/Short OI USD、raw skew、skewImpact）expected 列已改为'—（派生展示行，无独立期望值）'占位，不再回显 actual 自身（§10 ✅ 确认）；trader-usdc-final 已标注【守恒推论】并指向 TX2/TX4 的独立 ΔTrader 建模行
- TX1/TX3 创建阶段 13 个账本槽位全部有显式 0-Δ 期望及理由（buildLedgerDeltaExpectations create-order/create-decrease-order 分支），创建阶段快照钉在创建交易块（runner:478、551）避免 Service Keeper 紧接执行覆盖挂单态
- 证据选定：最新可用 SCN-009 PASS run = artifacts/runs/2026-08-06T152538-445Z（status PASS，含 executionEvidence 93 行 reconciliation + 原始证据 attachments/SCN-009-tx-fork-r0-0-scn-009-evidence.json，与 artifacts/latest 及 recovered/ 同源）；快照块 45042778/45042780/45042782，事件与回执齐全，可完整重放
- 五方守恒 ΣΔ 重放一致：open(−10000000+0+9994995+5005+0=0)、close(9681257+0−9987468+306211+0=0)、wholeFlow(−318743+0+7527+311216+0=0)，且 deltas.* 全部字段独立从 before/afterOpen/afterClose 快照重建逐 wei 相等（30 项全对）——rows openConservation/closeConservation/wholeFlowConservation 的 PASS 与复算一致
- 开仓执行价重放一致：⌈60060000000000000×(1e18+5100032065091026)/1e18⌉=60366307925829368==事件==row open-execution-price；取整方向与合约 PositionUtils.getExecutionPriceForIncrease 的 Precision.mulDiv(…,true) 逐字核对（多头 ceil）
- 开仓 dynamicSpread 独立复算一致（超出 row 声明的 NOT_VERIFIED 深度）：skewImpact clamp(1e16×0.5e18/1e18, ±5e15)=5e15 + constant 1e14 + depth 线性项 ⌊⌊50e30×1e18/askDepth⌋/100⌋=32065091026（exp 项 1603254552615<线性项 3206509102661，max 取线性）=5100032065091026==事件值；与 PositionPricingUtils.getDynamicSpread/getPriceImpactSpread/getSkewImpact 实现逐段对照
- 平仓执行价重放一致：close dynamicSpread=0（skewImpact −5e15 + 1e14 + depth <0 → 合约 dynamicSpread≤0 归零分支），execPrice=⌊60000000000000000×1e18/1e18⌋=index.min==事件==row close-execution-price；平多 floor 方向与 getExecutionPriceForDecrease 一致
- Position Fee 两次 floor 重放一致：开仓 ⌊⌊50e30×2e26/1e30⌋/999e21⌋=10010（档位 balanceWasImproved=false → POSITION_FEE_FACTOR(false)=2e26 与执行块 DataStore 值互证）；平仓 ⌊⌊50e30×1e26/1e30⌋/999e21⌋=5005（improved=true → 1e26）；与 PositionPricingUtils.getPositionFeesAfterReferral:462 双 floor 同序；calculatePositionFee 的 a*b/1e30/colMin 左结合两次整除保留了双 floor
- 5e29 分账重放一致：开仓 receiver=⌊10010×5e29/1e30⌋=5005 / pool=5005；平仓 receiver=⌊5005×5e29/1e30⌋=2502 / pool=2503（余数归池）；与 getPositionFeesAfterReferral:531 applyFactor + 差值口径一致；rows open/close-fee-receiver、pool-fee 全对
- sizeInTokens 开多格重放一致：⌊50e30/60366307925829368⌋=828276595306007==事件 sizeDeltaInTokens==afterOpen OI/仓位快照；全平走合约恒等分支精确清零（getDecreaseOrderSize sizeInUsd==rawSizeDelta 分支），row open-position-sizeInTokens 与 close 清零行与复算一致
- trader 各阶段 Δ 与最终 USDC 重放一致：开仓 −(collateralDelta 9989990 + fee 10010)=−10000000；平仓支付瀑布（③funding 20 → ④亏损 ⌈303404281639580000000000000000/999e21⌉=303708 → ⑤fee 5005 → ⑥全平余额 9681257）output=9681257==ΔTrader==row close-ledger-traderUsdc；最终 10000000000−10000000+9681257=9999681257==row trader-usdc-final（该行已正确标注【守恒推论】）；瀑布顺序与 DecreasePositionCollateralUtils.processCollateral 一致
- PnL 重放一致：828276595306007×60000000000000000−50e30=−303404281639580000000000000000==事件 basePnlUsd/uncappedBasePnlUsd==row close-uncapped-pnl；全平时 delta 式与合约全仓式恒等（PositionUtils.getPositionPnlUsd）
- claimable position fee 入账重放一致：开 +5005、平 +2502、全程 7507==DataStore claimable 快照 Δ==rows open/close-claimable-position-fee；claimableFeeAmountKey 3-arg（含 feeType）v0.3.1 签名正确性由非零读数与独立期望逐 wei 吻合反证（错 key 会静默读 0）
- funding 金额重放一致：仓位应付 ⌈828276595306007×23885363273401826484018228576294654236914/(1e30×999e21)⌉=20==事件 negativeFundingFeeAmount（MarketUtils.getFundingAmount roundUp+min 价亲核）；positionPaysLp floor 口径=19==Δclaimable(FUNDING)（MarketUtils.updateFundingState:583-596 亲核：>0 记 claimable、<0 LPVault transferOut）；1 wei 尘埃留 PositionVault 且被守恒式吸收
- funding factor 行重放一致：close long=clamp(3472222222222222222222+42672606957128361237950×1e18/1e18)=触 max 30606957128361237950279、short=触 min −4247209538305428716387，均==Funding 事件；执行块末读 fundingEma.lastEmaValue 恰等于合约本笔所用 EMA（ExponentialMovingAverage.saveValue 后同时间戳 emaValue()==lastEmaValue，亲核）——该复算在快照真钉执行块的前提下成立
- 开仓 collateral 公式与合约一致：expectedOpenCollateral = before + inputMargin − totalCost + positiveFunding = 9989990（IncreasePositionUtils.processCollateral:239-240 亲核）==事件==仓位快照
- close ΔLPVault/ΔPositionVault 期望建模与实测一致：ΔLP=poolFee 2503+亏损 303708=306211；ΔPosVault=−(payout+poolFee+loss)=−9987468；与 v031-facts §1 费用路由三段（协议费只记 claimable 留 PositionVault、LP 份额当场物理转、亏损 posVault→LPVault）互证
- 账本槽位圈定正确：LP 槽读 totalAssets() 非 balanceOf、五方守恒圈含 FeeHandler（balanceOf(0x009559c1B221243939f4DfbBB036fC87883c452c)，与合约仓 deployed_addresses.json 及 v031-facts §4 一致）、claimable 与 ERC20 两套账未混加（ledger.mjs:13/52-70）
- 2026-08-07 修复批次已在当前工作树生效并核实：ΔFeeHandler 改名（槽位 key 兼容保留）、守恒行独立分组'守恒'、执行价/Protocol Fee/Total Cost 共 4 处【恒等式】标注、trader-usdc-final【守恒推论】标注、CALCULATED 行 expected 改'—（派生展示行…）'、Grace 缺参数改输出 NOT_VERIFIED 行（git diff 逐段核对）
- Grace 行重放一致：graceEnd=1786008537+⌊0×1e18/1e18⌋=1786008537==仓位快照；graceBase=0/tierMultiplier=1e18 取自执行块链上参数
- orderType 枚举使用正确：开仓事件 orderType=0(MarketIncrease)、平仓=2(MarketDecrease)，与 src/order/Order.sol FX100 顺序一致（非 GMX 序）

## 五、审查 agent 备注

- 审查对象为当前工作树 execution-evidence.ts（含 2026-08-07 修复批次）。实测锚点：artifacts/runs/2026-08-06T152538-445Z（最新 SCN-009 PASS，legacy 三快照格式 → 走 create-and-increase/create-and-decrease 合并阶段；四阶段 create/execute 拆分路径按当前 runner 代码静态核对+合约锚点验证，尚无带 afterCreateOpen/afterCreateClose 的实跑证据，建议下次实跑后复核 TX1/TX3 拆分行）。全部 13 槽在 open/close/wholeFlow 三窗口逐 wei 手算复现，无一偏差；两处取整方向错误（ledger-01/02）均为共享构造器的潜伏缺陷，SCN-009 因全平恒等分支与多头方向而未暴露。守恒式本身（对取整免疫的总闸）实现与手册完全一致。
- 审查维度：buildFeeAndFundingRows（TestCode/src/reporting/execution-evidence.ts L726-928）及其依赖 formulas.ts / chain-parameter-snapshot.ts / ledger.mjs keys.mjs，合约锚点均在 Github/fx100-contracts@release-v0.3.1/src 逐函数复核（PositionPricingUtils.getPositionFees/getPositionFeesAfterReferral、MarketUtils.getFundingAmount/getNextFundingAmountPerSize/updateFundingState、FeeUtils.incrementClaimableFeeAmount、IncreasePositionUtils/DecreasePositionCollateralUtils、ExponentialMovingAverage、Precision、FX100Keys）。实测抽算用 TestCode/artifacts/latest/attachments/SCN-009-tx-fork-r0-0-scn-009-evidence.json（open/close 两相位的 positionFee、receiver/pool 切分、negativeFunding ceil、funding factor 双 clamp、claimable 三槽推进全部逐 wei 复现）。结论：本组行的公式与取整方向全部正确，主要风险集中在证据锚定与快照钉块（feefund-01 已知/02/03/04），不在算式本身。次要观察（未单列 finding）：expectedNegative 用 maxBigInt 把 latest<position 的负 Δ 钳 0——该情形在合约会 uint 下溢 revert，只在快照错块时出现，且 actual 通常非 0 仍会 FAIL 暴露，防御性可接受；net-funding 行是相邻两行的派生（两行同时反向错时可假 PASS，但属冗余展示行）；positionFeeCalculation 的 colPrice.min 取自事件（oracle 签名价独立重放属声明的 scope 边界，EVENT_ANCHORED 合理）。
- 审查范围：执行价 / OI-Skew-Spread / PnL / 市场账本行（buildPricingRows + deriveScn009Evidence 顶层行 + buildLedgerDeltaExpectations 的 OI 部分）。核对基准：fx100-contracts@release-v0.3.1 源码（PositionUtils.sol、PositionPricingUtils.sol、BaseOrderUtils.sol、Precision.sol、MarketUtils.sol、Price.sol 函数级复核）+ 手册 formulas.md/traps.md/v031-facts.md。真实证据重放用 TestCode/artifacts/latest/attachments/SCN-009-tx-fork-r0-0-scn-009-evidence.json（legacy 三快照路径；per-transaction 五快照路径亦走读）。总结论：执行价与 spread 分解的公式层是对的（含 1 wei 级取整方向），SCN-009 当前数据下所有本维度 PASS 行的期望值均可独立复现；发现的问题集中在（a）skew 复算未复刻开仓预备 tokenDelta（pricing-01，对本份 0-OI 证据不可见但公式层错位）、（b）共享构造器对空头/部分平的取整矩阵缺失（ledger-01，SCN-009 未触发）、（c）方向核对的 claim-vs-check 错位（position-01）、（d）已知快照/闸门结构问题确认影响本维度（snapshot-01、gate-01，known:true）。其余 traps §10 ⬜ 项（OrderCancelled/reasonBytes 解码、executionFee/WNT、pass() 无 raw 差值字段、check() fail-fast）不属本维度或为全局结构项，未重复立项；runner 的执行价方向断言（≥max/≤min）与『开仓后为多头』断言存在于 assertions 层，是 position-01/coverage 判断时已计入的缓解因素。
- 审查范围：scn-009-runner.ts（当前工作树）+ ledger.mjs + scn-009.spec.ts，辅以 events/scenario/actions/flows/keys/keccak/rpc.mjs 与 chain-parameter-snapshot.ts；合约锚点在 fx100-contracts@release-v0.3.1 源码逐一复核（IncreaseOrderUtils / PositionUtils.getExecutionPriceForIncrease/Decrease / PositionEventUtils / OrderEventUtils / FX100Keys）。总体结论：快照工具层（ledger.mjs）与事件解码层口径正确、纪律到位；runner 层的结构性短板集中在『算了不断言』（守恒、traderUsdcDelta）与『after 快照取 latest』两族——§10 四项 ⬜（latest 快照、无取消判别、WNT 零覆盖、fail-fast）全部核实仍在且均实际影响 SCN-009，另发现 7 项新问题，其中守恒不作断言（runner-01）为最高优先级：它使手册定义的总闸在测试判定中完全失效。未读取任何私钥类文件。
- 三方对照结论：SCN-009 的 T0/链上侧核对骨架（守恒、账本 Δ、仓位字段、执行价方向、fee/funding 复算、瀑布、Grace、签名真实性）覆盖良好且证据等级标注诚实；主要缺口集中在 (a) 已知结构性问题（快照取 latest、取消分诊、executionFee、质量闸门——traps §10 ⬜ 全部核实确影响 SCN-009，标 known:true）；(b) 新发现的覆盖缺口：订单参数回读、旁观市场 OI、缺读数被推导层兜 0、Entry 均价口径、positionPaysLp 金额恒等式、余额前置；(c) 共享构造器里两处与合约取整/口径不符的潜伏公式错（空头开仓 ceil、多头部分平 ceil、balanceWasImproved 预备值口径）——对 SCN-009 本身（多头+全平恒等分支）零影响，但会命中复用该构造器的后续用例。另：oiTokensΔ===position.sizeInTokensΔ 的直接恒等断言缺失，但两者分别对事件字段与独立派生值核对，多头 USD 模式下等价，未单列 finding。页面/钱包/三组价格链路为已知 pending（coverage.pending 如实记录）。
- 证据与状态：使用 artifacts/runs/2026-08-06T152538-445Z（SCN-009 PASS，tx-fork-private-key，mock-market #29，10 USDC/5x/50 USD 开多→全平）；attachments 的原始证据 JSON 与 recovered/、latest/attachments 同一批。重放脚本（BigInt 同序，71/71 通过）在 scratchpad/replay.mjs。结论：本次 run 的全部 PASS 行数值均与独立复算逐 wei 一致，无 status=PASS 但复算不一致的行；公式文本与合约实现在 SCN-009 覆盖的多头+全平路径上一致。新发现集中在共享构造器的『多头+全平』隐含假设（matrix-01/pricing-01/pnl-02）——当前两个场景恰好都落在正确格子里，属未来空头/部分平仓场景的假 FAIL 地雷；traps §10 的 ⬜ 项逐一核实均真实存在且与 SCN-009 相关（标 known:true），其中 latest 快照与 NOT_VERIFIED 不阻断两项影响最大。注意：results.json 内嵌的 reconciliation 行由 8-06 的旧版推导层生成（行 id/分组为旧命名），本审查以当前工作树代码 + 原始证据 JSON 为准双向核对。
