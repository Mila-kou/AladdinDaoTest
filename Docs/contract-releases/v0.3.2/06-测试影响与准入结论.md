# 06 测试影响与准入结论 — fx100-contracts release/v0.3.2

> 整理日期：2026-08-21（本文所载编译/测试实测与工作区现状核查执行于 2026-08-21，逐项注明，不追溯伪记）。
>
> **证据锚点约定**：本文所有结论均附「文件::函数/符号」锚点，路径相对合约仓根（主测仓 `Github/fx100-contracts@release-v0.3.2`，HEAD `13880f2`；对比基线 `Github/fx100-contracts@release-v0.3.1`，HEAD `d9a7fd2`；diff 范围 `d9a7fd2..13880f2`，27 提交 / 61 文件，冻结记录见 `00-版本证据.md`）。「未变化」结论以该 diff 范围为证据。工作区路径（TestCode/TestCase）相对 `/Users/milakou/Documents/FX100`。
>
> **输入底本**：`00-版本证据.md`、`01-代码变化分析.md`、`02-部署与升级手册.md`、`03-合约功能说明.md`、`04-参数目录.md`（§6 变化清单 11 项）、`05-重要参数边界场景.md`（38 条卡片）、`contract-fields-and-calculation-rules.md`（2026-08-19）。已有交付物只引用不重复。
>
> **状态纪律**：结论只允许 `READY_FOR_SYSTEM_TEST` 或 `NOT_READY`（deliverables.md 第 06 项），不使用「基本完成」类措辞掩盖缺口。资金/参数值一律整数原始值 + 精度标注；v0.3.2 部署值一律「待部署确认」。

---

## 0. 准入结论

# **NOT_READY**

一句话结论：**合约仓自带检查全部通过（forge build/test 退出码 0，85/85），但 v0.3.2 既无部署、无 v0.3.2 环境、无一条以 v0.3.2 为基线执行的边界/回归用例，台账零回填，升级路径无脚本——系统回归的前置条件不成立。**

| # | 阻塞事实 | 证据 |
| --- | --- | --- |
| 1 | v0.3.2 工作区内无任何部署记录，`04-参数目录.md` 全部「当前部署值」= 待部署确认，`05` 全部 38 条场景实际结果 = 待执行 | `00-版本证据.md` 部署网络行；`04-参数目录.md` §7-1；`05-重要参数边界场景.md` §12-1 |
| 2 | 三个测试环境均不能充当 v0.3.2 证据：tx-fork(99911)/oracle-fork(99912) 为 **v0.3.1 部署**的 Tenderly 镜像（status=ready），time-fork 登记 `pending-initialization` 且 chainId 仍为 84532（未按独立 fork chainId 约定初始化）——组 5 资金费时间推进类场景无处执行 | `TestCode/config/mock-resources.json`（2026-08-21 读取）；`02-部署与升级手册.md` §18.3 |
| 3 | TestCode 15 条 SCN spec 的全部历史批次 release 标签无一为 v0.3.2（73 个批次：`release-v0.3.1` ×36，其余为 FX100-时间戳标签）；`TestCase/E2E/SCENARIO-CHECKLIST.md` 2026-08-21 已回填 14 PASS / 1 FAIL（全部为 v0.3.1 fork 基线证据，头部口径说明已标注「不充当 v0.3.2 回归材料」；v0.3.2 条目仍为 0） | `TestCode/artifacts/run-batches/*/run.json`（2026-08-21 统计）；`TestCase/E2E/SCENARIO-CHECKLIST.md` 头部 |
| 4 | v0.3.1→v0.3.2 原位升级全路径仓库内无脚本（仅 02 手册手工步骤）；旧子账户授权迁移方案未定；keeper/前端/SDK/索引器适配均未获确认 | `02-部署与升级手册.md` §0/§13/§19-1/4/5/9 |
| 5 | 合约仓 test/ 覆盖面薄：19 个 .t.sol 文件（18 套件）/85 个测试函数，不等同全量回归；1,944 条账本宿主在 `docs/testing-standards` fork 分支（该分支引用已按工作区约定作废）——**2026-08-21 裁决：暂不纳入**，v0.3.2 测试范围显式接受「合约仓自带 85 函数 + TestCode 15 spec」覆盖面（§3.4） | 本文 §3.4；工作区 CLAUDE.md 分支禁用约定；结构审计 2026-08 记录 |

已就绪的信号面（不改变结论，但说明 NOT_READY 的解除路径是清晰的）：00~05 六份交付物齐备且交叉一致（例外见 §1.2）；`01` 的每个 P0 材料性变化在合约仓 forge 套件内均有直接命中的新增测试且全部通过（§2 矩阵「forge 映射」列）；05 已给出 38 条带精确整数期望值的边界场景与 SCN 挂靠方案，执行只差环境。

---

## 1. 交叉核对记录（00~05 贯通）

### 1.1 一致性核对表

| 核对项 | 结论 | 证据 |
| --- | --- | --- |
| 00 ↔ 01：HEAD/基线/diff 规模 | 一致（13880f2 / d9a7fd2 / 27 提交 61 文件 +1,997 −814） | `00-版本证据.md` 表；`01-代码变化分析.md` §1 |
| 01「验证结果：Foundry 测试通过退出码 0」 | 与本文 2026-08-21 实测一致（§3.2） | 本文 §3.2 |
| 01 §12 升级检查清单条目数 | **实核 14 项**（编排任务口径「15 项」系笔误，以文档为准）；`02` §13.1 亦按 14 项逐项展开，两文一致 | `01-代码变化分析.md` §12；`02-部署与升级手册.md` §13.1 |
| 04 §6 变化清单（11 项）↔ 05 §1.4 映射总表 | 11 项全部有落点：#1~#9/#11 各有卡片组；#10（`SWAP_ORDER_GAS_LIMIT` 死代码清理）明确豁免且理由成立（`04-参数目录.md` §3.10：两版运行时等价，v0.3.1 唯一引用 `src/gas/GasUtils.sol::estimateExecuteSwapOrderGasLimit` 本身无调用方） | `04-参数目录.md` §6；`05-重要参数边界场景.md` §1.4 |
| 05 覆盖维度 ↔ deliverables.md 第 05 项门槛 | 阈值三点/零值/最小非零/最大/越界/组合/正负/取整/选价/角色/开关/Oracle/资金不足/存量升级迁移全覆盖；Oracle 异常以回归映射处理（diff 范围内 `src/oracle/` 无行为改动） | `05-重要参数边界场景.md` §1.3 |
| 02 §18.3 将 forge build/test 执行责任移交 06 | 本文已执行并如实记录（§3） | 本文 §3 |
| 04 抽样部署参考值 ↔ 安全导出快照 | 04 自证「抽样值已与快照逐字核对」，本文未重复核对（快照文件仅允许来源已在 02 §15 固定） | `04-参数目录.md` §7-2；`02-部署与升级手册.md` §15 |
| 05 场景卡片总数 | 38 条（组1×9、组2×2、组3×4、组4×5、组5×4、组6×2、组7×2、组8×2、组9×4、组10×4），与其 §12-1「全部 38 条」自述一致 | `05-重要参数边界场景.md` §2~§11 |

### 1.2 贯通检查发现的缺口与不一致（本文新增发现）

| # | 发现 | 处置 |
| --- | --- | --- |
| G1 | **01 §8.3 单位修复**（剩余成本把 token 数量当 USD 记账，修复为乘抵押品价格后记 USD）未进 04 §6 清单（合理：非参数变化），也无 05 卡片、无 SCN 映射——属材料性行为变化中唯一无显式测试映射者 | 本文补登矩阵行 R15，映射到合约仓减仓套件回归（已通过）+ execution-evidence 对账层（待执行），并列入 §5 解除条件 C-4 |
| G2 | **01 §8.5 输出结构简化**（secondary output token/amount 字段删除、事件结构变化）无测试场景，且 `03` gap-4 声明事件字段级 schema 未展开——索引器适配面无可执行核对物 | 矩阵行 R17（人工适配核对）；§5 条件 C-4 |
| G3 | **01 §8.4 强平判定修复组**（清算用成交价规则、`balanceWasImproved` 传递、R5-B02 费档）在 05 仅隔离了负点差维度（B32-1-07），完整清算费用账本断言委托 SCN-068/SCN-079 与核对层（05 gap-7）但两者均未回填、断言未成文 | 矩阵行 R16 标注「E2E 断言未展开」；合约仓侧 `test/integration/Liquidation.t.sol` 11 条已通过兜底（§3.2） |
| G4 | 锚点表述不一致：02 §13.1 #6 写「`BaseRelayRouter.sol::_validateRelayFee` 段 L198-204」，04 §2.3/05 组 3 写 `_validateCall`。**已按源码裁决：校验位于 `src/router/relay/BaseRelayRouter.sol::_validateCall`（函数起 L189，fee token 检查 L198-204，revert `FxErrors.UnexpectedRelayFeeToken`），后续统一用 `_validateCall`**（2026-08-21 源码复核） | 记录在案，不需改动结论；02 后续修订时更正措辞 |
| G5 | time-fork chainId 口径：05 §1.2 按 mock-resources.json 登记写 84532（fork 保持原链 ID），而工作区技能约定预期独立 chainId 99913。当前登记 status=`pending-initialization`——**初始化时必须裁决 chainId 约定并回填登记**，否则用例-环境绑定校验会歧义。**2026-08 裁决：按 99913**（`config/environments/catalog.ts` fixedChainId + CURRENT.json environments.time-fork），并提供 `npm run env:vnet:create -- --env time-fork` 一键创建（Virtual TestNets API，自动回填与登记） | §5 条件 A-4 |
| G6 | 05 新增建议编号（SCN-B32-01~08、IT-VAULT-001~003、IT-MARKET-001）未回填 SCENARIO-CHECKLIST（仍 80 条）与 TestCase 域级目录，收编方式待裁决——**本文建议**：SCN-B32-01~08 按 05 §1.4 建议挂靠 S03/S05/S06 追加登记（不改动原 80 条编号）；IT-VAULT/IT-MARKET 落 TestCase 域级目录 | **2026-08-21 已收编**：SCN-B32-01～08 → `TestCase/E2E/SCENARIO-CHECKLIST.md` 文末「v0.3.2 增补场景」章节（头部统计追加「增补：8」，原 80 条不动）+ `TestCase/E2E/scenarios/S03-仓位管理与退出.md`（SCN-B32-05/06）/ `S05-长持费用价格冲击与多市场.md`（SCN-B32-01/02/07）/ `S06-Flash异常恢复与兼容.md`（SCN-B32-03/04/08）末尾「v0.3.2 增补（SCN-B32）」小节；IT-VAULT-001～003、IT-MARKET-001 → `TestCase/E2E/case/IT-v0.3.2-VAULT-MARKET.md`（`TestCase/E2E/README.md` 分层表已登记 case/ 目录）。注：05 §1.4 未分配 SCN-B32-02，收编时按表序补位为 R2-B10 封顶（B32-2-01/02，挂 S05，与 SCN-049 共用证据），待确认。§5 条件 B-1 已勾；全部实际结果=待执行，执行归 B-2 |

---

## 2. 变更 → 测试追踪矩阵

### 2.1 阅读说明

- **行来源**：`04-参数目录.md` §6 的 11 项材料性变化 + `01-代码变化分析.md` 中未进 04 §6 的行为/适配变化（R15~R19，§1.2 交叉核对补登）。每个材料性变化至少一条测试映射；无映射者显式标「缺口」并给补齐方案，不留空白行。
- **测试映射**：`B32-*` = 05 边界场景卡片；`SCN-xxx` = 既有 80 条用户级场景（`TestCase/E2E/SCENARIO-CHECKLIST.md`）；`SCN-B32-xx`/`IT-*` = 05 提出的新增建议编号（2026-08-21 已收编进 TestCase，见 G6 处置列）；`test/...` = 合约仓 forge 套件（本文 §3.2 已实测通过者标 ✅）。
- **自动化/人工**：`forge` = 合约仓单元/集成（已可执行）；`E2E-auto` = TestCode Playwright（有 spec 者注明，无 spec 为待编写）；`人工` = 手工执行或适配核对。
- **环境**：tx-fork(99911)/oracle-fork(99912)/time-fork(待建)；**全部 fork 现为 v0.3.1 镜像，v0.3.2 部署后须重建方可作为证据**（`02` §18.3）；`部署前置` = 需真实 v0.3.1→v0.3.2 升级路径。
- **能力**：M = Market 侧（CONFIG_KEEPER/CONTROLLER 写参、受控 OI、Strategy mock、角色分配）；O = Oracle 侧（单价/双价/推价）；T = 时间推进。
- **状态词汇**：`forge✅` = 2026-08-21 实测通过；`待执行` = 用例已设计未运行；`待编写` = 尚无 spec/用例文本；`阻塞(部署)`/`阻塞(time-fork)` = 前置环境不存在；`缺口` = 无显式映射，本文补登方案；`适配待确认` = 非测试类核对项未闭环。

### 2.2 矩阵

| # | 材料性变化（锚点） | 05 卡片 / SCN 映射 | forge 映射 | 自动化/人工 | 环境 | 能力 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | `MIN/MAX_DYNAMIC_SPREAD` 新增 int256 clamp（`src/constants/FX100Keys.sol::minDynamicSpreadKey/maxDynamicSpreadKey`；`src/pricing/PositionPricingUtils.sol::getDynamicSpread`；04 §6 #1/#2） | B32-1-01~05、B32-1-09 → SCN-B32-01（挂 S05）；部署核对必核项（B32-1-01 漏配即点差恒 0 事故） | `test/integration/ExecutionPriceUsdOrder.t.sol` ✅、`ExecutionPriceTokenOrder.t.sol` ✅（回归面） | forge + E2E-auto（待编写）+ 人工（部署读值核对 `DataStore.getInt` ×市场×方向） | tx-fork（重建后）；漏配事故形态需部署前置 | M（CONFIG_KEEPER setInt、角色三账户）；O 单价 | P0 | forge✅；边界待执行；部署核对阻塞(部署) |
| R2 | 负点差 + 清算/ADL floor-at-0（`PositionPricingUtils.sol::getDynamicSpread` allowNegativeSpread；`src/position/PositionUtils.sol::getExecutionPriceForDecrease`；04 §6 #3） | B32-1-06/07/08 → SCN-050（解除原 PENDING 标记）、SCN-068（新增负点差隔离数据集）、SCN-070、SCN-048、SCN-019 | `ExecutionPriceUsdOrder.t.sol` ✅、`Liquidation.t.sol` ✅ | forge + E2E-auto（SCN-070 已有 spec `TestCode/tests/S07/scn-070.spec.ts`，需 v0.3.2 环境重跑；其余待编写） | tx-fork + oracle-fork（推价/双价） | M 受控 OI；O 双价+推价 | P0 | forge✅；E2E 待执行 |
| R3 | R2-B10 大单 price impact 指数封顶（`PositionPricingUtils.sol::getPriceImpactSpread`，`LogExpMath.sol::MAX_NATURAL_EXPONENT`；04 §6 #3） | B32-2-01/02 → 并入 SCN-049 三点数据集 | `PositionPricing.t.sol` ✅（定价面回归） | forge + E2E-auto（待编写） | tx-fork | M 浅深度 fixture；O 单价 | P1 | forge✅；边界待执行 |
| R4 | 全局 `COLLATERAL_TOKEN` + Relay fee token 强约束 + `SetCollateralToken` 部署步骤（`FX100Keys.sol::COLLATERAL_TOKEN`；`src/market/MarketFactory.sol::createMarket`；`src/router/relay/BaseRelayRouter.sol::_validateCall` L198-204；`ignition/modules/Fx100Role.ts`；04 §6 #4/#11） | B32-3-01~04 → SCN-B32-03（挂 S06）+ IT-MARKET-001；02 §18.1 smoke 首项 | `RelayCreateOrder.t.sol` ✅（含 `UnexpectedRelayFeeToken` 断言 L114-123）、`SubaccountRelayCreateOrder.t.sol` ✅ | forge + E2E-auto（待编写）+ 人工（部署读值 `DataStore.getAddress(COLLATERAL_TOKEN)`） | tx-fork + Relay 签名服务 | M 角色（CONTROLLER 直写/白名单拒绝）；O 无 | P0 | forge✅；E2E 待执行；部署核对阻塞(部署) |
| R5 | 子账户 4 槽位模型 + EIP-712 变更（`src/subaccount/SubaccountUtils.sol::MAX_SUBACCOUNT_SLOTS/addSubaccount/validateSubaccount`；`src/router/relay/RelayUtils.sol::SUBACCOUNT_APPROVAL_TYPEHASH/REMOVE_SUBACCOUNT_SLOT_TYPEHASH`；04 §6 #5） | B32-4-01~05 → SCN-B32-04（挂 S06）；SCN-053/055/057/058 会话层关联回归 | `SubaccountSlots.t.sol` ✅（+164 行新增）、`SubaccountRelayCreateOrder.t.sol` ✅ | forge + E2E-auto（待编写）+ 人工（签名服务新旧 typed data 出签） | tx-fork + 签名服务 | M 无特殊；O 无 | P0 | forge✅；E2E 待执行 |
| R6 | `SUBACCOUNT_LIST` 移除 / 旧授权升级即失效（v0.3.1 `FX100Keys.sol::subaccountListKey` 删除；04 §6 #6；01 §5.4） | B32-10-03 → SCN-B32-08③；人工：产品侧重授权流程（02 §13.1 #5，无迁移脚本） | 无（升级形态不可单元化） | E2E-auto（待编写）+ 人工 | 部署前置 / tx-fork 升级演练 | M 无；O 无 | P0 | 阻塞(部署)；迁移方案适配待确认 |
| R7 | `CLAIMABLE_COLLATERAL_*` 六键 + `claimCollateral` 入口移除（v0.3.1 `src/router/ExchangeRouter.sol::claimCollateral`、`src/config/Config.sol::setClaimableCollateral*` 删除；04 §6 #7） | B32-10-04 → SCN-B32-08④；人工：前端/Keeper/索引器清理核对（01 §12 #7） | 无（纯移除，`cast sig` 比对 ABI 无此 selector 即证） | E2E-auto（待编写）+ 人工 | 部署前置 / tx-fork | M 无；O 无 | P1 | 阻塞(部署)；清理适配待确认 |
| R8 | 资金费逐仓结算 R4-B10（`src/market/MarketUtils.sol::settleFundingFees`(L445)/`updateFundingState`；`src/position/IncreasePositionUtils.sol::processCollateral`、`DecreasePositionCollateralUtils.sol::processCollateral`；04 §6 #8） | B32-5-01~04 → SCN-077（新增「更新指数不结算」反例断言）、SCN-078、SCN-073；SCN-029/030 主路回归 | `FundingSettlement.t.sol` ✅（+300 行新增）、`ShortPartialCloseFundingRegression.t.sol` ✅ | forge + E2E-auto（待编写） | **time-fork（待建）** + tx-fork + oracle-fork 双价 | M 受控 OI；O 双价；**T 时间推进（当前无环境）** | P0 | forge✅；E2E 阻塞(time-fork) |
| R9 | 减仓 minOutputAmount 实际校验 R4-B22（`src/order/DecreaseOrderUtils.sol::_validateOutputAmount`，USD 1e30 口径；01 §8.1） | B32-6-01/02 → SCN-B32-05（挂 S03）；SCN-022 关联（Est. Receive） | `ExecutionPriceUsdOrder.t.sol` ✅、`AutoCancel.t.sol` ✅（含 minOutput 断言） | forge + E2E-auto（SCN-022 已有 spec `TestCode/tests/S03/scn-022.spec.ts`，需扩展 minOutput 断言并在 v0.3.2 环境重跑） | oracle-fork（固定价保证输出可精算） | O 单价固定；M 无特殊 | P0 | forge✅；E2E 待执行 |
| R10 | USD 减仓 ceil 耗尽归一全平 R4-B20（`src/position/PositionUtils.sol::getDecreaseOrderSize`、`src/position/DecreasePositionUtils.sol` 归一分支、`OrderSizeDeltaAutoUpdated` 事件；01 §8.2） | B32-7-01/02 → SCN-B32-06（挂 S03）；SCN-023/032/052 关联回归 | `ExecutionPriceUsdOrder.t.sol::test_LongNearFullCloseThatConsumesAllTokensIsNormalizedToFullClose` ✅（直接命中） | forge + E2E-auto（SCN-023 已有 spec `TestCode/tests/S03/scn-023.spec.ts`，需 v0.3.2 重跑） | oracle-fork | O 单价固定；M 无 | P0 | forge✅；E2E 待执行 |
| R11 | Vault 提款到账校验 R4-B01（`src/vault/VaultBase.sol::_withdrawAssets`、`ErrorStrategyWithdrawalMismatch`；01 §9） | B32-8-01/02 → IT-VAULT-001~003（域级新建）；E2E 冒烟可挂 S06；组合场景 B32-5-04 | `test/vault/VaultBase.t.sol` ✅（+169 行新增） | forge + fork 集成（待编写；需可编程 Strategy mock） | tx-fork + Strategy mock（能力待建） | M Strategy mock 指定转账额；O 无 | P0 | forge✅；fork 集成待编写 |
| R12 | R2-B15 折扣+返佣封顶（`PositionPricingUtils.sol::getPositionFeesAfterReferral`；04 §6 #9） | B32-9-01~04 → SCN-B32-07（挂 S05） | `PositionPricing.t.sol::test_GetPositionFeesAfterReferral_CapsAffiliateRewardAtRemainingFee` ✅（+241 行套件）、`Referral.t.sol` ✅（+90 行） | forge + E2E-auto（待编写） | tx-fork（可设 pro tier/referral） | M CONFIG_KEEPER 写 tier 因子；O 无 | P1 | forge✅；边界待执行 |
| R13 | 每市场 COLLATERAL_TOKEN 孤儿数据（`src/market/MarketStoreUtils.sol::get/set` 改全局键；04 §4 表末行） | B32-10-02 → SCN-B32-08②；02 §13.1 #14 全量键值比对 | 无（升级形态） | E2E-auto（待编写）+ 人工（键值比对，基线 = 02 §15 安全导出） | 部署前置 / tx-fork 升级演练 | M 无；O 无 | P0 | 阻塞(部署) |
| R14 | v0.3.1 存量仓位跨版本首次动作（仓位/funding per-size 键无变化但消费链路全换；04 §3.9） | B32-10-01 → SCN-B32-08①（升级演练必测：漏配 clamp 即落 B32-1-01 事故第一现场） | 无（升级形态） | E2E-auto（待编写）+ 人工 | 部署前置（v0.3.1 状态之上升级） | M 配置齐套；O 单价；T 可选 | P0 | 阻塞(部署) |
| R15 | **单位修复：剩余成本 token→USD**（01 §8.3；提交范围内 `DecreasePositionCollateralUtils.sol` 记账口径）——§1.2 G1 缺口行 | 无 05 卡片；本文补登映射：execution-evidence 对账层（fx100-verify-handbook 守恒/ΣΔ 断言应能捕获 USD 口径错记）+ SCN-022/023 数值核对间接覆盖 | 减仓套件 `ExecutionPriceUsdOrder.t.sol`/`ExecutionPriceTokenOrder.t.sol` ✅（间接） | forge（间接）+ 核对层（待执行） | tx-fork（重建后） | O 单价；M 无 | P1 | **缺口**（无直接断言；解除见 §5 C-4） |
| R16 | 强平判定修复组：清算成交价规则 + `balanceWasImproved` 传递 + R5-B02 费档（HEAD 提交 `13880f2`；`src/position/PositionUtils.sol::isPositionLiquidatable` 调用链；01 §8.4）——§1.2 G3 | B32-1-07（仅负点差隔离维度）→ SCN-068/SCN-079 + 核对层承担完整费用账本断言（05 gap-7，断言未成文） | `Liquidation.t.sol` ✅（11 条，含 fee split/reconcile/grace period）、`FeeTier.t.sol` ✅ | forge + E2E-auto（SCN-068/079 待编写断言） | oracle-fork（推价触发清算）+ tx-fork | O 推价；M 受控 OI；T 保护期可涉 | P0 | forge✅；E2E 断言未展开（部分缺口） |
| R17 | **输出结构简化：secondary output 字段删除、减仓事件结构变化**（01 §8.5；`src/position/PositionEventUtils.sol` 在 diff 清单）——§1.2 G2 缺口行 | 无测试场景；映射：索引器 schema 适配核对（01 §12 #8）+ `03` gap-4 的事件字段级导出补齐后人工比对 | 无 | 人工（适配核对） | 不依赖链上环境（ABI/事件定义静态比对） | — | P2 | **缺口**（适配待确认；解除见 §5 C-4） |
| R18 | ExternalHandler 拒绝重复退款代币（`src/external/ExternalHandler.sol`；01 §10） | 无 05 卡片（域级即可，非用户旅程） | `test/external/ExternalHandler.t.sol::test_revertsForDuplicatedRefundToken` ✅（直接命中） | forge | 本地 forge | — | P2 | forge✅（域级覆盖成立，无需 E2E） |
| R19 | 错误更名 `EmptyTokenTranferGasLimit`→`EmptyTokenTransferGasLimit` 及 selector 变化面（01 §10；02 §14.4） | 无测试场景；映射：监控/前端错误解码表更新核对（人工） | 无（编译期即证） | 人工（适配核对） | 不依赖环境 | — | P2 | 适配待确认 |
| R20 | Reader 槽位读取与资金费口径统一（`src/reader/Reader.sol::getSubaccountSlots/getSubaccountInfo`；01 §10、§3.12） | B32-4 各卡片内 Reader 断言；SCN-021/022 显示值核对关联 | `SubaccountSlots.t.sol` ✅（Reader 断言内含）、`FundingSettlement.t.sol` ✅ | forge + E2E-auto（待执行） | tx-fork | M 无；O 单价 | P1 | forge✅；E2E 待执行 |

**矩阵自查**：04 §6 的 11 项中 #1~#9、#11 对应 R1~R14（#10 豁免有据）；01 独有变化补登 R15~R20。每行均有至少一条映射；R15/R17 因映射为「补登方案」而非既成用例，显式标缺口——这两行与 R16 的「断言未成文」是本矩阵仅有的三处映射弱点，全部进入 §5 解除条件。

---

## 3. 已实测检查记录（2026-08-21 执行）

执行环境：本机 macOS（Darwin 25.5.0），forge 1.7.1（commit `4072e48`，与 `02-部署与升级手册.md` §2 登记一致；仓库未固定 forge 版本，见 02 gap-6）；solc 0.8.29 按 `foundry.toml::solc`；工作树 = `Github/fx100-contracts@release-v0.3.2` @ `13880f2`（执行前 `git log -1` 复核）。

### 3.1 forge build

| 项 | 记录 |
| --- | --- |
| 命令 | `cd Github/fx100-contracts@release-v0.3.2 && forge build` |
| 退出码 | **0** |
| 输出摘要 | 编译成功；仅 forge-lint 告警（`unsafe-typecast` 等），无编译错误 |

### 3.2 forge test

| 项 | 记录 |
| --- | --- |
| 命令 | `cd Github/fx100-contracts@release-v0.3.2 && forge test` |
| 退出码 | **0** |
| 计数 | **18 个测试套件，85 通过 / 0 失败 / 0 跳过**（输出末行：`Ran 18 test suites in 53.68ms: 85 tests passed, 0 failed, 0 skipped (85 total tests)`） |
| 与 01 的一致性 | 01 §1「验证结果：Foundry 测试通过，退出码 0」得到独立复现 |
| v0.3.2 新增/大改套件全部在通过集内 | `SubaccountSlots.t.sol`(+164)、`FundingSettlement.t.sol`(+300)、`PositionPricing.t.sol`(+241)、`VaultBase.t.sol`(+169)、`Referral.t.sol`(+90)、`AutoCancel.t.sol`(+72)、`ExecutionPriceUsdOrder.t.sol`(+99)、`ExternalHandler.t.sol`(+33) 等 13 文件 +1,255 −20 行（`git diff --stat d9a7fd2..13880f2 -- test`） |

### 3.3 未运行项（如实登记）

| 检查 | 状态 | 原因 | 风险 |
| --- | --- | --- | --- |
| `npm run compile`（hardhat 编译） | **未运行** | 仓库 node_modules 未安装（02 §1.1 已核对不存在），`npm ci` 属部署前置动作，本次准入检查不代行部署方安装依赖 | 部署与公证走 hardhat 产物（optimizer runs 200 vs forge 10，02 §2.1），forge build 通过**不能**证明 hardhat 侧编译与 Ignition artifact 生成无误；该风险由 §5 条件 A-1 部署时消解 |
| Ignition 部署演练 / configure 脚本 dry-run | 未运行 | 无部署授权与目标环境；02 §16 的断点续跑行为亦未实测（02 gap-2） | 部署路径首跑即生产首验 |
| 05 全部 38 条边界场景、SCN 回归 | 未运行 | 无 v0.3.2 部署与 v0.3.2 环境（§0 阻塞事实 1/2） | 全部行为变化在真实链路上零验证 |

### 3.4 覆盖面声明（必须如实理解 §3.2 的含金量）

- 合约仓 test/ 共 **19 个 .t.sol 文件 / 85 个测试函数**（`find test -name "*.t.sol"`、`grep -rE "function (test|invariant)" test`，2026-08-21 计数），85/85 通过说明的是**本仓自带回归面**成立，**不等同全量回归**：85 个函数对 61 个变更文件（+1,997 行）而言是每变更点 1~11 条的抽查密度。
- 历史上 fx100 的 **1,944 条账本宿主用例位于 `docs/testing-standards` fork 分支**——该分支及 `test-hub/...` 引用已按工作区约定全部作废（工作区 CLAUDE.md），其资产去向（迁移入主仓 / 在 TestCode 重建 / 放弃）**已于 2026-08-21 裁决：暂不考虑、不纳入 v0.3.2 测试范围**（含 `Github/fx100-contracts@soso-test` 克隆的全部内容，不引用、不迁移、不重建；后续如需重新纳入另行立项）。因此 v0.3.2 的「测试通过」表述**显式只覆盖**合约仓自带 85 函数 + TestCode 15 spec（及本文矩阵后续新增用例）的面，本文其余结论据此理解；该项不再是准入阻塞项（§5 条件 C-3 已按裁决关闭）。

---

## 4. 测试现状登记（2026-08-21 工作区核查）

### 4.1 TestCode 自动化资产

| 项 | 事实 | 证据 |
| --- | --- | --- |
| SCN spec 存量 | **15 条**：S02 ×7（scn-009/010/011/012/013/015/016）、S03 ×4（scn-022/023/024/025）、S07 ×4（scn-065/066/067/070） | `TestCode/tests/**/scn-*.spec.ts` 逐一列举 |
| 执行批次 | 73 个批次目录；`run.json` release 标签统计：`release-v0.3.1` ×36、`dashboard-workflow-check` ×1、其余为 `FX100-YYYYMMDD-hhmm` 时间戳标签；**无任何 v0.3.2 批次** | `TestCode/artifacts/run-batches/*/run.json` |
| 数据核对层 | execution-evidence 对账改造进行中（目标：核对内容与核对公式正确），未以 v0.3.2 公式基线验收 | 工作区 CLAUDE.md 自动化现状（2026-08-11） |

### 4.2 环境

| 环境 | 登记状态 | chainId | v0.3.2 可用性 |
| --- | --- | --- | --- |
| tx-fork | ready（alias default-mock，含 Mock Market Bundle marketIndex 27） | 99911 | **不可用作 v0.3.2 证据**：v0.3.1 部署镜像，须于 v0.3.2 部署后重建并回填登记 |
| oracle-fork | ready | 99912 | 同上 |
| time-fork | **pending-initialization** | 84532（未按独立 chainId 约定初始化，见 §1.2 G5） | **不存在可用实例**；R8（资金费时间推进）整组阻塞 |

证据：`TestCode/config/mock-resources.json`（updatedAt 2026-08-13）。

### 4.3 台账

- `TestCase/E2E/SCENARIO-CHECKLIST.md`：80 条，2026-08-21 已回填 14 PASS / 1 FAIL（头部「PASS：14｜FAIL：1｜待执行：65」），**全部为 v0.3.1 fork 基线证据**（清单头部口径说明①）；v0.3.2 基线执行条目仍为 0；SCN-B32 增补 8 条见其文末章节（2026-08-21 收编，全部待执行）。
- 05 提出的 SCN-B32-01~08、IT-VAULT-001~003、IT-MARKET-001 已于 2026-08-21 收编（§1.2 G6 处置列 / §5 B-1）：清单文末增补 8 条 + S03/S05/S06 增补小节 + `TestCase/E2E/case/IT-v0.3.2-VAULT-MARKET.md`，全部待执行，不改变 NOT_READY。

### 4.4 部署

- v0.3.2：工作区内无部署记录（`00-版本证据.md` 冻结结论）；`04-参数目录.md` 全部「当前部署值」= 待部署确认。
- v0.3.1→v0.3.2 升级：仓库仅有全新部署脚本（`ignition/modules/Fx100Core.ts`）；原位升级为 02 §13 手工路径，无脚本、无回滚脚本（02 §17）。

---

## 5. NOT_READY 解除条件清单（逐条可勾选；全部勾满且无新增 FAIL 方可改判 READY_FOR_SYSTEM_TEST）

### A. 部署与环境（先决层）

- [ ] **A-1 完成 v0.3.2 部署**（全新部署按 02 §4，或原位升级按 02 §13 手工路径），hardhat 侧 `npm run compile` 与 Ignition 执行记录、`ignition/deployments/<id>/deployed_addresses.json`、三个 configure 输入文件入库归档（消解 §3.3 hardhat 编译未验风险）。
- [ ] **A-2 部署核对必核项通过**：`DataStore.getAddress(COLLATERAL_TOKEN)`=USDC=vault.asset()；每市场×方向 4 键 `DataStore.getInt(min/maxDynamicSpreadKey)` 读回与输入一致（**防 B32-1-01 点差恒 0 升级事故**）；02 §18.1 全部七项读检；复跑三 configure 脚本零 `applied`。
- [ ] **A-3 导出 v0.3.2 参数快照并回填 04**：`TestCode/tools/config-dump/dump-config.mjs` 全量导出，替换 04 全部「待部署确认」，并与 `TestCase/E2E/ContractCodeSummary/FX100-合约配置参数总表.csv` 对账（差异仅限预期新键/删除键，02 §13.1 #14 口径）。
- [ ] **A-4 基于 v0.3.2 部署重建三 fork**：tx-fork/oracle-fork 重建 + Mock Market Bundle 初始化；time-fork 完成首次初始化并**裁决 chainId 登记口径**（§1.2 G5）——**time-fork 部分已完成（2026-08-24）**：`env:vnet:create` 建 Virtual TestNet chainId=99913（fork Base Sepolia @45893847，仍为 v0.3.1 部署）、`env:init:mock` + `env:verify:mock` 全部通过；tx/oracle-fork 基于 v0.3.2 部署的重建仍待 A-1，三者回填 `TestCode/config/mock-resources.json`。
- [ ] **A-5 升级操作面裁决**（02 gaps 1/4/10）：原位升级/回滚补脚本或运维书面确认按手册手工执行；bootstrap 权限回收与 ProxyAdmin ownership 目标持有人决策；Pyth provider 启用流程明确。

### B. 用例执行与台账回填（执行层）

- [x] **B-1 新增编号收编裁决**：SCN-B32-01~08 按 05 §1.4 挂靠追加进 `TestCase/E2E/SCENARIO-CHECKLIST.md`（不改动原 80 条编号）；IT-VAULT-001~003、IT-MARKET-001 落 TestCase 域级目录（§1.2 G6 建议）。——**2026-08-21 已收编**：SCN-B32-01～08 落 `TestCase/E2E/SCENARIO-CHECKLIST.md` 文末「v0.3.2 增补场景」章节（8 行 `[ ]`，头部「增补：8」，原 80 条与总数不动）及 `scenarios/S03`（B32-05/06）/ `S05`（B32-01/02/07）/ `S06`（B32-03/04/08）末尾增补小节（7 列六字段，期望值保留 05 推导）；IT-VAULT-001～003、IT-MARKET-001 落 `TestCase/E2E/case/IT-v0.3.2-VAULT-MARKET.md`（README 分层表登记 case/ 目录）。SCN-B32-02 为按 05 §1.4 表序的补位号（R2-B10，与 SCN-049 共用证据）待确认。全部实际结果=待执行，执行与回写归 B-2/B-3。
- [ ] **B-2 执行 05 全部 38 条边界场景并回写实际结果**（期望值须按 A-3 部署真值以同公式重算，不得照抄 fixture 数值——05 gap-2）；组 10 四条随升级演练执行（依赖 A-1 走原位升级形态或 tx-fork 升级演练）。
- [ ] **B-3 矩阵 P0 行的 E2E/SCN 回归在 v0.3.2 环境跑通**：至少含 SCN-048/049/050/068/070/077/078 及既有 15 条 spec 的 v0.3.2 重跑；SCN-068/079 补齐清算费用账本断言（R16 部分缺口）。
- [ ] **B-4 TestCode 批次以 v0.3.2 标签登记**：新批次 `run.json` release 标注 v0.3.2 基线（当前 0 条），看板 rebuild + 核对层按 v0.3.2 公式基线出证。
- [x] **B-5 SCENARIO-CHECKLIST 回填非零**：执行过的场景逐条落状态/证据/执行人/日期，头部统计不再是 80 条全待执行。（**2026-08-21 已完成**：回填 14 PASS / 1 FAIL，均为 v0.3.1 fork 基线证据并在清单头部口径说明中标注；v0.3.2 重跑后按 B-3/B-4 再回填）

### C. 缺口裁决与适配确认（收口层）

- [ ] **C-1 旧子账户授权迁移方案定稿**（R6；02 gap-5）：产品侧确认「自然失效 + 强制重授权」流程与用户通知；升级后抽查 `Reader.getSubaccountSlots` 全空 + 重授权可用。
- [ ] **C-2 外围适配确认**：keeper 服务仓（负点差分路径、费档修正，02 gap-9）、前端/SDK（int256 点差解码、minOutputAmount 1e30 USD 口径警示——05 B32-6-01、claimCollateral 入口删除）、签名服务（新 EIP-712 域与 typehash）、索引器（R17 事件 schema）各出书面确认。
- [x] **C-3 1,944 条账本宿主去向裁决**（§3.4）：迁移/重建/放弃三选一并记录；若放弃，须在测试范围声明中显式接受 85+15 覆盖面。——**2026-08-21 已裁决：暂不纳入**（soso-test / docs/testing-standards 内容不再考虑），85+15 覆盖面已在 §3.4 与 §1 第 5 行显式接受。
- [ ] **C-4 R15/R17 缺口补齐**：单位修复（01 §8.3）在 execution-evidence 对账层落至少一条 USD 口径守恒断言；事件字段级 schema 从 `XxxEventUtils` 导出（03 gap-4）并完成索引器比对。
- [ ] **C-5 Basescan/Etherscan 公证路径补齐或豁免决策**（02 gap-3：有 etherscan 配置但无 hardhat-verify 依赖与脚本）；Node/forge 版本固定（02 gap-6）与 ProtocolTreasury `_disableInitializers` 观察项（02 gap-7）处置记录。

---

## 6. 各交付物 open_gaps 汇总（原文归口，供勾销追踪）

| 来源 | # | 缺口 | 对应解除条件 |
| --- | --- | --- | --- |
| 02 | 1 | 原位升级全路径（LPVault impl、无状态层重部署、角色切换/回收、回滚）无脚本，仅手册手工步骤 | A-5 |
| 02 | 2 | v0.3.2 未部署：部署值、ProxyAdmin/旧 impl 地址待确认；Ignition 命令与续跑未实测 | A-1/A-2 |
| 02 | 3 | Basescan/Etherscan 公证路径缺失（无 hardhat-verify 依赖与脚本） | C-5 |
| 02 | 4 | bootstrap 权限回收与 ProxyAdmin ownership 移交无脚本、无持有人决策 | A-5 |
| 02 | 5 | 旧子账户授权→新 4 槽位无迁移脚本，重授权流程待产品确认 | C-1 |
| 02 | 6 | Node.js 与 forge 版本仓库未固定（无 engines） | C-5 |
| 02 | 7 | ProtocolTreasury 裸实现未 `_disableInitializers`（观察项） | C-5 |
| 02 | 8 | 编译与测试本次（02 整理时）未运行，执行结果归 06 | **已闭环**：本文 §3 已实测（forge build/test 退出码 0；npm compile 未运行原因见 §3.3） |
| 02 | 9 | keeper 服务侧落地材料不在合约仓；负点差分路径与费档修正适配待服务仓确认 | C-2 |
| 02 | 10 | Pyth provider 部署后未启用，无启用脚本；01 清单实核 14 项（15 项系口径笔误） | A-5；14 项口径已在 §1.1 裁定 |
| 03 | 1 | 动态点差上下限、COLLATERAL_TOKEN、清算保护期等具体值全部待部署确认，需回填对账 | A-2/A-3 |
| 03 | 2 | 外部入口清单为静态整理，未做链上/fork 调用级可达性验证 | B-2/B-3（随场景执行自然覆盖） |
| 03 | 3 | RevenuePool/ProtocolTreasury 分账比例与 reward token 属部署期数据，未核对实值 | A-3 |
| 03 | 4 | 事件字段级 schema 未逐字段展开，索引器适配需另行导出 | C-4 |
| 04 | 1 | v0.3.2 部署值全部待确认，部署后 dump-config.mjs 重导快照回填 | A-3 |
| 04 | 2 | v0.3.1 每市场参数参考值仅抽核 market#1/#2，其余以 by-module 快照为准 | A-3 对账时全量覆盖 |
| 04 | 3 | clamp 键未配置点差恒 0 风险仅静态推导，未实测复现 | A-2 + B-2（B32-1-01） |
| 04 | 4 | 全部参数「验证方法」待执行（无部署环境） | A-2/A-3 |
| 05 | 1 | 38 条场景实际结果全部待执行；组 10 四条被部署路径阻塞 | B-2 |
| 05 | 2 | 期望值基于受控 fixture，实测须按部署真值同公式重算 | B-2 执行纪律 |
| 05 | 3 | exp() 支配分支仅量级论证未逐位复算（断言已设计落在可精确整数区间） | B-2 执行时以链上返回值/BigInt 复算 |
| 05 | 4 | B32-6-01 P3 订单终态（Cancelled/Frozen）归类未预设，待首轮执行回写 | B-2 |
| 05 | 5 | SCN-B32-01~08 / IT-* 未回填清单与域级目录 | B-1（**2026-08-21 已闭环**，收编路径见 §1.2 G6） |
| 05 | 6 | B32-1-06/07 skewRef 按 min=max 单价推导，双价实测需按 midPrice 重算 | B-2 执行纪律 |
| 05 | 7 | 清算/ADL 完整费用账本断言未展开，由 SCN-068/079 与核对层承担 | B-3（R16） |

---

## 7. 本交付物已知缺口（open gaps）

1. **矩阵状态为 2026-08-21 快照**：环境重建（A-4）、编号收编（B-1）后，R 行的「环境」「状态」列需再版；本文不自动跟踪后续勾选。
2. **R15（单位修复）与 R17（事件结构）只有补登映射方案，无既成用例**；R16 的 SCN-068/079 清算费用断言未成文——三者是矩阵内仅有的映射弱点，闭环依赖 §5 C-4/B-3。
3. **`npm run compile`（hardhat 编译链）未验证**（原因与风险见 §3.3），forge 通过不能替代；随 A-1 首次部署闭环。
4. **05 §1.4 引用的既有 SCN（如 SCN-048/050/068/077/078/079）在 TestCode 均无 spec**（现有 15 条不含它们），矩阵中标注的 E2E 映射对这些编号而言是「待编写」而非「待重跑」，工作量估算应据此计。
5. 各外围仓（fx100-apps、keeper、签名服务、索引器）的适配状态本文仅按 01/02 的静态要求登记为「适配待确认」，未核查任何外围仓代码；C-2 需各仓自证。
