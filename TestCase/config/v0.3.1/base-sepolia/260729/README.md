# base-sepolia v0.3.1 部署与参数快照

> `base_sepolia_v0.3.1_260729` / Base Sepolia / chainId `84532` / 参数 block `45389410` / 生成时间 `2026-08-12T15:05:42.971Z` / 合约 `release/v0.3.1` @ `d9a7fd2f23705c0bcfe44eaa2f40d9d5c170aaa0`。

本目录按“合约版本 / 环境 / 部署快照”归档真实 Base Sepolia 参数。`260729` 是源 deployment 标签中的部署日期；实际采集时间、区块和 RPC 以 `parameters.json.meta` 为准。

- `deployed-addresses.json`：从 `base_sepolia_v0.3.1_260729/deployed_addresses.json` 原样归档的 67 条部署地址。
- `parameters.json`：完整机读快照，chainId `84532`、block `45389410`。
- `parameters-by-module.csv`：config-dump 原生按模块表。
- `parameters.md`：人工阅读摘要。

原始参数生成证据仍保留在 `TestCode/artifacts/parameter-cache/base-sepolia/`，本目录是供 manifest、CURRENT 和测试文档使用的部署地址与参数正式只读归档。
