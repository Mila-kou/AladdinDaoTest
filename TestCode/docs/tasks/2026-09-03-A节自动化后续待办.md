# A 节自动化（tests/A/ct-base.spec.ts）后续待办

> 首个正式批次：`TestCode/artifacts/runs/2026-09-02T161000-175Z`（tx-fork-v0.3.2-260902-2305，CT-BASE-001～008 PASS，XT-BASE-009 SKIP）。以下是复核留下的低优先级项与观察，不影响批次结论。

## 低优先级修正（下次动 ct-base-runner.ts 时一并处理）

1. **CT-BASE-005 下限推导的抵押品价**：现按 `STABLE_PRICE(USDC)` 单价换算，合约用 `min(feed 调整价, STABLE_PRICE)`（`ChainlinkPriceFeedProvider.sol` 53～55 行）；tx-fork 上 USDC feed 0.999 < 锚 1.001，推导下限比合约真实下限低约 0.2%。改为取两者较小值。
2. **CT-BASE-007 DataStream 分支**：`PRICE_FEED` 为零时循环 `continue`，DataStream token 的 `MAX_ORACLE_PRICE_AGE` 断言实际不可达；且若可达，时间戳应取签名报告的 `observationsTimestamp` 而非 feed `updatedAt`。当前 tx-fork 无 DataStream 绑定 token，暂无影响。

## 观察（交部署清单负责人确认，非测试缺口）

- `additionalContracts.wnt` = Base Sepolia OP 预部署 WETH9 `0x4200…0006`，非 fx100 部署，CT-BASE-002 记 NOT_EXERCISED。
- `insuranceTreasury` 与 `ProtocolTreasury` 代码哈希相同（同一源码部署两次）；无 `InsuranceTreasury` 独立 artifact。
- `MockERC20PermitV2.json` 的 deployedBytecode 为空，mockUsdc 以 MockToken artifact 比对。
- `MAX_ORACLE_PRICE_AGE` = 60 s 而 mock feed 年龄常在数百至数千秒：对 ChainlinkPriceFeedProvider 路径合约不生效（时间戳取 block.timestamp），执行类用例仍应在 executeOrder 前刷新 mock 时间戳（traps §11）。

## 已定稿的约定

- 标题 `<ID> <标题> @p0 @readonly|@tx`；证据附件 `ct-base-00N-evidence.json`；`CaseEvidence { id, title, checks[{name, passed, actual, expected, note?}], data, blocked? }`，bigint 十进制字符串，NOT_EXERCISED = passed:true 且 actual/expected 均为 `NOT_EXERCISED`。
- 只读用例标 `@readonly`；CT-BASE-005 因 `applyCaseTrader` 注资标 `@tx`；CT-BASE-008 的 LIMITED 夹具只在 evm_snapshot/evm_revert 内授权。
- 正式跑法：`E2E_ENV=tx-fork E2E_TRADER_ASSIGNMENT=per-case npx playwright test tests/A --project=tx-fork`；A 全绿是 tx-fork 合约层准入恢复的依据。
