# 06 测试影响与准入结论 — fx100-contracts release/v0.3.2

> 整理日期：2026-08-21（本文所载编译/测试实测执行于 2026-08-21）；部署/环境现状更新于 2026-09-02，逐项注明，不追溯伪记。
>
> **证据锚点约定**：本文所有结论均附「文件::函数/符号」锚点，路径相对合约仓根（主测仓 `Github/fx100-contracts@release-v0.3.2`，HEAD `13880f2`；对比基线 `Github/fx100-contracts@release-v0.3.1`，HEAD `d9a7fd2`；diff 范围 `d9a7fd2..13880f2`，27 提交 / 61 文件，冻结记录见 `00-版本证据.md`）。「未变化」结论以该 diff 范围为证据。工作区路径（TestCode/TestCase）相对 `/Users/milakou/Documents/FX100`。
>
> **输入底本**：`00-版本证据.md`、`01-代码变化分析.md`、`02-部署与升级手册.md`、`03-合约功能说明.md`、`04-参数目录.md`（§6 变化清单 11 项）、`05-重要参数边界场景.md`（38 条卡片）、`contract-fields-and-calculation-rules.md`（2026-08-19）。已有交付物只引用不重复。
>
> **状态纪律**：结论只允许 `READY_FOR_SYSTEM_TEST` 或 `NOT_READY`（deliverables.md 第 06 项），不使用「基本完成」类措辞掩盖缺口。资金/参数值一律整数原始值 + 精度标注；v0.3.2 部署值必须绑定具体 deployment/network/block，当前已知快照为 `tx-fork-v0.3.2-260902`。

---

## 0. 准入结论

# **NOT_READY**

一句话结论：**总开关仍为 NOT_READY，因为 oracle-fork/time-fork 仍是 v0.3.1、原位升级路径无脚本、外围适配未确认；但自 2026-09-02 起准入按环境和层分开判——tx-fork 的合约层（CT 与链上 SCN，Inline Keeper + 私钥签名）已满足 A-1～A-3 与 A-4 的 tx-fork 部分，判为 READY_FOR_SYSTEM_TEST，其结果可正式登记进 `TestCase/E2E/versions/v0.3.2/results.md`。**

> **准入定义（2026-09-02 重构）**：某个环境的合约版本、部署、参数和角色核对过了，用例可以在上面跑、结果可以正式登记，该环境该层即准入。用例跑完才能判断的事情（回归结果、缺口补齐、外围适配签字）不属于准入，归 §5 的「发布建议条件」。总开关 `CURRENT.primary.admission` 表示"全部环境全部层"，分环境结论登记在 `CURRENT.primary.admissionScope`。

| 环境 / 层 | 准入 | 依据 |
| --- | --- | --- |
| tx-fork 合约层（CT、链上 SCN；Inline Keeper；私钥签名） | **READY_FOR_SYSTEM_TEST（2026-09-03 恢复）** | 恢复依据：`TestCode/tests/A` 正式批次 `TestCode/artifacts/runs/2026-09-02T161000-175Z`（release-v0.3.2，部署 `tx-fork-v0.3.2-260902-2305`）CT-BASE-001～008 全 PASS、无质量告警，即 02 §18.1 读检的自动化版；首批功能用例结果已登记到 `TestCase/E2E/versions/v0.3.2/results.md` 功能用例表（A 节 8 PASS + B1 合约层 6 PASS）。时间线：首次依据 tx-fork@v0.3.2（vnet 64aa3163，block 46282993）判 READY → 22:30 tx-fork 被重建为 v0.3.1 fork 回退 → 用户经环境页向导在新 vnet 5bd030ca 重部署 v0.3.2（manifest `tx-fork-v0.3.2-260902-2305`，部署流程内置 02 §18.1 读回校验全过）+ Mock 市场强制重建（Market #3）+ verify:mock 全 verified。**恢复标准（与测试看板 session 约定）**：`TestCode/tests/A` 的 CT-BASE-001～008 以正式 reporter 跑出的批次全部 PASS 后恢复 READY；A-1/A-3 结论对新部署沿用（configure 输入未变，快照目录 `TestCase/config/v0.3.2/tx-fork/260902-2305/`） |
| tx-fork 前端层（FT/XT）与 Relay/Flash | NOT_READY | 依赖 C-2 外围适配确认（前端/SDK 切 v0.3.2 ABI、签名服务新 EIP-712 域） |
| oracle-fork(99912) / time-fork(99913) | NOT_READY | 仍为 `base-sepolia@v0.3.1`（A-4 未完） |
| 升级迁移形态（SCN-B32-08、旧授权失效等） | NOT_READY | 依赖 A-5 升级操作面与 C-1 迁移方案 |

| # | 阻塞事实 | 证据 |
| --- | --- | --- |
| 1 | `tx-fork@v0.3.2` 部署、参数快照与全量对账已完成（2026-09-02，124/124 可复现，见 §3.6），不再是阻塞；`05` 的 38 条场景实际结果仍全部待执行，归发布建议层 B-2 | `04-参数目录.md` §7-1（已更新）；`05-重要参数边界场景.md` §12-1 |
| 2 | tx-fork(99911) 已是 **v0.3.2@13880f2** 私有测试部署，可用于后续执行，但还没有用例结果；oracle-fork(99912) 与 time-fork(99913) 均 ready，但仍为 **base-sepolia@v0.3.1**，不能产出 v0.3.2 的 Oracle/时间推进证据 | `Docs/contract-releases/CURRENT.json`（2026-09-02）；`TestCode/config/deployments/tx-fork-v0.3.2-260902.json` |
| 3 | TestCode 15 条 SCN spec 的全部历史批次 release 标签无一为 v0.3.2（73 个批次：`release-v0.3.1` ×36，其余为 FX100-时间戳标签）；v0.3.1 基线的 14 PASS / 1 FAIL 已迁到 `TestCase/E2E/versions/v0.3.1/results-scn.md`（`SCENARIO-CHECKLIST.md` 自 2026-09-02 起为 4 列设计索引，不记状态）；v0.3.2 条目仍为 0，归发布建议层 B-3/B-4 | `TestCode/artifacts/run-batches/*/run.json`（2026-08-21 统计）；`TestCase/E2E/versions/v0.3.1/results-scn.md`；`TestCase/E2E/versions/v0.3.2/results.md` |
| 4 | v0.3.1→v0.3.2 原位升级全路径仓库内无脚本（仅 02 手册手工步骤）；旧子账户授权迁移方案未定；keeper/前端/SDK/索引器适配均未获确认 | `02-部署与升级手册.md` §0/§13/§19-1/4/5/9 |
| 5 | 合约仓 test/ 覆盖面薄：19 个 .t.sol 文件（18 套件）/85 个测试函数，不等同全量回归；1,944 条账本宿主在 `docs/testing-standards` fork 分支（该分支引用已按工作区约定作废）——**2026-08-21 裁决：暂不纳入**，v0.3.2 测试范围显式接受「合约仓自带 85 函数 + TestCode 15 spec」覆盖面（§3.5） | 本文 §3.5；工作区 CLAUDE.md 分支禁用约定；结构审计 2026-08 记录 |

已就绪的信号面（不改变结论）：00~05 交付物齐备；`01` 的 P0 材料性变化在合约仓 forge 套件内均有直接命中的新增测试且全部通过（§2 矩阵「forge 映射」列）；05 已给出 38 条边界场景；tx-fork v0.3.2 全新部署、双市场、角色与参数快照也已就绪。剩余主要工作是执行/回填、参数全量对账、另两个 v0.3.2 环境和原位升级/外围适配闭环。

---

## 1. 交叉核对记录（00~05 贯通）

### 1.1 一致性核对表

| 核对项 | 结论 | 证据 |
| --- | --- | --- |
| 00 ↔ 01：HEAD/基线/diff 规模 | 一致（13880f2 / d9a7fd2 / 27 提交 61 文件 +1,997 −814） | `00-版本证据.md` 表；`01-代码变化分析.md` §1 |
| 01「验证结果：Foundry 测试通过退出码 0」 | 与本文 2026-08-21 实测一致（§3.2） | 本文 §3.2 |
| 01 §12 升级检查清单条目数 | **实核 14 项**（编排任务口径「15 项」系笔误，以文档为准）；`02` §13.1 亦按 14 项逐项展开，两文一致 | `01-代码变化分析.md` §12；`02-部署与升级手册.md` §13.1 |
| 04 §6 变化清单（11 项）↔ 05 §1.4 映射总表 | 11 项全部有落点：#1~#9/#11 各有卡片组；#10（`SWAP_ORDER_GAS_LIMIT` 死代码清理）明确豁免且理由成立（`04-参数目录.md` §3.10：两版运行时等价，v0.3.1 唯一引用 `src/gas/GasUtils.sol::estimateExecuteSwapOrderGasLimit` 本身无调用方） | `04-参数目录.md` §6；`05-重要参数边界场景.md` §1.4 |
| 05 覆盖维度 ↔ deliverables.md 第 05 项门槛 | 阈值三点/零值/最小非零/最大/越界/组合/正负/取整/选价/角色/开关/Oracle/资金不足/存量升级迁移全覆盖；Oracle 异常以回归映射处理（diff 范围内 `src/oracle/` 无行为改动） | `05-重要参数边界场景.md` §1.3 |
| 02 §18.3 将 forge build/test 执行责任移交 06 | 本文已执行并如实记录（§3） | 本文 §3 |
| 04 抽样部署参考值 ↔ 安全导出快照 | 04 自证「抽样值已与快照逐字核对」，本文未重复核对（快照文件仅允许来源已在 02 §15 固定） | `04-参数目录.md` §7-2；`02-部署与升级手册.md` §15 |
| 05 场景卡片总数 | 38 条（组1×9、组2×2、组3×4、组4×5、组5×4、组6×2、组7×2、组8×2、组9×4、组10×4），与其 §12-1「全部 38 条」自述一致 | `05-重要参数边界场景.md` §2~§11 |

### 1.2 贯通检查发现的缺口与不一致（本文新增发现）

| # | 发现 | 处置 |
| --- | --- | --- |
| G1 | **01 §8.3 单位修复**（剩余成本把 token 数量当 USD 记账，修复为乘抵押品价格后记 USD）未进 04 §6 清单（合理：非参数变化），也无 05 卡片、无 SCN 映射——属材料性行为变化中唯一无显式测试映射者 | 本文补登矩阵行 R15，映射到合约仓减仓套件回归（已通过）+ execution-evidence 对账层（待执行），并列入 §5 解除条件 C-4 |
| G2 | **01 §8.5 输出结构简化**（secondary output token/amount 字段删除、事件结构变化）无测试场景，且 `03` gap-4 声明事件字段级 schema 未展开——索引器适配面无可执行核对物 | 矩阵行 R17（人工适配核对）；§5 条件 C-4 |
| G3 | **01 §8.4 强平判定修复组**（清算用成交价规则、`balanceWasImproved` 传递、R5-B02 费档）在 05 仅隔离了负点差维度（B32-1-07），完整清算费用账本断言委托 SCN-068/SCN-079 与核对层（05 gap-7）但两者均未回填、断言未成文 | 矩阵行 R16 标注「E2E 断言未展开」；合约仓侧 `test/integration/Liquidation.t.sol` 11 条已通过兜底（§3.2） |
| G4 | 锚点表述不一致：02 §13.1 #6 写「`BaseRelayRouter.sol::_validateRelayFee` 段 L198-204」，04 §2.3/05 组 3 写 `_validateCall`。**已按源码裁决：校验位于 `src/router/relay/BaseRelayRouter.sol::_validateCall`（函数起 L189，fee token 检查 L198-204，revert `FxErrors.UnexpectedRelayFeeToken`），后续统一用 `_validateCall`**（2026-08-21 源码复核） | 记录在案，不需改动结论；02 后续修订时更正措辞 |
| G5 | time-fork chainId 口径已裁决为 **99913**（`config/environments/catalog.ts` fixedChainId + `CURRENT.json` environments.time-fork），并已完成创建/初始化，当前 status=`ready`。其未闭环点不再是 chainId 或实例不存在，而是底层仍为 `base-sepolia@v0.3.1`，需重建/切换到 v0.3.2 后才能产出目标版本证据 | §5 条件 A-4 |
| G6 | 05 新增建议编号（SCN-B32-01~08、IT-VAULT-001~003、IT-MARKET-001）未回填 SCENARIO-CHECKLIST（仍 80 条）与 TestCase 域级目录，收编方式待裁决——**本文建议**：SCN-B32-01~08 按 05 §1.4 建议挂靠 S03/S05/S06 追加登记（不改动原 80 条编号）；IT-VAULT/IT-MARKET 落 TestCase 域级目录 | **2026-08-21 已收编**：SCN-B32-01～08 → `TestCase/E2E/SCENARIO-CHECKLIST.md` 文末「v0.3.2 增补场景」章节（头部统计追加「增补：8」，原 80 条不动）+ `TestCase/E2E/scenarios/S03-仓位管理与退出.md`（SCN-B32-05/06）/ `S05-长持费用价格冲击与多市场.md`（SCN-B32-01/02/07）/ `S06-Flash异常恢复与兼容.md`（SCN-B32-03/04/08）末尾「v0.3.2 增补（SCN-B32）」小节；IT-VAULT-001～003、IT-MARKET-001 → `TestCase/E2E/versions/v0.3.2/cases/IT-VAULT-MARKET.md`（`TestCase/E2E/README.md` 分层表已登记真实目录）。注：05 §1.4 未分配 SCN-B32-02，收编时按表序补位为 R2-B10 封顶（B32-2-01/02，挂 S05，与 SCN-049 共用证据），待确认。§5 条件 B-1 已勾；全部实际结果=待执行，执行归 B-2 |

---

## 2. 变更 → 测试追踪矩阵

### 2.1 阅读说明

- **行来源**：`04-参数目录.md` §6 的 11 项材料性变化 + `01-代码变化分析.md` 中未进 04 §6 的行为/适配变化（R15~R19，§1.2 交叉核对补登）。每个材料性变化至少一条测试映射；无映射者显式标「缺口」并给补齐方案，不留空白行。
- **测试映射**：`B32-*` = 05 边界场景卡片；`SCN-xxx` = 既有 80 条用户级场景（`TestCase/E2E/SCENARIO-CHECKLIST.md`）；`SCN-B32-xx`/`IT-*` = 05 提出的新增建议编号（2026-08-21 已收编进 TestCase，见 G6 处置列）；`test/...` = 合约仓 forge 套件（本文 §3.2 已实测通过者标 ✅）。
- **自动化/人工**：`forge` = 合约仓单元/集成（已可执行）；`E2E-auto` = TestCode Playwright（有 spec 者注明，无 spec 为待编写）；`人工` = 手工执行或适配核对。
- **环境**：tx-fork(99911)=**v0.3.2@13880f2**；oracle-fork(99912)/time-fork(99913)=**base-sepolia@v0.3.1**。`部署前置` 在普通场景中已由 tx-fork 满足；在组 10/迁移场景中仍指真实 v0.3.1→v0.3.2 原位升级路径。
- **能力**：M = Market 侧（CONFIG_KEEPER/CONTROLLER 写参、受控 OI、Strategy mock、角色分配）；O = Oracle 侧（单价/双价/推价）；T = 时间推进。
- **状态词汇**：`forge✅` = 2026-08-21 实测通过；`待执行` = 用例已设计未运行；`待编写` = 尚无 spec/用例文本；`阻塞(升级演练)` = 缺 v0.3.1→v0.3.2 原位升级路径；`阻塞(v0.3.2 oracle/time 环境)` = 环境实例存在但版本仍为 v0.3.1；`缺口` = 无显式映射，本文补登方案；`适配待确认` = 非测试类核对项未闭环。

### 2.2 矩阵

| # | 材料性变化（锚点） | 05 卡片 / SCN 映射 | forge 映射 | 自动化/人工 | 环境 | 能力 | 优先级 | 状态 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- |
| R1 | `MIN/MAX_DYNAMIC_SPREAD` 新增 int256 clamp（`src/constants/FX100Keys.sol::minDynamicSpreadKey/maxDynamicSpreadKey`；`src/pricing/PositionPricingUtils.sol::getDynamicSpread`；04 §6 #1/#2） | B32-1-01~05、B32-1-09 → SCN-B32-01（挂 S05）；部署核对必核项（B32-1-01 漏配即点差恒 0 事故） | `test/integration/ExecutionPriceUsdOrder.t.sol` ✅、`ExecutionPriceTokenOrder.t.sol` ✅（回归面） | forge + E2E-auto（待编写）+ 人工（部署读值核对 `DataStore.getInt` ×市场×方向） | tx-fork v0.3.2；漏配事故形态需受控清键 | M（CONFIG_KEEPER setInt、角色三账户）；O 单价 | P0 | forge✅；tx-fork 四键已读回；边界/漏配反例待执行 |
| R2 | 负点差 + 清算/ADL floor-at-0（`PositionPricingUtils.sol::getDynamicSpread` allowNegativeSpread；`src/position/PositionUtils.sol::getExecutionPriceForDecrease`；04 §6 #3） | B32-1-06/07/08 → SCN-050（解除原 PENDING 标记）、SCN-068（新增负点差隔离数据集）、SCN-070、SCN-048、SCN-019 | `ExecutionPriceUsdOrder.t.sol` ✅、`Liquidation.t.sol` ✅ | forge + E2E-auto（SCN-070 已有 spec `TestCode/tests/S07/scn-070.spec.ts`，需 v0.3.2 环境重跑；其余待编写） | tx-fork(v0.3.2) + oracle-fork(v0.3.1，推价/双价) | M 受控 OI；O 双价+推价 | P0 | forge✅；tx-fork 子集待执行；Oracle 子集阻塞(v0.3.2 oracle/time 环境) |
| R3 | R2-B10 大单 price impact 指数封顶（`PositionPricingUtils.sol::getPriceImpactSpread`，`LogExpMath.sol::MAX_NATURAL_EXPONENT`；04 §6 #3） | B32-2-01/02 → 并入 SCN-049 三点数据集 | `PositionPricing.t.sol` ✅（定价面回归） | forge + E2E-auto（待编写） | tx-fork | M 浅深度 fixture；O 单价 | P1 | forge✅；边界待执行 |
| R4 | 全局 `COLLATERAL_TOKEN` + Relay fee token 强约束 + `SetCollateralToken` 部署步骤（`FX100Keys.sol::COLLATERAL_TOKEN`；`src/market/MarketFactory.sol::createMarket`；`src/router/relay/BaseRelayRouter.sol::_validateCall` L198-204；`ignition/modules/Fx100Role.ts`；04 §6 #4/#11） | B32-3-01~04 → SCN-B32-03（挂 S06）+ IT-MARKET-001；02 §18.1 smoke 首项 | `RelayCreateOrder.t.sol` ✅（含 `UnexpectedRelayFeeToken` 断言 L114-123）、`SubaccountRelayCreateOrder.t.sol` ✅ | forge + E2E-auto（待编写）+ 人工（部署读值 `DataStore.getAddress(COLLATERAL_TOKEN)`） | tx-fork + Relay 签名服务 | M 角色（CONTROLLER 直写/白名单拒绝）；O 无 | P0 | forge✅；tx-fork 全局键已读回；Relay/E2E 待执行 |
| R5 | 子账户 4 槽位模型 + EIP-712 变更（`src/subaccount/SubaccountUtils.sol::MAX_SUBACCOUNT_SLOTS/addSubaccount/validateSubaccount`；`src/router/relay/RelayUtils.sol::SUBACCOUNT_APPROVAL_TYPEHASH/REMOVE_SUBACCOUNT_SLOT_TYPEHASH`；04 §6 #5） | B32-4-01~05 → SCN-B32-04（挂 S06）；SCN-053/055/057/058 会话层关联回归 | `SubaccountSlots.t.sol` ✅（+164 行新增）、`SubaccountRelayCreateOrder.t.sol` ✅ | forge + E2E-auto（待编写）+ 人工（签名服务新旧 typed data 出签） | tx-fork + 签名服务 | M 无特殊；O 无 | P0 | forge✅；E2E 待执行 |
| R6 | `SUBACCOUNT_LIST` 移除 / 旧授权升级即失效（v0.3.1 `FX100Keys.sol::subaccountListKey` 删除；04 §6 #6；01 §5.4） | B32-10-03 → SCN-B32-08③；人工：产品侧重授权流程（02 §13.1 #5，无迁移脚本） | 无（升级形态不可单元化） | E2E-auto（待编写）+ 人工 | v0.3.1→v0.3.2 原位升级演练 | M 无；O 无 | P0 | 阻塞(升级演练)；迁移方案适配待确认 |
| R7 | `CLAIMABLE_COLLATERAL_*` 六键 + `claimCollateral` 入口移除（v0.3.1 `src/router/ExchangeRouter.sol::claimCollateral`、`src/config/Config.sol::setClaimableCollateral*` 删除；04 §6 #7） | B32-10-04 → SCN-B32-08④；人工：前端/Keeper/索引器清理核对（01 §12 #7） | 无（纯移除，`cast sig` 比对 ABI 无此 selector 即证） | E2E-auto（待编写）+ 人工 | tx-fork v0.3.2；若验证存量调用则需升级演练 | M 无；O 无 | P1 | 静态/tx-fork 验证待执行；外围清理适配待确认 |
| R8 | 资金费逐仓结算 R4-B10（`src/market/MarketUtils.sol::settleFundingFees`(L445)/`updateFundingState`；`src/position/IncreasePositionUtils.sol::processCollateral`、`DecreasePositionCollateralUtils.sol::processCollateral`；04 §6 #8） | B32-5-01~04 → SCN-077（新增「更新指数不结算」反例断言）、SCN-078、SCN-073；SCN-029/030 主路回归 | `FundingSettlement.t.sol` ✅（+300 行新增）、`ShortPartialCloseFundingRegression.t.sol` ✅ | forge + E2E-auto（待编写） | time-fork(99913, v0.3.1) + tx-fork(v0.3.2) + oracle-fork(v0.3.1) | M 受控 OI；O 双价；T 时间推进实例已有，但版本不匹配 | P0 | forge✅；E2E 阻塞(v0.3.2 oracle/time 环境) |
| R9 | 减仓 minOutputAmount 实际校验 R4-B22（`src/order/DecreaseOrderUtils.sol::_validateOutputAmount`，USD 1e30 口径；01 §8.1） | B32-6-01/02 → SCN-B32-05（挂 S03）；SCN-022 关联（Est. Receive） | `ExecutionPriceUsdOrder.t.sol` ✅、`AutoCancel.t.sol` ✅（含 minOutput 断言） | forge + E2E-auto（SCN-022 已有 spec `TestCode/tests/S03/scn-022.spec.ts`，需扩展 minOutput 断言并在 v0.3.2 环境重跑） | oracle-fork(99912, v0.3.1；固定价) | O 单价固定；M 无特殊 | P0 | forge✅；E2E 阻塞(v0.3.2 oracle/time 环境) |
| R10 | USD 减仓 ceil 耗尽归一全平 R4-B20（`src/position/PositionUtils.sol::getDecreaseOrderSize`、`src/position/DecreasePositionUtils.sol` 归一分支、`OrderSizeDeltaAutoUpdated` 事件；01 §8.2） | B32-7-01/02 → SCN-B32-06（挂 S03）；SCN-023/032/052 关联回归 | `ExecutionPriceUsdOrder.t.sol::test_LongNearFullCloseThatConsumesAllTokensIsNormalizedToFullClose` ✅（直接命中） | forge + E2E-auto（SCN-023 已有 spec `TestCode/tests/S03/scn-023.spec.ts`，需 v0.3.2 重跑） | oracle-fork(99912, v0.3.1) | O 单价固定；M 无 | P0 | forge✅；E2E 阻塞(v0.3.2 oracle/time 环境) |
| R11 | Vault 提款到账校验 R4-B01（`src/vault/VaultBase.sol::_withdrawAssets`、`ErrorStrategyWithdrawalMismatch`；01 §9） | B32-8-01/02 → IT-VAULT-001~003（域级新建）；E2E 冒烟可挂 S06；组合场景 B32-5-04 | `test/vault/VaultBase.t.sol` ✅（+169 行新增） | forge + fork 集成（待编写；需可编程 Strategy mock） | tx-fork + Strategy mock（能力待建） | M Strategy mock 指定转账额；O 无 | P0 | forge✅；fork 集成待编写 |
| R12 | R2-B15 折扣+返佣封顶（`PositionPricingUtils.sol::getPositionFeesAfterReferral`；04 §6 #9） | B32-9-01~04 → SCN-B32-07（挂 S05） | `PositionPricing.t.sol::test_GetPositionFeesAfterReferral_CapsAffiliateRewardAtRemainingFee` ✅（+241 行套件）、`Referral.t.sol` ✅（+90 行） | forge + E2E-auto（待编写） | tx-fork（可设 pro tier/referral） | M CONFIG_KEEPER 写 tier 因子；O 无 | P1 | forge✅；边界待执行 |
| R13 | 每市场 COLLATERAL_TOKEN 孤儿数据（`src/market/MarketStoreUtils.sol::get/set` 改全局键；04 §4 表末行） | B32-10-02 → SCN-B32-08②；02 §13.1 #14 全量键值比对 | 无（升级形态） | E2E-auto（待编写）+ 人工（键值比对，基线 = 02 §15 安全导出） | v0.3.1→v0.3.2 原位升级演练 | M 无；O 无 | P0 | 阻塞(升级演练) |
| R14 | v0.3.1 存量仓位跨版本首次动作（仓位/funding per-size 键无变化但消费链路全换；04 §3.9） | B32-10-01 → SCN-B32-08①（升级演练必测：漏配 clamp 即落 B32-1-01 事故第一现场） | 无（升级形态） | E2E-auto（待编写）+ 人工 | v0.3.1 存量状态之上原位升级 | M 配置齐套；O 单价；T 可选 | P0 | 阻塞(升级演练) |
| R15 | **单位修复：剩余成本 token→USD**（01 §8.3；提交范围内 `DecreasePositionCollateralUtils.sol` 记账口径）——§1.2 G1 缺口行 | 无 05 卡片；本文补登映射：execution-evidence 对账层（fx100-verify-handbook 守恒/ΣΔ 断言应能捕获 USD 口径错记）+ SCN-022/023 数值核对间接覆盖 | 减仓套件 `ExecutionPriceUsdOrder.t.sol`/`ExecutionPriceTokenOrder.t.sol` ✅（间接） | forge（间接）+ 核对层（待执行） | tx-fork v0.3.2 | O 单价；M 无 | P1 | **缺口**（无直接断言；解除见 §5 C-4） |
| R16 | 强平判定修复组：清算成交价规则 + `balanceWasImproved` 传递 + R5-B02 费档（HEAD 提交 `13880f2`；`src/position/PositionUtils.sol::isPositionLiquidatable` 调用链；01 §8.4）——§1.2 G3 | B32-1-07（仅负点差隔离维度）→ SCN-068/SCN-079 + 核对层承担完整费用账本断言（05 gap-7，断言未成文） | `Liquidation.t.sol` ✅（11 条，含 fee split/reconcile/grace period）、`FeeTier.t.sol` ✅ | forge + E2E-auto（SCN-068/079 待编写断言） | oracle-fork(99912, v0.3.1；推价触发) + tx-fork(v0.3.2) | O 推价；M 受控 OI；T 保护期可涉 | P0 | forge✅；断言未展开；Oracle 路径阻塞(v0.3.2 oracle/time 环境) |
| R17 | **输出结构简化：secondary output 字段删除、减仓事件结构变化**（01 §8.5；`src/position/PositionEventUtils.sol` 在 diff 清单）——§1.2 G2 缺口行 | 无测试场景；映射：索引器 schema 适配核对（01 §12 #8）+ `03` gap-4 的事件字段级导出补齐后人工比对 | 无 | 人工（适配核对） | 不依赖链上环境（ABI/事件定义静态比对） | — | P2 | **缺口**（适配待确认；解除见 §5 C-4） |
| R18 | ExternalHandler 拒绝重复退款代币（`src/external/ExternalHandler.sol`；01 §10） | 无 05 卡片（域级即可，非用户旅程） | `test/external/ExternalHandler.t.sol::test_revertsForDuplicatedRefundToken` ✅（直接命中） | forge | 本地 forge | — | P2 | forge✅（域级覆盖成立，无需 E2E） |
| R19 | 错误更名 `EmptyTokenTranferGasLimit`→`EmptyTokenTransferGasLimit` 及 selector 变化面（01 §10；02 §14.4） | 无测试场景；映射：监控/前端错误解码表更新核对（人工） | 无（编译期即证） | 人工（适配核对） | 不依赖环境 | — | P2 | 适配待确认 |
| R20 | Reader 槽位读取与资金费口径统一（`src/reader/Reader.sol::getSubaccountSlots/getSubaccountInfo`；01 §10、§3.12） | B32-4 各卡片内 Reader 断言；SCN-021/022 显示值核对关联 | `SubaccountSlots.t.sol` ✅（Reader 断言内含）、`FundingSettlement.t.sol` ✅ | forge + E2E-auto（待执行） | tx-fork | M 无；O 单价 | P1 | forge✅；E2E 待执行 |

**矩阵自查**：04 §6 的 11 项中 #1~#9、#11 对应 R1~R14（#10 豁免有据）；01 独有变化补登 R15~R20。每行均有至少一条映射；R15/R17 因映射为「补登方案」而非既成用例，显式标缺口——这两行与 R16 的「断言未成文」是本矩阵仅有的三处映射弱点，全部进入 §5 解除条件。

---

## 3. 已实测检查记录（2026-08-21 执行）

执行环境：本机 macOS（Darwin 25.5.0），forge 1.7.1（commit `4072e48`，与 `02-部署与升级手册.md` §2 登记一致；仓库未固定 forge 版本，见 02 gap-6）；solc 0.8.29 按 `foundry.toml::solc`；工作树 = `Github/fx100-contracts@release-v0.3.2` @ `13880f2`（执行前 `git log -1` 复核）。

### 3.1 forge build

| 项 | 记录 |
| --- | --- |
| 命令 | `cd Github/fx100-contracts@release-v0.3.2 && forge build` |
| 退出码 | **0** |
| 输出摘要 | 编译成功；仅 forge-lint 告警（`unsafe-typecast` 等），无编译错误 |

### 3.2 forge test

| 项 | 记录 |
| --- | --- |
| 命令 | `cd Github/fx100-contracts@release-v0.3.2 && forge test` |
| 退出码 | **0** |
| 计数 | **18 个测试套件，85 通过 / 0 失败 / 0 跳过**（输出末行：`Ran 18 test suites in 53.68ms: 85 tests passed, 0 failed, 0 skipped (85 total tests)`） |
| 与 01 的一致性 | 01 §1「验证结果：Foundry 测试通过，退出码 0」得到独立复现 |
| v0.3.2 新增/大改套件全部在通过集内 | `SubaccountSlots.t.sol`(+164)、`FundingSettlement.t.sol`(+300)、`PositionPricing.t.sol`(+241)、`VaultBase.t.sol`(+169)、`Referral.t.sol`(+90)、`AutoCancel.t.sol`(+72)、`ExecutionPriceUsdOrder.t.sol`(+99)、`ExternalHandler.t.sol`(+33) 等 13 文件 +1,255 −20 行（`git diff --stat d9a7fd2..13880f2 -- test`） |

### 3.3 未运行项（如实登记）

| 检查 | 状态 | 原因 | 风险 |
| --- | --- | --- | --- |
| `npm run compile`（hardhat 编译） | **2026-08-21 未运行；后续状态见 §3.4** | 当日仓库 node_modules 未安装，本次准入检查未代行部署方安装依赖 | 2026-09-02 Ignition 部署已成功，证明该部署路径能生成可用 artifact；但独立 `npm run compile` 日志仍未在本文归档 |
| Ignition 部署演练 / configure 脚本 dry-run | **2026-08-21 未运行；后续状态见 §3.4** | 当日无部署授权与目标环境 | 2026-09-02 已完成一次全新部署与三脚本 configure；「中断后续跑」行为仍未实测 |
| 05 全部 38 条边界场景、SCN 回归 | **仍未运行** | 2026-09-02 tx-fork v0.3.2 环境已就绪，但本文未收到任何场景执行证据；oracle/time 仍缺 v0.3.2 环境 | 全部行为变化仍缺系统回归证据 |

### 3.4 2026-09-02 部署跟进证据

| 项 | 记录 |
| --- | --- |
| 目标 | `tx-fork`，chainId 99911，fork Base Sepolia @46282748 |
| 版本 | `release/v0.3.2@13880f2416918f4fed3fea86d7f1023084a3ce0d` |
| 部署结果 | Ignition Fx100Core + configure 三脚本 + MockBTC/ETH 双市场 + 角色授权 + 读回校验通过 |
| 部署清单 | `TestCode/config/deployments/tx-fork-v0.3.2-260902.json` |
| 参数快照 | 正式入口 `TestCase/config/v0.3.2/tx-fork/260902/parameters.json`，已设置项 `parameters-by-module.csv`：block 46282993，1,969 条目，124 已设置，0 errors；`TestCode/artifacts/...` 仅作生成源证据 |
| 边界/回归 | 未因部署成功而自动获得 PASS；38 条边界场景和 v0.3.2 SCN 仍待执行 |

### 3.5 覆盖面声明（必须如实理解 §3.2 的含金量）

- 合约仓 test/ 共 **19 个 .t.sol 文件 / 85 个测试函数**（`find test -name "*.t.sol"`、`grep -rE "function (test|invariant)" test`，2026-08-21 计数），85/85 通过说明的是**本仓自带回归面**成立，**不等同全量回归**：85 个函数对 61 个变更文件（+1,997 行）而言是每变更点 1~11 条的抽查密度。
- 历史上 fx100 的 **1,944 条账本宿主用例位于 `docs/testing-standards` fork 分支**——该分支及 `test-hub/...` 引用已按工作区约定全部作废（工作区 CLAUDE.md），其资产去向（迁移入主仓 / 在 TestCode 重建 / 放弃）**已于 2026-08-21 裁决：暂不考虑、不纳入 v0.3.2 测试范围**（含 `Github/fx100-contracts@soso-test` 克隆的全部内容，不引用、不迁移、不重建；后续如需重新纳入另行立项）。因此 v0.3.2 的「测试通过」表述**显式只覆盖**合约仓自带 85 函数 + TestCode 15 spec（及本文矩阵后续新增用例）的面，本文其余结论据此理解；该项不再是准入阻塞项（§5 条件 C-3 已按裁决关闭）。

### 3.6 2026-09-02 准入证据（A-1 / A-2 / A-3）

| 条件 | 证据 | 结论 |
| --- | --- | --- |
| A-1 部署归档 | `TestCase/config/v0.3.2/tx-fork/260902/`：`ignition/journal.jsonl`（deployment-id `tx-fork-v032-260902r3`，仅含 tx/区块哈希）、`ignition/fx100.fork.tx-fork.json`、`configure-inputs/`（general / market / oracle.fork / shared_market）、`compile.log`（HEAD `13880f2` `npm run compile` 退出码 0，Nothing to compile；solc 0.8.29 / optimizer 200 / viaIR 见 Ignition build-info）；`deployed-addresses.json` 与 Ignition `deployed_addresses.json` 逐字节一致 | 勾销 |
| A-2 读检七项 | 在线 2 项 `read-checks-live-2026-09-02.md`（RelayRouter 与 SubaccountRelayRouter 域分隔符与源码常量一致；Reader 4 槽位）；离线 5 项 `read-checks-offline-2026-09-02.md`（COLLATERAL_TOKEN=USDC；MIN/MAX_DYNAMIC_SPREAD 8 条 = 输入换算；FEE_RECEIVER=RevenuePool；02 §9.2 矩阵 32/32 存在、旧地址无角色、角色哈希与 `Role.sol` 一致；120/120 configure 写入由归档输入复现，等价于零 applied）。偏差：DataStore CONTROLLER 多一个 TestCode 夹具账户（fork 专用）；限制：Mock 初始化后不得重跑 configureOracle。两条均已写入 02 §18.1 | 勾销（偏差已登记） |
| A-2 读检自动化正式批次（当前部署） | `TestCode/tests/A` CT-BASE-001～008 在部署 `tx-fork-v0.3.2-260902-2305`（vnet 5bd030ca）上以正式 reporter 跑出 `TestCode/artifacts/runs/2026-09-02T161000-175Z`：8 条全 PASS（环境身份 7 项、37 个核心合约 codehash 78/79、三市场元数据 15 项、精度 8 项、抵押链路 8 项、开仓参数 18 项、Oracle 绑定 17 项、角色权限 21 项；NOT_EXERCISED 5 项均已注明原因），证据 JSON 随批次归档 | 勾销（tx-fork 合约层准入恢复依据） |
| A-3 参数对账 | 124 条已设置值全部可由归档输入复现；124 个 key 与目录 245 个哈希重算一致；04 §4 移除键未出现；04 §2 卡片与链上一致；04 §3.2～3.5 已回填当前值并更正写入权限；剩余为目录缺陷与三项待决（04 §7-5/7-6） | 勾销（待决项不阻塞 tx-fork 合约层） |

证据有效范围：本表全部证据绑定 deployment `tx-fork-v0.3.2-260902`（Ignition `tx-fork-v032-260902r3`，vnet 64aa3163，block 46282993）。2026-09-02 22:30 tx-fork 被重建为 v0.3.1 fork（vnet 7bef3d7d）后，A-1 归档与 A-3 对账结论仍对该 deployment 成立，但**不能代表当前 tx-fork**；重部署得到新 deploymentId 后，A-2 七项读检必须重做，A-3 在 configure 输入文件不变时复核 deployed-addresses 后可沿用。**2026-09-03：当前部署 `tx-fork-v0.3.2-260902-2305` 的 A-2 已由 `tests/A` 正式批次 `TestCode/artifacts/runs/2026-09-02T161000-175Z` 重做并全 PASS（上表新增行）。**

复核纪律：三份离线报告各经一轮独立反向复核（`verification/*-recheck.json`），复核把原报告的 3 条 FAIL 改判为 NOT_STATED（04 默认值列引用的是被输入文件覆盖的 defaults.ts、§5 底本引用而非缺卡片、STABLE_PRICE 无 v0.3.2 期望），确认的缺陷全部是文档缺陷，链上值无一处与输入不符。

---

## 4. 测试现状登记（资产/台账核查 2026-08-21；环境/部署更新 2026-09-02）

### 4.1 TestCode 自动化资产

| 项 | 事实 | 证据 |
| --- | --- | --- |
| SCN spec 存量 | **15 条**：S02 ×7（scn-009/010/011/012/013/015/016）、S03 ×4（scn-022/023/024/025）、S07 ×4（scn-065/066/067/070） | `TestCode/tests/**/scn-*.spec.ts` 逐一列举 |
| 执行批次 | 73 个批次目录；`run.json` release 标签统计：`release-v0.3.1` ×36、`dashboard-workflow-check` ×1、其余为 `FX100-YYYYMMDD-hhmm` 时间戳标签；**无任何 v0.3.2 批次** | `TestCode/artifacts/run-batches/*/run.json` |
| 数据核对层 | execution-evidence 对账改造进行中（目标：核对内容与核对公式正确），未以 v0.3.2 公式基线验收 | 工作区 CLAUDE.md 自动化现状（2026-08-11） |

### 4.2 环境

| 环境 | 登记状态 | chainId | v0.3.2 可用性 |
| --- | --- | --- | --- |
| tx-fork | ready | 99911 | **v0.3.2@13880f2**；全新部署、双市场、角色和参数快照已就绪，可用于后续 v0.3.2 执行 |
| oracle-fork | ready | 99912 | **base-sepolia@v0.3.1**；不能产出 v0.3.2 Oracle 结果 |
| time-fork | ready | 99913 | **base-sepolia@v0.3.1**；实例与时间推进能力已有，但不能产出 v0.3.2 资金费结果 |

证据：`Docs/contract-releases/CURRENT.json`（updatedAt 2026-09-02）与 `TestCode/config/deployments/tx-fork-v0.3.2-260902.json`。

### 4.3 台账

- `TestCase/E2E/SCENARIO-CHECKLIST.md` 自 2026-09-02 起改为 4 列共享设计索引（80 条，不记状态）；v0.3.1 基线的 14 PASS / 1 FAIL 迁到 `TestCase/E2E/versions/v0.3.1/results-scn.md`，v0.3.2 结果只登记在 `TestCase/E2E/versions/v0.3.2/results.md`（当前 0 条 E2E PASS）；SCN-B32 增补 8 条迁到 `versions/v0.3.2/cases/SCN-B32.md`（全部待执行）。TestCode 的目录解析与 trader 映射已随该格式同步（`catalog.ts`、`generate-case-traders.ts`，2026-09-02 rebuild 与 traders:map 跑通）。
- 05 提出的 SCN-B32-01~08、IT-VAULT-001~003、IT-MARKET-001 已于 2026-08-21 收编（§1.2 G6 处置列 / §5 B-1）：清单文末增补 8 条 + S03/S05/S06 增补小节 + `TestCase/E2E/versions/v0.3.2/cases/IT-VAULT-MARKET.md`，全部待执行，不改变 NOT_READY。

### 4.4 部署

- v0.3.2：2026-09-02 已在 tx-fork 全新部署并生成部署清单/参数快照；`04-参数目录.md` 已回填关键新键，但其余参数卡片仍待全量对账。
- v0.3.1→v0.3.2 升级：仓库仅有全新部署脚本（`ignition/modules/Fx100Core.ts`）；原位升级为 02 §13 手工路径，无脚本、无回滚脚本（02 §17）。

---

## 5. 准入条件与发布建议条件（2026-09-02 重构）

> 准入只回答"这个环境这一层能不能开跑、结果能不能正式登记"，按环境分开判（结论见 §0 分环境表）；回归结果、缺口补齐和外围适配签字是跑完之后的事，归发布建议层，不再阻塞准入。总开关 `admission` 改 READY 的条件：A-1～A-5 与 C-1、C-2 全部勾满。

### A. 准入条件（环境与部署）

- [x] **A-1 完成 v0.3.2 部署归档**：**2026-09-02 勾销**——tx-fork 全新部署的 Ignition 执行记录、参数文件、三个 configure 输入、`npm run compile` 记录已归档到 `TestCase/config/v0.3.2/tx-fork/260902/`（证据见 §3.6）。原位升级形态的归档随 A-5 另计。
- [x] **A-2 部署核对必核项通过**：**2026-09-02 勾销，2026-09-03 对当前部署以 `tests/A` 正式批次 `TestCode/artifacts/runs/2026-09-02T161000-175Z` 重做全 PASS**——02 §18.1 七项读检全部 PASS（在线 2 项 + 离线 5 项，含配置输入与链上值 120/120 复现，等价于零 applied）；已登记偏差：CONTROLLER 多一个 TestCode 夹具账户；已登记限制：Mock 初始化后不得重跑 configureOracle（§3.6、02 §18.1）。
- [x] **A-3 导出 v0.3.2 参数快照并回填 04**：**2026-09-02 勾销**——124 条已设置值全部对账一致；04 §2、§3.2～3.5 已回填并更正写入权限；剩余目录缺陷与三项待决登记在 04 §7-5/7-6，不影响 tx-fork 合约层用例的执行与登记，但 `STABLE_PRICE(USDC)` 须在价格核对类用例执行前裁决；合成 BTC 乘数已于 2026-09-02 晚裁决为 18 位口径正确（TestCode 清单标注 8 位为登记错误，待修）。
- [ ] **A-4 基于 v0.3.2 部署完成所需环境绑定**：**tx-fork(99911) 部分已满足**（2026-09-02 晚：新 vnet 5bd030ca 上重部署 v0.3.2，manifest `tx-fork-v0.3.2-260902-2305`，绑定与 CURRENT.json 一致，Mock 市场 #3 verify 全 verified；同日 22:30 曾因 fork 重建为 v0.3.1 短暂失效）。**oracle-fork(99912)/time-fork(99913) 仍待切换**。本条按环境分别生效，三环境全部到位才勾销。
- [ ] **A-5 升级操作面裁决**（02 gaps 1/4/10）：原位升级/回滚补脚本或运维书面确认按手册手工执行；bootstrap 权限回收与 ProxyAdmin ownership 目标持有人决策；Pyth provider 启用流程明确。**只限制升级迁移形态用例（SCN-B32-08、B32-04⑤ 等），不阻塞 tx-fork 合约层。**

### C. 外围适配与裁决（限制特定用例层，不阻塞合约层）

- [ ] **C-1 旧子账户授权迁移方案定稿**（R6；02 gap-5）：产品侧确认「自然失效 + 强制重授权」流程与用户通知；升级后抽查 `Reader.getSubaccountSlots` 全空 + 重授权可用。**限制升级迁移形态用例。**
- [ ] **C-2 外围适配确认**：keeper 服务仓（负点差分路径、费档修正，02 gap-9）、前端/SDK（int256 点差解码、minOutputAmount 1e30 USD 口径警示——05 B32-6-01、claimCollateral 入口删除）、签名服务（新 EIP-712 域与 typehash）、索引器（R17 事件 schema）各出书面确认。**限制 FT/XT 前端层、Relay/Flash 与 Service Keeper 模式用例；tx-fork 合约层用 Inline Keeper + 私钥签名，不受此条限制。**
- [x] **C-3 1,944 条账本宿主去向裁决**（§3.5）：**2026-08-21 已裁决：暂不纳入**（soso-test / docs/testing-standards 内容不再考虑），85+15 覆盖面已在 §3.5 与 §1 第 5 行显式接受。

### B. 发布建议条件（回归执行与收口；不阻塞准入）

- [x] **B-1 新增编号收编裁决**：SCN-B32-01~08 按 05 §1.4 挂靠追加进 `TestCase/E2E/SCENARIO-CHECKLIST.md`（不改动原 80 条编号）；IT-VAULT-001~003、IT-MARKET-001 落 TestCase 域级目录（§1.2 G6 建议）。——**2026-08-21 已收编**：SCN-B32-01～08 落 `TestCase/E2E/SCENARIO-CHECKLIST.md` 文末「v0.3.2 增补场景」章节（8 行 `[ ]`，头部「增补：8」，原 80 条与总数不动）及 `scenarios/S03`（B32-05/06）/ `S05`（B32-01/02/07）/ `S06`（B32-03/04/08）末尾增补小节（7 列六字段，期望值保留 05 推导）；IT-VAULT-001～003、IT-MARKET-001 落 `TestCase/E2E/versions/v0.3.2/cases/IT-VAULT-MARKET.md`（README 分层表登记真实目录）。SCN-B32-02 为按 05 §1.4 表序的补位号（R2-B10，与 SCN-049 共用证据）待确认。全部实际结果=待执行，执行与回写归 B-2/B-3。
- [ ] **B-2 执行 05 全部 38 条边界场景并回写实际结果**（期望值须按 A-3 部署真值以同公式重算，不得照抄 fixture 数值——05 gap-2）；组 10 四条随原位升级演练执行（依赖 A-5 升级操作面裁决与可执行的 v0.3.1→v0.3.2 演练环境）。
- [ ] **B-3 矩阵 P0 行的 E2E/SCN 回归在 v0.3.2 环境跑通**：至少含 SCN-048/049/050/068/070/077/078 及既有 15 条 spec 的 v0.3.2 重跑；SCN-068/079 补齐清算费用账本断言（R16 部分缺口）。
- [ ] **B-4 TestCode 批次以 v0.3.2 标签登记**：新批次 `run.json` release 标注 v0.3.2 基线（当前 0 条），看板 rebuild + 核对层按 v0.3.2 公式基线出证。
- [x] **B-5 结果台账回填非零**：（**2026-08-21 已完成**：14 PASS / 1 FAIL，均为 v0.3.1 fork 基线证据，现登记于 `TestCase/E2E/versions/v0.3.1/results-scn.md`；v0.3.2 结果按 B-3/B-4 登记进 `versions/v0.3.2/results.md`，不回写共享设计索引）
- [ ] **C-4 R15/R17 缺口补齐**：单位修复（01 §8.3）在 execution-evidence 对账层落至少一条 USD 口径守恒断言；事件字段级 schema 从 `XxxEventUtils` 导出（03 gap-4）并完成索引器比对。
- [ ] **C-5 Basescan/Etherscan 公证路径补齐或豁免决策**（02 gap-3：有 etherscan 配置但无 hardhat-verify 依赖与脚本）；Node/forge 版本固定（02 gap-6）与 ProtocolTreasury `_disableInitializers` 观察项（02 gap-7）处置记录。

---

## 6. 各交付物 open_gaps 汇总（原文归口，供勾销追踪）

| 来源 | # | 缺口 | 对应解除条件 |
| --- | --- | --- | --- |
| 02 | 1 | 原位升级全路径（LPVault impl、无状态层重部署、角色切换/回收、回滚）无脚本，仅手册手工步骤 | A-5 |
| 02 | 2 | **部分闭环**：tx-fork 已实测 Ignition 全新部署、configure 三脚本和读回；仍缺中断续跑证据、Base Sepolia v0.3.2 部署值与原位升级 ProxyAdmin/旧 impl 地址 | A-1/A-2 |
| 02 | 3 | Basescan/Etherscan 公证路径缺失（无 hardhat-verify 依赖与脚本） | C-5 |
| 02 | 4 | bootstrap 权限回收与 ProxyAdmin ownership 移交无脚本、无持有人决策 | A-5 |
| 02 | 5 | 旧子账户授权→新 4 槽位无迁移脚本，重授权流程待产品确认 | C-1 |
| 02 | 6 | Node.js 与 forge 版本仓库未固定（无 engines） | C-5 |
| 02 | 7 | ProtocolTreasury 裸实现未 `_disableInitializers`（观察项） | C-5 |
| 02 | 8 | 编译与测试本次（02 整理时）未运行，执行结果归 06 | **已闭环**：本文 §3 已实测（forge build/test 退出码 0；npm compile 未运行原因见 §3.3） |
| 02 | 9 | keeper 服务侧落地材料不在合约仓；负点差分路径与费档修正适配待服务仓确认 | C-2 |
| 02 | 10 | Pyth provider 部署后未启用，无启用脚本；01 清单实核 14 项（15 项系口径笔误） | A-5；14 项口径已在 §1.1 裁定 |
| 03 | 1 | tx-fork 参数快照已有，但功能说明仍未将动态点差上下限、COLLATERAL_TOKEN、清算保护期等环境实值逐项嵌入/分层对账 | A-2/A-3 |
| 03 | 2 | 外部入口清单为静态整理，未做链上/fork 调用级可达性验证 | B-2/B-3（随场景执行自然覆盖） |
| 03 | 3 | RevenuePool/ProtocolTreasury 分账比例与 reward token 属部署期数据，未核对实值 | A-3 |
| 03 | 4 | 事件字段级 schema 未逐字段展开，索引器适配需另行导出 | C-4 |
| 04 | 1 | tx-fork v0.3.2 快照已导出，关键新键已回填；其余卡片尚未全量回填与 ContractCodeSummary 对账 | A-3 |
| 04 | 2 | v0.3.1 每市场参数参考值仅抽核 market#1/#2，其余以 by-module 快照为准 | A-3 对账时全量覆盖 |
| 04 | 3 | clamp 键未配置点差恒 0 风险仅静态推导，未实测复现 | A-2 + B-2（B32-1-01） |
| 04 | 4 | 部署流程已记录读回校验通过，但参数卡片的逐项验证证据尚未回填 | A-2/A-3 |
| 05 | 1 | 38 条场景实际结果全部待执行；组 10 四条被原位升级演练路径阻塞 | B-2/A-5 |
| 05 | 2 | 期望值基于受控 fixture，实测须按部署真值同公式重算 | B-2 执行纪律 |
| 05 | 3 | exp() 支配分支仅量级论证未逐位复算（断言已设计落在可精确整数区间） | B-2 执行时以链上返回值/BigInt 复算 |
| 05 | 4 | B32-6-01 P3 订单终态（Cancelled/Frozen）归类未预设，待首轮执行回写 | B-2 |
| 05 | 5 | SCN-B32-01~08 / IT-* 未回填清单与域级目录 | B-1（**2026-08-21 已闭环**，收编路径见 §1.2 G6） |
| 05 | 6 | B32-1-06/07 skewRef 按 min=max 单价推导，双价实测需按 midPrice 重算 | B-2 执行纪律 |
| 05 | 7 | 清算/ADL 完整费用账本断言未展开，由 SCN-068/079 与核对层承担 | B-3（R16） |

---

## 7. 本交付物已知缺口（open gaps）

1. **矩阵状态已更新至 2026-09-02 的部署/环境事实**：tx-fork 已标 v0.3.2，oracle/time 已标 v0.3.1；用例执行、外围适配与原位升级状态仍需后续回填。
2. **R15（单位修复）与 R17（事件结构）只有补登映射方案，无既成用例**；R16 的 SCN-068/079 清算费用断言未成文——三者是矩阵内仅有的映射弱点，闭环依赖 §5 C-4/B-3。
3. **已闭环（2026-09-02）**：`npm run compile` 记录、Ignition 执行记录与 configure 输入已归档（§3.6 A-1）。仍开放的三项裁决见 04 §7-6（`STABLE_PRICE(USDC)`、合成 BTC 乘数精度、Oracle provider 绑定），需在对应用例执行前落定。
4. **05 §1.4 引用的既有 SCN（如 SCN-048/050/068/077/078/079）在 TestCode 均无 spec**（现有 15 条不含它们），矩阵中标注的 E2E 映射对这些编号而言是「待编写」而非「待重跑」，工作量估算应据此计。
5. 各外围仓（fx100-apps、keeper、签名服务、索引器）的适配状态本文仅按 01/02 的静态要求登记为「适配待确认」，未核查任何外围仓代码；C-2 需各仓自证。
