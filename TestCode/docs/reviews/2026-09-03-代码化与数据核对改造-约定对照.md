# 代码化 / 数据核对改造 与既定约定的对照（2026-09-03）

> 对照对象：`CLAUDE.md` 测试标准、`docs/03-后续用例实施约定`、`docs/04-测试结果与看板`、`docs/06-深度数据核对重构方案`、`docs/08-测试用例完成代码化` 与工作区未提交代码（`src/reconciliation/`、`src/scenario-engine/`、`src/evidence/`）。
> 代码状态按 2026-09-03 16:25 工作区快照；此后代码仍在变动，关闭条目前请以当前代码复核。
> 当前结论以“二-d、2026-09-04 C4/C7/C8/C9 收口状态”为准；二-b、二-c 只保留当时的审计轨迹。

## 一、文档冲突类 —— 已直接修订，无需代码改动

| # | 冲突 | 处理 |
|---|---|---|
| D1 | 用例 ID 标准只认 `SCN` / `<UT\|IT\|E2E>-…`，未涵盖已自动化的 `CT/XT/FT-*` | `CLAUDE.md` 第 2 条改为四类 ID 规范（SCN 固定 80 条不增号；CT/XT/FT 正则、来源、不要求对应 SCN；域级；缺陷）并加"ID 必须先存在于设计文档"总则 |
| D2 | 03 §1 "一个自动化测试必须对应一个现有 SCN" 与 CT/XT/FT 自动化矛盾 | 03 §1 改为"对应一个现有 Case ID（SCN 或 CT/XT/FT）"，补 variantId/executionId 口径 |
| D3 | 08 §4 DSL 示例 `SCN-101/102` 属造号 | 08 §4 加标注：示意编号，真实用例必须用矩阵登记编号 |
| D4 | 06 §5 CheckRecord（`domain/txStep/crossChecks`、verdict `CALCULATED`）与 08 §9.2 / Zod Schema 不一致，06 未标注被取代 | 06 文首加"2026-09-03 修订说明"，列明被 08 取代的部分（CheckRecord、黄金样例改 XT-MKT-OPEN-001、N/A→NOT_RUN/NOT_APPLICABLE、§1 非 git 仓库已过时） |
| D5 | 03 §5 附件登记表未登记功能用例附件名 `<CaseID>-<env>-r<n>-<i>-<caseid>-evidence.json` | 已登记，并说明 Evidence V2 后附件名不再承担按编号选派生器的职责 |
| D6 | CLAUDE.md "自动化现状" 只提 15 条 SCN，未提 CT/XT/FT 自动化与 08 方案 | 已补一句指向 `versions/v0.3.2/results.md` 与 docs/08 |

## 二、代码实现违背准则 —— 需要代码人员修改

| # | 位置 | 违背的约定 | 现象 | 要求 |
|---|---|---|---|---|
| C1 | `src/reconciliation/adapters/market-flow-v1.ts` `MarketFlowAdapterOptions.transactionBaseUrl`、`url()`；`scripts/verify-reconciliation-v2.ts` `assert.ok(...transactionUrl.startsWith(baseUrl))` | 08 §9.4 "Evidence 保存执行时的 Explorer 身份，不用当前环境猜历史链接"；04 "不拼接虚假的 Tenderly 链接" | 交易链接可由调用方传入的 baseUrl 拼出，回放脚本还断言链接必须以传入 baseUrl 开头 | 删除 `transactionBaseUrl` 注入口，`explorer` 只来自证据自带的 `environment.explorerUrl` / fork 身份；证据无 explorer 身份时只输出 hash，不生成 URL；回放断言改为"历史证据自带链接才校验" |
| C2 | `src/reconciliation/packs/market-open.ts` 约 L105、169、173、177、238、245：`formula.inputs: []` | 06 §5 "每一个公式输入都带来源"；08 §9.4 页面必须显示公式展开值、参数与数据来源；CLAUDE.md 第 1 条"期望值必须附推导过程" | 标为 `FULL_RECOMPUTE` 的核对行（fee.position.amount、五方守恒、瀑布结算等）公式输入为空数组，来源不可追溯 | 每条带 `formula` 的 CheckRecord 必须填 `inputs[{name,value,source}]`（sizeDeltaUsd←intent、feeFactor←DataStore@block、price.min←Oracle@block…）；填不出来源的降级为 `EVENT_ANCHORED` 或 `NOT_VERIFIED`，不得保留 `FULL_RECOMPUTE` |
| C3 | `src/reconciliation/engine/aggregate-verdicts.ts` L30：`values.includes('NOT_RUN') → PASS_WITH_GAPS`；`verify-reconciliation-v2.ts` 断言黄金样例 `PASS_WITH_GAPS` | 06 §3 "纯链上用例无页面证据不影响协议核对结果"；08 §9.3 "本轮未要求执行的层记 NOT_RUN，不记 FAIL" | 所有 RPC 用例 frontend/indexer 恒为 NOT_RUN，总体永远 `PASS_WITH_GAPS`，"缺口"失去区分度 | 需裁决后改：建议 `NOT_RUN` 不计入 gap（只有声明了 capability 却缺数据的 `INCOMPLETE` 才降级），或在报告里把"未要求层"与"缺数据层"分开计数。裁决前以 06 §3 为准 |
| C4 | `src/scenario-engine/scenario-builder.ts` `ScenarioOptions{environment:string, actors, isolation}`；`state-machine.ts` 无环境类校验 | 03 §1 每条用例必填 `targetProject/marketMode/oracleMode/marketCompatibility/timeMode/signingMode/mockResourceAlias/environmentSetup`；08 §7 构建期必须拒绝"非 time-fork 推进时间、mock-only 切普通市场、Market 单配 Limit trigger、Limit 单缺触发价、未达可清算就清算" | DSL 头部只有一个环境字符串；状态机只校验引用/仓位状态，08 §7 列出的环境类拒绝规则一条都未实现 | `ScenarioOptions` 承载 03 §1 全部结构化环境字段（可从 case-registry 解析）；`state-machine.validate` 补齐 08 §7 清单并各加一条反向单测 |
| C5 | `src/reconciliation/packs/market-open.ts`（无 claimable 相关核对） | 06 §6.2 `ΔFeeReceiver` Token 余额与 `ΔClaimablePositionFee/FundingFee/LiquidationFee` 必须分开展示、分别守恒；06 §11 完成标准 | 阶段 A 核对包只有五方 Token 守恒，未读 DataStore claimable | 阶段 A 收口前补 `fee.claimable` 核对包（缺读数时按能力缺失记 `NOT_VERIFIED`，不得沉默省略） |
| C6 | 进行中：`src/reconciliation/schema/evidence-v2.ts` 已改为转发 `src/evidence/evidence-v2.ts`（16:23） | 08 §3.1 / §9.2 "唯一 EvidenceEnvelope 契约、唯一 Zod Schema" | 16:05 前两侧各有一套不兼容 Evidence V2（outcome 大小写、blockNumber 类型、TransactionRef/environment 结构）；现已合并 | 请确认 `npm run typecheck`、`test:reconciliation:v2`、`verify-scenario-engine` 三者在合并后同时通过，并删除 `@deprecated` 转发文件，只保留一处 import 路径 |
| C7 | 工作区：`src/scenarios/scn-009/010/070-runner.ts` 均有未提交修改 | 08 §3.1 "禁止两边同时修改同一 Runner 文件；核对侧不改 Runner" | 无法判定是哪一侧改的 | 提交前由执行侧确认这三处改动全部来自执行工作面；核对侧需要 Runner 变化时走 Evidence 契约升级（`schemaVersion` 递增 + 旧证据 Adapter） |

## 二-b、17:01 复核状态

| # | 状态 | 说明 |
|---|---|---|
| C1 | ✅ 已修 | `transactionBaseUrl` 注入口与回放脚本的 baseUrl 断言均已删除 |
| C2 | ✅ 已修 | `market-open.ts` 不再有空 `inputs`；仅有事件来源的 fee/价格行已诚实降级为 `EVENT_ANCHORED` 并附说明 |
| C3 | ✅ 已修 | `NOT_RUN` / `NOT_APPLICABLE` 不再计入 gap；只有 `INCOMPLETE` 或 warning 级 FAIL/NOT_VERIFIED 才降为 `PASS_WITH_GAPS`；Schema 不通过直接 `INVALID_EVIDENCE`。黄金样例本体因缺显式污染扫描凭证仍为 `PASS_WITH_GAPS`，补凭证后为 `PASS`——这是正确行为 |
| C4 | ⏳ 当时未动 | `scenario-engine/*` 自 15:34 无改动：DSL 环境字段与 08 §7 环境类拒绝规则仍缺；后续已收口，见二-d |
| C5 | ✅ 已修 | 新增独立 `fee.claimable` CheckPack；TX2/TX4 分行核对 `ΔFeeReceiver Token`、`ΔClaimablePositionFee/FundingFee/LiquidationFee`，不与 ERC20 五方守恒混加；缺 capability 或快照/事件字段时产生 blocking `NOT_VERIFIED`。黄金回放新增 8 行，34/34 PASS |
| C6 | ✅ 已修 | 无引用的 `src/reconciliation/schema/evidence-v2.ts` `@deprecated` 转发文件已删除；唯一契约与 Zod Schema 保留在 `src/evidence/evidence-v2.ts`，三项验证同时通过 |
| C7 | ⏳ 当时待确认 | 当前并非“一个用例一个 Runner 文件”：`ct-base-runner.ts` 聚合 8 个 CT 函数，`xt-mkt-runner.ts` 的三个执行原子复用 7 条 XT，`scn-009-runner.ts` 被多条 SCN 复用；但仍存在每 Case 导出函数和大型领域 Runner 的过渡形态。后续归属已确认，见二-d |

## 二-c、17:12 复核状态

| # | 状态 | 说明 |
|---|---|---|
| C5 | ✅ 已修 | 新增 `packs/fee-claimable.ts`（TX2/TX4 各 4 条：claimable position/funding/liquidation 与 FeeReceiver 余额分列，Expected Δ / Expected After 独立），缺读数走 `NOT_VERIFIED`；黄金样例回放 26→34 条 |
| C6 | ✅ 已修 | `schema/evidence-v2.ts` 转发文件已删除，`src/evidence/evidence-v2.ts` 为唯一契约 |
| C4 | ⏳ 当时未动 | `scenario-engine/*` 仍停在 15:34；后续已收口，见二-d |
| C7 | ⏳ 当时未动 | runner 修改归属与两个过渡 Runner 尚未处理；后续见二-d |
| 小项 | 📝 | 08 §11 现写"截至 2026-09-04"，应为 2026-09-03 |

## 二-d、2026-09-04 C4/C7/C8/C9 收口状态

| # | 状态 | 说明 |
|---|---|---|
| C4 | ✅ 已修 | 八字段环境契约已提到唯一共享入口 `src/domain/test-environment.ts`；Scenario `build()` 会在零副作用静态演练中覆盖 08 §7 八类拒绝，Harness 再核对实际 resolved 环境。Limit 使用合约同口径 min/max 触发判断；清算/ADL 必须消费带来源的条件评估参数，普通 Market 全平不再被误要求先调价；新增 `advanceTime` 一等动作与 `npm run test:scenario-engine` 反向测试。 |
| C7 | ✅ 归属已确认 / ⏳ 迁移待阶段 C/D | 交易执行任务已确认 `scn-009/010/070-runner.ts`、`ct-base-runner.ts`、`xt-mkt-runner.ts` 均由执行侧维护；Git 本身无法证明具体会话作者。当前不是一用例一文件，而是领域聚合 + 每 Case 导出的过渡形态。本轮不重写这些高复用 Runner；阶段 C 起禁止新增按编号 Runner，阶段 D 移除 `xt-mkt-runner.ts` 的旧公式/CaseCheck 双判定。数据核对侧只经 Evidence 契约协作，不共同修改 Runner。 |
| C6 文档 | ✅ 已修 | 08 已与代码同步：旧 `src/reconciliation/schema/evidence-v2.ts` 已删除，不再描述为兼容 re-export。 |
| C5 术语 | ✅ 已修 | `feeReceiverUsdc` 是历史 Evidence 槽位名，实际采集地址为 FeeHandler；新核对行已命名为 `fee.handler-token-balance`。DataStore `FEE_RECEIVER` 指向 RevenuePool，两者不再混称。 |
| C8 污染扫描契约 | ✅ 内核/原生 Executor 已修 / ⏳ B1 旧桥迁移 | 执行接线发现 V2 `WindowContaminationEvidence` 复用严格 `TransactionRef`，第三方交易无法诚实填写 role，且空扫描列表可伪造 CLEAN。按 08 §3.1 冻结 V2、新增 V3 与 V2→V3 Adapter；V3 使用独立 `ScannedTransactionRef` 和逐块 `inspectedBlocks`，完整性引擎校验闭区间覆盖、parentHash/区块链、transactionIndex/nonce、提交与终态交易、第三方与 unexpected 集合。原生 `live-scenario`/scanner/collector 已生产 V3；`xt-mkt-runner` 仍作为阶段 A 的 V2 `runMarketFlow` 桥，因而实际 B1 报告的 TX2/TX4 污染窗口仍是 `INCOMPLETE`，不得声称已 CLEAN。 |
| C9 Report/环境信任 | ✅ 已修 | Report V2 绑定 Evidence digest 与 CheckPlan id/version/digest；由 `reconciliation` 层按可信注册计划重放并对比整份 canonical Report，防止复制公开 digest 自造 PASS。生产端用单一 runtime builder 产生 Environment identity；消费端绑定 name/chainId/deploymentId/release、Fork provider/displayName 以及 Mock/deployed market identity。`test:reporter:evidence` 覆盖错 plan/digest/flow/checks 和环境子字段攻击。真实 `XT-MKT-OPEN-001` 批次 `2026-09-03T193835-118Z` 为 4 笔交易、34/34 核对 PASS，总状态因 V2 窗口凭证和 frontend/indexer 未执行而是 `PASS_WITH_GAPS`。 |

## 二-d、09-04 01:32 复核状态

| # | 状态 | 说明 |
|---|---|---|
| C4 | ✅ 已修 | `ScenarioOptions.environment` 改为结构化 `ScenarioEnvironmentDefinition`（targetProject/marketMode/oracleMode/marketCompatibility/timeMode/signingMode/mockResourceAlias），运行期再与 `resolvedEnvironment` 比对；`state-machine.ts` 已实现 08 §7 全部拒绝规则（readonly 禁写链、无市场资源禁下单、Market 单配 trigger、价格未满足禁 EXECUTED、清算/ADL 须 mock-only + 带来源的条件评估、movePrice 须 mock-oracle + mock-only、非 time-fork 禁推进时间） |
| C7 | ⏳ 未动 | scn-009/010/070 runner 修改归属、`ct-base-runner.ts` / `xt-mkt-runner.ts` 仍未处理 |
| 新增观察 | ✅ 合规 | 核对侧升级为 Evidence V3（严格 Schema + digest 绑定），保留 V2 冻结版与 `adapters/v2-to-v3.ts`，Check Pack 全部改读 V3，`check-plan-registry.ts` 仍按 flowType/capability 选包、无 caseId 分叉——符合 08 §3.1 "Schema 变更升级 schemaVersion 并保留旧证据 Adapter" |
| 小项 | ✅ | 08 §11 日期已与实际（09-04）一致 |

代码侧 7 条至此仅剩 C7（流程归属）。

## 三、需要决策（不属于代码错误）

- **状态词汇统一**：04 批次/用例状态（PASS/FAIL/FLAKY/BLOCKED/SKIP/MANUAL/NOT AUTOMATED，批次 PARTIAL 等）、`versions/results.md` 层状态（NOT_RUN/PASS/FAIL/BLOCKED/GAP）、08 报告状态（PASS/PASS_WITH_GAPS/FAIL/BLOCKED/INVALID_EVIDENCE + 层 INCOMPLETE/NOT_RUN/NOT_APPLICABLE）三套并存，需定一张映射表写进 04（与 C3 一并裁决）。
- **skills 出仓**：CLAUDE.md 与 08 §9.2 依赖的 `fx100-contract-release-workflow` / `fx100-verify-handbook` / `fx100-testcode-ops` 在 git 中显示已删除，`.claude/skills` 现为指向仓库外 `../../TestSkill/project/FX100` 的软链接，约定文档脱离了本仓库版本管理；需决定是回收进仓还是在 CLAUDE.md 登记外部路径与同步方式。
