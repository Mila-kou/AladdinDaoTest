---
project: fx100
layer: e2e
contractRelease: v0.3.2
title: SCN-B32 版本专项场景
cases: 8
status: draft
---

# v0.3.2 SCN-B32 版本专项场景

> 本文件保存只适用于 v0.3.2 的 8 条增补设计；共享 `scenarios/` 仅保留 SCN-001～080。执行状态统一记录在 [../results.md](../results.md)，不得回填到共享设计。

## 用例目录

| ID | 归属 | P | 场景 |
|---|---|---|---|
| SCN-B32-01 | S05 | P0 | 动态点差 MIN/MAX clamp、负点差及清算/ADL floor-at-0 |
| SCN-B32-02 | S05 | P1 | 大单 price impact 指数封顶 |
| SCN-B32-03 | S06 | P0 | 全局 COLLATERAL_TOKEN 与 Relay fee token 强约束 |
| SCN-B32-04 | S06 | P0 | 子账户 4 槽位模型与 EIP-712 新旧签名 |
| SCN-B32-05 | S03 | P0 | 减仓最小输出 `minOutputAmount` |
| SCN-B32-06 | S03 | P0 | USD 减仓 ceil 耗尽时归一全平 |
| SCN-B32-07 | S05 | P1 | 折扣与返佣封顶 |
| SCN-B32-08 | S06 | P0 | v0.3.1 → v0.3.2 升级迁移与存量兼容 |

非用户旅程的 Vault/Market 域级用例见 [IT-VAULT-MARKET](IT-VAULT-MARKET.md)。

## S03：仓位管理与退出
> 2026-08-21 收编自 `Docs/contract-releases/v0.3.2/05-重要参数边界场景.md`（卡片 B32-6-01/02、B32-7-01/02，组 6/7）；台账见 [`../results.md`](../results.md)。本节的 SCN-B32-05/06 计入头部 `cases: 8`，但不计入共享 SCN-001～080 的原 80 条，也不改动 SCN-021～032。数值为 05 卡片的受控 fixture；需要 v0.3.2 tx-fork 的可控 Mock Oracle，能力缺失时标 `BLOCKED`，不得借用当前 v0.3.1 oracle-fork 作为执行证据；实测须按 v0.3.2 部署真值以同公式重算（05 §12-2）。

| ID / 优先级 | 角色与意图 | 前置条件 | 测试数据 | 用户操作步骤 | 核对数据与期望结果 | 恢复与清理 |
|---|---|---|---|---|---|---|
| SCN-B32-05 / P0 | 测试工程师验证减仓 minOutputAmount 真实生效（R4-B22）、其 USD 1e30 口径与 min 选价 | v0.3.2 tx-fork + 可控 Mock Oracle + Inline Keeper；若能力不可用则本用例 `BLOCKED`；固定价下构造一笔全平减仓，链下按合约口径预算输出 outputAmount=5000000（5 USDC，6 位）；USDC price.min=1e24 → outputUsd=5000000×1e24=5e30；数据集② 改 USDC 双价 min=999900000000000000000000（0.9999e24）、max=1e24 | 数据集①（=B32-6-01）三笔独立订单仅变 `order.minOutputAmount`（1e30 USD 精度）：P1=4999999999999999999999999999999（5e30−1）、P2=5000000000000000000000000000000（=5e30）、P3=5000000000000000000000000000001（5e30+1）；数据集②（=B32-6-02）a) minOutputAmount=0；b) =5e30（按 max 价折算恰卡线） | 每点独立：1. 建减仓单；2. Keeper 执行；3. 记录执行成败、revert 参数、仓位/抵押是否变化、订单终态、outputUsd 所用价侧 | 推导见 05 卡片 B32-6-01/02；校验式 `outputUsd < minOutputAmount` 才 revert（`DecreaseOrderUtils.sol::_validateOutputAmount`），outputUsd 用 `oracle.getPrimaryPrice(outputToken).min`：① P1 通过（5e30 ≥ 5e30−1）、P2 通过（等号不触发）、P3 revert `InsufficientOutputAmount(5000000000000000000000000000000, 5000000000000000000000000000001)` 且仓位与抵押不变（终态 Cancelled/Frozen 以 OrderHandler 实现为准，首轮执行后回写；v0.3.1 对照 P3 照常成交）；② a) 通过（0 无门槛）；b) outputUsd=5000000×0.9999e24=4999500000000000000000000000000 < 5e30 → revert `InsufficientOutputAmount(4999500000000000000000000000000, 5000000000000000000000000000000)`（校验取 min 价，用户按 max 估算会被拒单）。口径警示：minOutputAmount 是 1e30 USD 值，不是 USDC 6 位数量 | 全平残留仓位，恢复 oracle 价 |
| SCN-B32-06 / P0 | 测试工程师验证 USD 减仓 ceil 恰好耗尽 token 时归一为全平、空头 floor 不归一（R4-B20） | v0.3.2 tx-fork + 可控 Mock Oracle（固定价 3,000 USD）+ Inline Keeper；若能力不可用则本用例 `BLOCKED`；多头仓位 sizeInUsd=30000000000000000000000000000000（30e30，30 USD）、sizeInTokens=10000000000000000（1e16，0.01 ETH），高于默认 10 USD 最小仓位；数据集② 同规模空头；无其他减仓单 | 数据集①（=B32-7-01）多头 USD 减仓三点（isSizeDeltaUsd=true）：P1=20e30、P2=29999999999999999999999999999999（30e30−1）、P3=30e30；数据集②（=B32-7-02）空头 sizeDelta=30e30−1 | 每点独立快照：1. 建减仓单并执行；2. 读事件（含 `OrderSizeDeltaAutoUpdated`）、实际 sizeDeltaUsd/sizeDeltaInTokens、剩余 sizeInUsd/sizeInTokens、OI 变化 | 推导见 05 卡片 B32-7-01/02；long tokens=`Calc.roundUpDivision(sizeInTokens×sizeDeltaUsd, sizeInUsd)`、short 用整除 floor（`PositionUtils.sol::getDecreaseOrderSize`），归一条件 `isSizeDeltaUsd && sizeDeltaUsd < sizeInUsd && tokens == sizeInTokens`（`DecreasePositionUtils.sol`）：① P1 tokens=ceil(1e16×20e30/30e30)=6666666666666667 部分平，剩余 sizeInUsd=10e30、sizeInTokens=3333333333333333；P2 tokens=ceil((30e46−1e16)/30e30)=10000000000000000=全部 token 而 sizeDeltaUsd<sizeInUsd → 触发归一，发 `OrderSizeDeltaAutoUpdated`（30e30−1 → 30e30）并改为全平，最终 (30e30, 1e16)、仓位双零；P3 命中 `sizeInUsd == sizeDelta` 短路直接全平、无归一事件（v0.3.1 对照：P2 以 30e30−1 记账但清空 token，留 1 单位 USD 残留不一致）；② short tokens=floor((30e46−1e16)/30e30)=9999999999999999 < 1e16 不满足归一 → 正常部分平，剩余 sizeInUsd=1、sizeInTokens=1（尘埃合法存量），无归一事件 | 全平尘埃仓位（sizeDelta=1 走短路分支） |

## S05：长持费用、价格冲击与多市场
> 2026-08-21 收编自 `Docs/contract-releases/v0.3.2/05-重要参数边界场景.md`（卡片 B32-1-01～09、B32-2-01/02、B32-9-01～04，组 1/2/9）；台账见 [`../results.md`](../results.md)。本节的 SCN-B32-01/02/07 计入头部 `cases: 8`，但不计入共享 SCN-001～080 的原 80 条，也不改动 SCN-045～052。参数值均为 05 §1.5 受控 fixture（执行前由 CONFIG_KEEPER 写入 fork），非部署值；实测按 v0.3.2 部署真值同公式重算（05 §12-2）。关联：SCN-050 原「负 spread 标 PENDING」在 v0.3.2 合入后可解除，按 SCN-B32-01 数据集⑥ 推导断言；SCN-049 与 SCN-B32-02 共用 v0.3.2 三点数据集；SCN-048 大单档对应数据集④。

| ID / 优先级 | 角色与意图 | 前置条件 | 测试数据 | 用户操作步骤 | 核对数据与期望结果 | 恢复与清理 |
|---|---|---|---|---|---|---|
| SCN-B32-01 / P0 | 测试工程师验证 v0.3.2 动态点差 int256 clamp、负点差让利与清算/ADL floor-at-0 隔离 | v0.3.2 tx-fork + 可控 Mock Oracle（供数据集⑦⑧；能力缺失则相应数据集 `BLOCKED`，不得借用 v0.3.1 oracle-fork）；05 §1.5 公共 fixture：CONSTANT_PRICE_SPREAD=1e14、PRICE_IMPACT_PARAMETER=6e17、BID/ASK depth=1e37、MAX_PRICE_IMPACT_SPREAD=5e15、SKEW_IMPACT_FACTOR=0（⑥⑦ 用 1e16）、MIN/MAX_SKEW_IMPACT=±5e15、ETH 单价 min=max=3e15（3,000 USD @1e12）；基准单 usdDelta=1e34（10,000 USD）；MIN/MAX_DYNAMIC_SPREAD 默认 −2e16/1e18（除 ①③④⑤ 另写）；数据集⑨ 三账户 K1 CONFIG_KEEPER / K2 LIMITED_CONFIG_KEEPER / K3 无角色 | 数据集①～⑨（=卡片 B32-1-01～09）：① clamp 两键未配置（getInt 均 0）；② a) sizeDelta=0 减仓（合约/Driver） b) usdDelta=1（库或 Reader 等价数学边界，不提交正常订单）；③ MIN 三点 min=109999999999999 / 110000000000000 / 110000000000001（raw=1.1e14）；④ MAX 三点 max=110000000000001 / 110000000000000 / 109999999999999；⑤ min=1e16、max=5e15（max<min）；⑥ 令 `D=floor(1e34/3e15)=3333333333333333333`，OI token long=0、short=`10D=33333333333333333330`，开多 usdDelta=1e34、pricingTokenDelta=D；⑦ 镜像 OI token long=10D、short=0，目标仓位 sizeInUsd=1e34/sizeInTokens=D，同规模全平三路 R1 用户 MarketDecrease / R2 Liquidation / R3 ADL；⑧ oracle 双价 min=3e15、max=3.003e15，开多与平多各 1e34；⑨ K3/K2/K1 各调 `Config.setInt(MIN_DYNAMIC_SPREAD, …, −2e16)`，K1 再写 −1e18 | 每数据集独立快照：1. CONFIG_KEEPER setInt/setUint 写 fixture（⑥⑦ 先造 token 级 OI 并 Reader 确认）；2. 除②b外提交对应订单并由 Inline Keeper 执行（②b 直接调用库/Reader 等价路径；⑦ R2 在 3,000 USD 预造已满足清算条件且 graceEnd 已过的仓位，R3 触发 ADL fixture）；3. 从执行前 OI、pricingTokenDelta 与配置独立推导 skewImpact、priceImpactSpread 和 raw；只从事件/Reader 读取 final dynamicSpread、executionPrice、balanceWasImproved、SecondaryOrderType，并读取 SetInt 事件值 / revert 选择器 | 推导见 05 卡片 B32-1-01～09；关键算式：线性项=1e34×1e18/1e37=1e15 > exp 项，priceImpactSpread=1e15/100=1e13，rawSpread=skewImpact+1e14+1e13（skew=0 时 1.1e14）；多头开仓价=ceil(price.max×(1e18+spread)/1e18)、减仓价=floor(price.min×(1e18−spread)/1e18)；`Calc.clamp` value<min→min、>max→max、等号取原值。① spread=0、执行价=3e15（clamp(1.1e14,0,0)=0，点差链路整体失效，升级漏配事故形态）；② a) spread=0、执行价=pickPrice(min)=3e15；b) 库/Reader 等价数学边界中 priceImpact=0、spread=1e14，但正常 E2E 会被最小仓位或 tokenDelta=0 校验拒绝，不得标成 Keeper 可成交证据；③ P1/P2 spread=110000000000000、执行价=3000330000000000；P3 spread=110000000000001、执行价=3000330000000001（买侧向上取整，必须同时断言 spread 与 executionPrice）；④ P1/P2 spread=110000000000000、P3=109999999999999，并记录 raw 理论上限 5e15+1e14+5e15=1.01e16 ≪ 默认 max 1e18（默认 max 永不生效）；⑤ spread=1e16（max<min 时 max 抬到 min 单点生效）、执行价=3e15+3e15×1e16/1e18=3030000000000000；⑥ token 比例从 (0,10D) 到 (D,10D)，balanceWasImproved=true，skewRef=909090909090909090，skewImpact=−9090909090909090 钳 −5e15，spread=−5e15+1e14+1e13=−4890000000000000，执行价=3e15×995110000000000000/1e18=2985330000000000（v0.3.1 对照 spread=0、执行价 3e15）；⑦ R1 spread=−4890000000000000、执行价=3e15×(1e18+4.89e15)/1e18=3014670000000000，R2/R3 spread=0、执行价=3e15（allowNegativeSpread=false → min 抬 0）；⑧ 开多取 max：3003000000000000+330330000000=3003330330000000，平多取 min：floor(3e15×999890000000000000/1e18)=2999670000000000；⑨ K3 revert `Unauthorized(msg.sender, "LIMITED / CONFIG KEEPER")`、K2 revert `InvalidBaseKey(baseKey)`、K1 正常值与 −1e18 均写入成功（`setInt` 不经 validateRange） | 恢复点差/skew/depth 配置与 OI，全平 |
| SCN-B32-02 / P1 | 测试工程师验证浅流动性大单 price impact 指数封顶不 revert（R2-B10；补位号，与 SCN-049 共用执行证据） | v0.3.2 tx-fork + Inline Keeper；浅深度 fixture：PRICE_IMPACT_PARAMETER=1e18、ASK_ORDER_BOOK_DEPTH=1e30（仅 1 USD 深度）、MAX_PRICE_IMPACT_SPREAD=5e15、常数点差 1e14、skew factor 0、MIN/MAX_DYNAMIC_SPREAD −2e16/1e18 | 数据集①（=B32-2-01）开多三点 orderSize（1e30 精度）：P1=129999999999999999999000000000000（130 USD−1e−18 USD）、P2=130000000000000000000000000000000（130 USD）、P3=130000000000000000001000000000000（130 USD+1e−18 USD，即 +1e12 raw）；数据集②（=B32-2-02）基准单 1e34，分别置 depth=0 / parameter=0（两组独立快照） | 每点：1. 写配置；2. 提交开多并 Keeper 执行；3. 按执行前配置独立推导 priceImpactSpread，读取事件/Reader 的 final dynamicSpread，记录是否 revert / panic | 推导见 05 卡片 B32-2-01/02；指数=mulDiv(orderSize,1e18,1e30)=130e18−1 / 130e18 / 130e18+1，`LogExpMath.sol::MAX_NATURAL_EXPONENT=130e18`：① P1 入 exp（exp(130−1e−18) 远超 cap）→ `Math.min(·,5e15)`、P2 等号仍入 exp 后同被 cap、P3 `> MAX` 直接 `return maxPriceImpactSpread`，三点独立推导的 priceImpactSpread=5000000000000000，事件/Reader 的 final dynamicSpread=0+1e14+5e15=5100000000000000，均不 revert（v0.3.1 对照 P3 进 `INVALID_EXPONENT` revert，即 R5-B02 清算 DoS 成因）；② 两组 `depth == 0` 或 `priceImpactParameter == 0` 守卫 → 独立推导 priceImpactSpread=0、不 panic，dynamicSpread=1e14 | 恢复 depth/parameter，处理仓位 |
| SCN-B32-07 / P1 | 测试工程师验证折扣+返佣封顶后的三金额不变式、minAffiliate 与 cap 优先级（R2-B15） | v0.3.2 tx-fork + Inline Keeper；sizeDeltaUsd=1e34；POSITION_FEE_FACTOR=5e26（positionFeeAmount=5e30/1e24=5000000 即 5 USDC）；USDC price.min=1e24；referral 码 affiliateRewardFactor=1e29、traderDiscountFactor=1e29（totalRebate 2e29）；MIN_AFFILIATE_REWARD_FACTOR(tier)=2e29（20%，故意高于 cap）；账户已设 pro tier；K1 持 CONFIG_KEEPER | 数据集①（=B32-9-01）PRO_DISCOUNT_FACTOR 三点：899999999999999999999999999999（9e29−1）/ 900000000000000000000000000000（9e29）/ 900000000000000000000000000001（9e29+1）；②（=B32-9-02）=1e30（100%）；③（=B32-9-03）=9.5e29；④（=B32-9-04）K1 `Config.setUint(PRO_DISCOUNT_FACTOR, abi.encode(tier), 1e30+1)` 与 =1e30 | 每点：1. CONFIG_KEEPER 写配置；2. 执行开仓；3. 读 PositionFees 分解（traderDiscountAmount、affiliateRewardAmount、adjustedAffiliateRewardFactor）；④ 只做写入、读回与 revert 记录 | 推导见 05 卡片 B32-9-01～04（`PositionPricingUtils.sol::getPositionFeesAfterReferral`：proDiscount>totalRebate → adjusted 先取 minAffiliate=2e29；cap=1e30−totalDiscount 晚于且优先于 minAffiliate 地板）：① P1 adjusted=1e29+1，discount=floor(5000000×(9e29−1)/1e30)=4499999、affiliate=500000、和=4999999；P2 adjusted=1e29，4500000+500000=5000000 恰等全额（协议净得 0）；P3 adjusted=1e29−1，4500000+499999=4999999；三点 discount+affiliate ≤ 5000000、不 revert（v0.3.1 对照 P2/P3 类组合下溢 revert）；② totalDiscount=1e30 → cap 取 0 → adjusted=0，affiliate=0、discount=5000000、协议净费 0、不 revert；③ adjusted=min(2e29, cap 5e28)=50000000000000000000000000000（5%），affiliate=250000、discount=4750000、和=5000000（「min 保底」在折扣逼近 100% 时不兑现）；④ 1e30+1 revert（`ConfigUtils.sol::validateRange` ≤1e30），1e30 等号写入成功——与 SCN-B32-01 数据集⑨ 对照：uint 配置有链上防线、int 点差 clamp 键没有 | 恢复 tier/referral/费率配置，全平 |

## S06：Flash、异常恢复与兼容
> 2026-08-21 收编自 `Docs/contract-releases/v0.3.2/05-重要参数边界场景.md`（卡片 B32-3-01/03、B32-4-01～05、B32-10-01～04，组 3/4/10）；台账见 [`../results.md`](../results.md)。本节的 SCN-B32-03/04/08 计入头部 `cases: 8`，但不计入共享 SCN-001～080 的原 80 条，也不改动 SCN-053～064。B32-3-02/04（建市组合约束与写入权限）与 B32-8-01/02（Vault 提款校验）为非用户旅程，落域级用例 [`IT-VAULT-MARKET.md`](IT-VAULT-MARKET.md)。关联：SCN-053/055/057/058 为 Flash 会话层回归，SCN-B32-04/08 给出其 v0.3.2 链上对应物。

| ID / 优先级 | 角色与意图 | 前置条件 | 测试数据 | 用户操作步骤 | 核对数据与期望结果 | 恢复与清理 |
|---|---|---|---|---|---|---|
| SCN-B32-03 / P0 | Flash/Relay 用户在全局 COLLATERAL_TOKEN 未配置或 feeToken 不符时被明确拒绝（v0.3.2 强约束） | v0.3.2 tx-fork + Relay 签名服务（新 EIP-712 域）；数据集①：`DataStore.getAddress(FX100Keys.COLLATERAL_TOKEN)`=address(0)（未执行 ignition `SetCollateralToken` 或 CONTROLLER 清空）；数据集②：该键=USDC、用户已授权 Relay；与 SCN-059 同环境执行 | 数据集①（=B32-3-01）a) MARKET_KEEPER 调 `createMarket(indexToken)`，b) 合法签名 Relay 请求 feeToken=USDC；数据集②（=B32-3-03）同一笔子账户/Relay 下单请求仅变 `relayParams.fee.feeToken`：F1=USDC、F2=WETH、F3=address(0) | 1. 只读确认键值；2. 分别构造、签名并提交各组请求；3. 记录交易成败与 revert 选择器/参数；4. 恢复键值=USDC | 推导见 05 卡片 B32-3-01/03（`MarketFactory.sol::createMarket` 首校验 `collateralToken == address(0)`；`BaseRelayRouter.sol::_validateCall` 先查全局键非零再比 `relayParams.fee.feeToken != collateralToken`）：① a/b 均 revert `FxErrors.EmptyToken()`——未配置=建市与全部 Relay 关断（v0.3.1 无此键、无此约束）；② F1 通过校验进入后续流程；F2 revert `UnexpectedRelayFeeToken(WETH, USDC)`；F3 revert `UnexpectedRelayFeeToken(address(0), USDC)`（零地址在 `_validateCall` 已截获，走不到 `_handleRelayFee` 的 EmptyToken 分支） | 恢复 COLLATERAL_TOKEN=USDC，清理订单 |
| SCN-B32-04 / P0 | Flash 用户管理 4 个子账户槽位（增/改/删/复用/开关）并在升级后用新 EIP-712 结构重签 | v0.3.2 tx-fork（`SubaccountRouter` 直连）+ Relay + 签名服务（可分别按 v0.3.1/v0.3.2 typed data 出签）；主账户 A 初始无槽位（4 键组缺省 slot=""、subaccount=address(0)、isActive=false）；数据集④ 需活动子账户 S1 且 integrationId 已配置、CONFIG_KEEPER 可写开关；数据集⑤ A 私钥可控（测试钱包） | ①（=B32-4-01）S1..S5 依次 `addSubaccount(Si,"si")`；②（=B32-4-02）A 已有 "s1"→S1(index0)、"s2"→S2(index1)，a) `addSubaccount(S9,"s1")` b) `addSubaccount(S2,"s3")`；③（=B32-4-03）a) `removeSubaccount(S1)` b) `removeSlot("s2")` c) `addSubaccount(S3,"s3")`，再用 S1/S2 各下一单；④（=B32-4-04）a) 非槽位地址 X 以子账户身份为 A 下单，b) 置 `SUBACCOUNT_INTEGRATION_DISABLED(integrationId)=true` 后 S1 下单；⑤（=B32-4-05）a) 按 v0.3.1 `SubaccountApproval`（无 `string slot`）签名走 Relay 授权，b) 按 v0.3.2 结构重签同请求，c) `REMOVE_SUBACCOUNT_SLOT_TYPEHASH` 新流程 removeSlot | 每组：1. 执行调用；2. `Reader.getSubaccountSlots(A)` 读 4 槽三元组 {slot, subaccount, isActive}；3. 记录 `AddSubaccount` 事件 slotIndex/slot 与 revert 选择器；④ 关开关回归；⑤ 记录签名校验结果与槽位状态 | 推导见 05 卡片 B32-4-01～05（`SubaccountUtils.sol::MAX_SUBACCOUNT_SLOTS=4 / addSubaccount / removeSubaccount / removeSlot / validateSubaccount / validateIntegrationId`；`RelayUtils.sol::SUBACCOUNT_APPROVAL_TYPEHASH`）：① 前 4 次成功 slotIndex 依次 0/1/2/3，第 5 次 revert `MaxSubaccountSlotsExceeded(A)`（边界由常量决定，不可运营调整）；② a) index0 更新为 {"s1",S9,true}、活动槽仍 2（同名有效槽 add 视为更新），b) revert `DuplicateSubaccount(A,S2)`；③ a) index0 地址清 address(0) 但槽名与 isActive 保留，b) index1 isActive=false，c) 新增落入第一个非活动槽 index1，S1/S2 下单均 revert `SubaccountNotAuthorized(A,Sx)`（validateSubaccount 只匹配 isActive && 地址相等）；④ a) revert `SubaccountNotAuthorized(A,X)`，b) revert `SubaccountIntegrationIdDisabled(integrationId)`，关断后恢复可下单；⑤ a) 旧签名校验失败 revert（typehash 增 `string slot` → digest 不同，具体错误以实现为准），b/c 成功——所有已缓存旧授权签名作废需强制重签 | 移除测试槽位，关闭 integration 开关，撤销测试授权 |
| SCN-B32-08 / P0 | 升级演练：v0.3.1 存量仓位 / 每市场 collateralToken / 旧子账户授权 / claimCollateral 调用方在 v0.3.2 上的行为 | 部署前置：在 v0.3.1 部署态（或 tx-fork 升级演练）创建多头仓位（含非零 funding per-size 快照）、经旧 `addSubaccount(address)` 授权 S1（写入 SUBACCOUNT_LIST）、27 市场每市场 collateralToken 槽位已有值（@v0.3.1 快照均 USDC，04 §4 表末行）；随后按 `02-部署与升级手册.md` 原位升级至 v0.3.2 并配置 clamp 两键；数据集② a 组未写全局 COLLATERAL_TOKEN、b 组已写 USDC；数据集④ 持有 v0.3.1 ABI 的调用方 | ①（=B32-10-01）存量仓位做一次部分减仓；②（=B32-10-02）Reader 读 market.collateralToken，建市/Relay 各一笔；③（=B32-10-03）`Reader.getSubaccountSlots(A)`、S1 为 A 下单、A 按新 ABI `addSubaccount(S1,"main")` 后重试；④（=B32-10-04）按 v0.3.1 selector 调 `ExchangeRouter.claimCollateral(...)`、CONFIG_KEEPER 调 `Config.setClaimableCollateralFactorForTime(...)` | 1. 升级前记录仓位五要素（sizeInUsd / sizeInTokens / collateral / 两 funding 快照）；2. 升级 + 配置；3. 逐组执行并读 funding 结算额、dynamicSpread 来源、market.collateralToken、4 槽状态、revert 形态；4. `cast sig` 比对新 ABI 无对应 selector，并核对 `scripts/configureGeneral.ts` 不再写 delay/divisor 两键 | 推导见 05 卡片 B32-10-01～04：① 仓位存储键与 funding per-size 键 v0.3.2 无 key 变化（04 §3.9），存量仓位字段原样可读；减仓 funding 按「市场最新 per-size − 仓位快照」逐仓结算且现金流走 `settleFundingFees` 新时点，执行价走 int256 clamp 链路；若漏配 clamp 键即落 SCN-B32-01 数据集① 事故（点差恒 0）——升级后首笔存量动作是第一现场；② a) `MarketStoreUtils.sol::get` 一律读全局键 → market.collateralToken=address(0)，建市/Relay 按 SCN-B32-03 ① revert `EmptyToken`（旧每市场槽位为孤儿数据不被消费），b) 写全局键后恢复 USDC——`SetCollateralToken` 是升级硬前置（fullKey 哈希不同，不能因链上旧值而跳过）；③ 升级后 4 槽全空（SUBACCOUNT_LIST 键已删除、槽位键从未写过），S1 下单 revert `SubaccountNotAuthorized(A,S1)`，重授权后成功——无自动迁移，需产品侧强制重授权；④ 两调用均失败 revert（v0.3.2 已删 `claimCollateral`、`setClaimableCollateralFactorForTime/ForAccount` 与 CLAIMABLE_COLLATERAL_* 六键，合约无对应 selector），以调用失败 + `cast sig` 比对为证 | 保留升级演练快照，全平仓位 |
