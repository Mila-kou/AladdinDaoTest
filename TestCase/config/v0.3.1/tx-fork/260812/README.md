# tx-fork v0.3.1 参数快照（2026-08-12）

这是 `release/v0.3.1` @ `d9a7fd2f23705c0bcfe44eaa2f40d9d5c170aaa0` 的历史 Fork 快照。目录环境与机读证据一致：`tx-fork / aladdindao/test`、chainId `99911`、block `45389192`、生成时间 `2026-08-12T16:11:15.294Z`。

原始 `deploymentName` 为 `base_sepolia_v0.3.1_260729`，表示该 Fork 使用的源部署标签，不表示参数采集发生在 Base Sepolia；因此快照叶子按实际证据日期命名为 `260812`。本快照不能当作 Base Sepolia 84532 同区块实值证据。

- `parameters.json`：机读快照。
- `parameters-by-module.csv`：按模块整理的数值表。
- `parameters.md`：人工阅读摘要。

本目录只用于 v0.3.1 回溯和对比，不得作为 v0.3.2 的当前配置或默认回退。详细生成时间、实际读取网络、区块和合约地址以 `parameters.json` 内的 `meta` 为准；文件内已包含 `roles` 和 `tokenMetadata`。
